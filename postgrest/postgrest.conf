db-schemas = "public"
db-anon-role = "anon"
db-pre-request = "public.require_workspace_claim"
jwt-role-claim-key = ".workspace_role"
server-host = "0.0.0.0"

# Cloud Run injects PORT=8080; PGRST_SERVER_PORT env in the deploy overrides
# this, kept here so local compose and prod agree.
server-port = 8080
