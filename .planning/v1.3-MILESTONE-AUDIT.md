---
milestone: v1.3
audited: 2026-06-02
status: tech_debt
scores:
  requirements: 14/14    # all v1.3 phase requirement codes satisfied per VERIFICATION
  phases: 10/10          # all phases verified (3 passed, 7 human_needed — no code gaps)
  integration: 6/6       # all cross-phase seams WIRED
  flows: 8/8             # all seam E2E flows intact (52/52 seam tests pass)
gaps:
  requirements: []       # no unsatisfied requirements — FAIL gate not triggered
  integration:
    - seam: "39 optimistic-locking — trigger migration"
      severity: warning
      file: "20260603000000_add_items_updated_at_trigger.sql"
      issue: "bare ADD COLUMN / CREATE TRIGGER (no IF NOT EXISTS / DROP IF EXISTS) — not re-runnable; bites a manual re-apply. Inconsistent with Phase 33's idempotent style."
    - seam: "34 ios-memory-optimization — ItemCard"
      severity: warning
      file: "ItemCard.tsx:27"
      issue: "dead prop dexieItemId — in interface + arePropsEqual comparator + passed by ItemList:372 but never destructured/used. Harmless dead surface from the props-driven refactor."
  flows: []              # zero broken flows
tech_debt:
  - phase: 31-sec-profiles-self-update-hardening
    items:
      - "Prod profiles.theme column missing despite migration history (phantom) — theme persistence off on prod; app degrades gracefully."
      - "database.types.ts stale vs prod (crm_classifications/crm_threads not synced) — cross-app type cleanup."
  - phase: 32-audio-blob-supabase-persistence
    items:
      - "audioLookup.ts:60 zero-byte placeholder blob for cross-device rows — known limitation, not playable if mistreated."
      - "5 warnings + 4 infos from 32-REVIEW.md — advisory follow-ups inherited from the photo path."
  - phase: 33-offline-reliability
    items:
      - "D-12 manual re-save UI deferred — retryBuffer/recorderError written but no UI consumer."
  - phase: 34-ios-memory-optimization
    items:
      - "PERF-2 continuous master-blob rework deferred (D-04/D-050, continuous gated off) — tracked note only."
      - "ItemList.tsx a.id! non-null assertions (WR-01/IN-01) — known quality gap."
  - phase: 37-a11y-foundation
    items:
      - "meta-viewport user-scalable=no (WCAG 1.4.4) — product decision pending across all 3 surfaces."
  - phase: 38-migration-retryability
    items:
      - "WR-04 cross-store non-atomicity duplicate vector accepted for v1 — needs natural-key/upsert follow-up phase."
      - "photoMigration.ts house/sale ++id collision — same oldId+type lookup lacks itemTable discriminator; out-of-scope flag, future phase."
  - phase: 39-optimistic-locking
    items:
      - "WR-01 gone-vs-skipped conflation (accepted design); WR-03 stale enqueue snapshot (low-sev); WR-04 strict-inequality on non-primitives (latent, all MERGE_FIELDS scalar); WR-05 exhaustion retry stacking (accepted for v1.3)."
  - phase: 40-ai-proxy-cloud-run-migration
    items:
      - "OPEN LANDMINE — ../tpc-ai-proxy .github/workflows/deploy.yml missing SUPABASE_URL/SUPABASE_ANON_KEY and uses extension-only ALLOWED_ORIGINS. A push to proxy main would crash-loop Cloud Run (config.js fail-closed). Proxy commits intentionally unpushed. CI hardening REQUIRED before pushing the proxy branch."
milestone_end_uat:   # human_needed items deferred to v1.3 milestone-end — now DUE
  - phase: 33
    item: "HV-1: real 4-tab concurrent drain → zero duplicate Gemini calls (needs live server)."
  - phase: 34
    item: "HV-1: iOS Safari on-device JS-heap memory smoke (PERF-1, manual per D-09)."
  - phase: 35
    item: "HV-1: visual fidelity of list-card failure row vs detail AiFailureBanner."
  - phase: 36
    item: "HV-1: live duplicate-receipt import → rollback, no false-success nav (Supabase 23505)."
  - phase: 37
    item: "HV-1: authed keyboard-only record/edit/save flow (UAT-37-01, credential-gated)."
  - phase: 38
    item: "HV-1: live retry-after-partial migration vs real Supabase — no duplicate rows."
  - phase: 39
    item: "HV-2: on-device race user-edit vs AI-merge does not silently lose the user's edit (HIGH RISK)."
nyquist:
  compliant_phases: [32, 37, 38, 39]
  partial_phases: [31, 33, 34, 35, 36]
  missing_phases: [40]
  overall: partial
---

# v1.3 Maturation — Milestone Audit

**Audited:** 2026-06-02
**Status:** ⚡ tech_debt — no critical blockers, all requirements satisfied, accumulated deferred items + milestone-end UAT now due.

## Summary

10 phases (31–40), all verified. 3 phases `passed` (31, 32, 40), 7 `human_needed`
(33–39). **Zero phases reported critical gaps.** Every `human_needed` status is a
deferred manual/live/on-device check — not a code defect — and several were
explicitly deferred to v1.3 milestone-end UAT per project policy
(`v13-push-uat-at-milestone-end`). That UAT is now due.

Cross-phase integration: all 6 high-risk seams WIRED end-to-end, **0 broken flows**,
52/52 seam tests passing. Two non-blocking warnings (dead prop, non-idempotent
trigger migration).

## Phases

| Phase | Status | Critical gaps | Nyquist |
|-------|--------|---------------|---------|
| 31 sec-profiles-self-update-hardening | passed | none | partial (draft) |
| 32 audio-blob-supabase-persistence | passed (20/20) | none | compliant |
| 33 offline-reliability | human_needed | none | partial (draft) |
| 34 ios-memory-optimization | human_needed | none | partial (draft) |
| 35 ai-correctness-track-2 | human_needed | none | partial (draft) |
| 36 ux-visibility-polish | human_needed | none | partial (draft) |
| 37 a11y-foundation | human_needed | none | compliant (wave0 incomplete) |
| 38 migration-retryability | human_needed | none | compliant |
| 39 optimistic-locking | human_needed | none | compliant |
| 40 ai-proxy-cloud-run-migration | passed | none | **missing** |

## Requirements Coverage

v1.3 requirement codes (SEC / REL-1..4 / PERF-1..3 / SC-1..5 / DAT-3 / a11y Codex
backlog #32/#33/#34/#46/#48/#49) are defined inline in the ROADMAP phase entries +
the 2026-05-27 audit backlog, **not** in `REQUIREMENTS.md` (whose traceability table
only covers v1.2 Phases 22–30). Coverage therefore cross-referenced against phase
VERIFICATION tables + SUMMARY frontmatter:

- **REL-1/2/3/4** (Phase 33) — all SATISFIED (backoff+ai_attempts, DB-atomic claim, classify-driven drain + BlockedQueueBadge, recorder always-settle). SUMMARY union {REL-1,REL-2,REL-3}.
- **PERF-1/2/3** (Phase 34) — SATISFIED (chunked encoder; PERF-2 deferred-tracked; provider-hoist + React.memo).
- **SC-1..5** (Phase 35) — VERIFIED (temperature:0, confab null-guard + Zod, no-clobber via userEditedFields, shared AiFailureBanner).
- **DAT-3** (Phase 39) — SATISFIED (updated_at trigger, preconditionUpdate CAS, per-field compare-and-skip, conflict ErrorToast). SUMMARY union {DAT-3}.
- **a11y** (Phase 37) — VERIFIED (focus-trap on 5 modals, 44px targets, icon aria-labels, ⋯ delete alternative, axe-clean).
- Phases 31, 32, 36, 38, 40 — verified against goal-backward observable truths (no formal REQ-IDs).

**No unsatisfied requirements. No orphaned requirements** (REQUIREMENTS.md does not
assign any v1.3 REQ-IDs, so orphan detection against the traceability table is N/A).
**FAIL gate not triggered.**

## Cross-Phase Integration

All six seams resolve WIRED (full detail in the integration report):

1. **Audio fallback (32) + optimistic-lock (39) + no-clobber (35)** — three write paths, three composing guards. Single-shot AI write deliberately uses flag-based no-clobber instead of CAS (documented in 39-03, verified safe). Storage fallback is read-only. ✅
2. **DB-atomic claim (33) + updated_at trigger (39)** — claim/reclaim/heartbeat touch only `ai_status`/`claimed_at` (not in MERGE_FIELDS); trigger reuses `set_updated_at()` attach-only; migration order correct. Heartbeat-bump → AI-merge 0-row → reconcile re-applies untouched fields. ✅
3. **Migration banner copy (36) + partial banner (38)** — two distinct surfaces (MigrationSplash modal vs MigrationRetryBanner), single `useDataMigration` instance owned by ProtectedRoute, passed via Outlet context. No copy divergence. ✅
4. **ItemCard/ItemList × 32+34+35+37** — one props-driven `React.memo` card; upload pill, AI-failure banner, OverflowMenu delete, aria/touch targets each occupy a distinct render slot. ✅
5. **Proxy cutover (40) + chunked encode (34) + temperature=0 (35)** — `proxy/` removed from tree, `.env.example` + tests repointed to Cloud Run, both gemini paths share chunked `blobToBase64` + `temperature:0` + Bearer JWT. ✅
6. **BlockedQueueBadge (33) + ErrorToast (36) + conflict toast (39)** — single global ErrorToast over single-slot notificationStore; badge is a complementary persistent header signal. ✅

### Warnings (non-blocking)

- ⚠️ `20260603000000_add_items_updated_at_trigger.sql` (Phase 39) — bare `ADD COLUMN` / `CREATE TRIGGER`, not re-runnable. Bites a manual re-apply only; not a forward-migration break.
- ⚠️ `ItemCard.tsx:27` `dexieItemId` — dead prop from the Phase 34 refactor.

## Tech Debt by Phase

**Phase 40 — ai-proxy-cloud-run-migration (HIGHEST PRIORITY):**
- 🔴 **OPEN LANDMINE** — `../tpc-ai-proxy/.github/workflows/deploy.yml` missing `SUPABASE_URL`/`SUPABASE_ANON_KEY` and uses extension-only `ALLOWED_ORIGINS`. A push to proxy `main` crash-loops Cloud Run (config.js fail-closed). Proxy commits intentionally unpushed. **CI hardening required before pushing the proxy branch.** (Tracked in MEMORY.md.)

**Phase 31:** prod `profiles.theme` column missing (theme off on prod); `database.types.ts` stale vs prod (crm_* not synced).
**Phase 32:** `audioLookup.ts:60` zero-byte placeholder blob (cross-device); 5 warn + 4 info advisory follow-ups from 32-REVIEW.md.
**Phase 33:** D-12 manual re-save UI deferred (retryBuffer write-only).
**Phase 34:** PERF-2 continuous master-blob rework deferred (D-04/D-050); `ItemList.tsx` `a.id!` assertions (WR-01/IN-01).
**Phase 37:** meta-viewport `user-scalable=no` WCAG 1.4.4 product decision pending.
**Phase 38:** WR-04 cross-store non-atomicity duplicate vector (needs natural-key/upsert follow-up); `photoMigration.ts` house/sale ++id collision (itemTable discriminator) deferred.
**Phase 39:** WR-01 accepted design, WR-03/WR-04/WR-05 latent/low-sev accepted for v1.3.

**Total: ~14 items across 8 phases** (1 critical-risk landmine in a sibling repo, rest advisory/accepted).

## Milestone-End UAT — NOW DUE

Per project policy, several `human_needed` checks were deferred to v1.3 milestone-end.
Run before `/gsd:complete-milestone`:

- **Phase 39 HV-2 (HIGH RISK):** on-device user-edit vs AI-merge race — confirm no silent lost write.
- Phase 33 HV-1: 4-tab concurrent drain → zero duplicate Gemini calls.
- Phase 34 HV-1: iOS Safari on-device JS-heap memory smoke (PERF-1).
- Phase 35 HV-1: list-card failure row visual parity with detail banner.
- Phase 36 HV-1: live duplicate-receipt import rollback (no false-success nav).
- Phase 37 HV-1: authed keyboard-only record/edit/save flow.
- Phase 38 HV-1: live retry-after-partial migration — no duplicate rows.

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 31 | exists | false (draft) | `/gsd:validate-phase 31` |
| 32 | exists | true | — |
| 33 | exists | false (draft) | `/gsd:validate-phase 33` |
| 34 | exists | false (draft) | `/gsd:validate-phase 34` |
| 35 | exists | false (draft) | `/gsd:validate-phase 35` |
| 36 | exists | false (draft) | `/gsd:validate-phase 36` |
| 37 | exists | true (wave0 incomplete) | — |
| 38 | exists | true | — |
| 39 | exists | true | — |
| 40 | **missing** | — | `/gsd:validate-phase 40` |

Overall: **partial** — 5 phases have draft (non-compliant) VALIDATION.md, 1 missing.
Discovery only; no auto-validation run.
