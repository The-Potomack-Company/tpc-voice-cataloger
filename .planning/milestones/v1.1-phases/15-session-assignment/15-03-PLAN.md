---
phase: 15-session-assignment
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified: []
autonomous: false
requirements: [ASGN-01, ASGN-02, ASGN-03, ASGN-04]
must_haves:
  truths:
    - "Admin can select a specialist when creating a session"
    - "Specialist sees only their scoped sessions"
    - "Admin can reassign from session detail"
    - "Admin sees all sessions grouped by specialist with names and status badges"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete session assignment feature across all four requirements.

Purpose: Confirm the end-to-end flow works correctly with real Supabase auth and RLS policies before marking the phase complete.
Output: Verified session assignment feature.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-session-assignment/15-CONTEXT.md
@.planning/phases/15-session-assignment/15-01-SUMMARY.md
@.planning/phases/15-session-assignment/15-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify session assignment end-to-end</name>
  <files>src/pages/Sessions.tsx, src/pages/NewSession.tsx, src/pages/SessionDetail.tsx, src/components/SessionCard.tsx</files>
  <action>
    Run the dev server with `npm run dev` and present the 24-step verification checklist to the user.
    This is a human-verify checkpoint -- the executor starts the dev server and guides the user through verification.

    **As Admin (ASGN-01 + ASGN-04):**
    1. Log in as admin user
    2. Navigate to "New Session" tab
    3. Verify an "Assign To" dropdown appears below the Notes field
    4. Verify the dropdown lists all active accounts (admin + specialists), sorted alphabetically
    5. Verify "Start Session" button is disabled until a specialist is selected
    6. Select a specialist and create a session
    7. Navigate back to Sessions list
    8. Verify sessions are grouped by specialist name within each section (Active/Completed/Archived)
    9. Verify each session card shows "Assigned to {name}" and a colored status badge
    10. Verify specialist group headers are collapsible with chevron toggle
    11. Verify all groups are expanded by default

    **As Admin (ASGN-03):**
    12. Tap into a session to view SessionDetail
    13. Verify an "Assigned to: {name}" field appears below the badges row
    14. Tap the assignee name -- verify a dropdown opens inline
    15. Select a different specialist -- verify the update applies immediately (no confirmation)
    16. Navigate back to Sessions list -- verify the session now appears under the new specialist's group

    **As Specialist (ASGN-02):**
    17. Log out and log in as a specialist user
    18. Navigate to Sessions list
    19. Verify a flat list of sessions (no specialist grouping, no assignee names, no status badges)
    20. Verify only sessions assigned to this specialist or created by them are visible
    21. Navigate to "New Session" tab
    22. Verify NO "Assign To" dropdown is shown
    23. Create a session -- verify it appears in the sessions list
    24. Tap into SessionDetail -- verify NO assignee field is visible
  </action>
  <read_first>
    - src/pages/Sessions.tsx
    - src/pages/NewSession.tsx
    - src/pages/SessionDetail.tsx
    - src/components/SessionCard.tsx
  </read_first>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all 24 verification steps pass
    - No visual regressions in specialist view
    - No errors in browser console during normal operation
    - Full test suite still passes
  </acceptance_criteria>
  <done>Human has verified all 4 ASGN requirements work correctly with real Supabase auth and RLS policies.</done>
</task>

</tasks>

<verification>
All four ASGN requirements verified manually with real auth:
- ASGN-01: Admin assigns specialist at creation via dropdown
- ASGN-02: Specialist sees scoped view (RLS-enforced)
- ASGN-03: Admin reassigns from session detail (immediate, no confirmation)
- ASGN-04: Admin sees all sessions grouped by specialist with names and status
</verification>

<success_criteria>
- Human confirms all 24 verification steps pass
- No visual regressions in specialist view
- No errors in browser console during normal operation
</success_criteria>

<output>
After completion, create `.planning/phases/15-session-assignment/15-03-SUMMARY.md`
</output>
