---
phase: 16-session-lifecycle
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/session-lifecycle.test.tsx
  - src/tests/return-dialog.test.tsx
  - src/tests/use-user-role.test.ts
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-03, LIFE-04, LIFE-05, LIFE-06]

must_haves:
  truths:
    - "Test stubs exist for all six LIFE requirements and can be run without errors"
    - "Each stub test is marked as todo or skipped so the suite stays green while stubs are unfilled"
  artifacts:
    - path: "src/tests/session-lifecycle.test.tsx"
      provides: "Stub tests covering LIFE-01 through LIFE-06"
      contains: "describe"
    - path: "src/tests/return-dialog.test.tsx"
      provides: "Stub tests for ReturnDialog component"
      contains: "describe"
    - path: "src/tests/use-user-role.test.ts"
      provides: "Stub tests for useUserRole hook"
      contains: "describe"
  key_links: []
---

<objective>
Create stub test files for all Phase 16 lifecycle requirements so that downstream plans can reference specific test commands in their verify blocks.

Purpose: Satisfy Nyquist compliance -- every plan task needs an automated verify command that targets real test files. These stubs establish the test scaffolding that Plans 01 and 02 will fill in as they implement features.
Output: Three test stub files with todo/skip markers for each LIFE requirement.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-session-lifecycle/16-CONTEXT.md
@.planning/phases/16-session-lifecycle/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub test files for session lifecycle, ReturnDialog, and useUserRole</name>
  <files>src/tests/session-lifecycle.test.tsx, src/tests/return-dialog.test.tsx, src/tests/use-user-role.test.ts</files>
  <action>
Create three test stub files. Each test uses `it.todo()` so the suite stays green while stubs are unfilled. Plans 01 and 02 will fill in implementations as they build each feature.

**1. Create `src/tests/session-lifecycle.test.tsx`:**
```typescript
import { describe, it } from 'vitest';

describe('Session Lifecycle', () => {
  describe('submit', () => {
    it.todo('LIFE-01: specialist can submit an active session for review');
    it.todo('LIFE-01: submit button is disabled when items are queued');
    it.todo('LIFE-01: submit confirmation dialog shows session name and lock warning');
  });

  describe('read-only', () => {
    it.todo('LIFE-02: submitted session is read-only for specialist');
    it.todo('LIFE-02: specialist cannot add items to submitted session');
    it.todo('LIFE-02: specialist sees blue status banner on submitted session');
  });

  describe('admin edit', () => {
    it.todo('LIFE-03: admin can edit item fields on submitted sessions');
    it.todo('LIFE-03: admin is never locked by session status');
  });

  describe('return', () => {
    it.todo('LIFE-04: admin can return submitted session to specialist');
    it.todo('LIFE-04: return dialog allows optional review notes');
    it.todo('LIFE-04: returned session status changes to returned');
  });

  describe('review notes', () => {
    it.todo('LIFE-05: returned session shows sticky amber banner with review notes');
    it.todo('LIFE-05: banner displays admin review notes text');
    it.todo('LIFE-05: returned session is unlocked for specialist editing');
  });

  describe('export', () => {
    it.todo('LIFE-06: export button is visible only to admin');
    it.todo('LIFE-06: specialist does not see export button');
    it.todo('LIFE-06: export sets session status to exported');
  });
});
```

**2. Create `src/tests/return-dialog.test.tsx`:**
```typescript
import { describe, it } from 'vitest';

describe('ReturnDialog', () => {
  it.todo('renders modal with title and session name');
  it.todo('textarea accepts review notes');
  it.todo('confirm button calls onConfirm with notes text');
  it.todo('cancel button calls onCancel and clears notes');
  it.todo('does not render when open is false');
});
```

**3. Create `src/tests/use-user-role.test.ts`:**
```typescript
import { describe, it } from 'vitest';

describe('useUserRole', () => {
  it.todo('returns admin role when profile has role admin');
  it.todo('returns specialist role when profile has role specialist');
  it.todo('returns isAdmin true for admin role');
  it.todo('returns isAdmin false for specialist role');
  it.todo('returns loading true initially, then false after query');
  it.todo('returns null role when no user is logged in');
});
```

After creating all three files, update `16-VALIDATION.md` frontmatter: set `wave_0_complete: true` and `nyquist_compliant: true`.
  </action>
  <verify>
    <automated>npx vitest run src/tests/session-lifecycle.test.tsx src/tests/return-dialog.test.tsx src/tests/use-user-role.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Three stub test files exist with todo markers for all LIFE-01 through LIFE-06 requirements. Suite runs green (todo tests are skipped). VALIDATION.md updated with wave_0_complete: true.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/tests/session-lifecycle.test.tsx src/tests/return-dialog.test.tsx src/tests/use-user-role.test.ts` passes (all todo tests are skipped, zero failures)
- All three test files exist in `src/tests/`
- VALIDATION.md has `wave_0_complete: true`
</verification>

<success_criteria>
- Stub test files exist for session-lifecycle, return-dialog, and use-user-role
- All todo tests appear in vitest output as skipped/todo
- Suite remains green (zero failures)
- VALIDATION.md wave_0_complete flag is set to true
</success_criteria>

<output>
After completion, create `.planning/phases/16-session-lifecycle/16-00-SUMMARY.md`
</output>
