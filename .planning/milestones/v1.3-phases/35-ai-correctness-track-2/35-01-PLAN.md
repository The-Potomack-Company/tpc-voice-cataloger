---
phase: 35-ai-correctness-track-2
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/db/types.ts
  - src/db/index.ts
  - src/tests/gemini-determinism.test.ts
  - src/tests/gemini-confab-guard.test.ts
  - src/tests/gemini-no-clobber.test.ts
  - src/tests/item-card-ai-failure.test.tsx
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]

must_haves:
  truths:
    - "Dexie v11 userEditedFields table exists and is queryable by itemId"
    - "Four RED test files exist, one per success criterion, each failing for the right reason (behavior absent, not import error)"
    - "SC-3 test can seed and read db.userEditedFields without a schema error"
    - "Decisions covered: D-05 (per-field user-edited provenance stored client-side in Dexie, not Supabase)"
  artifacts:
    - path: "src/db/index.ts"
      provides: "Dexie v11 schema with userEditedFields store"
      contains: "db.version(11)"
    - path: "src/db/types.ts"
      provides: "UserEditedField interface"
      contains: "interface UserEditedField"
    - path: "src/tests/gemini-determinism.test.ts"
      provides: "SC-1 RED test"
      min_lines: 40
    - path: "src/tests/gemini-confab-guard.test.ts"
      provides: "SC-2 RED test"
      min_lines: 40
    - path: "src/tests/gemini-no-clobber.test.ts"
      provides: "SC-3 RED test"
      min_lines: 40
    - path: "src/tests/item-card-ai-failure.test.tsx"
      provides: "SC-4 RED test"
      min_lines: 30
  key_links:
    - from: "src/db/index.ts"
      to: "src/db/types.ts"
      via: "UserEditedField import + EntityTable handle"
      pattern: "userEditedFields"
---

<objective>
Lay the Wave-0 foundation for Phase 35: the Dexie v11 `userEditedFields` provenance store (a hard prerequisite for the SC-3 no-clobber test to even run) plus the four RED test files — one per success criterion (SC-1 determinism, SC-2 confab guard, SC-3 no-clobber, SC-4 list-card failure row).

Purpose: TDD RED phase. These tests encode the exact behavior contracts the Wave-1/Wave-2 implementation plans must satisfy. Each test MUST fail because the behavior is absent, NOT because of an import/compile error. The v11 migration ships here (not in an implementation plan) because SC-3's test seeds `db.userEditedFields` directly and cannot run without the store.

Output: `src/db/types.ts` (UserEditedField interface), `src/db/index.ts` (v11 store), and four failing test files in `src/tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/35-ai-correctness-track-2/35-RESEARCH.md
@.planning/milestones/v1.3-phases/35-ai-correctness-track-2/35-PATTERNS.md
@.planning/milestones/v1.3-phases/35-ai-correctness-track-2/35-VALIDATION.md

<interfaces>
<!-- Existing contracts the executor builds against. Do not re-explore. -->

From src/db/index.ts (live):
- `import Dexie, { type EntityTable } from "dexie";`
- typed-handle cast block at lines 16-28 (each store is `EntityTable<Type, "primaryKey">`)
- version chain ends at `db.version(10).stores({...})` (lines 134-147); new-table bumps repeat all prior stores verbatim and add one line — NO `.upgrade()` (contrast v2 at :38 which reshapes data)
- compound-key analog: `sessionAudio: EntityTable<SessionAudio, "sessionId">` (line 22), store string `"sessionId, updatedAt"`

From src/db/types.ts (live):
- interface convention example `ItemAudio` (~line 65)

From src/tests/gemini-pipeline.test.ts (template for the 3 gemini tests — lines 1-103, 146-169):
- `vi.hoisted(() => ({ mockFrom, mockUpdate, mockEq, mockSelect, mockSingle, mockGetSession, mockRefreshSession }))`
- `vi.mock("../lib/supabase", ...)`, `vi.mock("../stores/sessionStore", () => ({ useSessionStore: { getState: () => ({ fetchItems: vi.fn().mockResolvedValue(undefined) }) } }))`
- `vi.stubEnv("VITE_GEMINI_PROXY_URL", "https://test-proxy.example.com/api")`
- `mockGeminiResponse(fields)` wraps fields in `{ candidates:[{ content:{ parts:[{ text: JSON.stringify(fields) }] } }] }`
- `createMockFrom({ updateCalls, existingItem })` captures every `.update()` payload into `updateCalls` (lines 146-169)
- `beforeEach` seeds `db.audio.add(...)` → `testAudioId`; call shape `await processAudioWithAi(testAudioId, "item-uuid-1", "session-uuid-1")`
- the pipeline always writes `{ ai_status: "processing" }` FIRST (updateCalls[0]) — every test must account for that leading write
- Dexie is REAL in this suite (fake-indexeddb)

From src/tests/item-card-audio-status.test.tsx (template for SC-4 — lines 5-83):
- `vi.mock("../services/gemini", () => ({ processAudioWithAi: vi.fn() }))`
- `vi.mock("dexie-react-hooks", () => ({ useLiveQuery: (fn) => { try { return fn(); } catch { return undefined; } } }))`
- `vi.mock("../db/items", () => ({ updateItemField: vi.fn(), deleteItem: vi.fn() }))`
- mocks for `useAudioUploadStatus`, `audioLookup`, `idMapping`, `db`
- `makeItem(overrides)` factory + `renderCard(item)` wrapper (MemoryRouter, props: sessionId, isExpanded, onToggle, audioCount, latestAudioId=10, photoCount, dexieItemId=10, isPending)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dexie v11 userEditedFields store + UserEditedField type</name>
  <files>src/db/types.ts, src/db/index.ts</files>
  <read_first>
    - src/db/types.ts (read the ItemAudio interface ~:65 for the convention)
    - src/db/index.ts (read the full file: the EntityTable import :1, the typed-handle cast block :16-28, and the entire version() chain — you must copy v10's store map verbatim into v11)
  </read_first>
  <action>
    Add to src/db/types.ts a `UserEditedField` interface per D-05 / Pitfall 1: `itemId: string` (the Supabase UUID — explicitly NOT the integer Dexie id; add a WHY-comment noting it is the same value updateItemField/processAudioWithAi pass) and `field: string`.

    In src/db/index.ts: (1) add `UserEditedField` to the type import from "./types"; (2) add `userEditedFields: EntityTable<UserEditedField, "itemId">;` to the typed-handle cast block (mirror the `sessionAudio` compound-store handle at :22); (3) append a new `db.version(11).stores({...})` AFTER the v10 block — repeat ALL eleven v10 stores unchanged and add the line `userEditedFields: "[itemId+field], itemId",` (compound primary key `[itemId+field]` plus secondary index `itemId`). Do NOT add an `.upgrade()` callback — new empty table, absence of a flag === field not user-edited.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -v "^$" | head; npx vitest --run src/tests/db-version.test.ts 2>/dev/null || echo "no db-version test — typecheck is the gate"</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/types.ts` exports `interface UserEditedField { itemId: string; field: string }`
    - `db.version(11)` exists in src/db/index.ts and its store map contains all 11 v10 stores plus `userEditedFields: "[itemId+field], itemId"`
    - No `.upgrade()` attached to v11
    - `db.userEditedFields` is a typed handle (`EntityTable<UserEditedField, "itemId">`) — `npx tsc --noEmit -p tsconfig.app.json` reports no new errors
  </acceptance_criteria>
  <done>v11 migration compiles; `db.userEditedFields` is queryable and typed; no upgrade callback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED tests SC-1 (determinism) and SC-4 (list-card failure row)</name>
  <files>src/tests/gemini-determinism.test.ts, src/tests/item-card-ai-failure.test.tsx</files>
  <read_first>
    - src/tests/gemini-pipeline.test.ts (the full harness — copy the hoisted-mock block, mockGeminiResponse, createMockFrom, and beforeEach verbatim)
    - src/tests/item-card-audio-status.test.tsx (the full component-test harness — copy the vi.mock set, makeItem, renderCard)
    - src/pages/ItemEntry.tsx lines 33-88 (the AiFailureBanner markup SC-4 asserts against: role="alert", "AI processing failed" copy, Retry control)
  </read_first>
  <behavior>
    SC-1 (gemini-determinism.test.ts):
    - Test A: spy globalThis.fetch; run processAudioWithAi once; parse the captured request body; assert `JSON.parse(body).payload.generationConfig.temperature === 0`. (RED now: temperature key absent.)
    - Test B: run processAudioWithAi twice with the SAME mocked Gemini response; assert the two final `updateCalls` catalog payloads are deeply equal (deterministic snapshot). Account for the leading `{ai_status:"processing"}` write.
    SC-4 (item-card-ai-failure.test.tsx):
    - Test A: renderCard(makeItem({ ai_status: "failed" })) with latestAudioId=10 → `screen.getByRole("alert")` has text matching /AI processing failed/i AND a Retry control is present. (RED now: card only renders the terse Badge.)
    - Test B: renderCard(makeItem({ ai_status: "done" })) → `screen.queryByRole("alert")` is null.
    - Set `mockUseAudioUploadStatus.mockReturnValue("none")` so the audio pill does not confuse the alert query.
  </behavior>
  <action>
    Create both files copying the respective analog harness verbatim, then add the behavior tests above. These are RED — they MUST fail because the behavior is missing (temperature key absent in the proxy body; ItemCard has no role="alert" row), NOT because of an import or mock error. Confirm the failure reason in each run output. Do NOT use `.skip` or `.todo`. The temperature assertion drills into `payload.generationConfig.temperature` — verify the proxy body shape matches gemini.ts (the request posts `{ model, payload }` and payload carries generationConfig).
  </action>
  <verify>
    <automated>npx vitest --run src/tests/gemini-determinism.test.ts src/tests/item-card-ai-failure.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and import cleanly (no module-resolution / mock-wiring errors in the run output)
    - gemini-determinism.test.ts FAILS on the `temperature === 0` assertion (key absent) — not on a thrown setup error
    - item-card-ai-failure.test.tsx FAILS on the missing `role="alert"` row for ai_status="failed"; the ai_status="done" absence case PASSES (already absent)
    - No `.skip`/`.todo`
  </acceptance_criteria>
  <done>SC-1 and SC-4 tests are RED for the correct (behavior-absent) reason; harness wiring is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED tests SC-2 (confab guard) and SC-3 (no-clobber)</name>
  <files>src/tests/gemini-confab-guard.test.ts, src/tests/gemini-no-clobber.test.ts</files>
  <read_first>
    - src/tests/gemini-pipeline.test.ts (harness — same as Task 2)
    - src/services/gemini.ts lines 319-405 (the write-back block + catch path the guard/filter will modify — so the test asserts on the right write shape)
    - src/db/index.ts (confirm db.userEditedFields exists from Task 1 — SC-3 seeds it directly)
  </read_first>
  <behavior>
    SC-2 (gemini-confab-guard.test.ts):
    - Case A: mockGeminiResponse({ transcript: null, title: "Oak table", estimate: "300", ...other fields }); run; assert the captured updateCalls are exactly `[{ai_status:"processing"}, {ai_status:"failed"}]` — NO updateCall has a `title` or `estimate` (or any catalog) key.
    - Case B: same with `transcript: "   "` (whitespace) → same assertion.
    - Assert the `ai.processing_failed` analytics event path is reached (the failed write is the only catalog-touching write).
    SC-3 (gemini-no-clobber.test.ts):
    - Case A (survives retry): `await db.userEditedFields.put({ itemId: "item-uuid-1", field: "title" })`; use createMockFrom({ existingItem: {non-null fields} }) so hasExistingData is true; pass isRetry=true; mock response with new title + new description; assert the final updateCall has NO `title` key but HAS `description`.
    - Case B (fresh clears flags): seed a flag; run a FRESH (isRetry=false / non-retry) extraction success; assert `await db.userEditedFields.where("itemId").equals("item-uuid-1").toArray()` is empty afterward.
    - beforeEach: `await db.userEditedFields.clear()` to isolate cases.
  </behavior>
  <action>
    Create both files from the gemini-pipeline harness. SC-3 seeds and reads the REAL db.userEditedFields (fake-indexeddb). Write the SC-3 test against the FINAL intended signature `processAudioWithAi(audioId, itemId, sessionId, isRetry?)` — pass the 4th arg explicitly in the retry case and omit it (or pass false) in the fresh case; this encodes the O-1 resolution (explicit isRetry param, not the hasExistingData proxy). These are RED: confab fields currently persist; flagged fields currently get clobbered; flags currently never clear. They MUST fail on behavior, not setup. No `.skip`/`.todo`.
  </action>
  <verify>
    <automated>npx vitest --run src/tests/gemini-confab-guard.test.ts src/tests/gemini-no-clobber.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Both files import cleanly and `db.userEditedFields` reads/writes succeed (Task 1 store present)
    - confab-guard FAILS because catalog fields ARE currently written on empty transcript (title/estimate present in updateCalls)
    - no-clobber Case A FAILS because `title` IS currently present in the final updateCall despite the flag
    - no-clobber Case B FAILS because flags are NOT currently cleared on fresh success
    - SC-3 calls processAudioWithAi with an explicit 4th `isRetry` arg in the retry case
    - No `.skip`/`.todo`
  </acceptance_criteria>
  <done>SC-2 and SC-3 tests are RED for behavior-absent reasons; Dexie provenance reads/writes work against the v11 store.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini model output → client | Model-produced JSON is untrusted; validated by Zod (existing) and, downstream, the confab guard (Wave 2). This plan only writes tests + a client-local store. |
| Client → Dexie (userEditedFields) | Client-local, browser-trusted store. Tamperable but only affects which fields a *local* retry skips — never server authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | Dexie userEditedFields (client-local) | accept | Provenance only gates local no-clobber on retry; no server trust placed in it. Documented accepted risk (RESEARCH Security Domain). |
| T-35-02 | Information Disclosure | userEditedFields rows | accept | Stores only `{itemId, field}` (field NAME, not value) — no PII, no secrets. |
| T-35-SC | Tampering | npm/pip/cargo installs | mitigate | No installs this phase — all libs pre-pinned (RESEARCH Package Legitimacy Audit: N/A). No new dependency added. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.app.json` reports no new errors (v11 store + UserEditedField type compile)
- `npx vitest --run src/tests/gemini-*.test.ts src/tests/item-card-ai-failure.test.tsx` shows 4 files, all RED for behavior-absent reasons (no setup/import failures)
- `grep -c "db.version(11)" src/db/index.ts` returns 1
- `grep -c "\.skip\|\.todo" src/tests/gemini-determinism.test.ts src/tests/gemini-confab-guard.test.ts src/tests/gemini-no-clobber.test.ts src/tests/item-card-ai-failure.test.tsx` returns 0
</verification>

<success_criteria>
- Dexie v11 `userEditedFields` store exists, typed, no upgrade callback; SC-3 test can seed/read it
- Four RED test files exist, one per SC, each failing for the correct behavior-absent reason
- No new npm dependency; no Supabase schema change; no `updated_at`; no `seed`
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/35-ai-correctness-track-2/35-01-SUMMARY.md` when done.
</output>
