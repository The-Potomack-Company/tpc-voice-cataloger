---
phase: 35-ai-correctness-track-2
plan: 03
type: tdd
wave: 1
depends_on: ["35-01"]
files_modified:
  - src/components/AiFailureBanner.tsx
  - src/components/ItemCard.tsx
  - src/pages/ItemEntry.tsx
autonomous: true
requirements: [SC-4]

must_haves:
  truths:
    - "A failed item card shows a full-width inline row with an icon, 'AI processing failed' copy, and a Retry control"
    - "The failure row renders only when ai_status === 'failed'"
    - "The detail-view banner and the list-card row share one component (visual parity)"
    - "Retry on the card reuses the existing processAudioWithAi handler"
  artifacts:
    - path: "src/components/AiFailureBanner.tsx"
      provides: "Shared failure-row component (role=alert, Retry)"
      min_lines: 30
    - path: "src/components/ItemCard.tsx"
      provides: "Inline failure row gated on isFailed"
      contains: "AiFailureBanner"
  key_links:
    - from: "src/components/ItemCard.tsx"
      to: "src/components/AiFailureBanner.tsx"
      via: "import + render when isFailed"
      pattern: "AiFailureBanner"
    - from: "src/pages/ItemEntry.tsx"
      to: "src/components/AiFailureBanner.tsx"
      via: "import (replaces local function)"
      pattern: "AiFailureBanner"
---

<objective>
Promote the terse `<Badge tone="err">Failed</Badge>` on the item card (ItemCard.tsx:199) to a full-width inline failure row — icon + "AI processing failed" copy + Retry CTA — that mirrors the detail-view `AiFailureBanner` (D-07), reusing the card's existing `handleRetryAi`/`processAudioWithAi` wiring (D-08). To guarantee list/detail visual parity, LIFT the local `AiFailureBanner` out of `ItemEntry.tsx` into a shared `src/components/AiFailureBanner.tsx` consumed by both. Renders only when `ai_status === "failed"`.

Purpose: a failed AI extraction must be visible in the list view, not just on the detail page — list/detail consistency was the explicit goal (CONTEXT).

Output: shared `AiFailureBanner.tsx`; `ItemEntry.tsx` imports it (deletes its local copy); `ItemCard.tsx` renders it gated on `isFailed`. Turns SC-4 green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.3-phases/35-ai-correctness-track-2/35-RESEARCH.md
@.planning/milestones/v1.3-phases/35-ai-correctness-track-2/35-PATTERNS.md

<interfaces>
From src/pages/ItemEntry.tsx (live, the source banner to lift — lines 28-88):
- local `function AiFailureBanner({ itemId, sessionId })`; derives `latestAudioId` itself via `useLiveQuery(audioRecordsForItem)`; `if (latestAudioId == null) return null;`
- markup: `<div role="alert" className="flex items-center justify-between gap-3 rounded-lg border border-err bg-err-wash px-3 py-2 text-sm" style={{ color: "var(--err)" }}>` containing a `tpc-status-dot tpc-status-dot-err` span + `<span className="font-medium truncate">AI processing failed</span>`, and a Retry `<button>` with the spinner SVG and `{retrying ? "Retrying" : "Retry"}`
- internal `handleRetry` calls `processAudioWithAi(latestAudioId, itemId, sessionId)`

From src/components/ItemCard.tsx (live):
- receives `latestAudioId: number | null` as a PROP (:23) — already available, no useLiveQuery needed on the card
- `isFailed = item.ai_status === "failed"` (:53), `retrying` state (:51), `handleRetryAi` (:59-68) already wired to `processAudioWithAi(latestAudioId, item.id, sessionId)`
- terse badge to replace/augment at `{isFailed && <Badge tone="err">Failed</Badge>}` (line ~199 — verify live before editing)
- imports `processAudioWithAi` from "../services/gemini" (:8)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared AiFailureBanner component and wire ItemEntry to it</name>
  <files>src/components/AiFailureBanner.tsx, src/pages/ItemEntry.tsx</files>
  <read_first>
    - src/pages/ItemEntry.tsx lines 1-90 (the local AiFailureBanner function, its imports useLiveQuery/audioRecordsForItem/processAudioWithAi/useState, and its single call site further down — grep for `<AiFailureBanner`)
    - src/components/ItemCard.tsx lines 1-70 (to match the prop-based latestAudioId convention the card uses)
  </read_first>
  <behavior>
    - The shared component renders the same role="alert" markup, "AI processing failed" copy, status dot, and Retry button as the current detail banner
    - It accepts `latestAudioId` as a PROP (so the card passes its prop and the detail view passes its own useLiveQuery result) plus `itemId`, `sessionId`
    - Returns null when latestAudioId == null (unchanged null-guard)
    - Retry calls processAudioWithAi with isRetry=true (see action — O-1)
  </behavior>
  <action>
    Create src/components/AiFailureBanner.tsx exporting `AiFailureBanner({ itemId, sessionId, latestAudioId })`. Move the markup verbatim from ItemEntry's local function (the `role="alert"` div, `border-err`/`bg-err-wash`, `tpc-status-dot-err`, "AI processing failed" copy, the spinner-SVG Retry button, `style={{ color: "var(--err)" }}`) — match the `err` token palette exactly (CONTEXT Discretion). Keep the local `retrying` state and the `if (latestAudioId == null) return null;` guard. The component takes `latestAudioId` as a prop (NOT a useLiveQuery) so both callers can supply their own source.

    In the Retry handler, call `processAudioWithAi(latestAudioId, itemId, sessionId, true)` — pass the 4th `isRetry=true` arg (this is the explicit retry signal resolving O-1; the 4th param is added in Plan 04, optional with default false, so this is forward-compatible and harmless until then).

    In src/pages/ItemEntry.tsx: delete the local `AiFailureBanner` function (lines ~28-88), import the shared one from "../components/AiFailureBanner", and at its existing render site pass `latestAudioId` from the page's own `useLiveQuery(audioRecordsForItem)` source (keep that query in ItemEntry; move only the presentational component). Remove now-unused imports (useState/processAudioWithAi) ONLY if nothing else in ItemEntry uses them — grep first.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json 2>&1 | head; npx vitest --run src/tests/item-card-ai-failure.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/AiFailureBanner.tsx` exists, exports `AiFailureBanner`, accepts `latestAudioId` as a prop, renders `role="alert"` + "AI processing failed" + a Retry button, returns null when latestAudioId is null
    - Retry calls `processAudioWithAi(latestAudioId, itemId, sessionId, true)`
    - `src/pages/ItemEntry.tsx` no longer defines a local `AiFailureBanner` (`grep -c "function AiFailureBanner" src/pages/ItemEntry.tsx` === 0) and imports the shared component
    - `npx tsc --noEmit -p tsconfig.app.json` reports no new errors
  </acceptance_criteria>
  <done>Shared AiFailureBanner exists; ItemEntry consumes it; detail view unchanged visually; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the failure row on ItemCard when ai_status is failed</name>
  <files>src/components/ItemCard.tsx</files>
  <read_first>
    - src/components/ItemCard.tsx (full — the JSX body, the terse `<Badge tone="err">Failed</Badge>` site ~:199, the `isFailed`/`retrying`/`handleRetryAi` already present at :51-68, and the `latestAudioId` prop at :23)
    - src/components/AiFailureBanner.tsx (the shared component from Task 1 — its props)
    - src/tests/item-card-ai-failure.test.tsx (the SC-4 test this turns green)
  </read_first>
  <behavior>
    - When `ai_status === "failed"`, the card renders the full-width AiFailureBanner row (role=alert, "AI processing failed", Retry) — replacing the terse Failed badge
    - When ai_status is any other value, no alert row renders
    - Retry on the card uses the existing processAudioWithAi path (D-08), threading isRetry=true
  </behavior>
  <action>
    In src/components/ItemCard.tsx, import `AiFailureBanner` from "./AiFailureBanner". Replace the terse `{isFailed && <Badge tone="err">Failed</Badge>}` (verify live line before editing) with `{isFailed && <AiFailureBanner itemId={item.id} sessionId={sessionId} latestAudioId={latestAudioId} />}`. Pass the card's existing `latestAudioId` prop. Since the shared component owns its own retrying state + retry handler, the card's now-redundant `handleRetryAi`/`retrying` may remain if still referenced elsewhere — do NOT delete them unless grep confirms no other use (e.g. a separate retry affordance). Update the `isRetry` threading: the shared component already passes `true` (Task 1). Remove the `Badge` import only if no other Badge usage remains in the file (grep first).

    Placement: render the row full-width within the card body where the badge previously sat, matching the detail-view layout (full-width inline). Match the `err` token palette via the shared component (no inline divergence).
  </action>
  <verify>
    <automated>npx vitest --run src/tests/item-card-ai-failure.test.tsx 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AiFailureBanner" src/components/ItemCard.tsx` >= 1 (import + render)
    - The terse `<Badge tone="err">Failed</Badge>` failed-state render is gone (`grep -c 'tone="err">Failed' src/components/ItemCard.tsx` === 0)
    - SC-4 test green: `getByRole("alert")` with /AI processing failed/i present for ai_status="failed"; `queryByRole("alert")` null for ai_status="done"
    - No new tsc errors
  </acceptance_criteria>
  <done>Failed card renders the shared full-width failure row; SC-4 green; reuses existing retry plumbing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Item AI status → UI render | `ai_status` is server-authored (set by the pipeline). The card only reads it to gate a presentational row. No untrusted input crosses into logic here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Information Disclosure | failure-row copy | accept | The row shows a generic "AI processing failed" string — no error internals, no user data leaked. |
| T-35-05 | Denial of Service | Retry button | accept | Retry is guarded by the existing `retrying` flag + `latestAudioId == null` null-guard (carried from the source banner); no new unbounded-trigger surface. |
| T-35-SC | Tampering | npm installs | mitigate | No installs this phase — all libs pre-pinned. |
</threat_model>

<verification>
- `npx vitest --run src/tests/item-card-ai-failure.test.tsx` green
- `grep -c "function AiFailureBanner" src/pages/ItemEntry.tsx` === 0 (local copy lifted)
- `grep -c "AiFailureBanner" src/components/ItemCard.tsx` >= 1
- `npx tsc --noEmit -p tsconfig.app.json` no new errors
- Manual (deferred to milestone-end UAT per STATE policy): list-card row visually matches the detail banner (icon, copy, Retry, token palette)
</verification>

<success_criteria>
- Failed item card shows a full-width inline failure row mirroring the detail banner, gated on ai_status==="failed"
- One shared component backs both list and detail views (parity)
- Retry reuses existing processAudioWithAi (D-08), threading isRetry=true
- SC-4 test green
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/35-ai-correctness-track-2/35-03-SUMMARY.md` when done.
</output>
