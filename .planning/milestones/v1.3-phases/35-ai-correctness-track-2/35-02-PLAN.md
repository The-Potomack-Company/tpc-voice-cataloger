---
phase: 35-ai-correctness-track-2
plan: 02
type: tdd
wave: 1
depends_on: ["35-01"]
files_modified:
  - src/services/gemini.ts
  - src/services/geminiContinuous.ts
autonomous: true
requirements: [SC-1]

must_haves:
  truths:
    - "Single-shot path sends temperature:0 in generationConfig"
    - "Continuous path sends temperature:0 in generationConfig"
    - "Two identical mocked single-shot runs produce identical catalog write payloads"
  artifacts:
    - path: "src/services/gemini.ts"
      provides: "temperature:0 in single-shot generationConfig"
      contains: "temperature: 0"
    - path: "src/services/geminiContinuous.ts"
      provides: "temperature:0 in continuous generationConfig"
      contains: "temperature: 0"
  key_links:
    - from: "src/services/gemini.ts"
      to: "Gemini proxy request body"
      via: "generationConfig.temperature"
      pattern: "temperature: 0"
    - from: "src/services/geminiContinuous.ts"
      to: "Gemini proxy request body"
      via: "generationConfig.temperature"
      pattern: "temperature: 0"
---

<objective>
Make Gemini extraction deterministic by setting `temperature: 0` (greedy decoding) in the `generationConfig` of BOTH AI paths — single-shot `gemini.ts` and continuous `geminiContinuous.ts` (D-01). No `seed`, no `topP`/`topK` changes (D-02). This turns the SC-1 determinism test GREEN.

Purpose: identical input → identical output is the precondition for the confab guard and no-clobber filter (Wave 2) to behave predictably, and for snapshot-based regression.

Output: a one-line addition to each path's existing `generationConfig` object.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.3-phases/35-ai-correctness-track-2/35-RESEARCH.md
@.planning/milestones/v1.3-phases/35-ai-correctness-track-2/35-PATTERNS.md

<interfaces>
From src/services/gemini.ts (live, verified):
- `generationConfig` object at lines 267-277: currently `{ responseMimeType: "application/json", responseSchema: (()=>{...})() }`

From src/services/geminiContinuous.ts (live, verified):
- `generationConfig` object at lines 165-168: currently `{ responseMimeType: "application/json", responseSchema: responseSchemaForGemini() }`

Note: CONTEXT cites stale lines (:249, :160); the live verified lines are :267 and :165 respectively.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add temperature:0 to both generationConfig objects</name>
  <files>src/services/gemini.ts, src/services/geminiContinuous.ts</files>
  <read_first>
    - src/services/gemini.ts lines 260-280 (the generationConfig object — confirm the live line before editing; do not trust CONTEXT's :249)
    - src/services/geminiContinuous.ts lines 160-170 (the generationConfig object — confirm live line; do not trust CONTEXT's :160)
    - src/tests/gemini-determinism.test.ts (the SC-1 test this turns GREEN — read what payload shape it asserts)
  </read_first>
  <behavior>
    - Single-shot: the proxy request body's `payload.generationConfig.temperature === 0`
    - Continuous: same temperature:0 present in its generationConfig
    - Two identical mocked single-shot runs → identical final catalog write payload (SC-1 Test B)
  </behavior>
  <action>
    In src/services/gemini.ts, add `temperature: 0,` as the FIRST key of the existing `generationConfig` object (per D-01, greedy decoding for deterministic snapshots). Add a brief WHY-comment referencing D-01. Do NOT add `seed`, `topP`, or `topK` (D-02).

    In src/services/geminiContinuous.ts, add the identical `temperature: 0,` first key to its `generationConfig` object. D-01 applies to BOTH paths. (The confab guard and no-clobber filter are NOT in this plan — they are Wave 2; do not touch the write-back/catch regions here.)
  </action>
  <verify>
    <automated>npx vitest --run src/tests/gemini-determinism.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "temperature: 0" src/services/gemini.ts` returns exactly 1 hit inside the generationConfig object
    - `grep -n "temperature: 0" src/services/geminiContinuous.ts` returns exactly 1 hit inside the generationConfig object
    - `grep -c "seed\|topP\|topK" src/services/gemini.ts src/services/geminiContinuous.ts` shows no new seed/topP/topK keys added
    - `src/tests/gemini-determinism.test.ts` is GREEN (temperature assertion + identical-runs snapshot both pass)
  </acceptance_criteria>
  <done>Both paths send temperature:0; SC-1 determinism test green; no seed/topP/topK added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Gemini proxy | Request-time sampling param. Proxy forwards `{model, payload}` verbatim (RESEARCH A1); temperature is request-only, holds no persistent state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03 | Tampering | generationConfig payload | accept | A client-set sampling param; lowering temperature reduces output variance, does not alter trust posture. No injection surface (the prompt build + SEC-5 sanitization at gemini.ts:104 are untouched by this plan). |
| T-35-SC | Tampering | npm installs | mitigate | No installs this phase — all libs pre-pinned. |
</threat_model>

<verification>
- `npx vitest --run src/tests/gemini-determinism.test.ts` green
- temperature:0 present in both generationConfig objects; no seed/topP/topK introduced
- The SEC-5 `sanitizeForDataBlock` call and system-prompt DATA-vs-INSTRUCTIONS rule remain untouched (this plan only edits generationConfig)
</verification>

<success_criteria>
- Both AI paths set temperature:0; SC-1 test green
- D-02 honored: no seed, no topP/topK
- No write-back/confab/no-clobber code touched (those are Wave 2)
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/35-ai-correctness-track-2/35-02-SUMMARY.md` when done.
</output>
