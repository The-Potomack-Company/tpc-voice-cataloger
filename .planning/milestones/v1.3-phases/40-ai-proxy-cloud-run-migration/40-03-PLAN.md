---
phase: 40-ai-proxy-cloud-run-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 40-02
files_modified:
  - proxy/
  - proxy/wrangler.toml
  - proxy/package.json
  - proxy/src/index.ts
  - .env.example
  - src/tests/gemini-pipeline.test.ts
  - src/tests/geminiContinuous.test.ts
autonomous: true
requirements: []   # cross-app infra (D-056); governed by D-013, D-049
must_haves:
  truths:
    - "The in-repo Cloudflare Worker (proxy/) no longer exists in the repo"
    - "No wrangler / tpc-gemini-proxy workspace or config bits remain in the repo"
    - "The proxy-URL tests reference the Cloud Run URL, not the retired Worker"
    - "Retirement is a single isolated commit so rollback = repoint VITE_GEMINI_PROXY_URL back"
    - "The cataloger build + full test suite pass after removal"
  artifacts:
    - path: ".env.example"
      provides: "no stale Worker/localhost:8787 proxy reference"
      contains: "tpc-ai-proxy-prod"
  key_links:
    - from: "repo"
      to: "Cloud Run proxy"
      via: "Worker removed; Cloud Run is the only AI proxy path"
      pattern: "VITE_GEMINI_PROXY_URL"
---

<objective>
Retire the in-repo Cloudflare Worker as the FINAL step of the migration (D-04),
GATED on the Cloud Run path being verified in prod (Plan 02 must have reported
"cutover-verified"). Delete `proxy/` and all `wrangler` / `tpc-gemini-proxy` bits,
repoint the proxy-URL tests off the retired Worker, and keep it all in ONE isolated
commit so rollback is a clean revert (rollback = repoint VITE_GEMINI_PROXY_URL back).

Purpose: Remove the now-dormant Worker so Cloud Run is the single AI proxy path,
without a post-soak follow-up (D-04 chose in-phase retirement).
Output: `proxy/` deleted, repo free of wrangler/tpc-gemini-proxy, tests + .env.example
updated, build + suite green, isolated retirement commit.

GATE: do NOT execute until Plan 02's SUMMARY records "Cloud Run path VERIFIED IN PROD".
If that gate is not met, STOP and report — retiring the Worker before prod is verified
removes the rollback target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3-phases/40-ai-proxy-cloud-run-migration/40-CONTEXT.md
@.planning/milestones/v1.3-phases/40-ai-proxy-cloud-run-migration/40-02-SUMMARY.md

<interfaces>
<!-- What "the Worker" physically is in this repo (discovered during planning):
     - proxy/ is a SELF-CONTAINED sub-package: proxy/package.json (name
       "tpc-gemini-proxy"), proxy/node_modules, proxy/wrangler.toml,
       proxy/src/index.ts, proxy/tsconfig.json, proxy/vitest.config.ts.
     - It is NOT a root npm workspace (root package.json has no "workspaces" key
       and no proxy/wrangler scripts). There is NO root wrangler.toml.
     - No CI (.github/) or vite.config reference to proxy/ or wrangler.
     => Retirement is a clean directory delete + test/env repoint. No root
        package.json or CI surgery needed. -->

Proxy-URL test files to repoint (D-04):
- src/tests/gemini-pipeline.test.ts line ~42 stubs `VITE_GEMINI_PROXY_URL` =
  "https://test-proxy.example.com/api"; line ~299 tests the unset->fail-closed path.
- src/tests/geminiContinuous.test.ts line ~147 stubs `VITE_GEMINI_PROXY_URL` =
  "https://proxy.test".
These stubs are arbitrary placeholders; repoint them to a Cloud-Run-shaped URL for
clarity/consistency. The fail-closed (unset) test must remain.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the in-repo Worker (proxy/) and all wrangler/tpc-gemini-proxy bits</name>
  <files>proxy/, proxy/wrangler.toml, proxy/package.json, proxy/src/index.ts</files>
  <read_first>
    - .planning/milestones/v1.3-phases/40-ai-proxy-cloud-run-migration/40-02-SUMMARY.md (CONFIRM "Cloud Run path VERIFIED IN PROD" gate before deleting — abort if absent)
    - proxy/package.json (name "tpc-gemini-proxy" — the workspace bits to drop)
    - the planning note in <interfaces> above (proxy/ is self-contained; no root wrangler.toml, no CI reference)
  </read_first>
  <action>
    First re-verify the Plan 02 gate: the 40-02-SUMMARY must state the Cloud Run path is
    verified in prod. If it does not, STOP and report (do not delete the rollback target).
    Then `git rm -r proxy/` to remove the entire self-contained Worker sub-package
    (proxy/src/index.ts, proxy/wrangler.toml, proxy/package.json, proxy/tsconfig.json,
    proxy/vitest.config.ts, proxy/node_modules). Re-grep the repo (excluding node_modules
    and .planning) for any lingering `wrangler`, `tpc-gemini-proxy`, or `proxy/` reference
    in package.json / vite.config.* / .github/ and remove any that surface (none expected
    per the planning discovery). Do NOT touch the cataloger `src/services/` consumers (they
    already point at the env var). Per D-04.
  </action>
  <verify>
    <automated>test ! -d proxy && ! grep -rn 'tpc-gemini-proxy\|wrangler' package.json vite.config.* .github/ 2>/dev/null | grep -v node_modules</automated>
  </verify>
  <acceptance_criteria>
    - `proxy/` directory does not exist.
    - `git status` shows `proxy/` files staged for deletion.
    - No `tpc-gemini-proxy` or `wrangler` reference remains in package.json, vite.config.*, or .github/ (excluding node_modules).
  </acceptance_criteria>
  <done>The in-repo Worker and all wrangler/tpc-gemini-proxy bits are removed; Cloud Run is the only AI proxy path.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint proxy-URL tests + scrub .env.example; verify build + suite green</name>
  <files>src/tests/gemini-pipeline.test.ts, src/tests/geminiContinuous.test.ts, .env.example</files>
  <read_first>
    - src/tests/gemini-pipeline.test.ts (the two VITE_GEMINI_PROXY_URL stubs + the unset fail-closed test at line ~299)
    - src/tests/geminiContinuous.test.ts (the VITE_GEMINI_PROXY_URL stub at line ~147)
    - .env.example (already repointed in Plan 02 — confirm no leftover localhost:8787 / Worker reference)
  </read_first>
  <action>
    Repoint the placeholder proxy-URL stubs in `src/tests/gemini-pipeline.test.ts` and
    `src/tests/geminiContinuous.test.ts` to a Cloud-Run-shaped URL (e.g. the prod URL
    `https://tpc-ai-proxy-prod-588770300226.us-east1.run.app/`) for consistency with the
    retired-Worker cleanup; the assertions are stub-URL-agnostic so only the literal value
    changes. KEEP the unset -> fail-closed test intact (SC3). Confirm `.env.example` has no
    residual `localhost:8787` or Worker reference (Plan 02 set the prod URL; scrub anything
    stale if present). Run the full cataloger suite + a production build to prove removing
    the Worker broke nothing.
  </action>
  <verify>
    <automated>npm test -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/tests/gemini-pipeline.test.ts` and `src/tests/geminiContinuous.test.ts` no longer reference `test-proxy.example.com` / `proxy.test`; they use a `run.app` URL.
    - The "fails immediately when VITE_GEMINI_PROXY_URL is not configured" test still passes.
    - `.env.example` contains no `localhost:8787`.
    - `npm test -- --run` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>Proxy-URL tests point at the Cloud Run URL, .env.example is Worker-free, and the cataloger build + full suite pass.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the retirement as one isolated rollback-friendly commit</name>
  <files>proxy/, src/tests/gemini-pipeline.test.ts, src/tests/geminiContinuous.test.ts, .env.example</files>
  <read_first>
    - git status (confirm ONLY the retirement changes are staged — proxy/ deletion + test repoint + .env.example scrub; nothing from other plans)
  </read_first>
  <action>
    Stage ONLY the retirement changes (proxy/ removal, the two test repoints, the
    .env.example scrub) and create a single isolated commit titled
    `chore(40): retire in-repo CF Worker (proxy/) — Cloud Run is sole AI proxy`. Keeping
    this as one commit makes rollback a clean `git revert` AND the operational rollback is
    independently a single env repoint of VITE_GEMINI_PROXY_URL back (D-04). Do NOT batch in
    any unrelated change. End the commit message with the Co-Authored-By trailer.
  </action>
  <verify>
    <automated>git log -1 --name-only | grep -q 'proxy/' && git log -1 --pretty=%s | grep -qi 'retire'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --name-only` shows proxy/ deletions + the test/.env files and NO unrelated files.
    - The latest commit subject references retiring the Worker.
    - `git revert HEAD` would cleanly restore the Worker (single-commit rollback target).
  </acceptance_criteria>
  <done>Worker retirement is one isolated commit; rollback is a clean revert or a single VITE_GEMINI_PROXY_URL repoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo history -> rollback capability | retirement must stay isolated so a regression can be reverted without unwinding other work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-09 | Denial of service | premature Worker deletion | mitigate | Task 1 re-checks the Plan 02 prod-verified gate before `git rm`; aborts if absent |
| T-40-10 | Repudiation | non-atomic retirement commit | mitigate | retirement isolated in one commit (Task 3); rollback = revert HEAD or repoint VITE_GEMINI_PROXY_URL |
| T-40-SC | Tampering | dependency change on removal | accept | deletion only; no new deps added |
</threat_model>

<verification>
- `proxy/` gone; no wrangler/tpc-gemini-proxy references remain (Task 1).
- Full suite + build green after removal; fail-closed test retained (Task 2).
- Single isolated retirement commit (Task 3).
</verification>

<success_criteria>
- CF Worker (proxy/, wrangler/tpc-gemini-proxy bits) retired ONLY after Cloud Run verified in prod (SC4, gate enforced in Task 1).
- .env.example + proxy-URL tests updated (SC4).
- One rollback commit; rollback = repoint VITE_GEMINI_PROXY_URL back (SC4, D-04).
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/40-ai-proxy-cloud-run-migration/40-03-SUMMARY.md` when done.
Record the retirement commit SHA and the rollback procedure (revert SHA OR repoint VITE_GEMINI_PROXY_URL to the old Worker URL).
</output>
