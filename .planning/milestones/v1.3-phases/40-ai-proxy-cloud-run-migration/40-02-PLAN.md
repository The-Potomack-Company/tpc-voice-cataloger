---
phase: 40-ai-proxy-cloud-run-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - .env.example
autonomous: false
requirements: []   # cross-app infra (D-056); governed by D-053
must_haves:
  truths:
    - "VITE_GEMINI_PROXY_URL points at the Cloud Run prod URL in prod and the -dev- URL in dev"
    - "AI processing succeeds end-to-end against the Cloud Run URL in dev AND prod"
    - "An unauthorized/cross-origin caller is rejected by the Cloud Run proxy"
    - "VITE_GEMINI_PROXY_URL-unset still fails closed (existing guarantee retained)"
    - "The request/response contract is unchanged — same JSON shape, gemini-2.5-flash, 25 MB cap"
  artifacts:
    - path: ".env.example"
      provides: "documented Cloud Run prod proxy URL"
      contains: "tpc-ai-proxy-prod-588770300226.us-east1.run.app"
  key_links:
    - from: "src/services/gemini.ts"
      to: "Cloud Run proxy"
      via: "fetch(VITE_GEMINI_PROXY_URL) with Authorization: Bearer"
      pattern: "VITE_GEMINI_PROXY_URL"
    - from: "src/services/geminiContinuous.ts"
      to: "Cloud Run proxy"
      via: "fetch(VITE_GEMINI_PROXY_URL) with Authorization: Bearer"
      pattern: "VITE_GEMINI_PROXY_URL"
---

<objective>
Flip the cataloger's `VITE_GEMINI_PROXY_URL` from the in-repo Cloudflare Worker onto
the verified Cloud Run service (prod + dev URLs), then verify AI processing end-to-end
in dev and prod. Config-only cutover — the request/response contract is UNCHANGED
(`gemini-2.5-flash`, 25 MB cap, same JSON shape); both consumers already attach the
`Authorization: Bearer` header the proxy now verifies (Plan 01).

Purpose: Move live cataloger AI traffic onto the shared Cloud Run proxy now that it
enforces JWT + serves the cataloger's web origins (SC1, SC3).
Output: Updated `VITE_GEMINI_PROXY_URL` in `.env.example` (prod URL as the documented
default) and the live Vercel env vars (prod + preview + dev), with end-to-end AI
verified on both environments.

Depends on Plan 01: the Cloud Run proxy MUST be live-verified (JWT + origins) in prod
before this flip. Do NOT execute this plan until 40-01 reported "prod-verified".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3-phases/40-ai-proxy-cloud-run-migration/40-CONTEXT.md
@.planning/milestones/v1.3-phases/40-ai-proxy-cloud-run-migration/40-01-SUMMARY.md

<interfaces>
<!-- The cutover surface. Both consumers read the same env var and already
     attach the Bearer header — NO client auth change is needed. -->

From src/services/gemini.ts (processAudioWithAi):
- `const proxyUrl = import.meta.env.VITE_GEMINI_PROXY_URL;` then throws
  "VITE_GEMINI_PROXY_URL is not configured..." when falsy (fail-closed — retained).
- `fetch(proxyUrl, { method:'POST', headers:{ 'Content-Type':'application/json',
  Authorization: \`Bearer ${accessToken}\` }, body: JSON.stringify({ model: 'gemini-2.5-flash', payload }) })`.

From src/services/geminiContinuous.ts:
- Same `import.meta.env.VITE_GEMINI_PROXY_URL` read + same `Authorization: Bearer ${accessToken}`
  fetch shape (line ~196). accessToken from `ensureFreshSession()`.

Cloud Run URLs (D-053):
- prod: `https://tpc-ai-proxy-prod-588770300226.us-east1.run.app/`
- dev:  `https://tpc-ai-proxy-dev-588770300226.us-east1.run.app/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint VITE_GEMINI_PROXY_URL to the Cloud Run prod URL in .env.example</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (current `VITE_GEMINI_PROXY_URL=http://localhost:8787`)
    - .planning/milestones/v1.3-phases/40-ai-proxy-cloud-run-migration/40-01-SUMMARY.md (confirm the exact prod URL + that prod is verified)
  </read_first>
  <action>
    In `.env.example`, change `VITE_GEMINI_PROXY_URL` from the localhost Worker value to
    the Cloud Run PROD URL `https://tpc-ai-proxy-prod-588770300226.us-east1.run.app/`
    (the documented default; D-053). Add a short comment noting the dev service URL
    `https://tpc-ai-proxy-dev-588770300226.us-east1.run.app/` for local/dev builds. Do
    NOT touch the request/response contract — this is a URL change only. Leave
    `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` unchanged.
  </action>
  <verify>
    <automated>grep -q 'VITE_GEMINI_PROXY_URL=https://tpc-ai-proxy-prod-588770300226.us-east1.run.app' .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains `VITE_GEMINI_PROXY_URL=https://tpc-ai-proxy-prod-588770300226.us-east1.run.app`.
    - `.env.example` no longer contains `localhost:8787`.
    - `.env.example` mentions the `tpc-ai-proxy-dev-` URL as the dev option.
  </acceptance_criteria>
  <done>.env.example documents the Cloud Run prod URL as the proxy default plus the dev URL; contract untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the contract is unchanged + fail-closed guarantee holds (no client code change)</name>
  <files>src/services/gemini.ts, src/services/geminiContinuous.ts</files>
  <read_first>
    - src/services/gemini.ts lines 232-235 (proxyUrl read + unset throw) and 300-319 (fetch shape, Bearer header, `model: 'gemini-2.5-flash'`)
    - src/services/geminiContinuous.ts (the VITE_GEMINI_PROXY_URL read + Bearer fetch shape)
    - src/tests/gemini-pipeline.test.ts line 299 (the existing VITE_GEMINI_PROXY_URL-unset fails-closed test)
  </read_first>
  <action>
    Verification-only task — NO source edit expected. Confirm both consumers already (a)
    read `import.meta.env.VITE_GEMINI_PROXY_URL`, (b) throw/fail-closed when it is unset,
    and (c) POST `{ model: 'gemini-2.5-flash', payload }` with an `Authorization: Bearer`
    header. Because the cutover is config-only and the contract is identical (D-056), no
    code change is required here. Run the existing cataloger suite to prove the contract +
    fail-closed guarantee still hold against the unchanged client code. If any consumer is
    found NOT attaching the Bearer header, STOP and flag — Plan 01's JWT verify would
    reject it (this is the one place a contract drift would surface).
  </action>
  <verify>
    <automated>npm test -- --run src/tests/gemini-pipeline.test.ts src/tests/geminiContinuous.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/services/gemini.ts` and `src/services/geminiContinuous.ts` both contain `Authorization` + `Bearer` and `VITE_GEMINI_PROXY_URL`.
    - `npm test -- --run src/tests/gemini-pipeline.test.ts src/tests/geminiContinuous.test.ts` exits 0.
    - The "fails immediately when VITE_GEMINI_PROXY_URL is not configured" test passes (fail-closed retained, SC3).
    - No source diff in `src/services/` (config-only cutover).
  </acceptance_criteria>
  <done>Both consumers confirmed contract-compatible with the Cloud Run proxy (Bearer attached, gemini-2.5-flash, fail-closed on unset); no client code change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Flip Vercel env + verify end-to-end AI against Cloud Run (dev + prod)</name>
  <what-built>`.env.example` repointed to the Cloud Run prod URL; client contract confirmed unchanged. The live Vercel env vars now need flipping (prod + preview + dev as applicable) and an end-to-end AI run verified against the Cloud Run proxy in BOTH dev and prod. Claude drives the Vercel env update via CLI/API where possible; this checkpoint confirms the live AI run actually succeeds against Cloud Run.</what-built>
  <how-to-verify>
    1. Claude updates `VITE_GEMINI_PROXY_URL` in Vercel: prod project env -> the `-prod-`
       URL; preview/dev env -> the `-dev-` URL (via `vercel env` CLI or dashboard if CLI
       auth is unavailable), then triggers/awaits a redeploy.
    2. Dev verify: on the dev/preview deploy, record an item and confirm AI processing
       completes (ai_status reaches "done", fields populate) — traffic hits the `-dev-`
       Cloud Run URL, not the old Worker.
    3. Prod verify: same end-to-end record-and-process on the prod deploy against the
       `-prod-` URL; confirm a 200 + populated fields.
    4. Negative check: confirm an unauthenticated / cross-origin caller is rejected
       (already covered live in Plan 01's prod curl; re-confirm the cataloger origin is
       accepted by virtue of step 2/3 succeeding).
    5. Confirm the old Worker is no longer receiving cataloger traffic (Cloud Run logs
       show the requests; the Worker is now dormant and ready for retirement in Plan 03).
  </how-to-verify>
  <resume-signal>Type "cutover-verified" once AI processing succeeds end-to-end against the Cloud Run URL in both dev and prod, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cataloger client -> Cloud Run proxy | the cataloger now sends its Bearer JWT to a new host; the JWT + origin must be accepted there |
| build-time env -> client bundle | VITE_GEMINI_PROXY_URL is baked into the Vite build; a wrong/empty value changes the live AI endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06 | Tampering | VITE_GEMINI_PROXY_URL env flip | mitigate | prod URL is the documented .env.example default; dev/preview pinned to the -dev- URL; live end-to-end verify in both environments before relying on it |
| T-40-07 | Denial of service | VITE_GEMINI_PROXY_URL unset/empty | mitigate | existing fail-closed throw retained + test (SC3); an empty value refuses to call rather than calling an attacker URL |
| T-40-08 | Spoofing | cataloger Bearer accepted by new host | mitigate | depends_on 40-01 (proxy verifies the same Supabase JWT); cutover gated on 40-01 prod-verified |
</threat_model>

<verification>
- `.env.example` shows the Cloud Run prod URL (Task 1 grep).
- Cataloger suite green incl. the fail-closed-on-unset test (Task 2).
- Live: AI processing succeeds end-to-end against Cloud Run in dev + prod (checkpoint).
</verification>

<success_criteria>
- VITE_GEMINI_PROXY_URL repoints to Cloud Run prod/dev URLs; contract unchanged (SC1).
- AI processing succeeds against the Cloud Run URL in dev + prod; unauthorized/cross-origin rejected; unset fails closed (SC3).
- No fallback to origin+quota-only auth — the proxy verifies JWT (SC2, inherited from Plan 01).
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/40-ai-proxy-cloud-run-migration/40-02-SUMMARY.md` when done.
Explicitly record that the Cloud Run path is VERIFIED IN PROD (gate for Plan 03's Worker retirement) and which Vercel envs were flipped.
</output>
