---
phase: 37-a11y-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - src/ui/OverflowMenu.tsx
  - src/components/SessionTile.tsx
  - src/components/SessionCard.tsx
  - src/components/ItemCard.tsx
  - src/tests/a11y/overflow-menu.test.tsx
  - src/tests/a11y/touch-targets.test.tsx
  - tests/e2e/keyboard-flow.spec.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Each SessionTile / SessionCard / ItemCard row exposes a keyboard- and SR-reachable ⋯ menu containing Delete, in addition to the kept swipe gesture (SC3, Codex #32, D-03)"
    - "Choosing Delete from the menu routes through the EXISTING ConfirmDialog/onDelete path — no new delete logic (SC3, D-04)"
    - "The ⋯ trigger is a 44px target with aria-label=More actions and a tooltip equal to that label (SC2, D-06/D-07)"
    - "The ⋯ menu opens/closes by keyboard, Escape closes it and restores focus to the trigger, and menu items show the A11Y-02 focus ring (SC2/SC3)"
    - "A keyboard-only record→edit→save Playwright flow runs an axe scan with zero violations (SC4, phase gate)"
  artifacts:
    - path: "src/ui/OverflowMenu.tsx"
      provides: "⋯ icon-button + keyboard menu primitive (D-03); reuses Icon dots, A11Y-02 ring"
      min_lines: 40
    - path: "tests/e2e/keyboard-flow.spec.ts"
      provides: "@axe-core/playwright keyboard-only record→edit→save scan"
      contains: "AxeBuilder"
  key_links:
    - from: "src/components/ItemCard.tsx"
      to: "setShowDeleteConfirm"
      via: "menu Delete reuses existing local ConfirmDialog setter"
      pattern: "setShowDeleteConfirm"
    - from: "src/components/SessionTile.tsx"
      to: "onDelete"
      via: "menu Delete calls the same onDelete prop SwipeableRow uses"
      pattern: "onDelete"
---

<objective>
Add the non-swipe delete affordance (D-03/D-04, Codex #32): a keyboard- and screen-reader-reachable `⋯` overflow menu on each row component whose Delete routes through the existing delete path — plus the 44px touch-target / icon-label pass (D-06/D-07) and the keyboard-only e2e gate (SC4). The existing swipe gesture stays as a power-user shortcut.

Purpose: Keyboard and AT users currently can only delete via a swipe they cannot perform. The ⋯ menu is the accessible-equivalent path.
Output: `OverflowMenu.tsx`, ⋯ wired into 3 rows, touch-target/menu tests, and the Playwright keyboard-flow e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-CONTEXT.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-RESEARCH.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-PATTERNS.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-UI-SPEC.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-01-SUMMARY.md

<interfaces>
<!-- Existing wiring the menu reuses (grep-verified file:line in 37-RESEARCH / 37-PATTERNS). Add NO new delete logic. -->

ItemCard (owns its delete locally):
  trigger setter exists: onClick={() => setShowDeleteConfirm(true)}  (ItemCard.tsx:280, :392)
  local ConfirmDialog already mounted: ItemCard.tsx:402 (destructive, onConfirm=handleDelete)
  → menu Delete calls setShowDeleteConfirm(true). Mount ⋯ in header/grid; ItemCard wrapped in SwipeableRow at :411.

SessionTile / SessionCard (parent owns the confirm):
  onDelete: () => void prop  (SessionTile.tsx:29, SessionCard.tsx:14) — today passed only to SwipeableRow.
  parent ConfirmDialog: Sessions.tsx:596.
  → menu Delete calls the SAME onDelete. Mount ⋯ in header region (SessionTile ~:107-201, SessionCard ~:107-168).

Icon API (src/ui/icons.tsx): <Icon name size aria-label/>; dots glyph at :101, trash at :163. Omitting aria-label auto-sets aria-hidden (:394). Use dots for trigger — do NOT add SVGs.

Icon-button trigger idiom (ItemPeekModal.tsx:35-43): button.tpc-btn.tpc-btn-ghost; existing padding:6 ≈28px — the ⋯ trigger must instead be 44px (min-h-11 min-w-11), grow padding not glyph (D-06).

Focus ring (A11Y-02, base.css:170): class-scoped only. Menu items MUST carry tpc-btn / tpc-card-interactive (or replicate box-shadow:0 0 0 3px var(--accent-wash)) to get a ring.

Menu-button ARIA (APG, [ASSUMED]): trigger aria-haspopup="menu" + aria-expanded; aria-label="More actions" (UI-SPEC); tooltip string == aria-label. Delete item label uses --err ink. Escape closes menu + restores focus to trigger. Honor prefers-reduced-motion on open/close.

TEST-MOCK CAUTION: SwipeableRow is vi.mock-ed in src/tests/session-tile.test.tsx, session-assignment.test.tsx, sessions-admin-view.test.tsx — adding ⋯ must not break those mocks.

e2e analog (tests/e2e/visual-smoke.spec.ts): auth/login approach + SUPABASE_URL gate. AxeBuilder import shape unverified (A5) — confirm on first run.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OverflowMenu primitive (⋯ trigger + keyboard menu)</name>
  <files>src/ui/OverflowMenu.tsx, src/tests/a11y/overflow-menu.test.tsx, src/tests/a11y/touch-targets.test.tsx</files>
  <read_first>
    - src/components/ItemPeekModal.tsx:35-43 (icon-button idiom)
    - src/ui/icons.tsx (dots :101, trash :163, Icon API :369-395)
    - src/ui/tokens/base.css:170-176 (A11Y-02 ring — menu items must adopt it)
    - 37-RESEARCH.md Pattern 3 (menu-button contract)
  </read_first>
  <behavior>
    - Trigger: a 44px (min-h-11 min-w-11) icon-button rendering <Icon name="dots"/>, aria-label="More actions", aria-haspopup="menu", aria-expanded reflecting open state, and a tooltip whose string equals the aria-label.
    - Menu: opens on click/Enter/Space; items keyboard-reachable; Escape closes the menu and restores focus to the ⋯ trigger; each item carries tpc-btn (or equivalent) so it shows the A11Y-02 focus ring. Open/close transition gated behind prefers-reduced-motion.
    - Accepts a list of actions (label + onSelect + optional destructive flag); the destructive item uses --err ink. (Only Delete is used this phase; the API leaves room for more — no extra actions added.)
  </behavior>
  <action>
    Write src/tests/a11y/overflow-menu.test.tsx and touch-targets.test.tsx FIRST: assert the trigger has aria-label="More actions" + aria-haspopup + min-h-11/min-w-11 classes (or ≥44px computed box), that the menu opens by keyboard, that Escape closes it and refocuses the trigger, that a Delete action's onSelect fires, and a jest-axe scan (color-contrast disabled) is clean. Run — RED. Then implement src/ui/OverflowMenu.tsx (hand-rolled per D-03; no Radix/headlessui) reusing the Icon dots glyph and the A11Y-02 ring. Run — GREEN. Refactor; keep green.
  </action>
  <verify>
    <automated>npm test -- --run src/tests/a11y/overflow-menu.test.tsx src/tests/a11y/touch-targets.test.tsx</automated>
  </verify>
  <done>OverflowMenu trigger is 44px + labeled, menu is keyboard-operable, Escape restores focus, items show the focus ring, jest-axe clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire ⋯ menu into ItemCard, SessionTile, SessionCard (existing delete path)</name>
  <files>src/components/ItemCard.tsx, src/components/SessionTile.tsx, src/components/SessionCard.tsx</files>
  <read_first>
    - src/components/ItemCard.tsx (delete setter :280,:392; local ConfirmDialog :402; SwipeableRow :411)
    - src/components/SessionTile.tsx (onDelete prop :29; SwipeableRow usage; header :107-201)
    - src/components/SessionCard.tsx (onDelete prop :14; header :107-168)
    - src/ui/OverflowMenu.tsx (Task 1 output)
    - src/tests/session-tile.test.tsx:13 (SwipeableRow vi.mock — must not break)
  </read_first>
  <behavior>
    - ItemCard: ⋯ menu Delete → setShowDeleteConfirm(true) (its existing local ConfirmDialog handles confirm). Swipe gesture kept.
    - SessionTile / SessionCard: ⋯ menu Delete → call the same onDelete prop SwipeableRow already calls; the parent (Sessions.tsx:596) owns the confirm. Swipe gesture kept.
    - No new delete logic, no new confirmation copy (D-04). The ⋯ button is the accessible-equivalent path alongside the kept swipe.
  </behavior>
  <action>
    Mount <OverflowMenu> in each row's header region with a single Delete action wired to the existing setter/prop above. Do not touch SwipeableRow or the confirm flow. Verify the SwipeableRow vi.mock sites still render — adjust those test mocks only if the menu addition requires it (prefer not to). Apply min-h-11 min-w-11 to the ⋯ trigger (handled by OverflowMenu) and run the belt-and-suspenders icon-label check on each row's existing icon buttons (audit found near-zero gaps — add aria-label only if one is genuinely missing).
  </action>
  <verify>
    <automated>npm test -- --run src/tests/session-tile.test.tsx src/tests/a11y/ && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>All three rows show a working ⋯→Delete that routes through the existing ConfirmDialog/onDelete; swipe still works; existing SwipeableRow-mock tests green; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Keyboard-only record→edit→save e2e with axe scan (SC4 gate)</name>
  <files>tests/e2e/keyboard-flow.spec.ts</files>
  <read_first>
    - tests/e2e/visual-smoke.spec.ts (auth/login + SUPABASE_URL gate approach)
    - playwright.config.ts (webServer + SUPABASE_URL gate)
    - 37-RESEARCH.md Code Examples (@axe-core/playwright) + Open Question 1 (auth path)
  </read_first>
  <action>
    Create tests/e2e/keyboard-flow.spec.ts driving record→edit→save with keyboard only (page.keyboard.press('Tab'/'Enter'/'Escape') — no mouse), asserting focus is always reachable, then running `const results = await new AxeBuilder({ page }).analyze(); expect(results.violations).toEqual([]);`. Verify the AxeBuilder import shape on first run (A5). For auth, match visual-smoke.spec.ts's storage-state/login approach; if an authed fixture is unavailable, scope the keyboard+axe pass to the deepest unauthenticated-reachable surface and record the authed-flow portion as a HUMAN-UAT item (do NOT silently drop SC4 — flag it in the SUMMARY). Honor the existing SUPABASE_URL gate so the spec skips cleanly when creds are absent, matching the existing e2e.
  </action>
  <verify>
    <automated>npx playwright test keyboard-flow 2>&1 | tail -15</automated>
  </verify>
  <done>keyboard-flow.spec.ts runs keyboard-only and asserts zero axe violations; either runs green with the auth fixture or cleanly skips behind the SUPABASE_URL gate with the authed portion recorded as HUMAN-UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| menu Delete → existing delete path | New affordance must not create an unconfirmed destructive path |
| keyboard user → menu focus | Menu must return focus to trigger, never strand it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Elevation of Privilege | OverflowMenu Delete | mitigate | Delete routes through the existing ConfirmDialog/onDelete (D-04) which already enforces RLS/auth in the delete handler — no new privileged or unconfirmed destructive path |
| T-37-06 | Denial of Service | OverflowMenu focus | mitigate | Escape closes the menu and restores focus to the ⋯ trigger (tested); user is never trapped in the menu |
| T-37-07 | Tampering | restore focus after row delete | mitigate | Inherits Plan 01's isConnected guard — deleting the row that owns the trigger does not throw (Pitfall 4) |
</threat_model>

<verification>
- `npm test -- --run src/tests/a11y/` + `npm test` full suite green (no SwipeableRow-mock regressions).
- `npx playwright test keyboard-flow` green OR skips behind SUPABASE_URL gate with authed portion logged as HUMAN-UAT.
- Manual (phase gate): screen-reader reaches and operates the ⋯ menu; keyboard-only delete completes.
</verification>

<success_criteria>
Every row has a keyboard/SR-reachable ⋯→Delete routing through the existing confirm path (swipe kept), the ⋯ trigger is a labeled 44px target, and the keyboard-only record→edit→save flow is axe-clean (or its authed portion is explicitly tracked as HUMAN-UAT).
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/37-a11y-foundation/37-03-SUMMARY.md` when done. If the e2e authed flow was deferred, write the HUMAN-UAT item for it.
</output>
