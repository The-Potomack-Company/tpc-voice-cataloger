---
phase: 37-a11y-foundation
plan: 02
type: tdd
wave: 2
depends_on:
  - 37-01
files_modified:
  - src/components/ConfirmDialog.tsx
  - src/components/ReturnDialog.tsx
  - src/components/ItemPeekModal.tsx
  - src/components/PhotoLightbox.tsx
  - src/components/MigrationSplash.tsx
  - src/tests/a11y/modals.test.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Every one of the 5 modal sites renders role=dialog + aria-modal=true, traps focus, and closes on Escape (SC1, Codex #33/#34/#48)"
    - "ItemPeekModal — which had NO role/aria-modal/Escape — now has all three (SC1, biggest single gap)"
    - "PhotoLightbox keeps its touch swipe nav, and opening its nested ConfirmDialog moves focus into the confirm while Escape returns to the lightbox (not all the way out) (SC1, nested-trap pitfall)"
    - "MigrationSplash keeps its opaque full-screen splash look and its opacity fade is gated behind prefers-reduced-motion (MOTION-04)"
    - "Each migrated modal passes a jest-axe scan (color-contrast disabled) with no violations (SC4)"
  artifacts:
    - path: "src/tests/a11y/modals.test.tsx"
      provides: "jest-axe scan + role/aria-modal/Escape assertions for all 5 modals + nested-trap test"
      contains: "toHaveNoViolations"
  key_links:
    - from: "src/components/ConfirmDialog.tsx"
      to: "src/ui/Modal.tsx"
      via: "panel body wrapped in <Modal>"
      pattern: "Modal"
    - from: "src/components/PhotoLightbox.tsx"
      to: "src/components/ConfirmDialog.tsx"
      via: "nested confirm inside the trapped lightbox"
      pattern: "ConfirmDialog"
---

<objective>
Migrate the five real modal components onto the `<Modal>` primitive from Plan 01 (D-02), closing the role/aria-modal/Escape/focus-trap gaps, and prove each with a jest-axe scan. This is the highest-leverage plan: migrating ConfirmDialog alone fixes 8 downstream render sites.

Purpose: Centralizes ARIA dialog semantics + focus management onto one tested primitive so keyboard/AT users can operate every dialog and cannot Tab out of one.
Output: 5 migrated components + `src/tests/a11y/modals.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-CONTEXT.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-RESEARCH.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-PATTERNS.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-UI-SPEC.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-01-SUMMARY.md

<interfaces>
<!-- From Plan 01. Use the final API recorded in 37-01-SUMMARY.md; the shape below is the planned surface. -->

<Modal open onClose ariaLabelledBy?/ariaLabel children initialFocusRef? />
  — portals to document.body, renders role=dialog + aria-modal, runs useFocusTrap (Tab-wrap/Escape/restore).

Modal site inventory (37-RESEARCH "Modal Site Inventory", grep-verified file:line):
  ConfirmDialog   src/components/ConfirmDialog.tsx:27   bg-black/50, NO role/aria/trap/Escape — 8 render sites inherit fix
  ReturnDialog    src/components/ReturnDialog.tsx:32    has <textarea> at :40-46 (initial-focus candidate)
  ItemPeekModal   src/components/ItemPeekModal.tsx:26   NO role/aria/Escape (close button only); <h2> at :31 → aria-labelledby; close btn padding:6 (~28px)
  PhotoLightbox   src/components/PhotoLightbox.tsx:113  swipe nav (onTouchStart/End); NESTED ConfirmDialog at :159
  MigrationSplash src/components/MigrationSplash.tsx:78 already role=dialog+aria-modal (:81-83), no real trap; opaque splash; opacity fade :78-79

jest-axe scan (portaled → scan document.body, NOT container):
  await axe(document.body, { rules: { 'color-contrast': { enabled: false } } }); expect(results).toHaveNoViolations();

Reduced-motion: window.matchMedia("(prefers-reduced-motion: reduce)").matches (Waveform.tsx:21-25 idiom).
  jsdom setup mock returns matches:false; override per-test to exercise the reduce branch (Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate ConfirmDialog, ReturnDialog, ItemPeekModal onto Modal</name>
  <files>src/components/ConfirmDialog.tsx, src/components/ReturnDialog.tsx, src/components/ItemPeekModal.tsx</files>
  <read_first>
    - src/components/ConfirmDialog.tsx (self — current portal/scrim at :24-54, destructive branch :44-46)
    - src/components/ReturnDialog.tsx (textarea :40-46, buttons :51,:58)
    - src/components/ItemPeekModal.tsx (no ARIA at :26, close button :35-43, <h2> :31)
    - src/ui/Modal.tsx (Plan 01 primitive)
  </read_first>
  <behavior>
    - ConfirmDialog: wraps its panel body in <Modal>; drops its own outer fixed inset-0 div (Modal supplies it); keeps the destructive red-button branch; Escape === cancel (non-destructive). No render-site signature change (8 callers untouched).
    - ReturnDialog: wrapped in <Modal>; textarea is the initial-focus target (initialFocusRef); Escape === cancel.
    - ItemPeekModal: wrapped in <Modal>, gaining role=dialog + aria-modal + Escape (previously absent); aria-labelledby points at its existing <h2>; close button grows to 44px (min-h-11 min-w-11, grow padding not glyph).
  </behavior>
  <action>
    For each component, replace the hand-rolled `fixed inset-0` overlay with `<Modal open onClose ...>`, passing onClose = the component's existing cancel/close handler so Escape reuses it. Preserve every existing prop and the destructive branch — callers must not change. For ItemPeekModal add aria-labelledby to the existing heading id and apply min-h-11 min-w-11 to the close button. Do NOT introduce new confirm/close logic. Update any existing test that asserted the old hand-rolled markup (e.g. return-dialog.test.tsx) only as needed to match the new role=dialog wrapper.
  </action>
  <verify>
    <automated>npm test -- --run src/tests/return-dialog.test.tsx src/tests/a11y/</automated>
  </verify>
  <done>All three render role=dialog + aria-modal, close on Escape, preserve their existing prop surface; existing dialog tests green. ItemPeekModal close button ≥44px.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate PhotoLightbox (nested-trap) and MigrationSplash (reduced-motion)</name>
  <files>src/components/PhotoLightbox.tsx, src/components/MigrationSplash.tsx</files>
  <read_first>
    - src/components/PhotoLightbox.tsx (fixed inset-0 :113, swipe onTouchStart/End, nested ConfirmDialog :159, close :120 / trash :132 already labeled, min-w-12 :124,:136)
    - src/components/MigrationSplash.tsx (role=dialog/aria-modal :81-83, opaque bg :78, opacity fade :78-79, conditional error buttons :111-124)
    - src/ui/Waveform.tsx:21-30 (reduced-motion idiom)
    - 37-RESEARCH.md Pitfall 1 (nested trap) + Pitfall 2 (recompute focusables)
  </read_first>
  <behavior>
    - PhotoLightbox: wrapped in <Modal> (gains role/aria-modal/trap/Escape); touch swipe nav (onTouchStart/End) preserved; close/trash buttons stay labeled. Its nested ConfirmDialog (now also a Modal) must own focus while open — inner trap takes precedence; Escape on the inner confirm returns focus to the lightbox, NOT all the way out.
    - MigrationSplash: keeps its opaque full-screen splash look (do NOT swap to the centered scrim); folds the real useFocusTrap in via <Modal> while preserving the opaque bg; opacity fade transition gated behind prefers-reduced-motion (instant when reduce set). Conditional error-state buttons must be reachable (relies on Plan 01's per-keydown focusable recompute).
  </behavior>
  <action>
    PhotoLightbox: wrap the lightbox body in <Modal>; keep the existing onTouch handlers and the nested ConfirmDialog as-is (it inherits trapping from Plan 01's Modal migration in Task 1). Do not duplicate the swipe-nav logic. For MigrationSplash, route through <Modal> but override the scrim/panel to retain the opaque full-screen splash (it is a splash, not a centered dialog) and wrap the opacity transition in the reduced-motion check (use the Waveform idiom). Keep its existing auto-dismiss + aria-modal behavior.
  </action>
  <verify>
    <automated>npm test -- --run src/tests/a11y/modals.test.tsx</automated>
  </verify>
  <done>PhotoLightbox traps + keeps swipe nav; nested-confirm focus behavior correct; MigrationSplash keeps opaque splash and gates its fade behind reduced-motion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: jest-axe scan suite for all 5 modals + nested-trap test</name>
  <files>src/tests/a11y/modals.test.tsx</files>
  <read_first>
    - src/tests/return-dialog.test.tsx (RTL portal render analog)
    - src/tests/setup.ts (jest-axe matcher wired in Plan 01)
    - 37-RESEARCH.md Code Examples (jest-axe under Vitest) + Pitfall 3 (color-contrast off) + Pitfall 5 (matchMedia override)
  </read_first>
  <behavior>
    - For each of the 5 modals: render open, assert role=dialog + aria-modal=true present, assert Escape calls the close/cancel handler, and assert `axe(document.body, { rules: { 'color-contrast': { enabled: false } } })` → toHaveNoViolations.
    - Nested-trap test: render PhotoLightbox, open the nested photo-delete ConfirmDialog, assert focus moves into the confirm and Escape returns focus to the lightbox (lightbox still open).
    - MigrationSplash reduced-motion test: override window.matchMedia to matches:true, assert the opacity fade class/transition is suppressed.
  </behavior>
  <action>
    Create src/tests/a11y/modals.test.tsx covering the behaviors above. Scan document.body (modals portal there), not the RTL container. Disable color-contrast (jsdom can't paint; already covered by v1.2 contrast.test.ts). For the reduced-motion assertion, override window.matchMedia per-test (the setup mock is overridable). Keep each modal's render minimal — supply the required props from the interfaces block.
  </action>
  <verify>
    <automated>npm test -- --run src/tests/a11y/modals.test.tsx</automated>
  </verify>
  <done>All 5 modals pass role/aria-modal/Escape + jest-axe assertions; nested-trap and reduced-motion tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard/AT user → nested dialogs | Two stacked traps (lightbox + confirm) must not strand or double-trap focus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03 | Denial of Service | PhotoLightbox nested ConfirmDialog | mitigate | Inner trap owns focus while open; Escape on the inner confirm returns to the lightbox (explicit test in Task 3) — user is never stranded between two traps |
| T-37-04 | Repudiation | ConfirmDialog migration | accept | Confirm semantics unchanged (D-04 reuse); no new destructive path introduced by wrapping in <Modal> — the same onConfirm fires |
</threat_model>

<verification>
- `npm test -- --run src/tests/a11y/modals.test.tsx` green.
- `npm test` full suite green — migrations must not regress the 8 ConfirmDialog callers or existing dialog tests.
- Manual (deferred to phase gate / HUMAN-UAT): screen-reader announces dialog role + label on each modal.
</verification>

<success_criteria>
All 5 modal sites are accessible dialogs (role=dialog + aria-modal + Escape + trap), ItemPeekModal's prior total gap is closed, PhotoLightbox's nested confirm behaves correctly, MigrationSplash honors reduced motion, and every modal passes jest-axe.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/37-a11y-foundation/37-02-SUMMARY.md` when done.
</output>
