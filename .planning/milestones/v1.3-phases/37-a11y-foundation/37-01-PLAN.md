---
phase: 37-a11y-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/tests/setup.ts
  - src/hooks/useFocusTrap.ts
  - src/ui/Modal.tsx
  - src/tests/a11y/use-focus-trap.test.tsx
  - src/tests/a11y/modal.test.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "A keyboard user cannot Tab out of an open modal; Tab from the last focusable wraps to the first and Shift+Tab from the first wraps to the last (SC1)"
    - "Escape closes any modal built on the primitive (SC1)"
    - "Closing a modal returns focus to the element that opened it, and does not throw if that element was removed (SC1, delete-flow safety)"
    - "<Modal> renders role=dialog + aria-modal=true and passes a jest-axe scan with no violations (SC1, SC4)"
    - "jest-axe toHaveNoViolations matcher is available to every test via src/tests/setup.ts; the only two new deps are the test-only dev-deps jest-axe + @axe-core/playwright (no shipped runtime dep, no @types/jest-axe) (SC4, D-05)"
  artifacts:
    - path: "src/hooks/useFocusTrap.ts"
      provides: "Zero-dep focus-trap hook (D-01): initial focus, Tab/Shift+Tab wrap, Escape, focus-restore"
      min_lines: 50
    - path: "src/ui/Modal.tsx"
      provides: "Shared dialog primitive (D-02): portal + scrim + role=dialog + aria-modal + useFocusTrap"
      min_lines: 30
    - path: "src/tests/setup.ts"
      provides: "jest-axe matcher wired via expect.extend(toHaveNoViolations)"
      contains: "toHaveNoViolations"
  key_links:
    - from: "src/ui/Modal.tsx"
      to: "src/hooks/useFocusTrap.ts"
      via: "hook call inside Modal panel"
      pattern: "useFocusTrap"
    - from: "src/tests/setup.ts"
      to: "jest-axe"
      via: "expect.extend"
      pattern: "toHaveNoViolations"
---

<objective>
Build the accessibility foundation every other Phase 37 plan depends on: install the two test-only dev-deps (D-05), wire the jest-axe matcher into the test setup, and hand-roll `useFocusTrap` (D-01) + the shared `<Modal>` primitive (D-02) test-first.

Purpose: Centralizes focus management so the 5 modal migrations (Plan 02) and the overflow menu (Plan 03) wire onto a single, already-tested contract instead of re-deriving trap logic per site.
Output: `useFocusTrap.ts`, `Modal.tsx`, their tests, and jest-axe wiring in `setup.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-CONTEXT.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-RESEARCH.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-PATTERNS.md
@.planning/milestones/v1.3-phases/37-a11y-foundation/37-UI-SPEC.md

<interfaces>
<!-- Existing idioms the executor reuses directly — no codebase exploration needed. -->

Portal + scrim shape to generalize into <Modal> (from src/components/ConfirmDialog.tsx, excerpted in 37-PATTERNS.md):
  createPortal(
    <div className="fixed inset-0 z-50 flex items-center justify-center ...scrim">
      <div className="...panel">{children}</div>
    </div>,
    document.body,
  )

ARIA attributes to add (already present on MigrationSplash.tsx:81-83 — copy the shape):
  role="dialog"  aria-modal="true"  aria-labelledby={headingId}

Focusable selector set (APG, from 37-RESEARCH Pattern 1 — recompute on EACH Tab keydown, not cached):
  a[href], button:not([disabled]), textarea:not([disabled]),
  input:not([disabled]), select:not([disabled]), [tabindex]:not([tabindex="-1"])

Reduced-motion idiom (verbatim from src/ui/Waveform.tsx:21-25 — only needed if Modal animates):
  window.matchMedia("(prefers-reduced-motion: reduce)").matches

Focus-ring (A11Y-02, src/ui/tokens/base.css:170-176): class-scoped, NO generic *:focus-visible.
  New focusables must carry tpc-btn / tpc-card-interactive (or replicate box-shadow) to get a ring.

Existing setup.ts already mocks matchMedia to matches:false (overridable per-test for the reduce branch).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dev-deps and wire jest-axe matcher</name>
  <files>package.json, package-lock.json, src/tests/setup.ts</files>
  <read_first>
    - src/tests/setup.ts (the file being modified — note existing matchMedia mock at :5-19)
    - 37-RESEARCH.md "Standard Stack" + "Package Legitimacy Audit" (deps verified [OK])
  </read_first>
  <action>
    Run `npm install -D jest-axe@10 @axe-core/playwright@4.11` (per D-05). Do NOT add `@types/jest-axe` — jest-axe@10 bundles its own types and the DefinitelyTyped stub is stale at 3.5.9 (would shadow real types). In src/tests/setup.ts, import `{ toHaveNoViolations }` from `jest-axe` and call `expect.extend(toHaveNoViolations)` so every test gets the matcher. Keep the existing matchMedia and MediaRecorder mocks untouched. Use a WHY-comment only if the no-@types caveat needs recording inline.
  </action>
  <verify>
    <automated>grep -q '"jest-axe"' package.json && grep -q '"@axe-core/playwright"' package.json && ! grep -q '@types/jest-axe' package.json && grep -q 'toHaveNoViolations' src/tests/setup.ts && echo OK</automated>
  </verify>
  <done>Both dev-deps in package.json, no @types/jest-axe, setup.ts extends expect with toHaveNoViolations. `npm test -- --run src/tests/setup` (or any existing test) still green — matcher wiring does not break the suite.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: useFocusTrap hook (RED → GREEN → REFACTOR)</name>
  <files>src/hooks/useFocusTrap.ts, src/tests/a11y/use-focus-trap.test.tsx</files>
  <read_first>
    - src/tests/return-dialog.test.tsx (analog: RTL + user-event on a portaled dialog)
    - src/ui/Waveform.tsx:21-30 (reduced-motion effect idiom, if needed)
    - 37-RESEARCH.md Pattern 1 contract table + Pitfall 2 (recompute focusables) + Pitfall 4 (unmounted trigger)
  </read_first>
  <behavior>
    - Initial focus: on mount, focus the first focusable in the panel (or the panel itself via tabIndex=-1 if none); honor an optional initialFocusRef.
    - Tab wrap: Tab on the last focusable → focus first + preventDefault. Shift+Tab on the first → focus last. Focusables recomputed on EACH keydown (a later-added textarea/button must be reachable).
    - Escape: keydown Escape calls onClose().
    - Focus restore: document.activeElement saved on mount; on unmount, restore focus to it ONLY if it exists and `isConnected` (delete flow can remove the trigger — must not throw, must not focus a detached node).
  </behavior>
  <action>
    Write src/tests/a11y/use-focus-trap.test.tsx FIRST covering all four behaviors above using @testing-library/user-event (`user.tab()`, `user.keyboard('{Escape}')`); include an explicit test that a trigger removed before close does not throw and falls back gracefully. Run — confirm RED. Then implement `useFocusTrap(panelRef, { onClose, initialFocusRef? })` in src/hooks/useFocusTrap.ts with the APG focusable selector from the interfaces block, per-keydown recompute, and the isConnected guard. Zero new deps (D-01). Run — confirm GREEN. Refactor for clarity if needed; keep tests green. Commit each phase (test: / feat: / refactor:).
  </action>
  <verify>
    <automated>npm test -- --run src/tests/a11y/use-focus-trap.test.tsx</automated>
  </verify>
  <done>All four contract behaviors pass; the unmounted-trigger test proves no throw. Hook has zero new imports beyond react.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Modal primitive (RED → GREEN → REFACTOR)</name>
  <files>src/ui/Modal.tsx, src/tests/a11y/modal.test.tsx</files>
  <read_first>
    - src/components/ConfirmDialog.tsx (portal+scrim analog to generalize)
    - src/components/MigrationSplash.tsx:78-83 (existing role=dialog/aria-modal shape)
    - src/hooks/useFocusTrap.ts (Task 2 output — Modal consumes it)
  </read_first>
  <behavior>
    - When open, renders (via createPortal to document.body) a fixed inset-0 z-50 scrim + centered panel with role="dialog" and aria-modal="true".
    - Accepts ariaLabelledBy (preferred, points at panel heading) OR ariaLabel; one must be present so the dialog has an accessible name.
    - Calls useFocusTrap(panelRef, { onClose }) so Tab-wrap / Escape / restore all work through the panel.
    - When open=false, renders nothing.
    - jest-axe scan of document.body (portaled content) with color-contrast rule disabled returns no violations.
  </behavior>
  <action>
    Write src/tests/a11y/modal.test.tsx FIRST: render an open Modal with a heading + a couple of buttons, assert role=dialog + aria-modal=true + accessible name, assert Escape calls onClose, and assert `await axe(document.body, { rules: { 'color-contrast': { enabled: false } } })` → `toHaveNoViolations()`. Scan document.body NOT container (Modal portals). Run — confirm RED. Then implement src/ui/Modal.tsx with prop surface `{ open, onClose, ariaLabelledBy?, ariaLabel?, children, initialFocusRef? }` (D-02; exact surface is Claude's discretion), portaling to document.body, scrim per UI-SPEC (`--bg-3` reduced-opacity), wiring useFocusTrap. Reuse the A11Y-02 focus ring by giving interactive children tpc-btn (callers own their buttons; Modal just supplies chrome). Gate any open transition behind the reduced-motion check (MOTION-04). Run — confirm GREEN. Refactor; keep green. Commit each phase.
  </action>
  <verify>
    <automated>npm test -- --run src/tests/a11y/modal.test.tsx</automated>
  </verify>
  <done>Modal renders role=dialog + aria-modal + accessible name, Escape closes, jest-axe clean. useFocusTrap is invoked inside Modal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard/AT user → DOM focus | Focus trap must never lock the user in with no exit |
| dev-dep install → repo | npm packages cross into the build/test toolchain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Denial of Service | useFocusTrap | mitigate | Escape ALWAYS calls onClose (tested); user can never be locked into the modal without an exit |
| T-37-02 | Tampering | restore-focus on close | mitigate | Null + `isConnected` guard before `.focus()` (Pitfall 4); detached/poisoned trigger cannot throw or steal focus |
| T-37-SC | Tampering | npm dev-dep install (jest-axe, @axe-core/playwright) | accept | Both [OK] in 37-RESEARCH Package Legitimacy Audit (slopcheck clean, Deque/nickcolley maintainers, ~1.6M+/4.4M weekly dl); test-only, never shipped to client bundle — no [ASSUMED]/[SUS] package, no blocking checkpoint required |
</threat_model>

<verification>
- `npm test -- --run src/tests/a11y/` green (trap + modal).
- `npm test` full suite green (matcher wiring does not regress the existing 84 test files).
- `grep -q toHaveNoViolations src/tests/setup.ts` — matcher available globally.
</verification>

<success_criteria>
useFocusTrap enforces Tab-wrap + Escape + focus-restore (with detached-trigger safety), Modal renders an accessible dialog and passes jest-axe, and the jest-axe matcher is wired for all downstream plans. Zero shipped runtime deps added.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/37-a11y-foundation/37-01-SUMMARY.md` when done. Record the final useFocusTrap API shape and Modal prop surface so Plans 02/03 wire against them without re-reading the source.
</output>
