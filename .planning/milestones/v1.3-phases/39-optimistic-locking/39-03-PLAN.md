---
phase: 39-optimistic-locking
plan: 03
type: tdd
wave: 2
depends_on: [39-01, 39-02]
files_modified:
  - src/services/geminiContinuous.ts
  - src/hooks/useWriteAheadQueue.ts
  - src/db/types.ts
autonomous: true
requirements: [DAT-3]
user_setup: []

must_haves:
  truths:
    - "When the AI continuous-merge write conflicts, a field the user changed since the merge read is SKIPPED, not overwritten (AI yields to user)"
    - "A field the user did NOT touch is re-applied with the fresh updated_at precondition"
    - "An offline write-ahead flush applies the updated_at precondition and routes a 0-row result through the same reconcile path as online writes"
    - "A 0-row flush does NOT delete the queue entry until reconcile resolves (no silent offline loss)"
    - "A legacy queue entry with no updated_at snapshot falls back to re-read-then-precondition instead of crashing or clobbering"
  artifacts:
    - path: "src/services/geminiContinuous.ts"
      provides: "mergeFieldsIntoItem records value-at-read and per-field compare-and-skip on conflict (D-06)"
    - path: "src/hooks/useWriteAheadQueue.ts"
      provides: "flush update branch applies .eq(updated_at) precondition + reconcile + legacy fallback"
    - path: "src/db/types.ts"
      provides: "WriteAheadEntry.payload documents the updated_at snapshot convention (no Dexie bump)"
  key_links:
    - from: "src/services/geminiContinuous.ts"
      to: "value-at-read snapshot per field"
      via: "compare currentDbValue !== valueAtRead → skip"
      pattern: "valueAtRead|value-at-read|skip"
    - from: "src/hooks/useWriteAheadQueue.ts"
      to: "supabase.from(table).update(rest).eq('id').eq('updated_at')"
      via: "flush precondition"
      pattern: "eq\\(\"updated_at\""
---

<objective>
Close the two remaining write paths: the AI continuous-merge (`mergeFieldsIntoItem`) gains D-06 per-field compare-and-skip so it never clobbers a field the user changed since the merge's read; and the offline write-ahead flush applies the `updated_at` precondition (snapshotted in Plan 02) and routes 0-row results through reconcile with a legacy-entry fallback.

Purpose: These are the two paths that can silently lose a user's edit — the headline race (user edit vs AI continuous chunk) and the offline reconnect race. Both compose the Plan 02 helper / reconcile contract; this plan adds the per-writer intent policy (AI yields to user; offline re-applies user intent) on top of the shared precondition machinery.

Output: `mergeFieldsIntoItem` compare-and-skip + flush precondition + `WriteAheadEntry` payload doc. Turns `continuous-merge-no-clobber.test.ts` (HEADLINE) and the extended `write-ahead-queue.test.ts` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/39-optimistic-locking/39-CONTEXT.md
@.planning/milestones/v1.3-phases/39-optimistic-locking/39-RESEARCH.md
@.planning/milestones/v1.3-phases/39-optimistic-locking/39-PATTERNS.md

<interfaces>
<!-- mergeFieldsIntoItem current per-field loop (geminiContinuous.ts:251-254) — where D-06 threads in. -->
```typescript
const sessionStore = useSessionStore.getState();
for (const [field, value] of updates) {
  await sessionStore.updateItemField(itemId, sessionId, field, value);
}
```

<!-- Current flush update branch to extend (useWriteAheadQueue.ts:87-96). -->
```typescript
} else if (entry.operation === "update") {
  const { id, ...rest } = entry.payload as { id: string; [key: string]: unknown };
  const { error } = await supabase.from(entry.table).update(rest).eq("id", id);
  if (error) throw error;
}
```

<!-- WriteAheadEntry.payload is Record<string,unknown> — NO Dexie bump needed (db/types.ts:105-112). -->
<!-- Reconcile helper from Plan 02. -->
From src/db/optimisticUpdate.ts: preconditionUpdate(id, buildPatch, prevUpdatedAt, opts)

<!-- Complementary guard — keep BOTH (db/items.ts:39-45). Phase 35 userEditedFields gates the AI RETRY path;
     Phase 39 compare-and-skip gates the continuous-MERGE path. mergeFieldsIntoItem calls
     sessionStore.updateItemField directly (bypasses the db/items.ts wrapper / Phase 35 flag by design). -->
<!-- CONTINUOUS_MODE_ENABLED = false (SessionDetail.tsx:35, D-050) — merge path is dormant; correctness still required. -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: D-06 per-field compare-and-skip in mergeFieldsIntoItem — GREEN the HEADLINE race test</name>
  <files>src/services/geminiContinuous.ts</files>
  <read_first>
    - src/tests/continuous-merge-no-clobber.test.ts (the RED HEADLINE spec from Plan 01 — the contract)
    - src/services/geminiContinuous.ts (lines 214-257 — mergeFieldsIntoItem builds `updates`, then the per-field loop at :251-254; and processContinuousChunk which already holds the item)
    - src/db/items.ts (lines 8-17 CATALOG_FIELDS; lines 39-45 — the Phase 35 userEditedFields complementary guard, keep both)
    - src/db/optimisticUpdate.ts (preconditionUpdate from Plan 02 — the reconcile machinery this composes)
  </read_first>
  <behavior>
    - On a write conflict (0-row), the merge re-reads the item and, per field it wants to write, compares the current DB value to the value-at-read captured at merge start.
    - currentDbValue !== valueAtRead → the user changed it → SKIP that field (AI yields to user, D-06/D-08).
    - currentDbValue === valueAtRead → safe → re-apply with the fresh updated_at precondition.
    - If all fields skip, the resulting patch is empty → no-op write (do not write).
    - A field the user did NOT touch survives and is re-applied. The user's changed field is NOT in the re-applied patch.
    - No extra Gemini round-trip — the value-at-read comes from the pre-merge item snapshot, not a re-fetch from the model.
  </behavior>
  <action>
    Thread a value-at-read snapshot into `mergeFieldsIntoItem`: capture the pre-merge item snapshot in `processContinuousChunk` (which already holds the item — RESEARCH Open Question 2) and pass it into `mergeFieldsIntoItem` as a new parameter. Replace the naive per-field loop at :251-254 so the merge composes the Plan 02 reconcile contract: build the patch via a `buildPatch(fresh)` that, on each (re-read) row, includes a field only when `fresh[field] === valueAtRead[field]` and skips it otherwise (D-06 compare-and-skip). Route through the shared precondition/reconcile path (`preconditionUpdate` or the same store path that uses it) rather than a bare `updateItemField` clobber. Do NOT add an extra Gemini call. Do NOT collapse this with the Phase 35 `userEditedFields` guard — keep both (different write paths; PATTERNS "complementary guard"). The merge path stays dormant (`CONTINUOUS_MODE_ENABLED=false`) but must be correct for revival; the test drives `mergeFieldsIntoItem` directly. NO fenced implementation beyond what the test asserts.
  </action>
  <verify>
    <automated>npx vitest --run src/tests/continuous-merge-no-clobber.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest --run src/tests/continuous-merge-no-clobber.test.ts` exits 0 — the HEADLINE: a field the user changed after merge-read is NOT overwritten; an untouched field IS re-applied.
    - `mergeFieldsIntoItem` receives a value-at-read snapshot parameter and the compare-and-skip is per-field (grep for the snapshot param + a `!==`/`===` field comparison).
    - No new Gemini/model call is added in the conflict path (grep shows no additional fetch to the proxy inside the reconcile branch).
    - The Phase 35 `userEditedFields` guard is untouched (still present in db/items.ts paths).
  </acceptance_criteria>
  <done>The AI continuous-merge yields to concurrent user edits per-field; the HEADLINE race test is GREEN; no extra Gemini cost.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Offline flush precondition + reconcile + legacy fallback; document payload snapshot convention</name>
  <files>src/hooks/useWriteAheadQueue.ts, src/db/types.ts</files>
  <read_first>
    - src/tests/write-ahead-queue.test.ts (the extended spec from Plan 01 — precondition-on-flush, 0-row reconcile, legacy fallback cases)
    - src/hooks/useWriteAheadQueue.ts (lines 80-140 — the update branch at :87-96, the permanent/transient classification at :115-140, the entry-delete at the end of the try)
    - src/db/types.ts (lines 105-112 — WriteAheadEntry.payload is Record<string,unknown>, NO Dexie bump)
    - src/db/optimisticUpdate.ts (preconditionUpdate / reconcile from Plan 02 — reuse, do not fork)
    - src/services/offlineQueue.ts (lines 119-132 — the 0-row idiom mirrored everywhere)
  </read_first>
  <behavior>
    - Flush of an `update` entry whose payload carries `updated_at`: pull `updated_at` OUT of the patch (it's a snapshot key, not a column to write), apply `.update(rest).eq("id", id).eq("updated_at", snapshot).select()`.
    - 0-row flush result routes through the SAME reconcile path as online writes (re-read → re-apply the user's field value → retry with fresh precondition, intent-preserving D-04). The queue entry is NOT deleted on a 0-row until reconcile resolves (Pitfall 5).
    - Legacy entry (payload has NO `updated_at`): fall back to re-read-then-precondition (fetch current `updated_at`, then apply precondition) — NOT an unconditional write, NOT a crash (Pitfall 6).
    - Genuine error → existing permanent/transient classification (classifyAiError) unchanged; FIFO/head-of-line behavior preserved.
  </behavior>
  <action>
    In the `update` branch at useWriteAheadQueue.ts:87-96: destructure `updated_at` out of `rest` so it is excluded from the written patch (it's a snapshot, not a column). When `updated_at` is present, apply the precondition `.eq("id", id).eq("updated_at", snapshot).select()` and route a 0-row result through the shared reconcile path (reuse `preconditionUpdate` / Plan 02 machinery — do NOT fork a second precondition impl). Re-apply the user's field value (intent-preserving, D-04). Do NOT call `db.writeAheadQueue.delete(entry.id)` for an entry whose write 0-rowed until reconcile resolves (Pitfall 5 — otherwise the offline edit silently vanishes). When `updated_at` is ABSENT (legacy entry enqueued before this deploy), re-read the row's current `updated_at` first, then apply the precondition (recommended fallback — Pitfall 6 / A2); do not do an unconditional last-writer-wins write. Leave the insert/delete branches and the existing `classifyAiError` permanent/transient handling and FIFO ordering untouched. In `src/db/types.ts`, add a WHY comment on `WriteAheadEntry.payload` documenting that an `updated_at` key carries the optimistic snapshot (no structural change, no Dexie version bump — payload is already `Record<string,unknown>`). NO fenced implementation beyond what tests assert.
  </action>
  <verify>
    <automated>npx vitest --run src/tests/write-ahead-queue.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest --run src/tests/write-ahead-queue.test.ts` exits 0 — precondition applied on flush; 0-row routes through reconcile and the edit survives; unrelated fields untouched; legacy entry handled without crash.
    - `useWriteAheadQueue.ts` update branch destructures `updated_at` out of the written patch and applies `.eq("updated_at"` (grep).
    - A 0-row flush does NOT delete the entry before reconcile resolves (grep/inspection: no unconditional `delete(entry.id)` on the 0-row path).
    - `src/db/types.ts` has a comment documenting the `updated_at` payload snapshot convention; `WriteAheadEntry` shape is otherwise unchanged (no Dexie version bump in `src/db/index.ts`).
  </acceptance_criteria>
  <done>Offline flush enforces the precondition, reconciles 0-row results without losing the edit, and tolerates legacy snapshot-less entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI merge write → Supabase | The merge's intended field values cross into the shared `items` row; a concurrent user edit must win per-field. |
| offline queue flush → Supabase | A queued snapshot (possibly hours stale) crosses on reconnect; the precondition gates it against server-side changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | Tampering | AI continuous-merge clobbering a user edit (HEADLINE lost-update) | mitigate | D-06 per-field compare-and-skip: on conflict, any field whose current DB value ≠ value-at-read is skipped (user wins). Proved by `continuous-merge-no-clobber.test.ts`. |
| T-39-04 | Tampering | offline flush silently dropping a stale edit | mitigate | Flush applies the `updated_at` precondition; 0-row routes through reconcile (re-apply intent), entry NOT deleted until reconcile resolves (Pitfall 5). |
| T-39-02 | Denial of Service | reconcile loop on flush (RLS-deny / deleted row → spin) | mitigate | Reuses the Plan 02 bounded helper: 3-attempt cap; re-read-returns-nothing stops the loop. Legacy fallback re-reads once then preconditions — no unbounded retry. |
| T-39-05 | Tampering | legacy snapshot-less entry doing last-writer-wins on flush | mitigate | Absent `updated_at` → re-read-then-precondition fallback (A2), not unconditional write — preserves no-clobber for in-flight legacy entries. |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | Phase adds NO packages; no install task. No `[ASSUMED]`/`[SUS]` package to gate. |
</threat_model>

<verification>
- `npx vitest --run src/tests/continuous-merge-no-clobber.test.ts` GREEN (HEADLINE).
- `npx vitest --run src/tests/write-ahead-queue.test.ts` GREEN (offline precondition + reconcile + legacy fallback).
- Full suite (`npm test`) GREEN at wave merge.
- Phase gate (before `/gsd:verify-work`): full suite green + manual migration-on-branch verification (trigger bumps `updated_at` on UPDATE).
</verification>

<success_criteria>
- AI continuous-merge skips any field the user changed since merge-read (D-06); untouched fields re-apply; no extra Gemini call.
- Offline flush applies the precondition, reconciles 0-row results (edit survives), tolerates legacy entries.
- No divergent second precondition implementation — both paths compose the Plan 02 helper.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/39-optimistic-locking/39-03-SUMMARY.md` when done.
</output>
