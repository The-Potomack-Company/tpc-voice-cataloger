---
phase: 39-optimistic-locking
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260603000000_add_items_updated_at_trigger.sql
  - src/db/database.types.ts
  - ../_workspace/Schema/schema.md
  - ../_workspace/Schema/migrations.md
  - src/tests/optimistic-update.test.ts
  - src/tests/continuous-merge-no-clobber.test.ts
  - src/tests/write-ahead-queue.test.ts
  - src/tests/supabase-types.test.ts
autonomous: true
requirements: [DAT-3]
user_setup: []

must_haves:
  truths:
    - "items table has an updated_at timestamptz column that auto-bumps on every UPDATE"
    - "Existing items rows are backfilled with updated_at = coalesce(created_at, now())"
    - "src/db/database.types.ts exposes items.Row.updated_at: string"
    - "RLS on items is unchanged by the migration"
    - "Wave-0 RED test files exist and fail (no implementation yet), proving the conflict/reconcile contract before impl"
  artifacts:
    - path: "supabase/migrations/20260603000000_add_items_updated_at_trigger.sql"
      provides: "updated_at column + backfill + BEFORE UPDATE trigger via set_updated_at()"
      contains: "create trigger items_updated_at"
    - path: "src/tests/optimistic-update.test.ts"
      provides: "RED spec for preconditionUpdate helper (0-row detect, re-read, 3-cap, exhaustion toast)"
      min_lines: 40
    - path: "src/tests/continuous-merge-no-clobber.test.ts"
      provides: "RED HEADLINE spec — AI merge skips user-changed field (D-06)"
      min_lines: 40
  key_links:
    - from: "supabase/migrations/20260603000000_add_items_updated_at_trigger.sql"
      to: "public.set_updated_at()"
      via: "execute function in trigger"
      pattern: "execute function public.set_updated_at"
    - from: "src/db/database.types.ts"
      to: "items.Row"
      via: "npm run db:types regen"
      pattern: "updated_at: string"
---

<objective>
Land the schema foundation for optimistic locking and stand up the Wave-0 RED test scaffolds that the implementation waves (Plans 02, 03) will turn GREEN.

Purpose: Phase 39 enforces optimistic concurrency on `items` writes via a `timestamptz` version token. That token does not exist yet — this plan adds the `items.updated_at` column, a `BEFORE UPDATE` trigger that bumps it, backfills existing rows, regenerates the generated types, and records the cross-app schema event. It also creates the failing tests so implementation is test-first (TDD mandate).

Output: One migration, regenerated `database.types.ts`, updated cross-app schema docs, and four RED test files (2 new, 2 extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/39-optimistic-locking/39-CONTEXT.md
@.planning/milestones/v1.3-phases/39-optimistic-locking/39-RESEARCH.md
@.planning/milestones/v1.3-phases/39-optimistic-locking/39-PATTERNS.md
@.planning/milestones/v1.3-phases/39-optimistic-locking/39-VALIDATION.md

<interfaces>
<!-- Reused DB asset — DO NOT redefine the function, attach only. -->
From supabase/migrations/20260421000000_create_updated_at_trigger.sql:
```sql
create or replace function public.set_updated_at()
returns trigger language plpgsql as $$
begin
  new.updated_at = now();
  return new;
end;
$$;
```

<!-- The ONLY proven attachment template in the cataloger (mirror this exactly). -->
From supabase/migrations/20260520120000_create_crm_v05_tables.sql:50-52:
```sql
create trigger crm_threads_updated_at
  before update on public.crm_threads
  for each row execute function public.set_updated_at();
```

<!-- The 0-row conflict idiom the RED tests must mock (data:[], error:null = CONFLICT). -->
From src/services/offlineQueue.ts:123-132 (Phase 33 claim CAS — exact precondition idiom Phase 39 generalizes):
```typescript
const { data: claimed } = await supabase
  .from("items").update({...}).eq("id", item.id).eq("ai_status", "queued").select("id");
if (!claimed || claimed.length === 0) return; // 0-row = lost the CAS
```

<!-- Existing mock idioms the new tests extend. -->
update-item-field-notify.test.ts: vi.hoisted({ mockFrom, mockNotifyError, mockEnqueueWrite }) + vi.mock("../lib/supabase", () => ({ supabase: { from: mockFrom } }))
gemini-no-clobber.test.ts: vi.hoisted chain { mockFrom, mockUpdate, mockEq, mockSelect, ... }
supabase-types.test.ts: type-level Tables<'items'> assertions
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Migration — add items.updated_at column + backfill + trigger (Claude-owned, Codex barred per D-046)</name>
  <files>supabase/migrations/20260603000000_add_items_updated_at_trigger.sql</files>
  <read_first>
    - supabase/migrations/20260520120000_create_crm_v05_tables.sql (lines 50-52 — the crm_threads_updated_at trigger template to mirror)
    - supabase/migrations/20260421000000_create_updated_at_trigger.sql (the set_updated_at() fn to REUSE, not redefine)
    - supabase/migrations/20260318000002_create_items.sql (original items DDL — confirm no updated_at, confirm RLS already enabled)
  </read_first>
  <action>
    Create a new migration that does exactly three things on public.items, in order: (1) `alter table public.items add column updated_at timestamptz not null default now()`; (2) backfill `update public.items set updated_at = coalesce(created_at, now())` per D-03; (3) `create trigger items_updated_at before update on public.items for each row execute function public.set_updated_at()` — mirroring the crm_threads_updated_at trigger. Do NOT redefine `set_updated_at()` (it already exists from 20260421000000 — attach only). Do NOT add any RLS statement, do NOT touch `enable row level security` (already on, must stay undisturbed per Security V4). Do NOT add an app-side updated_at to any write — the trigger owns the bump (Pitfall 2 / Don't Hand-Roll). This task is Claude-owned; it MUST NOT be delegated to Codex (D-046 bars Codex from supabase/ + schema). Use timestamp 20260603000000 (after the latest existing migration); adjust only if a later migration already claims it.
  </action>
  <verify>
    <automated>grep -q "create trigger items_updated_at" supabase/migrations/20260603000000_add_items_updated_at_trigger.sql && grep -q "execute function public.set_updated_at" supabase/migrations/20260603000000_add_items_updated_at_trigger.sql && grep -q "coalesce(created_at" supabase/migrations/20260603000000_add_items_updated_at_trigger.sql && ! grep -qi "row level security" supabase/migrations/20260603000000_add_items_updated_at_trigger.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists and contains: the `add column updated_at timestamptz not null default now()`, the `coalesce(created_at, now())` backfill, and `create trigger items_updated_at ... execute function public.set_updated_at()`.
    - Migration contains NO `create or replace function public.set_updated_at` (reuse, not redefine).
    - Migration contains NO `row level security` statement (RLS undisturbed).
    - Migration contains NO app-side `updated_at = ` assignment in any UPDATE patch (only the backfill UPDATE sets it explicitly).
  </acceptance_criteria>
  <done>Migration mirrors the crm_threads template, adds the column, backfills, and attaches the trigger; RLS untouched.</done>
</task>

<task type="execute">
  <name>Task 2: Regenerate database.types.ts + update cross-app schema docs (Claude-owned, Codex barred)</name>
  <files>src/db/database.types.ts, ../_workspace/Schema/schema.md, ../_workspace/Schema/migrations.md</files>
  <read_first>
    - src/db/database.types.ts (lines 339-363 — items.Row currently has created_at but NO updated_at)
    - ../_workspace/Schema/schema.md (items section — and the stale line claiming the updated_at trigger is "set on sessions"; no migration attaches it there)
    - ../_workspace/Schema/migrations.md (cross-app migration log format)
    - package.json (the db:types script — `npx supabase gen types ... --project-id wgrknodfxdjtddsirldw`)
  </read_first>
  <action>
    Run `npm run db:types` to regenerate src/db/database.types.ts from the live shared Supabase project after the migration is applied to the branch DB (the migration apply itself is the manual-only step in 39-VALIDATION.md — if the column is not yet present in the project, apply the migration via the Supabase MCP `apply_migration` or coordinate before regen). The regenerated file MUST add `updated_at: string` to `items.Row` and `updated_at?: string` to items Insert/Update. Do NOT hand-edit the generated file. Then update `../_workspace/Schema/schema.md`: add `updated_at timestamptz not null default now()` to the items table definition and note the BEFORE UPDATE trigger; opportunistically FIX the stale claim that the updated_at trigger is "set on sessions" (no migration attaches it there — the only real attachments are crm_threads and now items). Append a row to `../_workspace/Schema/migrations.md` logging migration 20260603000000 (cross-app event per D-03). All three edits are Claude-owned; Codex is barred (D-046).
  </action>
  <verify>
    <automated>grep -q "updated_at: string" src/db/database.types.ts && grep -qi "items" ../_workspace/Schema/schema.md && grep -q "20260603000000" ../_workspace/Schema/migrations.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/database.types.ts` items.Row contains `updated_at: string` (regenerated, not hand-edited — git diff shows a generated-shape change).
    - `../_workspace/Schema/schema.md` items definition lists `updated_at` and the trigger; the stale "set on sessions" claim is corrected.
    - `../_workspace/Schema/migrations.md` has a new row referencing migration `20260603000000`.
  </acceptance_criteria>
  <done>Types regenerated with updated_at; cross-app schema docs reflect the new column + trigger and the stale sessions claim is fixed.</done>
</task>

<task type="execute">
  <name>Task 3: Wave-0 RED tests — create the 4 conflict/reconcile spec files (TDD-first)</name>
  <files>src/tests/optimistic-update.test.ts, src/tests/continuous-merge-no-clobber.test.ts, src/tests/write-ahead-queue.test.ts, src/tests/supabase-types.test.ts</files>
  <read_first>
    - src/tests/update-item-field-notify.test.ts (lines 1-30 — vi.hoisted mockFrom + supabase/notificationStore/enqueueWrite mock idiom)
    - src/tests/gemini-no-clobber.test.ts (lines 1-35 — vi.hoisted chain mock with update/eq/select)
    - src/tests/write-ahead-queue.test.ts (existing — extend, do not rewrite)
    - src/tests/supabase-types.test.ts (lines 1-25 — type-level Tables<'items'> assertion idiom)
    - src/services/offlineQueue.ts (lines 119-132 — the proven .update().eq().eq().select() + length===0 idiom these tests mirror)
  </read_first>
  <action>
    Author four RED test files describing the Phase 39 contract before any implementation exists. Use the existing `vi.hoisted` mockFrom idiom; the chain MUST support `.update().eq().eq().select()` resolving `{ data, error }` (two `.eq()` calls — id + updated_at). CONFLICT mocks MUST be `{ data: [], error: null }` (NOT an error return — Pitfall 1; a test that mocks an error passes while the real path is broken).
    (a) `src/tests/optimistic-update.test.ts` (NEW): spec the shared `preconditionUpdate` helper from `src/db/optimisticUpdate.ts` (does not exist yet). Cases: success (`data.length>=1` → applied, returns fresh row); 0-row conflict triggers re-read then re-apply; loop refreshes `prev` to `fresh.updated_at` between attempts (assert the second write uses the new token, not the stale one — Pitfall 4); re-read returns nothing → stop, do NOT loop (deleted/RLS-deny disambiguation); 3-attempt exhaustion calls `notifyError(message, retry)` with a retry callback.
    (b) `src/tests/continuous-merge-no-clobber.test.ts` (NEW, HEADLINE): drive `mergeFieldsIntoItem` (from `src/services/geminiContinuous.ts`) DIRECTLY — never via the continuous UI (`CONTINUOUS_MODE_ENABLED=false`, dormant). Mock the AI write to 0-row-conflict while a field the user changed after merge-read differs from value-at-read. Assert that field is NOT included in the re-applied patch (D-06 compare-and-skip — AI yields to user). Assert a field the user did NOT touch IS re-applied. Label the suite "continuous-merge path (dormant — D-050)".
    (c) `src/tests/write-ahead-queue.test.ts` (EXTEND): add cases — enqueue captures the item's `updated_at` snapshot into the payload (D-04); on flush the update branch applies `.eq("updated_at", snapshot).select()`; a 0-row flush routes through reconcile and does NOT delete the queue entry (Pitfall 5); a legacy entry whose payload has NO `updated_at` falls back to re-read-then-precondition (not unconditional write, not crash) (Pitfall 6).
    (d) `src/tests/supabase-types.test.ts` (EXTEND): add a type-level assertion that `Tables<'items'>['updated_at']` is `string`.
    These tests are EXPECTED TO FAIL now (RED) — `optimisticUpdate.ts` and the wiring do not exist. Do NOT add implementation to make them pass; that is Plans 02/03. NO fenced implementation code in this task beyond test bodies.
  </action>
  <verify>
    <automated>test -f src/tests/optimistic-update.test.ts && test -f src/tests/continuous-merge-no-clobber.test.ts && grep -q "data: \[\]" src/tests/optimistic-update.test.ts && grep -q "updated_at" src/tests/supabase-types.test.ts && echo "FILES_OK — RED expected"</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist; `optimistic-update.test.ts` and `continuous-merge-no-clobber.test.ts` are new, the other two are extended (git diff shows added cases, not rewrites).
    - Every CONFLICT mock resolves `{ data: [], error: null }` (grep confirms `data: []`, no conflict case mocks an `error`).
    - `continuous-merge-no-clobber.test.ts` imports and calls `mergeFieldsIntoItem` directly (grep `mergeFieldsIntoItem`), not the continuous recorder UI.
    - Running `npx vitest --run src/tests/optimistic-update.test.ts` FAILS (RED) because `src/db/optimisticUpdate.ts` does not exist — this is the expected Wave-0 state.
  </acceptance_criteria>
  <done>Four RED test files encode the conflict/reconcile/compare-skip/offline contract; they fail because implementation is deferred to Plans 02–03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (PostgREST UPDATE) | Client supplies the `updated_at` precondition snapshot; the DB enforces the match atomically. The snapshot originates from the client's own prior read, not from untrusted external input. |
| migration → shared Postgres | DDL/DML run against the shared project; must not disturb existing RLS on `items`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-00 | Tampering | migration on shared `items` | mitigate | Add column + trigger ONLY; no RLS statement, no data delete; backfill is additive. Verify (automated grep) that the migration contains no `row level security` line. |
| T-39-01 | Information Disclosure | regenerated `database.types.ts` | accept | Generated types expose only the new column name (`updated_at`), already public schema metadata. No secrets. |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | Phase adds NO packages (RESEARCH Standard Stack: "Installation: none"). No install task in this phase; no `[ASSUMED]`/`[SUS]` packages to gate. |
</threat_model>

<verification>
- `npm run db:types` produces a `database.types.ts` whose `items.Row` includes `updated_at: string`.
- Migration applied to a Supabase branch: `UPDATE items SET title='x' WHERE id=…` advances `updated_at` past its prior value (manual-only per 39-VALIDATION.md — Claude-owned, D-046).
- The four RED test files exist; `optimistic-update.test.ts` fails because `src/db/optimisticUpdate.ts` is absent (correct Wave-0 state).
</verification>

<success_criteria>
- `items.updated_at` column + `items_updated_at` BEFORE UPDATE trigger exist in the migration, reusing `set_updated_at()`.
- Existing rows backfilled `coalesce(created_at, now())`.
- `database.types.ts` regenerated (not hand-edited) with `updated_at: string`.
- Cross-app `schema.md` + `migrations.md` updated; stale "trigger on sessions" claim fixed.
- Four RED test files in place, failing pending implementation.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/39-optimistic-locking/39-01-SUMMARY.md` when done.
</output>
