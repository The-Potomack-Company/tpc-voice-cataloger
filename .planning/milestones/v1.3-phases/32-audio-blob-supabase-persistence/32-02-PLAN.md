---
phase: 32-audio-blob-supabase-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - src/db/database.types.ts
# autonomous false: Codex review gate (D-046) + [BLOCKING] prod db push (D-08) + authorized prod RLS reads
autonomous: false
requirements: []
decisions_covered: [D-02, D-04, D-07, D-08]

must_haves:
  truths:
    - "The create_audio migration passes a Codex adversarial review with no unresolved high-severity findings before any push (D-046)"
    - "`supabase db push --dry-run` shows ONLY the create_audio migration pending (no unsafe sibling rides along)"
    - "The migration is applied to prod (wgrknodfxdjtddsirldw) and recorded in migration history"
    - "On prod, the `audio` table + `audio` bucket + items.completed_at + pg_cron/pg_net + purge-old-audio job exist"
    - "Cross-user RLS denies a specialist reading another session's audio blob (T-32-01 proven live)"
    - "The session OWNER can read/list their own audio objects under the column-scoped RLS (the Phase-31-style self-deny did not recur) (T-32-08 proven live)"
    - "`npm run db:types` regenerates src/db/database.types.ts and the `audio` table appears in generated types"
  artifacts:
    - path: "src/db/database.types.ts"
      provides: "Regenerated types including the new public.audio table (so supabase.from('audio') typechecks for plans 03-05)"
      contains: "audio"
  key_links:
    - from: "node_modules/.bin/supabase db push"
      to: "prod wgrknodfxdjtddsirldw"
      via: "--dry-run isolation then --yes apply"
      pattern: "db push"
    - from: "src/db/database.types.ts"
      to: "public.audio"
      via: "npm run db:types regen"
      pattern: "audio"
---

<objective>
Gate the authored `create_audio` migration through the mandatory Codex adversarial review (D-046), apply it to prod isolated from any unsafe sibling via the `--dry-run` gate, verify the new bucket/table/RLS/pg_cron exist and that cross-user RLS denies a foreign-session blob read (T-32-01), and regenerate `src/db/database.types.ts` so the client plans (03-05) typecheck against the live `audio` table.

Purpose: This is the [BLOCKING] in-phase prod cutover for the audio durability surface. Claude owns the schema/auth/RLS change; Codex reviews adversarially but does NOT implement (D-046). The push is mandatory — build + type checks pass WITHOUT it (types come from config, not the live DB), so skipping it creates a false-positive verification state and the client code in plans 03-05 cannot upload/read against a non-existent table.

Output: prod has the audio table/bucket/RLS/pg_cron + items.completed_at; `src/db/database.types.ts` regenerated WITH the audio table; Codex verdict + RLS-deny proof + dry-run isolation evidence recorded in SUMMARY.

**CONTEXT.md decisions implemented by this plan:** D-046 (mandatory Codex review before push), D-08 (in-phase prod push enabling pg_cron/pg_net + the purge job), D-02/D-04 (the RLS shape verified live), D-07 (items.completed_at applied to prod). The migration itself was authored in plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-CONTEXT.md
@.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-RESEARCH.md
@.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-VALIDATION.md
@.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-01-SUMMARY.md

<interfaces>
<!-- Concrete commands the executor needs. -->

CLI: node_modules/.bin/supabase (v2.81.3, vendored). Prod ref: wgrknodfxdjtddsirldw.
  Dry-run isolation:      node_modules/.bin/supabase db push --dry-run
  Non-interactive apply:  SUPABASE_DB_PASSWORD=*** node_modules/.bin/supabase db push --yes
    (SUPABASE_ACCESS_TOKEN may be needed in env for non-TTY; user has pre-authorized prod db push as routine ops.)
  If not linked/authed → user-only one-liner: node_modules/.bin/supabase login ; node_modules/.bin/supabase link --project-ref wgrknodfxdjtddsirldw

Types regen: npm run db:types   (then confirm the `audio` table is present in src/db/database.types.ts — this IS expected to diff, unlike Phase 31 which was grants-only).

Live verification (authorized prod reads; Supabase MCP `list_tables` / SQL):
  - Confirm table public.audio exists with the unique storage_path index + upload_status CHECK.
  - Confirm bucket 'audio' exists (select id,name,public from storage.buckets where id='audio').
  - Confirm items.completed_at column exists.
  - Confirm pg_cron job: select jobname, schedule from cron.job where jobname='purge-old-audio'.
  - Confirm extensions: select extname from pg_extension where extname in ('pg_cron','pg_net').

T-32-01 cross-user RLS deny (the security proof — manual/SQL, no automated harness; supabase/tests absent):
  Using a disposable specialist token for user B, attempt to read/list a storage.objects path under a session owned by user A (audio/{A-sessionId}/...). Expected: denied / empty (RLS scopes to created_by|assigned_to). Also assert user B select on public.audio for an item in user A's session returns no rows. Record request + observed result in SUMMARY.

Migration-isolation note: run `db push --dry-run` and confirm the ONLY pending migration is 20260601000000_create_audio.sql (or whatever timestamp plan 01 used). If ANY other migration is pending, STOP and escalate (a stray sibling could mutate prod unexpectedly — see the Phase 31 receipt-NULL precedent).
</interfaces>
</context>

<read_first>
- supabase/migrations/20260601000000_create_audio.sql  (the migration under review — read IN FULL before submitting to Codex)
- supabase/functions/purge-audio/index.ts  (the cron-invoked reaper — Codex reviews its secret gate too)
- ../_workspace/Decisions/D-046-claude-owns-schema-auth-codex-barred.md  (review contract)
- ../_workspace/Schema/schema.md  (confirm plan 01's audio-table update matches what gets pushed)
- 32-RESEARCH.md §Common Pitfalls 1 (column-scope) & 2 (orphan binaries) + §Security Domain STRIDE table
- .planning/milestones/v1.3-phases/31-sec-profiles-self-update-hardening/31-02-PLAN.md  (the proven Codex→dry-run→apply→db:types sequence this plan mirrors)
</read_first>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Mandatory Codex adversarial review of the create_audio migration + purge-audio edge fn (D-046)</name>
  <files>supabase/migrations/20260601000000_create_audio.sql, supabase/functions/purge-audio/index.ts</files>
  <what-built>The plan-01 migration (audio table + bucket + column-scoped storage RLS + items.completed_at + pg_cron/pg_net + purge-old-audio job) and the service-role purge-audio edge function. Claude authored both; per D-046 Codex adversarially reviews auth/schema/RLS changes before apply and is barred from implementing.</what-built>
  <action>
Submit BOTH the migration and the purge-audio edge function to Codex for adversarial review (D-046 — auth/schema/RLS is Claude-owned, Codex reviews but does NOT implement). Focus the review on, per the T-32 STRIDE register: (1) EVERY storage.objects specialist policy uses the column-qualified `storage.foldername(storage.objects.name)` — NOT the buggy unqualified `name` form that silently denied owners in the Phase 31 photo bug (Pitfall 1); (2) all four CRUD verbs are scoped (select/insert/update/delete) — the UPDATE policy exists because upsert:true issues an UPDATE on retry, the DELETE policy is scoped to own sessions only (D-04); (3) the audio TABLE policies scope via item→session ownership; (4) the purge-audio function CANNOT be invoked by an unauthenticated/cross-user caller to delete arbitrary blobs (secret gate present; no caller-supplied paths; service-role bypasses RLS so the gate is load-bearing — T-32-04); (5) the pg_cron body uses pg_net.http_post, NOT `DELETE FROM storage.objects` (which orphans the binary — the leak D-04 closes); (6) retention keys on items.completed_at, not created_at (D-03/D-07); (7) idempotency / re-runnability. If ANY finding is `high` severity, Claude fixes the migration/function (Codex does NOT) and re-reviews until clean. Record the Codex verdict (pass / findings + resolutions) in the SUMMARY.
  </action>
  <how-to-verify>
1. Submit the migration + purge-audio fn to Codex with the review focus above.
2. Codex returns findings. If any is `high` severity, Claude fixes (Codex does not) and re-reviews until clean.
3. Record the verdict (pass / findings + resolutions) in the SUMMARY.
  </how-to-verify>
  <verify>
    <automated>MISSING — Codex adversarial review is a human-gated D-046 control; verdict recorded in SUMMARY. The column-scope grep gate already passed in plan 01 Task 1.</automated>
  </verify>
  <acceptance_criteria>
    - Codex adversarial review run on BOTH the migration and the purge-audio edge function (auth/schema/RLS → D-046 mandatory)
    - Zero unresolved high-severity findings; any findings fixed by Claude (not Codex) and re-reviewed
    - Codex verdict recorded in SUMMARY
  </acceptance_criteria>
  <done>Codex review passed with no unresolved high-severity findings; verdict recorded in SUMMARY.</done>
  <resume-signal>Type "approved" once Codex review passes with no unresolved high-severity findings, or describe findings to fix.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] db push --dry-run isolation gate + apply create_audio to prod (D-08)</name>
  <files>supabase/migrations/20260601000000_create_audio.sql</files>
  <what-built>The Codex-reviewed migration, ready to apply to prod via the vendored Supabase CLI. This is the in-phase prod cutover (D-08). Marked blocking because it mutates prod (new bucket + RLS + extensions) and may require user-only CLI auth; the user has pre-authorized `supabase db push` to prod as routine ops.</what-built>
  <action>
PRECONDITION: verify the CLI is linked + authed — run `node_modules/.bin/supabase db push --dry-run`; if it errors not-linked/not-authed, STOP and hand the user the one-liner `node_modules/.bin/supabase login` then `node_modules/.bin/supabase link --project-ref wgrknodfxdjtddsirldw` (user-only auth; SUPABASE_ACCESS_TOKEN in env covers non-TTY).

ISOLATION GATE (Phase 31 precedent — `db push` is greedy and applies ALL pending migrations): the `--dry-run` output's ONLY pending migration MUST be the create_audio migration (20260601000000_create_audio.sql or its bumped timestamp). If ANY other migration is listed pending, STOP — do not push — and escalate.

APPLY: `SUPABASE_DB_PASSWORD=<from env/user> node_modules/.bin/supabase db push --yes`. Confirm the CLI reports the migration applied + recorded in migration history.

POST-APPLY live confirmation (authorized prod reads via Supabase MCP / SQL): confirm `public.audio` table exists (with the storage_path unique index + upload_status CHECK), bucket 'audio' exists, `items.completed_at` exists, extensions pg_cron + pg_net are present, and the `purge-old-audio` cron job is scheduled. Record outputs in SUMMARY.
  </action>
  <how-to-verify>
1. PRECONDITION — `node_modules/.bin/supabase db push --dry-run`; if not linked/authed, STOP + hand the login+link one-liner.
2. ISOLATION GATE — only the create_audio migration may be pending; STOP + escalate if anything else is.
3. APPLY — `SUPABASE_DB_PASSWORD=<...> node_modules/.bin/supabase db push --yes`.
4. POST-APPLY — confirm audio table + bucket + items.completed_at + pg_cron/pg_net + purge-old-audio job exist (SQL/MCP reads).
  </how-to-verify>
  <verify>
    <automated>MISSING — prod apply is verified via the dry-run isolation output + CLI apply confirmation + authorized prod existence reads (manual). The types-regen automated check runs in Task 4.</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --dry-run` shows ONLY the create_audio migration pending (isolation confirmed)
    - Migration applied to prod (wgrknodfxdjtddsirldw) and recorded in migration history
    - Live reads confirm: public.audio table (+ unique storage_path index + CHECK), bucket 'audio', items.completed_at, pg_cron + pg_net extensions, purge-old-audio cron job
  </acceptance_criteria>
  <done>Migration applied to prod isolated from any sibling; audio table/bucket/RLS/pg_cron/completed_at confirmed live; evidence in SUMMARY.</done>
  <resume-signal>Type "applied" once the dry-run shows only the create_audio migration and the push succeeds; or report the isolation/auth blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Cross-user RLS deny proof on prod (T-32-01 / D-02)</name>
  <files>(authorized prod reads — no files modified)</files>
  <read_first>
- 32-RESEARCH.md §Security Domain (known threat patterns) + §Common Pitfalls 1
- supabase/migrations/20260601000000_create_audio.sql (the storage policies being proven)
  </read_first>
  <action>
Prove the column-scoped storage RLS actually denies cross-user blob reads (the load-bearing security control; the anon key is public per D-003 so RLS is the only boundary).

Using a disposable specialist token for user B (NOT an admin, NOT the session owner), attempt to read/list a `storage.objects` path under bucket 'audio' beneath a session owned by user A (e.g. `audio/{A-sessionId}/{itemId}/...`). Expected: denied / empty result. Separately, as user B, `select * from public.audio` for an item belonging to user A's session — expected: zero rows (table RLS via item→session ownership). If EITHER read returns user A's audio, the RLS is broken (column-scope collision or missing scope); STOP and escalate. Conversely, sanity-check that the OWNER (user A token) CAN read their own audio object (proves the column-scope fix did not over-deny — the Phase 31 self-deny failure mode). Record the requests + observed results in SUMMARY.
  </action>
  <verify>
    <automated>MISSING — cross-user RLS deny is an authorized prod read with a disposable specialist token; no automated harness exists (supabase/tests absent). Verified by inspecting query results; recorded in SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - User B (foreign session) storage read/list under user A's audio path → denied/empty
    - User B select on public.audio for user A's item → zero rows
    - Owner (user A) CAN read their own audio object (column-scope fix did not over-deny)
    - All three results recorded in SUMMARY
  </acceptance_criteria>
  <done>Cross-user RLS deny proven live (foreign read denied, owner read allowed); evidence in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate database.types.ts with the audio table (V — types gate for plans 03-05)</name>
  <files>src/db/database.types.ts</files>
  <read_first>
- src/db/database.types.ts (the file regenerated; confirm `audio` table appears)
- 31-02-PLAN.md Task 4 (the db:types regen pattern — note: Phase 31 expected ZERO diff; THIS phase expects the audio table to appear, so a diff is correct)
  </read_first>
  <action>
Run `npm run db:types` to regenerate `src/db/database.types.ts` from the live prod schema. UNLIKE Phase 31 (grants-only, zero diff), THIS phase adds a table — the regen MUST introduce the `audio` table (and `items.completed_at`) into the generated types. Confirm the `audio` table is present in the generated `Tables<"audio">` shape with `item_id`, `storage_path`, `mime_type`, `upload_status`, `created_at`, and that `items` now carries `completed_at`. If the regen does NOT show the audio table, the push did not land or the CLI is pointed at the wrong project — STOP and investigate before committing. Commit the regenerated file.
  </action>
  <verify>
    <automated>npm run db:types && grep -c '"audio"\|audio:' src/db/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:types` runs clean and regenerates src/db/database.types.ts
    - The generated types include the `audio` table (Tables<"audio"> resolves) with item_id/storage_path/mime_type/upload_status/created_at
    - `items` row type includes `completed_at`
    - Regenerated file committed
  </acceptance_criteria>
  <done>database.types.ts regenerated WITH the audio table + items.completed_at; committed; plans 03-05 can now typecheck supabase.from('audio').</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon-key client → Storage API → storage.objects (bucket 'audio') | Public anon key; column-scoped storage RLS is the enforced boundary — proven live by T-32-01 (Task 3). |
| local CLI → prod migration history | `db push` is greedy — applies ALL pending; the --dry-run isolation gate (Task 2) prevents a stray sibling cutover. |
| pg_cron (server) → purge-audio | Secret-gated server-to-server; reviewed by Codex (Task 1) before it can ever be scheduled on prod. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | cross-user audio blob read on prod | mitigate | Column-scoped storage RLS verified live by Task 3 (foreign read denied, owner read allowed) |
| T-32-04 | Spoofing / Elevation | purge-audio invocable by cross-user/unauth caller | mitigate | Codex review (Task 1) confirms the secret gate + self-computed path set before prod scheduling |
| T-32-07 | Tampering / data loss | a stray sibling migration rides along on the greedy push | mitigate | `db push --dry-run` isolation gate (Task 2) — STOP if any migration other than create_audio is pending |
| T-32-08 | DoS (self-inflicted) | column-scope collision over-denies the owner | mitigate | Task 3 owner-read sanity check (the Phase 31 self-deny failure mode) |
| T-32-SC | Tampering | CLI/package supply chain | accept | No installs; vendored CLI v2.81.3 pinned + unchanged (RESEARCH §Standard Stack) |

ASVS L1: the high-severity Information-Disclosure threat (T-32-01) carries a live-verified `mitigate` disposition (Task 3). The only `accept` is the no-install supply-chain item.
</threat_model>

<verification>
- Codex review passed (no unresolved high findings) before any push.
- `db push --dry-run` isolation: only create_audio pending.
- Prod: audio table + bucket + items.completed_at + pg_cron/pg_net + purge-old-audio job exist.
- T-32-01: foreign-session storage + table reads denied; owner read allowed.
- `npm run db:types` regenerates types WITH the audio table + items.completed_at.
</verification>

<success_criteria>
- Codex adversarial review passed (migration + edge fn), no unresolved high findings.
- Migration applied to prod isolated from any sibling.
- Cross-user RLS deny proven live; owner access preserved.
- database.types.ts regenerated with the audio table — plans 03-05 unblocked.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-02-SUMMARY.md` when done.
</output>
