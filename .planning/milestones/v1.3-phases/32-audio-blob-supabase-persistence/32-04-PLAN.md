---
phase: 32-audio-blob-supabase-persistence
plan: 04
type: execute
wave: 4
depends_on:
  - 32-03
files_modified:
  - src/hooks/useAudioRecorder.ts
  - src/services/gemini.ts
  - src/db/audioLookup.ts
  - src/tests/audio-lookup.test.ts
autonomous: true
requirements: []
decisions_covered: [D-02, D-05, D-07]
tdd: true

must_haves:
  truths:
    - "On stopRecording, the audio row carries sessionId and the upload is enqueued+drained fire-and-forget without blocking AI processing"
    - "The upload enqueue uses the UUID itemId and a mime-derived ext, never the legacy-int coercion or hardcoded .opus"
    - "processAudioWithAi falls back to Storage download when the local Dexie blob is missing, resolving the row by item_id (UUID)"
    - "When an item reaches ai_status 'done', items.completed_at is set (the D-07 retention baseline)"
    - "audioRecordsForItem also unions Supabase audio rows for the item (cross-device audio is visible by count; per rule (a) the status pill is silent on cross-device-only rows — accepted limitation)"
  artifacts:
    - path: "src/hooks/useAudioRecorder.ts"
      provides: "sessionId persisted on the audio row + fire-and-forget enqueueAudioUpload after db.audio.add"
      contains: "enqueueAudioUpload"
    - path: "src/services/gemini.ts"
      provides: "Storage download fallback + completed_at write on done"
      contains: "completed_at"
    - path: "src/db/audioLookup.ts"
      provides: "audioRecordsForItem extended to union Supabase audio"
  key_links:
    - from: "src/hooks/useAudioRecorder.ts"
      to: "enqueueAudioUpload + drainAudioQueue"
      via: "fire-and-forget after db.audio.add"
      pattern: "enqueueAudioUpload"
    - from: "src/services/gemini.ts"
      to: "supabase.storage.from('audio').download"
      via: "fallback when db.audio.get is undefined, keyed by item_id"
      pattern: "storage.from\\(\"audio\"\\).download"
---

<objective>
Wire the recorder and AI pipeline to the durable-audio plumbing: thread `sessionId` onto the Dexie audio row and fire-and-forget `enqueueAudioUpload(...).then(drainAudioQueue)` after `db.audio.add` (D-05 parallel, never blocking AI); add the cross-device Storage download fallback to `processAudioWithAi` (keyed by `item_id` UUID); set `items.completed_at` when an item reaches `ai_status: 'done'` (D-07); and extend `audioRecordsForItem` to union Supabase audio rows. Makes the plan-01 `audio-storage-fallback.test.ts` scaffold green and the extended `audio-lookup.test.ts` green.

Purpose: This connects the queue/status primitives (plan 03) to the actual recording + AI + lookup flow. The four PATTERNS.md threading issues all land here: UUID-not-int itemId, missing sessionId on the row, cross-device item_id keying, and mime/ext correctness. The D-07 completed_at write is the retention clock the pg_cron purge keys on.

Output: recordings upload in the background, cross-device retry pulls from Storage, completed items stamp completed_at, and ItemCard's audio lookup sees Supabase audio.

**CONTEXT.md decisions implemented by this plan:** D-02 (sessionId on the row + path token threading), D-05 (parallel fire-and-forget upload + Storage fallback as the non-primary read path), D-07 (set completed_at on the single-item AI-done write-path; continuous-mode write-paths are OUT of scope per D-050).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-CONTEXT.md
@.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-PATTERNS.md
@.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-RESEARCH.md
@.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-03-SUMMARY.md

<interfaces>
<!-- Concrete seams + contracts. -->

useAudioRecorder.onstop seam — src/hooks/useAudioRecorder.ts:180-205.
  - sessionIdRef ALREADY exists (:43, set at :139); itemIdRef ALREADY exists (:42, set at :138). Currently neither sessionId is written on the row nor is any upload enqueued.
  - Delta 1: in the db.audio.add({...}) call (:187-194), add `sessionId: sessionIdRef.current`. (NOTE the existing buggy `itemId: itemIdRef.current as unknown as number` at :188 stays for the Dexie row's legacy shape — but the UPLOAD path below must use the UUID string, not this coercion.)
  - Delta 2: after `const id = await db.audio.add(...)` resolves (after :194), fire-and-forget exactly like PhotoCapture.tsx:131-136:
      const ext = extFromMime(detectedMimeTypeRef.current);
      enqueueAudioUpload({ dexieAudioId: id as number, itemId: itemIdRef.current /* UUID string, NOT the as-unknown-as-number */, sessionId: sessionIdRef.current, ext })
        .then(() => drainAudioQueue()).catch(() => {});
  - import enqueueAudioUpload, drainAudioQueue from ../services/audioUploadQueue; extFromMime from ../utils/audio.
  - AI trigger in RecordButton.tsx:23 stays UNCHANGED (D-05 parallel/immediate).

processAudioWithAi fallback seam — src/services/gemini.ts:189-202.
  Current: `const audioRecord = await db.audio.get(audioId); if (!audioRecord) throw ...; ... blobToBase64(audioRecord.blob)`.
  Delta: when db.audio.get(audioId) is undefined, resolve by item_id (UUID) NOT the local integer audioId (Pitfall 4):
    const { data: row } = await supabase.from('audio').select('storage_path, mime_type').eq('item_id', itemId).maybeSingle();
    if (!row) throw new Error(`Audio for ${itemId} not in Dexie or Storage`);
    const { data: dl } = await supabase.storage.from('audio').download(row.storage_path);
    if (!dl) throw new Error(`Storage blob missing for ${row.storage_path}`);
    then use dl (a Blob) for blobToBase64 + use row.mime_type for the baseMimeType strip.
  Keep the existing mimeType.split(';')[0] content-type strip (:225). Discretion (CONTEXT): may re-cache the downloaded blob into Dexie — optional, document the choice.

completed_at write seam (D-07) — src/services/gemini.ts:314-317 (the supabaseUpdate object where `ai_status: 'done'` is set).
  Delta: add `completed_at: new Date().toISOString()` to the supabaseUpdate object alongside `ai_status: 'done'`.
  OUT OF SCOPE (D-050 continuous gated off): geminiContinuous.ts:212 and continuousModeStore.ts:118 — do NOT wire (note in SUMMARY).

audioRecordsForItem extension — src/db/audioLookup.ts:13-20.
  Current: unions byUuid + byLegacy Dexie rows, deduped by id (the `seen` Set). DAT-7 comment :5-12 stays.
  Delta: ALSO query Supabase audio rows for the item (supabase.from('audio').select(...).eq('item_id', itemId)) and merge them into the returned list so cross-device audio (no local Dexie row) is visible to ItemCard. Preserve the dedupe pattern. RULE (a), Dexie-authoritative (see Task 3): Supabase rows have no Dexie integer `id` — contribute them ONLY when no Dexie row exists for the item, leave their id undefined (raises `.length`/count, NOT the integer `latestAudioId` reduce). Known limitation: cross-device-only audio shows the count but the status pill is silent. Do NOT widen `useAudioUploadStatus` and do NOT break the existing byUuid/byLegacy union.

itemId rule (PATTERNS issue 1): the upload path + the audio metadata + the Storage fallback ALL key on the Supabase UUID `itemId`, never the legacy int or the `as unknown as number` coercion.
</interfaces>
</context>

<read_first>
- src/hooks/useAudioRecorder.ts:42-43,138-139,180-205  (refs + onstop seam — sessionIdRef/itemIdRef exist; db.audio.add)
- src/components/PhotoCapture.tsx:120-143  (the fire-and-forget enqueue-after-add analog to mirror)
- src/services/gemini.ts:160-226  (processAudioWithAi seam; db.audio.get + blobToBase64 + mime strip) + :314-329 (the done write-path)
- src/hooks/usePhotoUrl.ts:18-35  (Storage read-fallback pattern)
- src/db/audioLookup.ts (whole file — the union to extend, DAT-7 comment)
- src/services/audioUploadQueue.ts  (plan 03 — enqueueAudioUpload/drainAudioQueue signatures) + src/utils/audio.ts (extFromMime)
- src/tests/audio-storage-fallback.test.ts + src/tests/audio-lookup.test.ts  (the scaffolds to make green)
- 32-PATTERNS.md §"useAudioRecorder.ts" + §"gemini.ts processAudioWithAi" + §"audioLookup.ts" + §"items.completed_at" + §Prominent Issues 1-4
  </read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread sessionId + fire-and-forget enqueue in useAudioRecorder.onstop</name>
  <files>src/hooks/useAudioRecorder.ts</files>
  <read_first>
- src/hooks/useAudioRecorder.ts:42-43,138-139,180-205 (refs + onstop + db.audio.add)
- src/components/PhotoCapture.tsx:130-136 (fire-and-forget enqueue analog)
- src/services/audioUploadQueue.ts (enqueueAudioUpload + drainAudioQueue) + src/utils/audio.ts (extFromMime)
- 32-PATTERNS.md §"useAudioRecorder.ts" + Prominent Issues 1,2,4
  </read_first>
  <behavior>
    - After stopRecording resolves, the Dexie audio row carries sessionId = the recording session's UUID
    - enqueueAudioUpload is called once with { dexieAudioId: <the new id>, itemId: <UUID string>, sessionId: <UUID>, ext: extFromMime(detected mime) } and a drain is kicked
    - The enqueue is fire-and-forget: a rejected enqueue/drain does NOT throw out of onstop and does NOT block the existing stopResolveRef resolution or AI trigger
    - No `.opus` hardcoded; ext follows detectedMimeTypeRef
  </behavior>
  <action>
In `src/hooks/useAudioRecorder.ts` `onstop` (:180-205): (1) add `sessionId: sessionIdRef.current` to the `db.audio.add({...})` object (:187-194). (2) After `const id = await db.audio.add(...)` resolves, before/after the existing `store.getState().setLastSaved` call, add the fire-and-forget enqueue mirroring `PhotoCapture.tsx:130-136`: compute `const ext = extFromMime(detectedMimeTypeRef.current)` then `enqueueAudioUpload({ dexieAudioId: id as number, itemId: itemIdRef.current, sessionId: sessionIdRef.current, ext }).then(() => drainAudioQueue()).catch(() => {})`. Use `itemIdRef.current` as the UUID STRING for the upload — do NOT reuse the `as unknown as number` coercion from the db.audio.add row. Import `enqueueAudioUpload, drainAudioQueue` from `../services/audioUploadQueue` and `extFromMime` from `../utils/audio`. Leave the AI trigger (RecordButton.tsx) untouched (D-05).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
    <!-- No useAudioRecorder test file exists; the recorder behavioral assertion (sessionId on row + fire-and-forget enqueue with UUID itemId + mime-derived ext) is covered by src/tests/audio-upload-queue.test.ts (plan 03 GREEN) which asserts the enqueue contract this task feeds. tsc gate proves the wiring typechecks. -->
  </verify>
  <acceptance_criteria>
    - db.audio.add object includes `sessionId: sessionIdRef.current`
    - enqueueAudioUpload called with UUID `itemId` (itemIdRef.current, not coerced), sessionId, dexieAudioId, mime-derived ext; followed by drainAudioQueue; `.catch(()=>{})` makes it fire-and-forget
    - Imports added; AI trigger path unchanged; project typechecks
  </acceptance_criteria>
  <done>Recorder threads sessionId onto the row and enqueues a background upload fire-and-forget; mime-derived ext; UUID itemId.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Storage download fallback in processAudioWithAi + completed_at on done (D-07)</name>
  <files>src/services/gemini.ts</files>
  <read_first>
- src/services/gemini.ts:160-226 (processAudioWithAi seam) + :314-329 (done write-path)
- src/hooks/usePhotoUrl.ts:18-35 (Storage fallback read pattern)
- src/tests/audio-storage-fallback.test.ts (the scaffold to make green)
- 32-PATTERNS.md §"gemini.ts processAudioWithAi" + §"items.completed_at" + Pitfall 4
  </read_first>
  <behavior>
    - processAudioWithAi: when db.audio.get(audioId) returns a record, behavior is unchanged (uses the local blob)
    - when db.audio.get(audioId) returns undefined, it resolves the audio row from Supabase by item_id (UUID), downloads storage_path from the 'audio' bucket, and feeds that blob to blobToBase64
    - if neither Dexie nor Storage has the blob, it throws a clear error (does not silently no-op)
    - the AI-done write sets items.completed_at to an ISO timestamp alongside ai_status:'done'
  </behavior>
  <action>
At `src/services/gemini.ts:189-202`: replace the throw-on-missing with a fallback. Keep `const audioRecord = await db.audio.get(audioId)`. If `audioRecord` exists, use `audioRecord.blob` + `audioRecord.mimeType` as today. If undefined, resolve by item_id (UUID, NOT the integer audioId — Pitfall 4): `supabase.from('audio').select('storage_path, mime_type').eq('item_id', itemId).maybeSingle()`; if no row, throw `Audio for ${itemId} not in Dexie or Storage`; else `supabase.storage.from('audio').download(row.storage_path)`; if no data, throw `Storage blob missing`; use the downloaded blob for `blobToBase64` and `row.mime_type` for the `baseMimeType` strip (:225). Optionally re-cache the downloaded blob into `db.audio` (CONTEXT discretion — document the choice in SUMMARY).

At `src/services/gemini.ts:314-317`: add `completed_at: new Date().toISOString()` to the `supabaseUpdate` object alongside `ai_status: "done"` (D-07 — the pg_cron 30-day clock keys on this). Do NOT touch geminiContinuous.ts / continuousModeStore.ts (D-050 out of scope) — note this in SUMMARY.
  </action>
  <verify>
    <automated>npx vitest --run src/tests/audio-storage-fallback.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/tests/audio-storage-fallback.test.ts` passes (GREEN)
    - Fallback resolves the audio row by `item_id` (UUID), downloads from `storage.from('audio')`, feeds the blob to blobToBase64; throws clearly when both sources miss
    - Local-blob path unchanged when db.audio.get returns a record
    - supabaseUpdate at the done write-path includes `completed_at` (ISO string); continuous-mode write-paths untouched
  </acceptance_criteria>
  <done>Cross-device Storage fallback (keyed by item_id UUID) green; completed_at stamped on AI-done.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend audioRecordsForItem to union Supabase audio</name>
  <files>src/db/audioLookup.ts, src/tests/audio-lookup.test.ts</files>
  <read_first>
- src/db/audioLookup.ts (whole file — the byUuid/byLegacy union + DAT-7 comment + `seen` dedupe)
- src/tests/audio-lookup.test.ts (extend for the Supabase-audio union assertion)
- 32-PATTERNS.md §"audioLookup.ts" + Prominent Issue 3
  </read_first>
  <behavior>
    - audioRecordsForItem('<uuid>') still returns the byUuid + byLegacy Dexie union, deduped by id (existing behavior preserved)
    - when a Supabase audio row exists for the item but no local Dexie row (cross-device), the returned list includes a representation of that Supabase audio so ItemCard sees count > 0
    - no duplicate entries when a Dexie row and a Supabase row describe the same audio
  </behavior>
  <action>
Extend `audioRecordsForItem` (`src/db/audioLookup.ts:13-20`) — do NOT replace. Keep the byUuid + byLegacy Dexie queries and the `seen` Set dedupe. Add a query `supabase.from('audio').select('id, item_id, mime_type, storage_path, upload_status, created_at').eq('item_id', itemId)` and merge its rows into the returned `ItemAudio[]`, projecting to the shape ItemCard consumes (it reads `.id` for the latestAudioId reduce and `.length` for the count).

CHOSEN RULE (W-3 — rule (a), Dexie-authoritative for latestAudioId): Supabase `audio.id` is a UUID string but `useAudioUploadStatus` and the ItemCard `latestAudioId` reduce expect the Dexie integer id. To avoid a type-contract change with cross-plan blast radius, **Dexie rows remain authoritative for `latestAudioId`**: contribute a Supabase row to the returned list ONLY when there is no Dexie row for that item (cross-device-only audio), and do NOT assign it a fake integer id — leave its id undefined/null so it raises `.length` (count > 0, audio visible) without participating in the integer-keyed `latestAudioId` reduce. Consequence (KNOWN LIMITATION): on a pure cross-device session (audio recorded on device A, viewed on device B with no local Dexie row) the ItemCard audio COUNT shows correctly but the upload-status PILL is silent (no Dexie integer id to drive `useAudioUploadStatus`). This is an accepted limitation for Phase 32 — no `useAudioUploadStatus` type widening, no surrogate UUID projection. Document it in the SUMMARY and the CONTEXT/VALIDATION known-limitations note.

Extend `src/tests/audio-lookup.test.ts` to assert: (a) Dexie-only union unchanged; (b) Supabase-only audio (no Dexie row) yields a non-empty result whose count is > 0 (the cross-device-visible case); (c) no duplicate when both a Dexie row and a Supabase row describe the same item's audio (Dexie row wins the latestAudioId reduce).
  </action>
  <verify>
    <automated>npx vitest --run src/tests/audio-lookup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/tests/audio-lookup.test.ts` passes including the new Supabase-union assertions
    - Existing byUuid/byLegacy Dexie union behavior preserved (DAT-7); `seen` dedupe intact
    - Cross-device (Supabase-only) audio surfaces in the result; no double-count when Dexie + Supabase both describe the item's audio
  </acceptance_criteria>
  <done>audioRecordsForItem unions Supabase audio without breaking the Dexie DAT-7 union; cross-device audio visible to ItemCard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (recorder) → Storage upload | Background upload authorized by the plan-02 column-scoped insert/update storage RLS. |
| client (processAudioWithAi) → Storage download | Cross-device read authorized by the column-scoped select storage RLS; resolves by item_id then path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-12 | Information Disclosure | fallback keyed by local integer audioId leaks/misses cross-device | mitigate | Fallback resolves by item_id (UUID) via maybeSingle, never the integer audioId (Pitfall 4, Task 2) |
| T-32-13 | Tampering | upload path reuses the legacy-int itemId coercion → wrong RLS path scope | mitigate | enqueue uses itemIdRef.current UUID string, not the as-unknown-as-number coercion (Task 1) |
| T-32-14 | DoS | a rejected enqueue/upload aborts onstop or blocks AI | mitigate | fire-and-forget `.catch(()=>{})`; AI trigger untouched (D-05, Task 1) |
| T-32-SC | Tampering | npm supply chain | accept | No new package installs (RESEARCH §Standard Stack) |
</threat_model>

<verification>
- `npx vitest --run src/tests/audio-storage-fallback.test.ts` + `src/tests/audio-lookup.test.ts` pass.
- Recorder writes sessionId + enqueues fire-and-forget; AI trigger unchanged.
- processAudioWithAi falls back to Storage keyed by item_id; completed_at set on done.
- Per-wave: `npm test -- --run` green.
</verification>

<success_criteria>
- sessionId threaded onto the row; background upload enqueued without blocking AI (D-05).
- Cross-device Storage fallback keyed by item_id UUID (Pitfall 4 closed).
- completed_at stamped on AI-done (D-07 retention baseline live).
- audioRecordsForItem unions Supabase audio (cross-device visibility) without breaking DAT-7.
- All four PATTERNS threading issues resolved across plans 03+04.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-04-SUMMARY.md` when done.
</output>
