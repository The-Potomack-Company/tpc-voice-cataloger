---
phase: 32-audio-blob-supabase-persistence
plan: 05
type: execute
wave: 4
depends_on:
  - 32-03
files_modified:
  - src/stores/sessionStore.ts
  - src/components/ItemCard.tsx
autonomous: true
requirements: []
decisions_covered: [D-04, D-06]
tdd: true

must_haves:
  truths:
    - "On item hard-delete, the audio Storage blobs for that item are removed (not just the cascaded metadata row) — the photo orphan leak is closed for audio"
    - "ItemCard surfaces the audio upload status (pending / uploaded / failed) for the item's latest audio"
    - "On failed upload status, ItemCard shows a manual retry affordance that re-enqueues the upload"
  artifacts:
    - path: "src/stores/sessionStore.ts"
      provides: "deleteItem removes audio Storage blobs before/around the items delete (D-04)"
      contains: "storage.from(\"audio\").remove"
    - path: "src/components/ItemCard.tsx"
      provides: "useAudioUploadStatus pill + failed-retry wired (D-06)"
      contains: "useAudioUploadStatus"
  key_links:
    - from: "src/stores/sessionStore.ts deleteItem"
      to: "supabase.storage.from('audio').remove"
      via: "select storage_path by item_id then remove"
      pattern: "storage.from\\(\"audio\"\\).remove"
    - from: "src/components/ItemCard.tsx"
      to: "useAudioUploadStatus + retryFailedUploads"
      via: "status pill + failed onClick"
      pattern: "retryFailedUploads"
---

<objective>
Close the storage-orphan gap for audio (D-04) and surface upload durability in the UI (D-06): on item hard-delete, remove the item's audio Storage blobs via `supabase.storage.from('audio').remove(paths)` (the FK cascade only drops the metadata row — the binary must be explicitly removed, the exact photo leak this phase fixes for audio); and wire `useAudioUploadStatus` into `ItemCard` as a pending/uploaded/failed pill with a failed→retry affordance that re-enqueues. Makes the plan-01 `item-card-audio-status.test.tsx` scaffold green.

Purpose: D-04 (hard-delete orphan cleanup) and D-06 (status surface + manual retry) are the user-facing durability guarantees. The client `.remove()` is the immediate cleanup; the plan-01 pg_cron purge-audio reaper is the orphan backstop for delete paths that bypass deleteItem.

Output: deleting an item frees its audio binaries; ItemCard shows durability state and lets the user retry a failed upload. Depends on plan 03 (status hook + retryFailedUploads). No file overlap with plan 04, so runs in the same wave.

**CONTEXT.md decisions implemented by this plan:** D-04 (client-side storage.remove on hard-delete — the primary cleanup; pg_cron is the backstop), D-06 (useAudioUploadStatus pill on ItemCard + failed manual-retry re-enqueue).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-CONTEXT.md
@.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-PATTERNS.md
@.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-RESEARCH.md
@.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-03-SUMMARY.md

<interfaces>
<!-- Concrete seams + contracts. -->

deleteItem seam — src/stores/sessionStore.ts:492-537.
  Keep the existing optimistic-delete + revert + trackEvent structure (:496-536) INTACT.
  Delta — before the existing `supabase.from('items').delete().eq('id', itemId)` (:506-510) add (no in-repo analog — first storage.remove() in the codebase):
    const { data: rows } = await supabase.from('audio').select('storage_path').eq('item_id', itemId);
    if (rows?.length) { await supabase.storage.from('audio').remove(rows.map(r => r.storage_path)); }
  The FK ON DELETE CASCADE still drops the metadata row; this adds the binary removal (D-04). A remove() failure should not block the item delete — log + continue (the pg_cron reaper is the backstop). Do NOT use DELETE FROM storage.objects.

ItemCard seam — src/components/ItemCard.tsx.
  Already present: `latestAudioId` from audioRecordsForItem (:47-62, the audioData live query); `handleRetryAi` AI-retry pattern (:63-72); Badge primitive (import :15).
  Delta (mirror PhotoCapture.tsx:33,38 status-hook + failed→retry):
    import { useAudioUploadStatus } from '../hooks/useAudioUploadStatus';
    import { retryFailedUploads } from '../services/audioUploadQueue';
    const uploadStatus = useAudioUploadStatus(latestAudioId ?? undefined);
    Render a Badge pill reflecting uploadStatus: 'pending'/'uploading' → info/neutral tone "Uploading"; 'uploaded' → ok tone "Saved"; 'failed' → err tone + a retry button onClick={() => retryFailedUploads()}; 'none' → render nothing.
    Use the existing Badge tones (default/ok/warn/err/info). Do NOT add ad-hoc inline styles (LIB-primitive convention from v1.2).

retryFailedUploads (plan 03) resets failed queue rows to pending + drains.
</interfaces>
</context>

<read_first>
- src/stores/sessionStore.ts:492-537  (deleteItem — optimistic delete + revert + trackEvent to keep intact)
- src/components/ItemCard.tsx:1-72  (imports, audioData/latestAudioId live query, handleRetryAi pattern, Badge)
- src/components/PhotoCapture.tsx:33,38  (status-hook + failed→retry click analog)
- src/hooks/useAudioUploadStatus.ts + src/services/audioUploadQueue.ts (plan 03 — useAudioUploadStatus + retryFailedUploads signatures)
- src/ui/Badge.tsx  (tones: default/ok/warn/err/info — the pill primitive)
- src/tests/item-card-audio-status.test.tsx  (the scaffold to make green)
- 32-PATTERNS.md §"sessionStore.ts deleteItem" + §"ItemCard.tsx" + Prominent Issue 5 (no storage.remove anywhere)
  </read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove audio Storage blobs on item hard-delete (D-04)</name>
  <files>src/stores/sessionStore.ts</files>
  <read_first>
- src/stores/sessionStore.ts:492-537 (deleteItem)
- 32-RESEARCH.md §Code Examples (hard-delete cleanup) + Anti-Patterns (DELETE FROM storage.objects orphans)
- 32-PATTERNS.md §"sessionStore.ts deleteItem" + Prominent Issue 5
  </read_first>
  <behavior>
    - deleteItem(itemId, sessionId) selects the item's audio storage_paths and calls storage.from('audio').remove(paths) before the items delete
    - when the item has no audio rows, no remove() call is made (rows?.length guard)
    - a remove() failure is logged but does NOT prevent the item delete (pg_cron reaper is the backstop) and does NOT throw
    - the existing optimistic-delete + RLS-failure revert + trackEvent behavior is unchanged
  </behavior>
  <action>
In `src/stores/sessionStore.ts` `deleteItem` (:492-537): before the existing `supabase.from('items').delete().eq('id', itemId)` (:506-510), query the audio metadata rows for the item (`supabase.from('audio').select('storage_path').eq('item_id', itemId)`) and, if any, `await supabase.storage.from('audio').remove(rows.map(r => r.storage_path))`. Wrap the remove in a try/catch (or check its returned error) so a Storage failure logs + continues rather than aborting the item delete — the pg_cron purge-audio reaper is the orphan backstop. Do NOT use `DELETE FROM storage.objects`. Leave the optimistic-delete set(), the RLS-failure revert (:512-531), and both trackEvent calls intact. This is the FIRST `storage.remove()` in the codebase (photos never delete binaries — the leak D-04 closes for audio).
  </action>
  <verify>
    <automated>npm test -- --run src/tests/sessionStore 2>&1 | tail -20 || npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - deleteItem selects audio storage_paths by item_id and calls `supabase.storage.from('audio').remove(paths)` guarded by rows?.length
    - remove() failure logs + continues (does not abort the item delete or throw)
    - No `DELETE FROM storage.objects`; existing optimistic-delete/revert/trackEvent preserved
    - Project typechecks
  </acceptance_criteria>
  <done>Item hard-delete removes the audio binaries (D-04) with the pg_cron reaper as backstop; existing delete semantics intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ItemCard audio upload-status pill + failed-retry (D-06)</name>
  <files>src/components/ItemCard.tsx</files>
  <read_first>
- src/components/ItemCard.tsx:1-72 (imports, audioData/latestAudioId, handleRetryAi, Badge import)
- src/components/PhotoCapture.tsx:33,38 (status-hook + failed→retry analog)
- src/ui/Badge.tsx (tones)
- src/hooks/useAudioUploadStatus.ts + src/services/audioUploadQueue.ts (plan 03)
- src/tests/item-card-audio-status.test.tsx (the scaffold to make green)
- 32-PATTERNS.md §"ItemCard.tsx"
  </read_first>
  <behavior>
    - ItemCard renders an upload-status pill driven by useAudioUploadStatus(latestAudioId): 'uploaded' → ok/"Saved" pill; 'pending'|'uploading' → info/"Uploading" pill; 'failed' → err pill + retry control; 'none' → no pill
    - clicking the failed-state retry control calls retryFailedUploads()
    - the pill uses the Badge LIB primitive (no ad-hoc inline styles)
  </behavior>
  <action>
In `src/components/ItemCard.tsx`: import `useAudioUploadStatus` from `../hooks/useAudioUploadStatus` and `retryFailedUploads` from `../services/audioUploadQueue`. Compute `const uploadStatus = useAudioUploadStatus(latestAudioId ?? undefined)` (latestAudioId already exists at :61). Render a `Badge` pill mapping uploadStatus → tone+label per the behavior block (uploaded→ok "Saved"; pending/uploading→info "Uploading"; failed→err with a retry control `onClick={() => retryFailedUploads()}`; none→render nothing). Mirror the PhotoCapture.tsx:33,38 status-hook + failed→retry idiom. Use Badge tones only — no ad-hoc inline styles (v1.2 LIB-primitive convention). Keep the existing AI-status badges + handleRetryAi untouched.
  </action>
  <verify>
    <automated>npx vitest --run src/tests/item-card-audio-status.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/tests/item-card-audio-status.test.tsx` passes (GREEN)
    - Pill reflects uploadStatus (uploaded/pending/uploading/failed/none) via Badge tones; 'none' renders nothing
    - 'failed' shows a retry control that calls retryFailedUploads()
    - No ad-hoc inline styles; existing AI-status UI + handleRetryAi unchanged; project typechecks
  </acceptance_criteria>
  <done>ItemCard surfaces audio durability (pending/uploaded/failed) with a failed-retry that re-enqueues (D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (deleteItem) → Storage remove | Authorized by the plan-02 column-scoped DELETE storage RLS — a specialist can only remove blobs under sessions they own. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Information Disclosure / cost | orphaned audio binaries persist after item delete | mitigate | deleteItem storage.remove (Task 1, D-04) + pg_cron purge-audio orphan backstop (plan 01) |
| T-32-15 | Tampering | a specialist deletes another session's blob via deleteItem | mitigate | the column-scoped DELETE storage RLS (plan 02) denies cross-session remove; deleteItem only operates on items RLS already authorizes |
| T-32-16 | DoS | storage.remove failure aborts the item delete | mitigate | remove() failure logged + continues; item delete proceeds; pg_cron backstop reaps the orphan (Task 1) |
| T-32-SC | Tampering | npm supply chain | accept | No new package installs (RESEARCH §Standard Stack) |
</threat_model>

<verification>
- `npx vitest --run src/tests/item-card-audio-status.test.tsx` passes.
- deleteItem removes audio binaries (storage.remove by item_id paths) without aborting on failure; no DELETE FROM storage.objects.
- ItemCard pill reflects upload status + failed-retry re-enqueues.
- Per-wave: `npm test -- --run` green (full suite, all plan-01 scaffolds now green).
</verification>

<success_criteria>
- Hard-delete frees audio binaries (D-04); pg_cron reaper is the documented backstop.
- ItemCard surfaces pending/uploaded/failed + manual retry (D-06) via LIB Badge.
- Existing delete semantics + ItemCard AI UI preserved.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/32-audio-blob-supabase-persistence/32-05-SUMMARY.md` when done.
</output>
