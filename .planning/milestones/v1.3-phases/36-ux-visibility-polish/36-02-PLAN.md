---
phase: 36-ux-visibility-polish
plan: 02
type: tdd
wave: 2
depends_on:
  - 36-01
files_modified:
  - src/pages/NewSession.tsx
  - src/pages/SessionDetail.tsx
  - src/pages/Login.tsx
  - src/tests/new-session-import-rollback.test.tsx
  - src/tests/session-export-notify.test.tsx
  - src/tests/login-page.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "An export failure surfaces a sticky toast with a Try Again affordance that re-runs the export (SC1)"
    - "An import that throws mid-loop undoes every row it created (compensating deletes in reverse) and surfaces a sticky retry toast — no orphan session/items remain; atomicity via client-side rollback, NOT a transactional RPC, so no schema change this phase (SC2, D-03)"
    - "A new-session create failure surfaces a toast instead of dying silently"
    - "A login failure renders friendly copy via toUserMessage — never raw Supabase/GoTrue text (SC4a)"
  artifacts:
    - path: "src/pages/NewSession.tsx"
      provides: "Atomic handleImport with client-side compensating rollback (D-01) + doCreate failure surfacing"
      contains: "deleteSession"
    - path: "src/pages/SessionDetail.tsx"
      provides: "Export failure → sticky notifyError with retry (D-08)"
      contains: "notifyError"
    - path: "src/pages/Login.tsx"
      provides: "Friendly login error copy via toUserMessage"
      contains: "toUserMessage"
  key_links:
    - from: "src/pages/NewSession.tsx"
      to: "src/db/sessions.ts deleteSession + src/db/items.ts deleteItem"
      via: "compensating deletes in catch, reverse order, best-effort"
      pattern: "delete(Session|Item)"
    - from: "src/pages/SessionDetail.tsx / Login.tsx"
      to: "src/lib/toUserMessage.ts + notificationStore"
      via: "toUserMessage(err) → notifyError / setError"
      pattern: "toUserMessage"
---

<objective>
Wire the user-facing operation handlers — export, new-session/import, login —
into the Phase 36 error-visibility layer. The one piece of genuinely new logic
is the import compensating rollback (D-01); export and login are catch-site
rewires through the helper from Plan 01.

Purpose: Close SC1 (export visible+retry), SC2 (import atomic), and SC4a
(friendly login copy). These three pages share no files with Plan 03, so both
run in parallel in Wave 2.
Output: modified NewSession/SessionDetail/Login + their tests, green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-CONTEXT.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-RESEARCH.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-PATTERNS.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-UI-SPEC.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (Wave 1) — import directly, do not re-derive. -->
src/lib/toUserMessage.ts:  toUserMessage(err: unknown): string
src/stores/notificationStore.ts:  notifyError(message, retry?) — single-slot, deduped, sticky when retry attached

<!-- Existing operations these handlers already call (verified). -->
src/db/sessions.ts:  createSession(name, mode, notes?, assignedTo?): Promise<string>;  deleteSession(id): Promise<void>  (→ useSessionStore.deleteSession, unwinds Dexie via store)
src/db/items.ts:  createBlankItem(sessionId, mode): Promise<string>;  deleteItem(id, sessionId): Promise<void>;  updateItemField(itemId, sessionId, field, value)
Non-hook notify pattern (src/stores/sessionStore.ts:468):  useNotificationStore.getState().notifyError(msg, retryFn)
id-tracking rollback precedent (src/db/migration.ts:168-177): accumulate landed ids, delete only what landed.
</interfaces>

<scope_guards>
- NewSession already surfaces `accountsError` inline (:49-52) with good copy — DO NOT double-handle (Pitfall 3). Only route its text through toUserMessage if raw error leakage is possible; otherwise leave it.
- Export is idempotent/read-only → retry closure is simply `() => handleExport()` / `() => handleExportSpreadsheet()`; NO stale-edit guard (that guard at sessionStore.ts:478-484 is field-edit-specific, not for export).
- A3/Q2 (research open question): confirm deleteSession/deleteItem store actions also purge the Dexie idMapping/cache rows created during the failed import. If they do NOT, add explicit Dexie cleanup to the compensating block. Verify by reading useSessionStore.deleteSession/deleteItem before relying on cascade.
- Test mocks mirror src/tests/update-item-field-notify.test.ts (vi.hoisted). Ignore the 18 pre-existing localStorage.clear failures.
</scope_guards>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Import compensating rollback + doCreate failure surfacing (SC2, D-01)</name>
  <files>src/pages/NewSession.tsx, src/tests/new-session-import-rollback.test.tsx</files>
  <read_first>
    - src/pages/NewSession.tsx:78-122 (doCreate + handleImport — the file being modified)
    - src/db/migration.ts:160-177 (id-tracking + selective cleanup precedent — canonical analog)
    - src/stores/sessionStore.ts:468 (non-hook notify pattern); inspect useSessionStore.deleteSession/deleteItem for Dexie unwind (A3/Q2)
    - src/tests/update-item-field-notify.test.ts (mock pattern)
  </read_first>
  <behavior>
    - Test: handleImport where createBlankItem throws on the 2nd receipt → deleteItem called for the 1 created item AND deleteSession called for the created session (reverse order), then notifyError called with a retry callback
    - Test: a clean import creates session + all items, navigates, and does NOT call deleteSession/deleteItem or notifyError
    - Test: doCreate where createSession throws → notifyError called with a retry callback; no navigation
    - Test: the retry callback passed to notifyError re-invokes the same operation (wholesale, D-08)
  </behavior>
  <action>
    RED first: write src/tests/new-session-import-rollback.test.tsx mocking createSession/createBlankItem/updateItemField/deleteSession/deleteItem and notificationStore (vi.hoisted). Assert the compensating-delete + notify behavior above. Confirm failing.
    GREEN:
    handleImport (NewSession.tsx:100-121): introduce `let createdSessionId: string | undefined` and `const createdItemIds: string[] = []`. In the try, assign createdSessionId from createSession, push each itemId from createBlankItem before updateItemField. Wrap in try/catch: on catch, iterate createdItemIds.reverse() calling `await deleteItem(id, createdSessionId!).catch(() => {})`, then if createdSessionId `await deleteSession(createdSessionId).catch(() => {})`, then `useNotificationStore.getState().notifyError("Import didn't finish — changes were undone. Try again.", () => handleImport(receipts, skipped))`. Move navigate() to the success path only.
    doCreate (NewSession.tsx:78-91): wrap createSession in try/catch; on catch `notifyError(toUserMessage(err), () => doCreate())`. Use the UI-SPEC copy "Couldn't create the session — nothing was saved. Try again." for the create-failure toast (override the generic where the spec prescribes specific copy).
    If A3/Q2 read shows deleteSession/deleteItem do NOT clear Dexie idMapping/cache, add explicit Dexie cleanup in the catch. Import toUserMessage + useNotificationStore. WHY-comments only.
  </action>
  <verify>
    <automated>npx vitest run src/tests/new-session-import-rollback.test.tsx</automated>
  </verify>
  <done>Mid-loop import failure leaves zero orphan rows and shows a sticky retry toast; clean import unaffected; doCreate failure surfaces. All green.</done>
  <acceptance_criteria>
    - On import throw, every created item + the session are deleted in reverse order (best-effort .catch)
    - notifyError invoked with copy "Import didn't finish — changes were undone. Try again." and a retry callback that re-runs handleImport
    - doCreate failure invokes notifyError(toUserMessage(err), retry); navigation only on success
    - Clean import path unchanged (no deletes, no notify)
    - `npx vitest run src/tests/new-session-import-rollback.test.tsx` green
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Export failure surfacing (SC1) + login friendly copy (SC4a)</name>
  <files>src/pages/SessionDetail.tsx, src/pages/Login.tsx, src/tests/session-export-notify.test.tsx, src/tests/login-page.test.tsx</files>
  <read_first>
    - src/pages/SessionDetail.tsx:239-274 (handleExport + handleExportSpreadsheet — the file being modified)
    - src/pages/Login.tsx:22-25 (raw error.message render — the file being modified) and :104-108 (role="alert" surface)
    - src/stores/sessionStore.ts:468-487 (canonical notify+retry analog)
    - src/tests/login-page.test.tsx (existing — EXTEND, do not recreate); src/tests/update-item-field-notify.test.ts (mock pattern)
  </read_first>
  <behavior>
    - Test (export): handleExport where exportSession throws → notifyError called with friendly copy + a retry callback that re-runs handleExport; same for handleExportSpreadsheet
    - Test (login): a bad-credentials auth error → setError receives "Wrong email or password", NEVER the raw error.message
    - Test (login): a network error → setError receives "Connection problem — try again"
  </behavior>
  <action>
    RED first: write src/tests/session-export-notify.test.tsx (export catch → notify) and EXTEND src/tests/login-page.test.tsx with the two friendly-copy cases. Confirm failing.
    GREEN:
    SessionDetail.tsx (:244-248 and the identical :269-271): replace the `console.error(...)`/swallow in both export catch blocks with `useNotificationStore.getState().notifyError(toUserMessage(err), () => handleExport())` (and `() => handleExportSpreadsheet()` respectively). Use UI-SPEC copy "Export failed — your data wasn't downloaded." as the message (override toUserMessage's generic for this known-operation failure; still funnel unknown shapes through toUserMessage if you prefer a single call — planner's call is the fixed export string per UI-SPEC). Toast is sticky because retry is attached. Keep the finally setExporting(false).
    Login.tsx (:24-25): change `setError(error.message)` → `setError(toUserMessage(error))`. Existing role="alert" paragraph is the render surface — unchanged.
    Import toUserMessage + useNotificationStore. WHY-comments only.
  </action>
  <verify>
    <automated>npx vitest run src/tests/session-export-notify.test.tsx src/tests/login-page.test.tsx</automated>
  </verify>
  <done>Export failures surface a sticky retry toast; login renders only friendly copy. All green.</done>
  <acceptance_criteria>
    - Both export catch blocks call notifyError with a retry callback (no remaining console-only swallow)
    - Login setError receives a toUserMessage string; raw error.message never reaches the render surface
    - `npx vitest run src/tests/session-export-notify.test.tsx src/tests/login-page.test.tsx` green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase/network → UI | Export, import, and login backend errors cross into user-visible copy |
| Client orchestration → Supabase+Dexie | Import writes land across two stores; partial failure must not leave inconsistent state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02 | Information Disclosure | Login.tsx error copy | mitigate | setError(toUserMessage(error)) — single non-branching "Wrong email or password"; no raw GoTrue/email-exists oracle (ASVS V2) |
| T-36-03 | Tampering / data integrity | NewSession.handleImport | mitigate | Client-side compensating deletes (D-01) unwind every landed row on failure; no orphan session/items persist |
| T-36-04 | Information Disclosure | SessionDetail export catch | mitigate | Fixed "Export failed…" copy / toUserMessage funnel; raw err never rendered (ASVS V7) |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH: zero dependencies) |
</threat_model>

<verification>
- `npx vitest run src/tests/new-session-import-rollback.test.tsx src/tests/session-export-notify.test.tsx src/tests/login-page.test.tsx` green
- `npm test` shows no NEW failures beyond the 18 pre-existing localStorage.clear failures
- Manual (deferred to UAT per VALIDATION.md): trigger export/login failure in preview, confirm toast renders bottom-24 and Try Again re-runs
</verification>

<success_criteria>
- SC1: export failure → visible sticky toast + working retry
- SC2: import is atomic from the user's POV (rollback on failure, no orphans)
- SC4a: login shows friendly copy, no raw backend text
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-02-SUMMARY.md` when done
</output>
