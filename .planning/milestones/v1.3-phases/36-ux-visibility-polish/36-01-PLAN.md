---
phase: 36-ux-visibility-polish
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/toUserMessage.ts
  - src/stores/notificationStore.ts
  - src/components/ErrorToast.tsx
  - src/tests/to-user-message.test.ts
  - src/tests/error-toast.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Raw Supabase/JSON/auth error text never reaches the user — every mapped error class resolves to friendly copy, unmapped falls back to a generic message"
    - "A toast carrying a retry callback stays on screen until the user acts (no 6s auto-dismiss); an informational toast still auto-dismisses at 6s — single-slot, latest-wins, retry re-runs the same op (D-02, D-04, D-06)"
    - "Re-firing the identical message that is already displayed does not re-set the store (no flicker/spam) (D-05)"
  artifacts:
    - path: "src/lib/toUserMessage.ts"
      provides: "Central error→friendly-copy mapping helper (D-09)"
      exports: ["toUserMessage"]
    - path: "src/stores/notificationStore.ts"
      provides: "Single-slot notification store with dedupe"
      contains: "notifyError"
    - path: "src/components/ErrorToast.tsx"
      provides: "Global toast with retry-sticky auto-dismiss gating"
      contains: "retry"
  key_links:
    - from: "src/components/ErrorToast.tsx"
      to: "src/stores/notificationStore.ts"
      via: "useNotificationStore message + retry selectors"
      pattern: "useNotificationStore"
    - from: "all phase-36 catch sites (plans 02, 03)"
      to: "src/lib/toUserMessage.ts"
      via: "import toUserMessage(err) before notifyError"
      pattern: "toUserMessage"
---

<objective>
Build the shared error-visibility contract layer that every other Phase 36 plan
consumes: the central `toUserMessage(err)` copy-mapping helper (D-09), plus the
two DAT-4 behavior changes — dedupe (D-05) and retry-sticky auto-dismiss (D-06).

Purpose: Plans 02 and 03 wire silent catch sites into this layer. Defining the
contract first (helper signature + store/toast behavior) means downstream
executors import a known API instead of exploring the codebase.
Output: `toUserMessage.ts`, modified `notificationStore.ts` + `ErrorToast.tsx`,
and their tests, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-CONTEXT.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-RESEARCH.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-PATTERNS.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-UI-SPEC.md

<interfaces>
<!-- Contracts this plan establishes / relies on. Use directly — no exploration. -->

Existing network-token detection to seed toUserMessage (src/stores/sessionStore.ts:9-13):
  isNetworkError(err): matches "Failed to fetch", "ERR_INTERNET_DISCONNECTED",
  "NetworkError", "network" on String(err.message).

Current notificationStore (src/stores/notificationStore.ts, full file):
  message: string | null; retry: (() => void) | null;
  notifyError: (message, retry?) => set({ message, retry: retry ?? null });
  dismiss: () => set({ message: null, retry: null });

Current ErrorToast auto-dismiss (src/components/ErrorToast.tsx:11-19):
  useEffect runs a 6s setTimeout→dismiss for every non-null message; deps [message].
  Retry button already wired (~:36-47): onClick → retry(); dismiss(). Do NOT change it.

New contract this plan exports (downstream plans import):
  toUserMessage(err: unknown): string
</interfaces>

<test_pattern>
<!-- vi.hoisted mock shape every phase-36 test mirrors. Source: src/tests/update-item-field-notify.test.ts:1-34 -->
vi.hoisted({ mockNotifyError, mockDismiss, ... });
vi.mock("../stores/notificationStore", () => ({
  useNotificationStore: { getState: () => ({ notifyError: mockNotifyError, dismiss: mockDismiss }) },
}));
For error-toast.test.tsx use vi.useFakeTimers() to assert sticky-when-retry + 6s informational dismissal.
Ignore the 18 pre-existing `localStorage.clear is not a function` failures in persist-scoping.test.ts / photo-migration.test.ts (STATE.md — unrelated).
</test_pattern>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: toUserMessage(err) helper (D-09)</name>
  <files>src/lib/toUserMessage.ts, src/tests/to-user-message.test.ts</files>
  <read_first>
    - src/stores/sessionStore.ts:9-13 (isNetworkError token set to mirror)
    - src/tests/update-item-field-notify.test.ts (vi.hoisted mock pattern — canonical analog)
    - 36-UI-SPEC.md "Central copy mapping" table (the three prescribed strings)
  </read_first>
  <behavior>
    - Test: a bad-credentials shape ({ message: "Invalid login credentials" }) → "Wrong email or password" (case-insensitive match)
    - Test: a network shape ({ message: "Failed to fetch" }) and NetworkError → "Connection problem — try again"
    - Test: navigator.onLine === false with an unmapped message → "Connection problem — try again"
    - Test: an unmapped error ({ message: "boom" }), a plain string, null, and undefined → "Something went wrong"
    - Test: a site-known-good message is NOT special-cased here — helper only maps known shapes + generic fallback (Pitfall 4: it does not flatten good copy at call sites; that is the caller's choice)
  </behavior>
  <action>
    RED first: write src/tests/to-user-message.test.ts covering the behavior cases above using the vi.hoisted mock pattern (no store mock needed — pure fn). Run, confirm failing.
    GREEN: create src/lib/toUserMessage.ts exporting `toUserMessage(err: unknown): string`. Read err.message via String((err as { message?: string })?.message ?? ""). Match auth bad-credentials case-insensitively (/invalid login credentials|invalid email or password/i) → "Wrong email or password". Match network tokens (mirror isNetworkError: failed to fetch / networkerror / err_internet_disconnected / \bnetwork\b, case-insensitive) OR navigator.onLine === false → "Connection problem — try again". Else → "Something went wrong". Do NOT duplicate isNetworkError — inline the equivalent token check here (per PATTERNS: seed from it, this is the new single funnel). No I/O, no comments unless a WHY-comment.
  </action>
  <verify>
    <automated>npx vitest run src/tests/to-user-message.test.ts</automated>
  </verify>
  <done>toUserMessage exported; all mapping + fallback cases green.</done>
  <acceptance_criteria>
    - src/lib/toUserMessage.ts exports a single pure `toUserMessage(err: unknown): string`
    - Returns one of exactly three strings: "Wrong email or password", "Connection problem — try again", "Something went wrong"
    - All test cases in <behavior> pass via `npx vitest run src/tests/to-user-message.test.ts`
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: notificationStore dedupe (D-05) + ErrorToast retry-sticky (D-06)</name>
  <files>src/stores/notificationStore.ts, src/components/ErrorToast.tsx, src/tests/error-toast.test.tsx</files>
  <read_first>
    - src/stores/notificationStore.ts (full file — self-edit)
    - src/components/ErrorToast.tsx:11-19 (auto-dismiss effect — self-edit) and :36-47 (retry button, leave unchanged)
    - src/tests/update-item-field-notify.test.ts (mock pattern)
    - 36-UI-SPEC.md "Toast lifecycle" table (D-04/05/06 rules)
  </read_first>
  <behavior>
    - Test (dedupe, D-05): calling notifyError("X") when message is already "X" does NOT replace state (retry unchanged); calling notifyError("Y") replaces it
    - Test (sticky, D-06): with a retry callback attached, advancing fake timers 6s+ does NOT call dismiss — toast persists
    - Test (auto-dismiss, D-06): with retry === null, advancing fake timers past 6s DOES call dismiss exactly once
    - Test: a11y — the Dismiss icon button exposes an accessible name "Dismiss" (aria-label or sr-only), per UI-SPEC WCAG 4.1.2
  </behavior>
  <action>
    RED first: write src/tests/error-toast.test.tsx with vi.useFakeTimers(), mounting ErrorToast against a real (or lightly-mocked) notificationStore, covering the behavior cases. Confirm failing.
    GREEN:
    (a) notificationStore.ts — change notifyError to dedupe: `set((s) => (s.message === message ? s : { message, retry: retry ?? null }))`. No signature change. dismiss unchanged.
    (b) ErrorToast.tsx — gate the auto-dismiss effect on retry: early-return when `message === null || retry !== null`; add `retry` to the dep array (so [message, retry]). Retryable toasts never schedule a timer; informational keep the 6s timer. Do NOT touch the retry-button onClick (retry(); dismiss()).
    (c) Ensure the Dismiss `x` button carries aria-label="Dismiss" (or sr-only label) if not already present.
    Match existing code style; WHY-comment only on the sticky-gate invariant.
  </action>
  <verify>
    <automated>npx vitest run src/tests/error-toast.test.tsx</automated>
  </verify>
  <done>Dedupe skips identical re-fire; retryable toast survives 6s; informational toast dismisses at 6s; Dismiss button has accessible name. All green.</done>
  <acceptance_criteria>
    - notificationStore.notifyError no-ops when message equals current message
    - ErrorToast schedules the 6s dismiss timer only when retry === null
    - ErrorToast effect deps include retry
    - Dismiss icon button has an accessible name "Dismiss"
    - `npx vitest run src/tests/error-toast.test.tsx` green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase/network → UI copy | Backend error objects (auth JSON, fetch failures, stack text) cross into user-visible strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Information Disclosure | toUserMessage / all catch sites | mitigate | Generic fallback ("Something went wrong") for unmapped errors; raw error.message never returned — only the three fixed strings (ASVS V7) |
| T-36-02 | Information Disclosure | login error copy (consumed in plan 02) | mitigate | Single non-branching "Wrong email or password" — no email-exists oracle (ASVS V2 / D-09) |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH: zero dependencies); audit N/A |
</threat_model>

<verification>
- `npx vitest run src/tests/to-user-message.test.ts src/tests/error-toast.test.tsx` green
- `npm test` shows no NEW failures beyond the 18 pre-existing localStorage.clear failures
</verification>

<success_criteria>
- toUserMessage exported and consumable by plans 02/03 (key_links)
- Retryable toasts sticky; informational auto-dismiss; identical messages deduped
- No raw backend error text path exists through this layer
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-01-SUMMARY.md` when done
</output>
