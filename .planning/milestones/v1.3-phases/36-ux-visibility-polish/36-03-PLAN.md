---
phase: 36-ux-visibility-polish
plan: 03
type: tdd
wave: 2
depends_on:
  - 36-01
files_modified:
  - src/hooks/useDataMigration.ts
  - src/components/MigrationSplash.tsx
  - src/hooks/useUserRole.ts
  - src/services/offlineQueue.ts
  - src/tests/data-migration.test.ts
  - src/tests/use-user-role.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "A partial migration (≥1 item skipped) NEVER shows full-success copy — the splash reports honest partial state from the DAT-1 `partial` flag, fixed now and decoupled from Phase 38's retry banner (SC3, D-07)"
    - "A role-load failure in useUserRole keeps isAdmin=false (fails closed, never grants admin) AND surfaces the failure rather than silently demoting (SC4 / ASVS V4)"
    - "A silent fetch-read failure in offlineQueue routes through notifyError so the user sees a connection problem (SC4)"
  artifacts:
    - path: "src/hooks/useDataMigration.ts"
      provides: "Threads result.partial into a distinct 'partial' state"
      contains: "partial"
    - path: "src/components/MigrationSplash.tsx"
      provides: "Honest partial copy branch (no false success)"
      contains: "partial"
    - path: "src/hooks/useUserRole.ts"
      provides: "Fail-closed role-load with failure signal (distinguish load-error from not-admin)"
      contains: "isAdmin"
    - path: "src/services/offlineQueue.ts"
      provides: "Fetch-read failure surfaced via notifyError (visibility only; still returns [])"
      contains: "notifyError"
  key_links:
    - from: "src/hooks/useDataMigration.ts"
      to: "src/db/migration.ts return { partial }"
      via: "branch on result.partial → state:'partial'"
      pattern: "partial"
    - from: "src/hooks/useUserRole.ts / src/services/offlineQueue.ts"
      to: "src/lib/toUserMessage.ts + notificationStore"
      via: "toUserMessage(error) → notifyError on definite error"
      pattern: "toUserMessage"
---

<objective>
Close the three remaining silent paths that do not touch Plan 02's pages:
migration false-success copy (SC3, D-07), useUserRole silent admin-demotion
(SC4 / ASVS V4 fail-closed), and the offlineQueue silent fetch-read failure
(SC4). All consume Plan 01's helper/store; none overlap Plan 02 → parallel in
Wave 2.

Purpose: Make partial migration honest and the genuinely-silent admin/fetch
paths visible without regressing fail-closed access control or the empty-return
contract of offlineQueue.
Output: modified useDataMigration/MigrationSplash/useUserRole/offlineQueue +
extended tests, green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-CONTEXT.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-RESEARCH.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-PATTERNS.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-UI-SPEC.md
@.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (Wave 1) — import directly. -->
src/lib/toUserMessage.ts:  toUserMessage(err: unknown): string
src/stores/notificationStore.ts:  notifyError(message, retry?) — deduped, sticky when retry attached

<!-- Existing shapes (verified). -->
src/db/migration.ts:177 returns { migrated, skipped, partial: skipped > 0 }
useDataMigration MigrationState union: "checking" | "not-needed" | "in-progress" | "complete" | "error"
  — success path (:35-40) sets "complete" always, IGNORING result.partial (the Codex #2 bug)
MigrationSplash props.state union: "in-progress" | "complete" | "error" (:5); honest copy already in error branch (:58-59)
useUserRole (full file): on Supabase error → setRole(null) (indistinguishable from not-admin); undefined = loading; isAdmin = role === "admin"
offlineQueue.ts:47-52: on read error returns [] with console.warn only (empty-return contract is intentional)
Non-hook notify (sessionStore.ts:468): useNotificationStore.getState().notifyError(msg, retryFn?)
</interfaces>

<scope_guards>
- useUserRole MUST fail closed: on load error keep isAdmin=false / never default to admin (ASVS V4 / RESEARCH Open Q3). Distinguish load-error from not-admin (e.g. a "error" sentinel distinct from null), keep undefined=loading. Surface only on a definite error, not transient blips — do NOT toast on every render.
- offlineQueue: visibility ONLY. Function still returns [] by design (do NOT change the empty-return contract). The notify is informational (no retry) → keeps 6s auto-dismiss.
- Do NOT implement Phase 38's retry-from-Settings migration flow (D-07). Phase 36 only stops the banner claiming success when partial.
- Pitfall 3: useUserRole is the ONE genuinely-silent admin path. Do NOT touch AccountManagement/NewSession/SessionDetail admin consumers — they already surface inline.
- EXTEND src/tests/data-migration.test.ts and src/tests/use-user-role.test.ts (both exist). Ignore the 18 pre-existing localStorage.clear failures.
</scope_guards>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Migration partial honesty (SC3, D-07)</name>
  <files>src/hooks/useDataMigration.ts, src/components/MigrationSplash.tsx, src/tests/data-migration.test.ts</files>
  <read_first>
    - src/hooks/useDataMigration.ts:28-44 (the success-path bug — file being modified)
    - src/db/migration.ts:160-177 (return { partial } shape)
    - src/components/MigrationSplash.tsx:5 (props union) and :48-60 (copy branches, honest error copy at :58-59)
    - src/tests/data-migration.test.ts (existing — EXTEND)
  </read_first>
  <behavior>
    - Test: a migration result with skipped > 0 / partial:true drives state to "partial" (NOT "complete")
    - Test: a clean migration (skipped 0, partial:false) still drives state "complete"
    - Test: MigrationSplash with state="partial" renders honest copy ("Some items couldn't be migrated. Your data is safe.") and does NOT render the full-success string
    - Test: MigrationSplash with state="complete" renders the success copy
  </behavior>
  <action>
    RED first: EXTEND src/tests/data-migration.test.ts with the partial-vs-complete cases above. Confirm failing.
    GREEN:
    useDataMigration.ts: add "partial" to MigrationState union. On the success path (:35-40), branch: if `result.partial` (or result.skipped > 0) set state:"partial" else "complete"; thread migrated/skipped through in both. The catch (:41-42) still only handles thrown exceptions — partial is a success-path outcome.
    MigrationSplash.tsx: widen props.state union to include "partial" (:5). Add a "partial" copy branch rendering the honest copy "Some items couldn't be migrated. Your data is safe." (reuse the error-branch wording at :58-59 or a dedicated partial heading) using WarnBanner styling per UI-SPEC (warn family, role="status"). "complete" keeps "All sessions are now synced to the server." only on a true clean run. Do NOT add a retry/Settings flow (D-07).
    WHY-comments only.
  </action>
  <verify>
    <automated>npx vitest run src/tests/data-migration.test.ts</automated>
  </verify>
  <done>Partial migration shows honest partial copy, never "complete"; clean run unchanged. All green.</done>
  <acceptance_criteria>
    - useDataMigration sets state:"partial" when result.partial/skipped>0; "complete" only on a clean run
    - MigrationSplash renders honest partial copy for state="partial" and never the success string in that state
    - No Phase 38 retry flow added
    - `npx vitest run src/tests/data-migration.test.ts` green
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: useUserRole fail-closed visibility (SC4/ASVS V4) + offlineQueue fetch surfacing (SC4)</name>
  <files>src/hooks/useUserRole.ts, src/services/offlineQueue.ts, src/tests/use-user-role.test.ts</files>
  <read_first>
    - src/hooks/useUserRole.ts (full file — being modified; note fail-closed requirement)
    - src/services/offlineQueue.ts:47-52 (read-failure swallow — being modified; empty-return contract is intentional)
    - src/stores/sessionStore.ts:468 (non-hook notify analog)
    - src/tests/use-user-role.test.ts (existing — EXTEND)
  </read_first>
  <behavior>
    - Test: useUserRole when the profiles fetch returns an error → isAdmin stays false (fail closed) AND the failure is signaled (distinct state/sentinel, e.g. role/error distinguishable from the not-admin null case) — surfaced via notifyError(toUserMessage(error), retry) on a definite error
    - Test: useUserRole when the fetch succeeds with role "admin" → isAdmin true; with no row/role null → isAdmin false, NO error signal (this is "not admin", not a failure)
    - Test: useUserRole while loading (role undefined) → loading true, no notify
    - Test (offlineQueue): getQueuedItems on a read error still returns [] AND calls notifyError(toUserMessage(error)) with no retry (informational); on success returns the rows and does NOT notify
  </behavior>
  <action>
    RED first: EXTEND src/tests/use-user-role.test.ts with the fail-closed + error-signal + not-admin + loading cases. Add offlineQueue read-failure assertions (either in use-user-role.test.ts is wrong — create/extend the appropriate test; if no offlineQueue test exists, add the offlineQueue case to a small new describe within an existing offlineQueue test file or src/tests/use-user-role.test.ts is NOT the home — place offlineQueue assertions in the nearest existing offlineQueue test or a new src/tests/offline-queue-notify.test.ts). Confirm failing.
    GREEN:
    useUserRole.ts: on the Supabase error branch (:23-24), keep isAdmin false (do NOT set role to "admin"); introduce a way to distinguish load-error from not-admin (e.g. a separate error state or an "error" role sentinel) and call useNotificationStore.getState().notifyError(toUserMessage(error), () => /* re-trigger the effect/refetch */) on a definite error only. Preserve undefined=loading and the cancelled guard. isAdmin must remain `role === "admin"` (a load error → not "admin" → false). Surface only on definite error, not transient (Q3).
    offlineQueue.ts (:47-52): keep `return []` and the existing behavior, but add `useNotificationStore.getState().notifyError(toUserMessage(error))` (no retry → informational, auto-dismiss) before/at the read-failure branch. Do NOT change the empty-return contract.
    Import toUserMessage + useNotificationStore. WHY-comments only.
  </action>
  <verify>
    <automated>npx vitest run src/tests/use-user-role.test.ts</automated>
  </verify>
  <done>Role-load failure stays fail-closed and is surfaced; not-admin/loading unaffected; offlineQueue read failure is visible while still returning []. All green.</done>
  <acceptance_criteria>
    - useUserRole isAdmin is false on load error (never grants admin) and the error is signaled + surfaced via notifyError
    - not-admin (null role) and loading (undefined) cases produce no error signal/notify
    - offlineQueue read failure returns [] AND calls notifyError (informational, no retry)
    - `npx vitest run src/tests/use-user-role.test.ts` (+ any offlineQueue test) green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase role fetch → access control | A failed profiles role read must not silently change effective privilege |
| Supabase queue read → UI | A failed offline-queue read crosses into user-visible state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Elevation of Privilege | useUserRole | mitigate | Fail closed — load error keeps isAdmin=false; never defaults to admin (ASVS V4). Error is surfaced, not used to grant access |
| T-36-06 | Information Disclosure | useUserRole / offlineQueue notify copy | mitigate | toUserMessage funnel — raw Supabase error never rendered (ASVS V7) |
| T-36-07 | Repudiation / silent failure | offlineQueue read | mitigate | Read failure now surfaces a toast instead of console-only; empty-return contract preserved (no data fabricated) |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH: zero dependencies) |
</threat_model>

<verification>
- `npx vitest run src/tests/data-migration.test.ts src/tests/use-user-role.test.ts` green (+ offlineQueue test if added)
- `npm test` shows no NEW failures beyond the 18 pre-existing localStorage.clear failures
- Manual (deferred to UAT per VALIDATION.md): force a partial migration; confirm the splash reflects partial, not success
</verification>

<success_criteria>
- SC3: migration copy honest vs the DAT-1 partial flag — no false success
- SC4: admin role-load failure visible AND fail-closed; silent fetch failure visible
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/36-ux-visibility-polish/36-03-SUMMARY.md` when done
</output>
