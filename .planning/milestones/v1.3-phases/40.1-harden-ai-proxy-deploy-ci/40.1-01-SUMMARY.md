# Plan 40.1-01 Summary — Create proxy deploy secrets (ship-order step 1)

**Status:** Complete
**Plan:** 40.1-01 (wave 1, autonomous)
**Target:** `The-Potomack-Company/tpc-ai-proxy` (GH repo secrets; no files changed in either repo)

## What was done

Created the four GitHub repo secrets the hardened deploy workflow (plan 02) references, so the preflight gate passes and Cloud Run boots with the env config.js fail-closed requires.

| Secret | Source | Notes |
|--------|--------|-------|
| `ALLOWED_ORIGINS_PROD` | live value (project memory) | `chrome-extension://plielbnfpgcjjohnehcnagmlkmhjcpdn,https://tpc-cataloging-app.vercel.app` — eyeball-checked to contain the cataloger origin |
| `ALLOWED_ORIGINS_DEV` | live value (project memory) | `chrome-extension://nhpailgcmoimhpgcaaknmglchlkplbig,https://tpc-cataloging-app.vercel.app` — eyeball-checked |
| `SUPABASE_URL` | cataloger `.env.local` (`VITE_SUPABASE_URL`) | piped via stdin, never echoed |
| `SUPABASE_ANON_KEY` | cataloger `.env.local` (`VITE_SUPABASE_ANON_KEY`) | piped via stdin, never echoed; 46-char publishable-format key |

Deviation from plan: values sourced from `.env.local` (not `.env` — that file does not exist in the cataloger; `.env.local` is the real local env per `src/lib/supabase.ts`). No human-action checkpoint needed since the keys were present.

Legacy shared `ALLOWED_ORIGINS` secret left intact (its retirement is plan 02 Task 5, post-cutover).

## Verification

- `gh secret list --repo The-Potomack-Company/tpc-ai-proxy` shows all four new names + the still-present legacy `ALLOWED_ORIGINS`, `GCP_DEPLOY_SA`, `GCP_WIF_PROVIDER`.
- Task 1 automated gate passed: both origin values contain `https://tpc-cataloging-app.vercel.app`.
- Task 2 automated gate passed: `SUPABASE_(URL|ANON_KEY)` count == 2.
- No secret value was echoed into any log (origin values are public; Supabase values piped via stdin).

## key-files.created

(none — GH secrets only)

## Ship-order

Step 1 of 3 complete. Plan 02 may now safely edit + commit the workflow (step 2) and push the app commits (step 3).

## Self-Check: PASSED
