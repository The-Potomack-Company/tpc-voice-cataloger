# Phase 40.1: Harden tpc-ai-proxy deploy.yml CI before proxy push - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-03
**Phase:** 40.1-harden-ai-proxy-deploy-ci
**Areas discussed:** Guardrail strength, Secret sourcing, ALLOWED_ORIGINS fix, Scope edges, Dev trigger

---

## Guardrail strength

| Option | Description | Selected |
|--------|-------------|----------|
| Preflight gate | Add missing SUPABASE_* env_vars + CI step asserting required secrets non-empty before deploy; fail fast in CI | ✓ |
| Preflight + health check | Above plus post-deploy curl /health step that fails the run | |
| Minimal — just add vars | Only add the two env_vars; rely on Cloud Run crash | |

**User's choice:** Preflight gate
**Notes:** Health-check captured as deferred idea, not required to close landmine.

---

## Secret sourcing (Supabase)

| Option | Description | Selected |
|--------|-------------|----------|
| Both as GH secrets | SUPABASE_URL + SUPABASE_ANON_KEY as repo secrets, referenced via ${{ secrets.* }} | ✓ |
| URL inline, key as secret | Hardcode public SUPABASE_URL as plain env_var; only ANON_KEY as secret | |

**User's choice:** Both as GH secrets

---

## ALLOWED_ORIGINS fix

| Option | Description | Selected |
|--------|-------------|----------|
| Update via gh CLI | gh secret set to include cataloger origin | ✓ |
| Document only | Leave secret; note required value for manual update | |

**User's choice:** Update via gh CLI
**Notes:** Split into per-service secrets (ALLOWED_ORIGINS_PROD/_DEV) once dev wiring was confirmed.

---

## Scope edges

| Option | Description | Selected |
|--------|-------------|----------|
| Fix BODY_CAP_BYTES→MAX_BODY_BYTES | Rename dead env var so 25MB body cap applies | ✓ |
| Also wire dev service | Extend hardening to tpc-ai-proxy-dev | ✓ |

**User's choice:** Both folded into scope

---

## Dev trigger

| Option | Description | Selected |
|--------|-------------|----------|
| Both on main push | Matrix deploys prod + dev on every main push, per-service origin secrets | ✓ |
| Dev on dispatch only | Prod auto on push; dev via workflow_dispatch | |

**User's choice:** Both on main push

---

## Claude's Discretion

- Matrix shape (strategy.matrix vs two jobs), preflight implementation, env parameterization — planner/executor decide.
- ALLOWED_MODELS stays manually-set + revision-preserved (comma footgun), not added to env_vars.

## Deferred Ideas

- Post-deploy health-check step (curl /health, fail run on bad revision) — deferred from D-01.
