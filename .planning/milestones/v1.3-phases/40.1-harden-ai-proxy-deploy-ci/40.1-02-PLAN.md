---
phase: 40.1-harden-ai-proxy-deploy-ci
plan: 02
type: execute
wave: 2
depends_on: ["40.1-01"]
# THE EDITED FILE LIVES IN THE SIBLING REPO, NOT THE CATALOGER.
# Absolute path: /home/spoods/Projects/TPC/tpc-ai-proxy/.github/workflows/deploy.yml
target_repo: The-Potomack-Company/tpc-ai-proxy
files_modified:
  - ../tpc-ai-proxy/.github/workflows/deploy.yml
autonomous: false   # gcloud reauth (live verify) + git-push gate are user-only

must_haves:
  truths:
    - "Pushing proxy main no longer crash-loops prod: deploy.yml sets SUPABASE_URL + SUPABASE_ANON_KEY"
    - "A preflight CI step fails the run with a clear ::error:: if any required secret is empty, before any GCP call"
    - "deploy.yml deploys both tpc-ai-proxy-prod and tpc-ai-proxy-dev via a matrix"
    - "The cataloger origin survives: the comma-bearing ALLOWED_ORIGINS value is quoted in env_vars"
    - "ALLOWED_MODELS (service-only, never in git) survives deploy via env_vars_update_strategy: merge"
    - "Secrets exist (plan 01) BEFORE this workflow is committed (the commit itself triggers a deploy)"
    - "The two unpushed phase-40 app commits (18ef932, f52adc4) are pushed only AFTER the hardened workflow + secrets are live"
  artifacts:
    - path: "../tpc-ai-proxy/.github/workflows/deploy.yml"
      provides: "Hardened Cloud Run deploy: preflight gate + matrix + Supabase env + renamed body cap + quoted origins"
      contains: "strategy"
    - path: "../tpc-ai-proxy/.github/workflows/deploy.yml"
      provides: "Renamed body cap var read by config.js:42"
      contains: "MAX_BODY_BYTES"
  key_links:
    - from: "deploy.yml env_vars ALLOWED_ORIGINS"
      to: "matrix.allowed_origins (per-leg secret)"
      via: "quoted reference to prevent comma split"
      pattern: "ALLOWED_ORIGINS=\"\\$\\{\\{ matrix.allowed_origins \\}\\}\""
    - from: "deploy.yml preflight step"
      to: "required GH secrets"
      via: "map-to-env then bash -z test (secrets forbidden in if:)"
      pattern: "::error::"
    - from: "deploy.yml deploy step"
      to: "ALLOWED_MODELS on the live service"
      via: "env_vars_update_strategy: merge"
      pattern: "env_vars_update_strategy:\\s*merge"
---

<objective>
Harden `../tpc-ai-proxy/.github/workflows/deploy.yml` (in the SIBLING proxy repo)
to close the deploy landmine, then — gated behind the user — push the two
unpushed phase-40 app commits. Implements D-01 (preflight gate + Supabase env),
D-04 (BODY_CAP_BYTES → MAX_BODY_BYTES), D-05 (prod+dev matrix), and the RESEARCH
comma-safety correction (quote the comma-bearing ALLOWED_ORIGINS, set merge
strategy to preserve ALLOWED_MODELS).

Purpose: a future missing secret fails fast in CI with a clear message instead
of crash-looping Cloud Run, and the next proxy push lands the JWT-verify env the
proxy's fail-closed config.js requires.
Output: one hardened workflow file committed in the proxy repo; the two app
commits pushed after the hardened deploy is live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.3-phases/40.1-harden-ai-proxy-deploy-ci/40.1-CONTEXT.md
@.planning/milestones/v1.3-phases/40.1-harden-ai-proxy-deploy-ci/40.1-RESEARCH.md

<cross_repo_note>
The file edited here lives at
`/home/spoods/Projects/TPC/tpc-ai-proxy/.github/workflows/deploy.yml` — the
SIBLING repo `The-Potomack-Company/tpc-ai-proxy`, NOT the cataloger. ALL git
operations (`git add`/`commit`/`log`/`push`) for the workflow and the app
commits run with the proxy repo as cwd
(`/home/spoods/Projects/TPC/tpc-ai-proxy`). DO NOT commit the workflow change
into the cataloger repo. The cataloger repo only holds this GSD planning
artifact.
</cross_repo_note>

<interfaces>
<!-- The env contract the workflow must satisfy. From ../tpc-ai-proxy/src/config.js. -->
<!-- config.js throws fail-closed on missing: ALLOWED_ORIGINS, GEMINI_API_KEY, -->
<!-- SUPABASE_URL, SUPABASE_ANON_KEY. Reads MAX_BODY_BYTES (NOT BODY_CAP_BYTES). -->
<!-- ALLOWED_MODELS defaults in code but is set manually on the service; -->
<!-- it must NOT go in env_vars (comma breaks parsing) and merge preserves it. -->

config.js:24  process.env.ALLOWED_ORIGINS      (throws if empty)
config.js:42  process.env.MAX_BODY_BYTES       (falls back to 25MB default if unset)
config.js:45  process.env.GEMINI_API_KEY       (throws if unset — kept as Secret Manager ref)
config.js:53  process.env.SUPABASE_URL         (throws if unset — NEW)
config.js:57  process.env.SUPABASE_ANON_KEY    (throws if unset — NEW)
config.js:35  process.env.ALLOWED_MODELS       (defaults; service-set; keep OUT of env_vars)
</interfaces>

<canonical_patterns>
<!-- All three required patterns are spelled out with citations in RESEARCH §Architecture Patterns. -->
- Pattern 1 (preflight, D-01): RESEARCH lines 129-156 — map each required secret
  into the step's env:, then bash `-z` test in run:; emit `::error::` + exit 1.
  GitHub forbids secrets.* in if: — DO NOT write `if: secrets.X == ''`.
- Pattern 2 (matrix, D-05): RESEARCH lines 158-200 — one job, strategy.matrix.include
  with per-leg `service` + `allowed_origins: ${{ secrets.ALLOWED_ORIGINS_<LEG> }}`;
  fail-fast: false. secrets[matrix.key] dynamic indexing is unsupported — map per leg.
- Pattern 3 (comma-safe, CRITICAL): RESEARCH lines 202-210 — quote the value:
  ALLOWED_ORIGINS="${{ matrix.allowed_origins }}". Unquoted, the action splits on
  the inner comma and silently drops the cataloger origin (Pitfall 1).
</canonical_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite deploy.yml — preflight gate + matrix + Supabase env + rename + quoting</name>
  <files>../tpc-ai-proxy/.github/workflows/deploy.yml</files>
  <read_first>
    - ../tpc-ai-proxy/.github/workflows/deploy.yml (current single-prod workflow being replaced)
    - ../tpc-ai-proxy/src/config.js (the env contract — see <interfaces>)
    - .planning/milestones/v1.3-phases/40.1-harden-ai-proxy-deploy-ci/40.1-RESEARCH.md (§Architecture Patterns 1-3, Pitfalls 1-5, §Runtime State Inventory)
  </read_first>
  <action>
    Edit `/home/spoods/Projects/TPC/tpc-ai-proxy/.github/workflows/deploy.yml`
    (sibling repo). Keep `on: { push: branches: [main], workflow_dispatch }`,
    the `permissions: { contents: read, id-token: write }`, the `auth@v2` WIF
    step (workload_identity_provider = secrets.GCP_WIF_PROVIDER, service_account
    = secrets.GCP_DEPLOY_SA), and `setup-gcloud@v2` — all untouched.

    Add a `strategy` with `fail-fast: false` and `matrix.include` of two legs:
    leg 1 `service: tpc-ai-proxy-prod`, `allowed_origins: ${{ secrets.ALLOWED_ORIGINS_PROD }}`;
    leg 2 `service: tpc-ai-proxy-dev`, `allowed_origins: ${{ secrets.ALLOWED_ORIGINS_DEV }}`.

    Insert a preflight step BEFORE the auth step that maps the required secrets
    into its `env:` (SUPABASE_URL, SUPABASE_ANON_KEY from secrets.*;
    ALLOWED_ORIGINS from matrix.allowed_origins; GCP_WIF_PROVIDER, GCP_DEPLOY_SA
    from secrets.*) and runs a bash loop testing each with `-z`, collecting
    missing names, emitting `::error::Missing required secret(s): ...` and
    `exit 1` if any are empty (Pattern 1). The preflight must print only secret
    NAMES, never values.

    In the `deploy-cloudrun@v2` step set `service: ${{ matrix.service }}`,
    `region: us-east1`, `source: .`, `project_id: gen-lang-client-0662587427`,
    keep `secrets: GEMINI_API_KEY=gemini-api-key:latest`, keep
    `flags: "--max-instances=1 --allow-unauthenticated"`. Add
    `env_vars_update_strategy: merge` (preserves service-only ALLOWED_MODELS —
    Pitfall 3). In `env_vars` set: `ALLOWED_ORIGINS="${{ matrix.allowed_origins }}"`
    (QUOTED — Pattern 3 / Pitfall 1), `SUPABASE_URL=${{ secrets.SUPABASE_URL }}`,
    `SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}`, `RATE_LIMIT_RPM=15`, and
    `MAX_BODY_BYTES=26214400` (renamed from BODY_CAP_BYTES — D-04). Do NOT add
    ALLOWED_MODELS to env_vars. Remove the old single-prod `service:` line, the
    old `ALLOWED_ORIGINS=${{ secrets.ALLOWED_ORIGINS }}` line, the `BODY_CAP_BYTES`
    line, and the stale `^|^` caret comment block (the caret hack is a gcloud-CLI
    codepath, not the action's — Anti-Patterns).

    Then statically validate the file: `python3 -c "import yaml,sys;
    yaml.safe_load(open('/home/spoods/Projects/TPC/tpc-ai-proxy/.github/workflows/deploy.yml'))"`
    must exit 0. If `actionlint` is on PATH, run it too (optional bonus).
  </action>
  <verify>
    <automated>cd /home/spoods/Projects/TPC/tpc-ai-proxy && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))" && grep -q 'strategy' .github/workflows/deploy.yml && grep -q 'matrix' .github/workflows/deploy.yml && grep -q 'MAX_BODY_BYTES=26214400' .github/workflows/deploy.yml && grep -q 'env_vars_update_strategy:\s*merge' .github/workflows/deploy.yml && grep -q 'ALLOWED_ORIGINS="${{ matrix.allowed_origins }}"' .github/workflows/deploy.yml && grep -q '::error::' .github/workflows/deploy.yml && ! grep -q 'BODY_CAP_BYTES' .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - `python3 yaml.safe_load` on deploy.yml exits 0
    - deploy.yml contains a `strategy:` and a `matrix:` (or `matrix.include`) block
    - matrix has both `tpc-ai-proxy-prod` and `tpc-ai-proxy-dev` service legs
    - env_vars contains `MAX_BODY_BYTES=26214400`
    - deploy.yml does NOT contain the string `BODY_CAP_BYTES`
    - env_vars contains the QUOTED `ALLOWED_ORIGINS="${{ matrix.allowed_origins }}"`
    - env_vars contains `SUPABASE_URL=${{ secrets.SUPABASE_URL }}` and `SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}`
    - `env_vars_update_strategy: merge` present
    - a preflight step exists before the auth step, emits `::error::`, and maps secrets to env (no `secrets.*` inside any `if:`)
    - ALLOWED_MODELS does NOT appear in env_vars
    - the auth@v2 / setup-gcloud@v2 / WIF-secret wiring is unchanged
  </acceptance_criteria>
  <done>deploy.yml is the hardened multi-leg workflow; parses as valid YAML; all grep gates pass; file NOT yet committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pre-commit dry verify — confirm secrets present before the commit triggers a deploy</name>
  <what-built>
    The hardened deploy.yml is staged in the proxy repo working tree but not yet
    committed. Plan 01 created the four secrets. This checkpoint confirms the
    designed-safe preconditions before the commit (which IS a push-to-main that
    triggers a real Cloud Run deploy of BOTH services).
  </what-built>
  <how-to-verify>
    1. Run: `gh secret list --repo The-Potomack-Company/tpc-ai-proxy` and confirm
       all five referenced secrets are present: SUPABASE_URL, SUPABASE_ANON_KEY,
       ALLOWED_ORIGINS_PROD, ALLOWED_ORIGINS_DEV, GCP_WIF_PROVIDER, GCP_DEPLOY_SA.
    2. Confirm `tpc-ai-proxy-dev` exists on Cloud Run (RESEARCH A1 is unverified —
       gcloud auth expired). If you want to verify: `gcloud auth login`
       (interactive, user-only) then
       `gcloud run services list --project gen-lang-client-0662587427 --region us-east1`.
       Acceptable alternative: accept that deploy-cloudrun creates the dev
       service on first deploy.
    3. Confirm you accept that committing deploy.yml will immediately deploy both
       prod and dev from current proxy `main` (app code unchanged — old code
       tolerates the extra env vars).
  </how-to-verify>
  <resume-signal>Type "approved" to let Task 3 commit + monitor the deploy, or describe a blocker (e.g. a missing secret, dev service concern).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit the hardened workflow in the proxy repo (triggers the deploy)</name>
  <files>../tpc-ai-proxy/.github/workflows/deploy.yml</files>
  <read_first>
    - .planning/milestones/v1.3-phases/40.1-harden-ai-proxy-deploy-ci/40.1-RESEARCH.md (§7 ship order, §Verification Approach)
  </read_first>
  <action>
    With cwd `/home/spoods/Projects/TPC/tpc-ai-proxy` (proxy repo), stage ONLY
    `.github/workflows/deploy.yml` and commit on `main` with a message like
    `ci: harden deploy.yml — preflight secret gate + prod/dev matrix + Supabase env (40.1)`.
    Then push `main` to origin so the workflow runs:
    `git push origin main`. Watch the run with
    `gh run watch --repo The-Potomack-Company/tpc-ai-proxy` (or `gh run list`).
    Expected: the preflight passes (plan 01 set the secrets) and BOTH matrix legs
    deploy. If the preflight fails on a missing secret, that is the designed-safe
    failure (Pitfall 4) — STOP, fix the secret (plan 01 task), re-run; do NOT
    push the app commits. This commit is ship-order step 2/3.
  </action>
  <verify>
    <automated>cd /home/spoods/Projects/TPC/tpc-ai-proxy && git log -1 --pretty=%s | grep -qi 'harden deploy' && git status --porcelain .github/workflows/deploy.yml | grep -q . && echo committed-and-clean || git diff --quiet .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1` in the proxy repo shows the harden-deploy commit touching only `.github/workflows/deploy.yml`
    - the commit was pushed to `origin/main` (`git status` shows up to date or ahead-then-pushed)
    - the triggered workflow run reached the deploy step (preflight passed) — confirmed via `gh run list`/`gh run watch`
    - no cataloger-repo file was committed
  </acceptance_criteria>
  <done>Hardened workflow committed + pushed; the triggered deploy passed preflight and deployed both services (or stopped safely at preflight with a clear error).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Ship-order step 3 — push the two unpushed phase-40 app commits</name>
  <what-built>
    The hardened workflow is live and the SUPABASE_* env is now set on both Cloud
    Run services. The two phase-40 app commits that REQUIRE SUPABASE_* are still
    unpushed (18ef932 widen allowlist, f52adc4 add Supabase-JWT verify). Pushing
    them now is safe — the env already exists, so config.js won't throw.
  </what-built>
  <how-to-verify>
    1. Confirm the Task 3 deploy succeeded (both prod + dev revisions healthy).
       If gcloud is authed: hit `/health` on
       `https://tpc-ai-proxy-prod-588770300226.us-east1.run.app/` and the dev URL.
    2. In the proxy repo, confirm the two commits are still ahead:
       `git -C /home/spoods/Projects/TPC/tpc-ai-proxy log --oneline origin/main..main`
       should list 18ef932 and f52adc4 (unless Task 3's push already carried them —
       if so, they're already live and this step is a no-op confirmation).
    3. Push them: `git -C /home/spoods/Projects/TPC/tpc-ai-proxy push origin main`.
       This triggers another deploy; the preflight passes and config.js boots
       cleanly because SUPABASE_* is already set.
    4. After the deploy, confirm the proxy still serves and rejects a cross-origin
       caller (the JWT-verify path is now the live auth contract).
  </how-to-verify>
  <resume-signal>Type "approved" once both app commits are pushed and the post-push deploy is healthy, or describe a failure (e.g. crash-loop, CORS rejection of the cataloger origin).</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Retire the legacy shared ALLOWED_ORIGINS secret (cutover cleanup)</name>
  <files>(none — GH repo secret deletion on The-Potomack-Company/tpc-ai-proxy)</files>
  <read_first>
    - .planning/milestones/v1.3-phases/40.1-harden-ai-proxy-deploy-ci/40.1-RESEARCH.md (§Runtime State Inventory — orphan cleanup)
    - .planning/milestones/v1.3-phases/40.1-harden-ai-proxy-deploy-ci/40.1-CONTEXT.md (D-05 — retire shared ALLOWED_ORIGINS after cutover)
  </read_first>
  <action>
    Now that deploy.yml no longer references `secrets.ALLOWED_ORIGINS` (replaced
    by per-service `ALLOWED_ORIGINS_PROD`/`_DEV`), delete the now-inert legacy
    secret: `gh secret delete ALLOWED_ORIGINS --repo The-Potomack-Company/tpc-ai-proxy`.
    Optionally note (do NOT execute — gcloud reauth is user-only) that the live
    prod service still carries an orphan `BODY_CAP_BYTES` env from the last manual
    deploy; config.js ignores it, so removal via
    `gcloud run services update tpc-ai-proxy-prod --remove-env-vars BODY_CAP_BYTES
    --project gen-lang-client-0662587427 --region us-east1` is optional tidy-up,
    left for the user. First confirm the workflow no longer greps for the legacy
    secret name before deleting.
  </action>
  <verify>
    <automated>cd /home/spoods/Projects/TPC/tpc-ai-proxy && ! grep -q 'secrets.ALLOWED_ORIGINS\b' .github/workflows/deploy.yml && ! gh secret list --repo The-Potomack-Company/tpc-ai-proxy | grep -qE '^ALLOWED_ORIGINS\s'</automated>
  </verify>
  <acceptance_criteria>
    - deploy.yml contains no reference to the bare `secrets.ALLOWED_ORIGINS` (only the per-service `_PROD`/`_DEV`)
    - `gh secret list` no longer shows the bare `ALLOWED_ORIGINS` secret
    - the per-service `ALLOWED_ORIGINS_PROD` / `ALLOWED_ORIGINS_DEV` secrets remain
  </acceptance_criteria>
  <done>Legacy shared secret retired; only the two per-service origin secrets remain. Cutover complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → GCP (WIF) | Keyless OIDC token exchange; least-priv SA; unchanged this phase |
| CI workflow logs ← secret values | Untrusted readers of run logs must not see secret values |
| proxy → browser (CORS) | ALLOWED_ORIGINS is the access boundary; a comma-split would drop the cataloger origin |
| `git push origin main` → Cloud Run | Each push triggers a real prod+dev deploy; the fail-open risk this phase closes lives here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-04 | Denial of Service | missing required secret → config.js throws → Cloud Run crash-loop | mitigate | Preflight `-z` gate (D-01) fails the run with `::error::` before any GCP call (Pattern 1) — fail in CI, not prod |
| T-40.1-05 | Denial of Service | comma in ALLOWED_ORIGINS splits → cataloger origin dropped → CORS lockout | mitigate | Quote the value: `ALLOWED_ORIGINS="${{ matrix.allowed_origins }}"` (Pattern 3 / Pitfall 1) |
| T-40.1-06 | Information Disclosure | secret value echoed in preflight / step logs | mitigate | Preflight prints NAMES only; GH auto-masks `secrets.*`; never `echo "${SUPABASE_URL}"` |
| T-40.1-07 | Tampering | `overwrite` strategy nukes service-only ALLOWED_MODELS | mitigate | `env_vars_update_strategy: merge` set explicitly (Pitfall 3) |
| T-40.1-08 | Tampering | workflow committed into the wrong repo (cataloger) | mitigate | All git ops run with proxy repo as cwd; commit stages only `.github/workflows/deploy.yml` |
| T-40.1-09 | Elevation of Privilege | supply-chain: unpinned/poisoned action | accept | Actions stay on existing major-tag pins (checkout@v4, auth@v2, setup-gcloud@v2, deploy-cloudrun@v2); no version bump in scope; WIF SA is least-priv and unchanged |
| T-40.1-SC | Tampering | npm/pip/cargo installs | n/a | This phase installs NO packages — pure YAML edit + gh/git CLI; package-legitimacy gate not applicable |
</threat_model>

<verification>
- Static: `python3 yaml.safe_load(deploy.yml)` exits 0; all grep gates in Task 1 pass.
- Pre-deploy (no prod mutation): `gh secret list` shows all 6 referenced secrets (Task 2 checkpoint). The preflight is the in-CI net — a missing secret fails before any GCP call.
- Deploy proof (Task 3/4): `gh run list`/`gh run watch` shows the run passed preflight and reached/completed the deploy step for both legs.
- No `--dry-run` exists for deploy-cloudrun (RESEARCH §Verification Approach); the committed workflow IS the deploy. The preflight + the strict secrets-first ship order are the safety net. Live `/health` curl is user-gated (gcloud reauth).
</verification>

<success_criteria>
deploy.yml hardened (preflight gate + prod/dev matrix + Supabase env + quoted
comma-safe origins + MAX_BODY_BYTES rename + merge strategy), committed to the
proxy repo, and the triggered deploy passed preflight for both legs. The two
phase-40 app commits are pushed only after the hardened deploy is live, with no
crash-loop. Legacy shared `ALLOWED_ORIGINS` secret retired; per-service secrets
remain. ship-order steps 1→2→3 honored.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/40.1-harden-ai-proxy-deploy-ci/40.1-02-SUMMARY.md` when done.
</output>
