---
phase: 38-migration-retryability
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - src/hooks/useDataMigration.ts
  - src/components/ProtectedRoute.tsx
  - src/ui/WarnBanner.tsx
  - src/components/MigrationRetryBanner.tsx
  - src/layouts/AppLayout.tsx
  - src/tests/migration-partial.test.tsx
  - src/tests/migration-retry-banner.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "useDataMigration surfaces the split counters (failed, alreadyMigrated) so the banner can show the failed-only N (D-06/D-10)"
    - "A persistent, dismissible in-layout banner reads partial state from the SINGLE shared hook instance via Outlet context — it never re-calls useDataMigration/useLiveQuery (no second migration spawned)"
    - "The banner renders 'N item(s) not yet synced' (N = failed count) with a 'Retry sync' action that re-runs the whole idempotent migration; renders null when not partial or failed === 0 (SC3)"
    - "WarnBanner gains an OPTIONAL action slot; every existing caller (SessionDetail, SessionCard) renders identically (back-compat)"
    - "MigrationSplash is unchanged (D-08); ProtectedRoute keeps feeding it the failed count via its existing skipped prop"
  artifacts:
    - path: "src/components/MigrationRetryBanner.tsx"
      provides: "persistent partial-migration banner reading Outlet context (D-07)"
      exports: ["MigrationRetryBanner"]
    - path: "src/ui/WarnBanner.tsx"
      provides: "additive optional action slot (UI-SPEC §Component Inventory)"
      contains: "action?"
    - path: "src/tests/migration-retry-banner.test.tsx"
      provides: "SC3 banner render/copy/null assertions"
  key_links:
    - from: "src/components/ProtectedRoute.tsx"
      to: "src/components/MigrationRetryBanner.tsx"
      via: "<Outlet context={migration} /> shared down to AppLayout child route"
      pattern: "Outlet context=\\{migration\\}"
    - from: "src/components/MigrationRetryBanner.tsx"
      to: "src/hooks/useDataMigration.ts"
      via: "useOutletContext<ReturnType<typeof useDataMigration>>() — read, never re-call"
      pattern: "useOutletContext"
    - from: "src/layouts/AppLayout.tsx"
      to: "src/components/MigrationRetryBanner.tsx"
      via: "mounted next to PhotoMigrationBanner"
      pattern: "MigrationRetryBanner"
---

<objective>
Surface the partial-migration state from Plan 01 in the UI (SC3): plumb the `failed`/`alreadyMigrated` split through `useDataMigration` (D-06), add an optional `action` slot to `WarnBanner` (back-compat), create a persistent dismissible `MigrationRetryBanner` that reads the single shared hook instance via react-router `Outlet` context, and mount it in `AppLayout`.

Purpose: After a partial migration, the user needs a recoverable later-session path to retry — "N items not yet synced — Retry sync" — separate from the at-login `MigrationSplash` (D-08, unchanged). The banner must read the existing hook instance (Pattern 4 / Option A) so it does NOT spawn a second migration.

Output: Modified hook + ProtectedRoute + WarnBanner + AppLayout; new `MigrationRetryBanner`; extended partial test + new banner test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/38-migration-retryability/38-CONTEXT.md
@.planning/milestones/v1.3-phases/38-migration-retryability/38-RESEARCH.md
@.planning/milestones/v1.3-phases/38-migration-retryability/38-PATTERNS.md
@.planning/milestones/v1.3-phases/38-migration-retryability/38-UI-SPEC.md
@.planning/milestones/v1.3-phases/38-migration-retryability/38-VALIDATION.md
@.planning/milestones/v1.3-phases/38-migration-retryability/38-01-SUMMARY.md

<interfaces>
<!-- Contracts extracted from live source. Plan 01 already shipped the new migration return shape. -->

migrateToSupabase return (after Plan 01): `{ migrated, alreadyMigrated, failed, partial: failed > 0 }`.

Hook today (src/hooks/useDataMigration.ts) — MigrationStatus has `migrated, skipped` (:15-21); success branch maps `migrated, skipped` (:39-44); returns `{ ...status, retry: runMigration }` (:61). `state` includes "in-progress" and "partial". REPLACE `skipped` with `failed` + add `alreadyMigrated` in the interface and the success-branch mapping.

WarnBanner (src/ui/WarnBanner.tsx) props (:15-25): `{ icon?, title, body?, onDismiss?, className? }`; render keeps `role="status"`; dismiss X already present (:47-56). Add OPTIONAL `action?: { label: string; onClick: () => void; busy?: boolean }` rendered between the body div (:43-46) and the dismiss button (:47).

PhotoMigrationBanner (src/components/PhotoMigrationBanner.tsx) — persistent-banner precedent: `return null` when nothing to show, pluralized count. MigrationRetryBanner mirrors STRUCTURE but its data source is Outlet context, NOT useLiveQuery.

ProtectedRoute (src/components/ProtectedRoute.tsx): hook instance at :26; renders MigrationSplash for partial state until dismissed (:60-76, passes `skipped={migration.skipped}` at :71); bare `<Outlet />` at :86. Change line 86 to `<Outlet context={migration} />` and change :71 to pass the failed count (`skipped={migration.failed}`) so MigrationSplash stays UNCHANGED (D-08).

AppLayout (src/layouts/AppLayout.tsx): banner cluster — InstallBanner/OfflineIndicator/BlockedQueueBadge/PhotoMigrationBanner at :83-86; its `<Outlet />` (inside <main>) is the AppLayout child route, so a banner here reads ProtectedRoute's Outlet context.

UI-SPEC LOCKS (38-UI-SPEC.md): warn family only (no accent fill); title `{N} item{s} not yet synced`; body `Your data is safe — retry to finish syncing.`; CTA text `Retry sync` (never bare Retry) + `refresh` glyph; in-flight label `Retrying…` + `aria-busy`; keep `role="status"` (NOT role="alert"); 44px targets (`min-h-11 min-w-11`); N = failed only; banner persistent (no auto-dismiss), dismiss hides for the session.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plumb split counters through useDataMigration + add WarnBanner action slot</name>
  <files>src/hooks/useDataMigration.ts, src/components/ProtectedRoute.tsx, src/ui/WarnBanner.tsx, src/tests/migration-partial.test.tsx</files>
  <read_first>
    - src/hooks/useDataMigration.ts (full, :1-62) — MigrationStatus :15-21, success mapping :39-44, return :61
    - src/ui/WarnBanner.tsx (full, :1-59) — props :15-25, render :40-58
    - src/components/ProtectedRoute.tsx:60-86 — MigrationSplash skipped prop at :71, Outlet at :86
    - src/tests/migration-partial.test.tsx (header :1-50 mocks ../db/migration, tests hook→splash via renderHook)
  </read_first>
  <behavior>
    - The hook's returned status carries `failed` and `alreadyMigrated` (replacing `skipped`); success branch sets `state: result.partial ? "partial" : "complete"`, `failed: result.failed`, `alreadyMigrated: result.alreadyMigrated`.
    - WarnBanner with no `action` renders byte-identically to today (back-compat — SessionDetail/SessionCard unaffected); with `action`, it renders a 44px `tpc-btn` button (refresh icon + label, `aria-busy` when busy, disabled when busy) between body and dismiss X, inside the existing `role="status"` container.
  </behavior>
  <action>
    Edit src/hooks/useDataMigration.ts: in `MigrationStatus` replace `skipped: number` with `alreadyMigrated: number; failed: number`; update the initial useState (:24-30) accordingly; in runMigration's success branch (:39-44) map `failed: result.failed, alreadyMigrated: result.alreadyMigrated` (drop `skipped`). The `partial`/`in-progress` states and `retry: runMigration` return stay. Keep the existing WHY-comment on the partial state.

    Edit src/components/ProtectedRoute.tsx: change the MigrationSplash `skipped={migration.skipped}` prop (:71) to `skipped={migration.failed}` so MigrationSplash's signature stays UNCHANGED (D-08) while reading the renamed counter. (Do NOT touch MigrationSplash.tsx.)

    Edit src/ui/WarnBanner.tsx: add `action?: { label: string; onClick: () => void; busy?: boolean }` to WarnBannerProps after `onDismiss` (:23) with a WHY-comment ("optional action rendered before the dismiss X"). Destructure `action` in the component params. Render it between the body `<div>` (:46) and the `onDismiss` button (:47): a `<button type="button" onClick={action.onClick} disabled={action.busy} aria-busy={action.busy || undefined} className="tpc-btn shrink-0 min-h-11 min-w-11 text-warn underline">` containing `<Icon name="refresh" size={18} aria-hidden /> {action.label}`. Gate on `{action && (...)}`. Keep `role="status"` and the warn family — no accent.

    Update src/tests/migration-partial.test.tsx hook assertions to the new return shape (`failed`/`alreadyMigrated` instead of `skipped`).
  </action>
  <verify>
    <automated>npx vitest run src/tests/migration-partial.test.tsx 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - useDataMigration's returned object exposes `failed` and `alreadyMigrated` (no `skipped`); migration-partial.test.tsx asserts the new shape and passes.
    - WarnBanner renders unchanged when `action` is omitted (existing callers compile and their tests stay green); when `action` is provided it renders a `min-h-11 min-w-11` refresh button with `aria-busy` honored, inside `role="status"`.
    - ProtectedRoute passes `migration.failed` to MigrationSplash's `skipped` prop; MigrationSplash.tsx is untouched; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Hook surfaces the split counters; WarnBanner has a back-compat action slot; splash stays unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MigrationRetryBanner (Outlet context) + ProtectedRoute context pass + AppLayout mount + SC3 test</name>
  <files>src/components/MigrationRetryBanner.tsx, src/components/ProtectedRoute.tsx, src/layouts/AppLayout.tsx, src/tests/migration-retry-banner.test.tsx</files>
  <read_first>
    - src/components/PhotoMigrationBanner.tsx (full, :1-37) — null-render + pluralization precedent
    - src/components/ProtectedRoute.tsx:26,86 — hook instance + bare `<Outlet />`
    - src/layouts/AppLayout.tsx:11,83-86 — PhotoMigrationBanner import + banner cluster mount
    - src/ui/WarnBanner.tsx (action slot added in Task 1)
    - 38-UI-SPEC.md §Copywriting Contract + §Interaction Contract (locked copy + behavior)
  </read_first>
  <behavior>
    - MigrationRetryBanner reads `useOutletContext<ReturnType<typeof useDataMigration>>()`; returns null when `state !== "partial"`, when locally dismissed, or when `failed === 0`. It NEVER calls useDataMigration or useLiveQuery (no second migration).
    - When partial with failed > 0: renders WarnBanner title `${failed} item${failed === 1 ? "" : "s"} not yet synced`, body `Your data is safe — retry to finish syncing.`, an action `{ label: busy ? "Retrying…" : "Retry sync", onClick: retry, busy }` where `busy = state === "in-progress"`, and onDismiss hiding it for the session.
    - Dismiss does not mark migration complete (banner re-derives from context next session while partial).
    - SC3 test: render the banner with a mocked useOutletContext returning `{ state: "partial", failed: 2, retry: vi.fn() }` → asserts "2 items not yet synced" + a "Retry sync" button; with `failed: 0` (or non-partial state) → asserts null render; clicking Retry calls the context `retry`.
  </behavior>
  <action>
    Create src/components/MigrationRetryBanner.tsx mirroring PhotoMigrationBanner's structure but sourcing state from `useOutletContext` (import from "react-router"), typed `ReturnType<typeof useDataMigration>`. Use a local `useState(false)` dismissed flag. Guard: `if (m.state !== "partial" || dismissed || m.failed === 0) return null;`. Compute `const busy = m.state === "in-progress";`. Render `<WarnBanner title={...} body="Your data is safe — retry to finish syncing." onDismiss={() => setDismissed(true)} action={{ label: busy ? "Retrying…" : "Retry sync", onClick: m.retry, busy }} />`. Copy is LOCKED by UI-SPEC — do not paraphrase.

    Edit src/components/ProtectedRoute.tsx line 86: `return <Outlet context={migration} />;` (was bare `<Outlet />`). No other change — the partial-splash path (:60-76) and write-ahead drain (:42) are untouched.

    Edit src/layouts/AppLayout.tsx: import MigrationRetryBanner and mount it as a sibling immediately after `<PhotoMigrationBanner />` (:86).

    Create src/tests/migration-retry-banner.test.tsx (SC3): mock `react-router`'s `useOutletContext` (or wrap in a MemoryRouter with a route whose element supplies context) to return the states in the behavior block; assert the title/CTA copy, the null renders, and that clicking "Retry sync" invokes the context `retry`. Reuse the existing setup.ts (jest-dom + fake-indexeddb already loaded).
  </action>
  <verify>
    <automated>npx vitest run src/tests/migration-retry-banner.test.tsx 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MigrationRetryBanner renders "2 items not yet synced" + a "Retry sync" button when context is `{ state: "partial", failed: 2 }`, and null when `failed === 0` or state is not "partial" (SC3).
    - Clicking "Retry sync" calls the context `retry`; busy state shows "Retrying…" with `aria-busy` and does not re-call useDataMigration (test asserts useDataMigration is not imported/instantiated by the banner — verified by grep `useDataMigration` absent from MigrationRetryBanner.tsx except in the type import).
    - ProtectedRoute returns `<Outlet context={migration} />`; AppLayout mounts `<MigrationRetryBanner />` after PhotoMigrationBanner; `npx tsc --noEmit` clean.
    - `npm test` full suite green (back-compat: no existing WarnBanner/ProtectedRoute/AppLayout test regresses).
  </acceptance_criteria>
  <done>Partial state is surfaced via a persistent banner reading the single shared hook instance; Retry re-runs the idempotent migration — SC3 + SC4 (UI half) complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook (ProtectedRoute) → banner (AppLayout) via Outlet context | Migration state is shared from a single in-tree hook instance; no external input crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04 | Tampering (duplicate migration) | MigrationRetryBanner state source | mitigate | The banner reads `useOutletContext` (the single ProtectedRoute hook instance) and never calls useDataMigration/useLiveQuery — a second instance would spawn a parallel migration. Asserted by the banner test (no useDataMigration call) + grep gate in acceptance. |
| T-38-05 | Repudiation / false-clear | partial counter semantics in UI | mitigate | Banner N = `failed` only (D-10); idempotent skips (`alreadyMigrated`) never show, so a clean retry makes the banner disappear rather than falsely persist. Enforced by the failed-only guard + null-on-failed-0 test. |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | No package installs — `react-router`, WarnBanner, vitest all already in the tree. Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- `npx vitest run src/tests/migration-partial.test.tsx src/tests/migration-retry-banner.test.tsx` green (SC3).
- `npx tsc --noEmit` clean; `npm test` full suite green at wave merge.
- Manual UAT (per 38-VALIDATION.md): force a partial migration, confirm the persistent banner shows the correct N, dismiss persists across reload, "Retry sync" clears it when complete with no duplicate Supabase rows.
</verification>

<success_criteria>
- SC3: partial state surfaces via the DAT-1 `partial` flag in a persistent dismissible banner with a working Retry; retry re-runs the migration.
- SC4 (UI half): the banner reflects partial state (failed count) and the retry path drives the idempotent Plan-01 migration.
- D-06/D-07/D-08/D-10 honored; MigrationSplash unchanged; WarnBanner back-compat.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/38-migration-retryability/38-02-SUMMARY.md` when done.
</output>
