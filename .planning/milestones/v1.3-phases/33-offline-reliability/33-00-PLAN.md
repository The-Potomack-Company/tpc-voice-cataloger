---
phase: 33-offline-reliability
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - ../_workspace/Schema/schema.md
  - ../_workspace/Schema/migrations.md
  - supabase/migrations/20260601000000_add_items_claim_columns.sql
  - src/db/database.types.ts
  - src/utils/backoff.ts
  - src/utils/aiErrorClass.ts
  - src/tests/backoff.test.ts
  - src/tests/error-classify.test.ts
  - src/tests/blocked-badge.test.tsx
  - src/tests/supabase-types.test.ts
autonomous: false
requirements: [REL-1, REL-2, REL-3]
user_setup:
  - service: supabase
    why: "Prod schema push for the two-column items migration; CLI auth (supabase login/link) is user-only"
    env_vars:
      - name: SUPABASE_DB_PASSWORD
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string password"
    dashboard_config:
      - task: "Ensure vendored Supabase CLI is logged in + linked to project wgrknodfxdjtddsirldw (same as Phase 31)"
        location: "node_modules/.bin/supabase login + link"

must_haves:
  truths:
    - "items table has claimed_at (timestamptz null) and ai_attempts (int not null default 0) columns on prod"
    - "database.types.ts items Row/Insert/Update include claimed_at and ai_attempts"
    - "Pure backoff helpers nextEligibleAt/isInBackoff exist and are unit-tested"
    - "Pure classifyAiError helper exists returning permanent|transient and is unit-tested"
    - "Blocked-badge component test exists (RED until REL-3 ships) defining tone=err + count + click-to-detail expectations"
    - "Migration adds NO broad GRANT UPDATE; new columns inherit items RLS"
  artifacts:
    - path: "supabase/migrations/20260601000000_add_items_claim_columns.sql"
      provides: "Additive two-column DDL"
      contains: "add column if not exists claimed_at"
    - path: "src/utils/backoff.ts"
      provides: "nextEligibleAt, isInBackoff, ATTEMPT_CAP, BACKOFF_BASE_MS, BACKOFF_CAP_MS"
      exports: ["nextEligibleAt", "isInBackoff"]
    - path: "src/utils/aiErrorClass.ts"
      provides: "classifyAiError taxonomy (D-08)"
      exports: ["classifyAiError"]
    - path: "src/tests/backoff.test.ts"
      provides: "backoff math bounds tests"
    - path: "src/tests/error-classify.test.ts"
      provides: "D-08 taxonomy table tests"
    - path: "src/tests/blocked-badge.test.tsx"
      provides: "blocked-count badge component contract (Wave-0 RED stub)"
  key_links:
    - from: "src/utils/backoff.ts"
      to: "items.claimed_at + items.ai_attempts"
      via: "drain reads columns, computes window"
      pattern: "nextEligibleAt\\(.*claimed"
    - from: "supabase/migrations/20260601000000_add_items_claim_columns.sql"
      to: "../_workspace/Schema/schema.md"
      via: "schema.md items column list updated FIRST"
      pattern: "claimed_at"
---

<objective>
Land the cross-app foundation for Phase 33: the single two-column `items` migration (`claimed_at`, `ai_attempts`) following the workspace 4-step schema protocol with a Phase-31-style sibling-isolation push gate, regenerate `database.types.ts`, and create the pure helper modules + Wave-0 test stubs every later plan depends on.

Purpose: REL-1 (backoff/attempts) and REL-2 (DB claim) cannot function until the two columns exist on prod and the generated types reflect them. The pure `backoff.ts` and `aiErrorClass.ts` helpers + their tests are the Nyquist scaffold the REL-1/REL-2/REL-3 drains consume. Without the prod push, build + types pass falsely (types come from config, not the live DB), so the push is BLOCKING.

Output: applied migration, updated schema.md + migrations.md, regenerated types, two pure util modules, three new test files (two green, one RED until REL-3), extended supabase-types test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/33-offline-reliability/33-CONTEXT.md
@.planning/milestones/v1.3-phases/33-offline-reliability/33-RESEARCH.md
@.planning/milestones/v1.3-phases/33-offline-reliability/33-PATTERNS.md
@../CLAUDE.md

<cross_app_note>
This phase has NO driving feature note (`grep -l "app:" ../_workspace/Features/*.md` returns nothing for this app); work is per-repo. The migration is still a cross-app schema event — cataloger + dashboard read the items table. Start from `../_workspace/Schema/schema.md`, not local belief.
</cross_app_note>

<interfaces>
<!-- Helper contracts later plans consume. Executor implements these signatures exactly. -->
From src/utils/backoff.ts (NEW):
  export const BACKOFF_BASE_MS = 5_000;   // D-06
  export const BACKOFF_CAP_MS = 300_000;  // D-06
  export const ATTEMPT_CAP = 5;           // D-07
  export function nextEligibleAt(claimedAt: Date | null, attempts: number): number;
  export function isInBackoff(claimedAt: Date | null, attempts: number): boolean;

From src/utils/aiErrorClass.ts (NEW):
  export function classifyAiError(error: unknown): "permanent" | "transient";  // D-08
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pure backoff + error-classifier helpers with tests (Wave-0 GREEN)</name>
  <read_first>
    - src/utils/formatEstimate.ts (pure-util module shape analog — named exports, null-tolerant, no side effects)
    - src/tests/formatEstimate.test.ts (pure-util test analog — flat table of it() cases, no mocks)
    - src/services/gemini.ts:146-151 (isTransientNetworkError seed that classifyAiError generalizes)
    - src/services/gemini.ts:285-286 (proxy throws `new Error("Proxy returned HTTP <status>: ...")` — status is text-only; regex-parse it)
    - src/services/gemini.ts:296-298 (Zod failure string `Zod validation failed: ...`)
    - 33-RESEARCH.md:239-278 (backoff helper §3 + classifier code), 33-PATTERNS.md:97-145
  </read_first>
  <behavior>
    - backoff: nextEligibleAt(null, n) === 0 and nextEligibleAt(date, 0) === 0 (never tried → eligible now)
    - backoff: for attempts>0, returned value is in [claimedAt.getTime(), claimedAt.getTime() + min(CAP, BASE*2^attempts)) (full jitter upper bound)
    - backoff: isInBackoff returns true when Date.now() < nextEligibleAt, false otherwise (use a far-past claimedAt for false, mock Date/Math.random for deterministic bounds)
    - classify: !navigator.onLine → "transient"; AbortError DOMException → "transient"; /Failed to fetch|NetworkError|Load failed|abort/ → "transient"
    - classify: "Proxy returned HTTP 429" → "transient"; HTTP 500/503 → "transient"; HTTP 400/401/404 → "permanent"
    - classify: "Zod validation failed: ..." → "permanent"; "unsupported format" → "permanent"; unknown → "transient" (safe default)
  </behavior>
  <action>
    Create `src/utils/backoff.ts` exporting BACKOFF_BASE_MS=5_000, BACKOFF_CAP_MS=300_000, ATTEMPT_CAP=5, and pure functions nextEligibleAt(claimedAt, attempts) and isInBackoff(claimedAt, attempts) using the full-jitter formula `claimedAt.getTime() + Math.random() * Math.min(CAP, BASE * 2 ** attempts)`. Add a WHY-comment only on the full-jitter line citing D-06. Create `src/utils/aiErrorClass.ts` exporting classifyAiError(error) that generalizes gemini.ts:146-151 into the D-08 taxonomy via the regex-parse approach (NOT a gemini.ts typed-error refactor — that is a deferred future cleanup per RESEARCH Open Question 1; keeps the diff tight). Add a WHY-comment on the default `return "transient"` noting "retry not drop". Create `src/tests/backoff.test.ts` and `src/tests/error-classify.test.ts` mirroring formatEstimate.test.ts (no mocks; toggle navigator.onLine via Object.defineProperty for the offline case; mock Math.random/Date for deterministic backoff bounds). Both files must pass.
  </action>
  <verify>
    <automated>npx vitest run src/tests/backoff.test.ts src/tests/error-classify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/tests/backoff.test.ts src/tests/error-classify.test.ts` exits 0
    - `grep -q "export function nextEligibleAt" src/utils/backoff.ts` and `grep -q "export function isInBackoff" src/utils/backoff.ts`
    - `grep -q "export function classifyAiError" src/utils/aiErrorClass.ts`
    - aiErrorClass.ts contains NO import from gemini.ts and NO edit to gemini.ts (regex-parse, not typed-error refactor)
  </acceptance_criteria>
  <done>Both new util modules exist with the exact exported signatures; both test files pass; classifier covers the full D-08 table.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration + schema.md/migrations.md + blocked-badge RED test stub (steps 1-2 of schema protocol)</name>
  <read_first>
    - supabase/migrations/20260318000002_create_items.sql:13-14 (ai_status enum ALREADY has queued+processing → NO enum DDL)
    - supabase/migrations/20260529000000_lock_profiles_self_update.sql (Phase-31 header-comment + column-grant discipline; mirror style, add NO GRANT)
    - ../_workspace/Schema/schema.md:50-78 (canonical items column list — add claimed_at, ai_attempts ~line 64)
    - ../_workspace/Schema/migrations.md (cross-app migration log — append this migration)
    - ../CLAUDE.md "Schema = single source of truth" (4-step protocol)
    - src/tests/layout.test.tsx (component test analog for the blocked-badge stub)
    - src/ui/Badge.tsx + src/components/OfflineIndicator.tsx (tone="err" + render-nothing pattern the badge will follow)
    - 33-PATTERNS.md:32-50 (migration DDL), 33-RESEARCH.md:368-385 (security domain — no column grant)
  </read_first>
  <action>
    Step 1 of the protocol FIRST: edit `../_workspace/Schema/schema.md` items section (~line 64) to add `claimed_at timestamptz null` and `ai_attempts integer not null default 0` to the column list, and append a row to `../_workspace/Schema/migrations.md` for `20260601000000_add_items_claim_columns`. Step 2: create `supabase/migrations/20260601000000_add_items_claim_columns.sql` with a short WHY header (mirror 20260529000000 style) and the additive DDL `alter table public.items add column if not exists claimed_at timestamptz, add column if not exists ai_attempts integer not null default 0;`. NO GRANT statements, NO enum DDL (enum already has queued+processing), NO RLS changes — new columns inherit items RLS (Phase-31 rule, T-33-01). Also create the Wave-0 RED stub `src/tests/blocked-badge.test.tsx` mirroring layout.test.tsx, asserting the future blocked-count badge renders with `tone="err"` + the count next to OfflineIndicator and click opens a detail list; mark its body with `it.todo` or a skipped block so it does not fail the suite until REL-3 implements it (it defines the REL-3 D-10 contract now).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260601000000_add_items_claim_columns.sql && grep -q "claimed_at" ../_workspace/Schema/schema.md && grep -L "grant" supabase/migrations/20260601000000_add_items_claim_columns.sql</automated>
  </verify>
  <acceptance_criteria>
    - migration file exists and contains both `add column if not exists claimed_at` and `add column if not exists ai_attempts integer not null default 0`
    - `grep -vi '^--' supabase/migrations/20260601000000_add_items_claim_columns.sql | grep -ci 'grant'` returns 0 (T-33-01: no column-grant regression)
    - `grep -c "create type\|alter type" supabase/migrations/20260601000000_add_items_claim_columns.sql` returns 0 (no enum DDL)
    - `grep -q "claimed_at" ../_workspace/Schema/schema.md` and `grep -q "ai_attempts" ../_workspace/Schema/schema.md`
    - migrations.md has a row referencing 20260601000000_add_items_claim_columns
    - src/tests/blocked-badge.test.tsx exists and the suite does not fail on it (todo/skip)
  </acceptance_criteria>
  <done>schema.md + migrations.md updated first, migration SQL authored with no GRANT and no enum DDL, blocked-badge contract test stubbed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Prod schema push with sibling-isolation gate + regenerate types (steps 3-4)</name>
  <read_first>
    - .planning/milestones/v1.3-phases/31-*/31-02-PLAN.md (db push gate precedent — dry-run isolation then --yes)
    - 33-RESEARCH.md:215-219 (Pitfall 2: push applies ALL pending migrations — dry-run MUST show ONLY this one)
    - package.json db:types script (project-id wgrknodfxdjtddsirldw)
  </read_first>
  <what-built>
    Tasks 1-2 produced the migration SQL, updated schema.md/migrations.md, and the pure helpers. This task applies the migration to PROD and regenerates the generated types. The dry-run isolation check + schema.md/migration authoring were autonomous; the actual prod push needs user-only Supabase CLI auth, so this is a blocking human checkpoint.
  </what-built>
  <how-to-verify>
    1. Run the isolation gate: `node_modules/.bin/supabase db push --dry-run` — confirm the ONLY pending migration listed is `20260601000000_add_items_claim_columns`. If ANY other migration appears, STOP and report.
    2. Apply: `SUPABASE_DB_PASSWORD=*** node_modules/.bin/supabase db push --yes`.
    3. Regenerate types: `npm run db:types`. Expect exactly a 2-column diff on items (claimed_at, ai_attempts added to Row/Insert/Update). Report if any unexpected table/column diff appears (note: STATE.md flags pre-existing crm_* / theme drift — those are KNOWN and not introduced here).
    4. Confirm A4 schema-drift-checker (Checkpoint G) fired on the schema/migration edit and wrote ../_workspace/Schema/drift.md.
    5. Extend src/tests/supabase-types.test.ts with assertions that items Row/Insert/Update include claimed_at and ai_attempts, then run `npx vitest run src/tests/supabase-types.test.ts`.
  </how-to-verify>
  <verify>
    <automated>npx vitest run src/tests/supabase-types.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - dry-run showed ONLY 20260601000000_add_items_claim_columns pending before apply
    - prod push reported success
    - `grep -c "claimed_at" src/db/database.types.ts` >= 3 (Row + Insert + Update)
    - `grep -c "ai_attempts" src/db/database.types.ts` >= 3
    - `npx vitest run src/tests/supabase-types.test.ts` passes with the new column assertions
  </acceptance_criteria>
  <resume-signal>Type "approved" once the push succeeded, types regenerated with the 2-column diff, and supabase-types.test.ts passes — or describe the dry-run/diff anomaly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → prod items table | DDL applied to shared Supabase project (cataloger + dashboard read items) |
| app drain client → items columns | new operational columns written by the drain under authed Supabase client + RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | items migration (claimed_at, ai_attempts) | mitigate | DDL-only, NO `GRANT UPDATE`; new columns inherit existing items RLS (Phase-31 lesson). Acceptance criterion greps migration for zero grant lines. |
| T-33-02 | Denial-of-wallet | concurrent Gemini drains | mitigate | The two new columns enable the REL-2 DB-atomic claim (33-02) that structurally prevents duplicate spend. This plan lays the column foundation. |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase (RESEARCH Package Legitimacy Audit). No slopcheck surface. |
</threat_model>

<verification>
- `npx vitest run src/tests/backoff.test.ts src/tests/error-classify.test.ts src/tests/supabase-types.test.ts` all green
- Migration applied to prod; `database.types.ts` reflects both columns
- No GRANT regression in the migration; no enum DDL
- A4 drift check fired
</verification>

<success_criteria>
- items table on prod has claimed_at + ai_attempts; types regenerated
- backoff.ts + aiErrorClass.ts exist with the exact signatures REL-1/REL-2/REL-3 consume
- backoff + classify tests green; blocked-badge contract stub present (RED/skipped until REL-3)
- schema.md + migrations.md updated first per the 4-step protocol
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/33-offline-reliability/33-00-SUMMARY.md` when done.
</output>
