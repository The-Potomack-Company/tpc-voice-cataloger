---
phase: 33-offline-reliability
plan: 03
type: execute
wave: 2
depends_on: [33-00]
files_modified:
  - src/hooks/useWriteAheadQueue.ts
  - src/components/BlockedQueueBadge.tsx
  - src/layouts/AppLayout.tsx
  - src/tests/write-ahead-queue.test.ts
  - src/tests/blocked-badge.test.tsx
autonomous: true
requirements: [REL-3]

must_haves:
  truths:
    - "On a PERMANENT write-ahead failure the failing entry plus its dependent same-item entries are dropped and the drain CONTINUES to later entries"
    - "On a TRANSIENT write-ahead failure the drain halts and FIFO order is preserved (entries remain)"
    - "The unconditional break-on-first-failure is gone; the do-NOT-emit-analytics comment is preserved"
    - "A blocked-count badge (tone=err) renders next to OfflineIndicator in the AppLayout header when count>0, renders nothing when count==0"
    - "Clicking the badge opens a detail list of blocked items"
    - "The handleReconnect drain order (writeAhead → fetchSessions → photos → audio) is unchanged"
  artifacts:
    - path: "src/hooks/useWriteAheadQueue.ts"
      provides: "classify-driven drop/continue vs halt"
      contains: "classifyAiError"
    - path: "src/components/BlockedQueueBadge.tsx"
      provides: "blocked-count badge + detail trigger (D-10)"
      contains: "tone=\"err\""
    - path: "src/tests/blocked-badge.test.tsx"
      provides: "badge render + click-to-detail (un-skipped from 33-00 stub)"
  key_links:
    - from: "src/hooks/useWriteAheadQueue.ts"
      to: "src/utils/aiErrorClass.ts"
      via: "classifyAiError(err) branch"
      pattern: "classifyAiError"
    - from: "src/layouts/AppLayout.tsx"
      to: "src/components/BlockedQueueBadge.tsx"
      via: "mounted next to OfflineIndicator"
      pattern: "BlockedQueueBadge"
---

<objective>
REL-3: stop one permanent write-ahead failure from blocking every later write, and surface a blocked-count badge. Replace the unconditional `break` (useWriteAheadQueue.ts:66) with classify-driven behavior (D-09): classify the error via `classifyAiError`; on PERMANENT, drop the failing entry plus its dependent same-item entries (matched by `payload.id`/`tempId`) and CONTINUE; on TRANSIENT, halt-and-backoff preserving FIFO. Mount a `Badge tone="err"` blocked-count badge next to `OfflineIndicator` in the AppLayout header (D-10); count = items with `ai_status='failed'`; click opens a detail list.

Purpose: today a single permanent failure (4xx / no-audio) `break`s the drain so every later write is stuck forever with no UI signal. Classification + selective drop unblocks the queue; the badge makes blocked items visible.

Output: hardened `useWriteAheadQueue.ts`, new `BlockedQueueBadge.tsx`, badge mounted in AppLayout, write-ahead + badge tests green (un-skips the 33-00 stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.3-phases/33-offline-reliability/33-CONTEXT.md
@.planning/milestones/v1.3-phases/33-offline-reliability/33-RESEARCH.md
@.planning/milestones/v1.3-phases/33-offline-reliability/33-PATTERNS.md

<interfaces>
From src/utils/aiErrorClass.ts (created in 33-00):
  export function classifyAiError(error: unknown): "permanent" | "transient";

WriteAheadEntry (src/db/types.ts:91-98):
  { id?: number; table: string; operation: "insert"|"update"|"delete";
    payload: Record<string, unknown>; tempId?: string; createdAt: number }

Dependent-entry match idiom (useWriteAheadQueue.ts:78-83, hasPendingForItem):
  db.writeAheadQueue.filter((e) => (e.payload as Record<string,unknown>).id === itemId)

Badge primitive (src/ui/Badge.tsx): <Badge tone="err">{count}</Badge>
OfflineIndicator (src/components/OfflineIndicator.tsx): returns null when nothing to show — mirror for count==0
AppLayout header mount anchor: OfflineIndicator at AppLayout.tsx:81
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify-driven drop/continue vs halt in the write-ahead drain</name>
  <read_first>
    - src/hooks/useWriteAheadQueue.ts:29 (FIFO orderBy createdAt — unchanged)
    - src/hooks/useWriteAheadQueue.ts:31-55 (insert/update/delete dispatch)
    - src/hooks/useWriteAheadQueue.ts:56-67 (the catch + unconditional break + do-NOT-emit-analytics comment)
    - src/hooks/useWriteAheadQueue.ts:78-83 (hasPendingForItem payload.id match idiom to reuse for dependents)
    - src/db/types.ts:91-98 (WriteAheadEntry shape: payload, tempId)
    - src/utils/aiErrorClass.ts (classifyAiError — from 33-00)
    - 33-RESEARCH.md:232-235 (Pitfall 5: drop dependents with the permanent entry), :179 (do-NOT-re-enqueue-analytics anti-pattern)
    - 33-PATTERNS.md:149-172 (replace break; dependent detection; preserve FIFO + analytics comment)
    - src/tests/write-ahead-queue.test.ts:1-40 (vi.hoisted + vi.mock supabase + uiStore; teardown db.delete/open)
  </read_first>
  <behavior>
    - PERMANENT failure (classifyAiError→"permanent"): the failing entry is deleted from writeAheadQueue AND every entry whose payload.id (or tempId) matches the same item is deleted; the drain CONTINUES to later non-matching entries (assert a later entry still processes)
    - TRANSIENT failure (classifyAiError→"transient"): the drain breaks; the failing entry and all later entries REMAIN in the queue in FIFO order (assert count unchanged after the failing index)
    - the do-NOT-emit-analytics comment + no trackEvent call in the failure path are preserved
    - FIFO orderBy("createdAt") unchanged
  </behavior>
  <action>
    In the catch block (useWriteAheadQueue.ts:56-67) replace the unconditional `break` with: `const kind = classifyAiError(err);`. If `kind === "permanent"`: delete the failing entry, then find + delete dependent same-item entries by matching `payload.id` (and `tempId` when present) reusing the hasPendingForItem filter idiom (78-83), then `continue` the loop (drain the rest). If `kind === "transient"`: `break` (halt-and-backoff, preserve FIFO — later updates depend on earlier inserts). Preserve the existing console.error and the do-NOT-emit-analytics WHY-comment verbatim; do NOT call trackEvent in this path (re-enqueue loop, RESEARCH anti-pattern). Add cases to write-ahead-queue.test.ts: a permanent-classified error drops the entry + its same-item dependents and the drain continues to a later unrelated entry; a transient-classified error halts and leaves all remaining entries in FIFO order.
  </action>
  <verify>
    <automated>npx vitest run src/tests/write-ahead-queue.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "classifyAiError" src/hooks/useWriteAheadQueue.ts`
    - the unconditional break is gone — break now only on the transient branch (`grep -c "break" ...` reflects the single transient break)
    - do-NOT-emit-analytics comment still present (`grep -qi "do NOT emit" src/hooks/useWriteAheadQueue.ts`) and no trackEvent in the catch
    - `npx vitest run src/tests/write-ahead-queue.test.ts` passes the permanent-drop-continue + transient-halt-FIFO cases
  </acceptance_criteria>
  <done>Permanent failures drop entry+dependents and continue; transient failures halt preserving FIFO; analytics anti-pattern preserved; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: BlockedQueueBadge component mounted next to OfflineIndicator</name>
  <read_first>
    - src/ui/Badge.tsx (tone="err" primitive to reuse)
    - src/components/OfflineIndicator.tsx (render-nothing-when-empty pattern; aria-live/role=status structure)
    - src/layouts/AppLayout.tsx:80-82 (header region; OfflineIndicator mount at :81 — mount badge adjacent)
    - src/layouts/AppLayout.tsx:57-63 (handleReconnect drain order INVARIANT — do not touch)
    - src/tests/blocked-badge.test.tsx (the RED/skipped stub from 33-00 defining the contract — un-skip + implement)
    - src/services/offlineQueue.ts getQueuedItems pattern (query items by ai_status — reuse for the failed-count read)
    - 33-PATTERNS.md:175-186 (badge mount + tone="err" + render-nothing + blocked-count = ai_status='failed')
  </read_first>
  <action>
    Create `src/components/BlockedQueueBadge.tsx`: query the count of items with `ai_status='failed'` (Supabase select head/count, mirroring the getQueuedItems query shape), refresh on the same `online` cadence / mount. Render `null` when count===0 (mirror OfflineIndicator); otherwise render `<Badge tone="err">{count}</Badge>` as a clickable element (button) that toggles a detail list of the blocked items (detail-view internal structure is Claude's discretion — a simple list of item ids/labels is sufficient; reuse existing list primitives if present). Mount `<BlockedQueueBadge />` in AppLayout.tsx immediately adjacent to `<OfflineIndicator />` (:81) WITHOUT altering the handleReconnect drain order (57-63) or the header structure. Un-skip and complete `src/tests/blocked-badge.test.tsx` (stubbed in 33-00): assert tone="err" + count render when blocked>0, nothing when 0, and that a click reveals the detail list.
  </action>
  <verify>
    <automated>npx vitest run src/tests/blocked-badge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/BlockedQueueBadge.tsx` exists and contains `tone="err"`
    - `grep -q "BlockedQueueBadge" src/layouts/AppLayout.tsx` and it sits next to OfflineIndicator
    - handleReconnect drain order unchanged: `grep -A4 "handleReconnect = async" src/layouts/AppLayout.tsx` still shows processWriteAheadQueue → fetchSessions → drainPhotoQueue → drainQueue in order
    - blocked-badge.test.tsx is no longer skipped and passes (render-with-count, render-nothing-at-0, click-to-detail)
  </acceptance_criteria>
  <done>Blocked-count badge renders next to OfflineIndicator (tone="err", nothing at 0), click opens detail, drain order untouched, test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| write-ahead drain → Supabase tables | replayed offline writes; a permanent failure must not strand later writes |
| badge → items.ai_status read | read-only count under existing RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06 | Denial of Service | write-ahead break-on-first-failure | mitigate | Classify-driven drop/continue removes the single-failure head-of-line block; one bad write no longer freezes the queue. |
| T-33-07 | Information disclosure | analytics re-enqueue on failure | mitigate | Preserve the do-NOT-emit-analytics rule; no trackEvent in the failure path (prevents unbounded queue growth + repeated event spam). |
| T-33-08 | Tampering | dependent-entry drop by payload.id | accept | Dropping same-item dependents alongside a permanent insert prevents orphan 404-forever updates (Pitfall 5); operates only on the local Dexie queue, no privilege surface. |
| T-33-SC | Tampering | npm installs | accept | Zero packages installed (RESEARCH audit). |
</threat_model>

<verification>
- `npx vitest run src/tests/write-ahead-queue.test.ts src/tests/blocked-badge.test.tsx` green
- unconditional break removed; classify branch present; analytics comment preserved
- badge mounted next to OfflineIndicator; drain order unchanged
</verification>

<success_criteria>
- One permanent write-ahead failure no longer blocks all later writes
- Transient failures halt preserving FIFO
- Blocked items are visible via a tone="err" badge with a detail list
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/33-offline-reliability/33-03-SUMMARY.md` when done.
</output>
