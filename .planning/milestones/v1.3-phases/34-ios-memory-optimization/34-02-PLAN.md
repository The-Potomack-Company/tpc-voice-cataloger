---
phase: 34-ios-memory-optimization
plan: 02
type: tdd
wave: 1
depends_on: [34-00]
files_modified:
  - src/components/ItemList.tsx
  - src/components/ItemCard.tsx
  - src/tests/item-card-audio-status.test.tsx
  - docs/runbooks/ios-memory-smoke.md
autonomous: true
requirements: [PERF-3]
must_haves:
  truths:
    - "An ai_status/recording-state change on one item does NOT re-render the other N-1 ItemCards"
    - "ItemCard is prop-driven for audioCount/latestAudioId/photoCount/dexieItemId/isPending — zero live queries or async effects for these values"
    - "latestAudioId still reaches handleRetryAi (retry works) via the prop"
    - "Photo counts remain house-mode-only (D-07)"
    - "A two-part memory smoke runbook (Chrome heap + iOS Safari Web Inspector) with the COOP/COEP caveat is documented (D-09)"
  artifacts:
    - path: "src/components/ItemList.tsx"
      provides: "single aggregate useLiveQuery → Map<itemId, ItemMeta>; slices threaded as primitive props"
      contains: "useLiveQuery"
    - path: "src/components/ItemCard.tsx"
      provides: "prop-driven, React.memo-wrapped card; dev-only __itemCardRenderCounts"
      contains: "React.memo"
    - path: "docs/runbooks/ios-memory-smoke.md"
      provides: "D-09 manual memory verification procedure + COOP/COEP caveat"
      contains: "measureUserAgentSpecificMemory"
  key_links:
    - from: "src/components/ItemList.tsx"
      to: "src/components/ItemCard.tsx"
      via: "primitive meta props (audioCount, latestAudioId, photoCount, dexieItemId, isPending)"
      pattern: "audioCount="
    - from: "src/components/ItemCard.tsx handleRetryAi"
      to: "processAudioWithAi"
      via: "latestAudioId prop"
      pattern: "processAudioWithAi"
---

<objective>
PERF-3: collapse the ~4N Dexie subscriptions/effects (one set per ItemCard × N items)
into ONE aggregate `useLiveQuery` in `ItemList` that builds a `Map<itemId, ItemMeta>`.
`ItemList` threads each slice to `ItemCard` as PRIMITIVE props; `ItemCard` becomes
prop-driven and is wrapped in `React.memo` so a one-item state change re-renders only
that card. Add a dev-only render counter (D-08). Document the manual memory smoke (D-09).

Purpose: during recording, every audio/photo/queue write currently re-fires all 4N
subscriptions → render storm. One aggregate + memo eliminates the fan-out.
Output: prop-driven memoized ItemCard; ItemList aggregate; render-count test green;
audio-status test prop-signature updated; memory-smoke runbook.

TDD mode ON — `type: tdd`. The render-count RED test (Plan 00) drives this to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.3-phases/34-ios-memory-optimization/34-RESEARCH.md
@.planning/milestones/v1.3-phases/34-ios-memory-optimization/34-PATTERNS.md
@.planning/milestones/v1.3-phases/34-ios-memory-optimization/34-VALIDATION.md

<interfaces>
<!-- The contract ItemList → ItemCard. Primitive props (NOT a meta object) for clean React.memo shallow-compare. -->

ItemCardProps (extend the current 5-field interface, ItemCard.tsx:21-27, with 5 meta props):
  interface ItemCardProps {
    item: SupabaseItem;
    sessionId: string;
    isExpanded: boolean;
    onToggle: () => void;
    readOnly?: boolean;
    audioCount: number;
    latestAudioId: number | null;
    photoCount: number;
    dexieItemId: number | string | null;
    isPending: boolean;
  }

Dev-only render counter (export from ItemCard.tsx — the render-count test imports this):
  export const __itemCardRenderCounts: Map<string, number>;

Aggregate ItemMeta + module-level stable default (in ItemList.tsx):
  interface ItemMeta { audioCount: number; latestAudioId: number | null; photoCount: number; dexieItemId: number | string | null; isPending: boolean; }
  const EMPTY_META = new Map<string, ItemMeta>();  // module scope — stable identity

Existing per-item helpers to COMPOSE once each in the aggregate (do NOT re-implement):
  audioRecordsForItem(item.id)   // src/db/audioLookup — already imported in ItemList.tsx:3
  getDexieItemId(item.id)        // src/db/idMapping
  hasPendingForItem(item.id)     // src/hooks/useWriteAheadQueue
  db.photos.where("itemId").equals(dexieItemId).count()  // src/db (house mode only)
  useLiveQuery                   // dexie-react-hooks
</interfaces>
</context>

<feature>
  <name>ItemList aggregate hoist + prop-driven memoized ItemCard</name>
  <files>src/components/ItemList.tsx, src/components/ItemCard.tsx, src/tests/item-card-audio-status.test.tsx, docs/runbooks/ios-memory-smoke.md</files>
  <read_first>
    - src/components/ItemCard.tsx:1-90 (the 4 reactive surfaces to strip: dexieItemId effect :37-41, isPending effect :43-47, audioData useLiveQuery :49-63, photoCount useLiveQuery :79-88; keep showDeleteConfirm/retrying :31-32 and useAudioUploadStatus :66)
    - src/components/ItemCard.tsx:68-77, 377-378 (handleRetryAi + retry-disabled guard — must keep working off the latestAudioId PROP — Pitfall 4)
    - src/components/ItemList.tsx:1-35 (imports + useSessionItems :30), 264-310 (render site / items.map; the `compact` branch :199-235 does NOT use ItemCard — leave untouched)
    - 34-RESEARCH.md:182-296 (Pattern 2 aggregate, Pattern 3 memo + dev counter, Pitfalls 2/3/4/5)
    - 34-PATTERNS.md:62-199 (ItemList aggregate wiring + imports to add; ItemCard strip list; render-count test note)
    - src/tests/item-card-audio-status.test.tsx (renders ItemCard directly via `renderCard` :67-78 — prop signature must gain the 5 meta props)
  </read_first>
  <behavior>
    - RED (Plan 00): `item-card-render-count.test.tsx` — currently fails (no `__itemCardRenderCounts`, all cards re-render).
    - GREEN target: render `ItemList` with 3 items; flip ONE item's `ai_status`; only that card's `__itemCardRenderCounts` entry increments; the other 2 stay flat.
    - Regression: `item-card-audio-status.test.tsx` stays green after `renderCard` passes the 5 new required props (audio pill still renders Pending/Uploaded/Failed-retry per Phase 32 D-06).
    - Retry: an item with audio still retries (handleRetryAi reads `latestAudioId` prop); retry button disabled + "No audio to retry" title when `latestAudioId` is null.
    - Photo count: house-mode item shows photo count; non-house item shows 0 (D-07).
  </behavior>
  <implementation>
    ItemList.tsx:
    - Add imports: `useLiveQuery` (dexie-react-hooks), `getDexieItemId` (../db/idMapping), `hasPendingForItem` (../hooks/useWriteAheadQueue), `db` (../db). `audioRecordsForItem` already imported (line 3).
    - Declare module-level `interface ItemMeta {...}` (5 fields) and `const EMPTY_META = new Map<string, ItemMeta>();` (stable identity — avoids empty-state churn).
    - Above the return, add ONE `useLiveQuery(async () => { build Map }, [items], EMPTY_META)`. Loop body per item composes the existing helpers once each: `audioRecordsForItem(item.id)` → audioCount + latestAudioId (reduce-max idiom copied verbatim from ItemCard.tsx:53-56); `(await getDexieItemId(item.id)) ?? item.id` → dexieItemId; `item.mode === "house" && dexieItemId != null ? db.photos.where("itemId").equals(dexieItemId).count() : 0` → photoCount (D-07 house-only guard); `hasPendingForItem(item.id)` → isPending. `map.set(item.id, {...})`.
    - In the `items.map` render (lines 264-310, the NON-compact ItemCard site only), look up `const meta = itemMeta.get(item.id);` and spread PRIMITIVE props onto `<ItemCard>`: `audioCount={meta?.audioCount ?? 0}`, `latestAudioId={meta?.latestAudioId ?? null}`, `photoCount={meta?.photoCount ?? 0}`, `dexieItemId={meta?.dexieItemId ?? null}`, `isPending={meta?.isPending ?? false}`. Leave the `compact` branch and `handleRetryAll` untouched (one-shot, not subscriptions).

    ItemCard.tsx:
    - Extend `ItemCardProps` with the 5 meta props (see interface above).
    - REMOVE the 4 reactive surfaces: dexieItemId state+effect, isPending state+effect, audioData `useLiveQuery` + its derived audioCount/latestAudioId, photoCount `useLiveQuery`. Read these from props instead.
    - KEEP local UI state `showDeleteConfirm`, `retrying`, and `useAudioUploadStatus(latestAudioId ?? undefined)` per-card (Pitfall 5 — latestAudioId is now a prop; the hook is one cheap keyed read, leave it).
    - `handleRetryAi` reads the `latestAudioId` prop (Pitfall 4); retry-disabled guard `disabled={retrying || !latestAudioId}` and the "No audio to retry" title keep working.
    - Drop now-unused imports (`getDexieItemId`, `audioRecordsForItem`, `hasPendingForItem`, and `useLiveQuery` / `db` if no longer referenced — check before removing).
    - Add `export const __itemCardRenderCounts = new Map<string, number>();` and, inside the component body, a dev-only counter: `if (import.meta.env.MODE !== "production") __itemCardRenderCounts.set(item.id, (__itemCardRenderCounts.get(item.id) ?? 0) + 1);` (no-op in prod — first dev-flagged instrumentation in the repo; source RESEARCH Pattern 3).
    - Rename the function to `ItemCardImpl` and `export const ItemCard = React.memo(ItemCardImpl);`. Primitive props make the default shallow compare correct (Pitfall 2 — memo is what DELIVERS the fan-out reduction).

    item-card-audio-status.test.tsx:
    - Update the `renderCard` helper's `<ItemCard ... />` to pass the 5 new required props (sensible defaults consistent with the existing mocks: e.g. audioCount=1, latestAudioId=10, photoCount=0, dexieItemId=10, isPending=false). Do not change the assertions — the audio pill behavior is unchanged; only the prop surface is.
  </implementation>
</feature>

<task type="auto">
  <name>Task: Document the D-09 manual memory smoke runbook</name>
  <files>docs/runbooks/ios-memory-smoke.md</files>
  <read_first>
    - 34-CONTEXT.md:62-73 (D-09 — two-part procedure + COOP/COEP caveat)
    - 34-VALIDATION.md:62-68 (manual-only verifications table)
    - 34-RESEARCH.md:410-413 (Manual Verification section)
  </read_first>
  <action>
    Create `docs/runbooks/ios-memory-smoke.md` capturing the D-09 two-part manual procedure (this is the deliverable for the memory side of PERF-1; it is intentionally NOT a CI test):
    (a) Desktop Chrome heap snapshot: take a snapshot, run a 5-minute single-mode recording/cataloging session, snapshot again; assert no monotonic multi-MB-per-recording climb from retained binary strings (the PERF-1 fix should show bounded growth).
    (b) iOS Safari Web Inspector JS-heap timeline: real-device check — record the JS-heap timeline during a session, confirm no runaway growth / no tab reload (OOM).
    Document the COOP/COEP caveat verbatim in intent: `performance.measureUserAgentSpecificMemory()` is Chromium-only and requires cross-origin isolation (COOP/COEP headers); the PWA is not cross-origin isolated, so that API is unavailable on the iOS Safari target — only the Web Inspector timeline applies on-device. Include the literal token "measureUserAgentSpecificMemory" so the artifact check matches. Link back to Phase 34 (PERF-1) and note PERF-3's render-count test is the CI-side win (D-08).
  </action>
  <verify>
    <automated>test -f docs/runbooks/ios-memory-smoke.md &amp;&amp; grep -q "measureUserAgentSpecificMemory" docs/runbooks/ios-memory-smoke.md &amp;&amp; grep -qi "web inspector" docs/runbooks/ios-memory-smoke.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/runbooks/ios-memory-smoke.md`.
    - Contains both parts (Chrome heap snapshot + iOS Safari Web Inspector timeline).
    - Contains the COOP/COEP caveat and the literal "measureUserAgentSpecificMemory".
    - Verify command prints `OK`.
  </acceptance_criteria>
  <done>Manual memory-smoke runbook committed; D-09 captured (not planned as a CI test).</done>
</task>

<verification>
- RED→GREEN: `npx vitest --run src/tests/item-card-render-count.test.tsx` → green (one-item flip re-renders only that card).
- Regression: `npx vitest --run src/tests/item-card-audio-status.test.tsx` → green (renderCard updated with 5 meta props).
- `grep -c "useLiveQuery" src/components/ItemCard.tsx` → 0 (all per-card live queries removed).
- `grep -q "React.memo" src/components/ItemCard.tsx` → present.
- Runbook: `test -f docs/runbooks/ios-memory-smoke.md && grep -q measureUserAgentSpecificMemory docs/runbooks/ios-memory-smoke.md`.
- Wave merge gate: `npm test` green minus the 18 known pre-existing `localStorage.clear` failures (persist-scoping.test.ts / photo-migration.test.ts) — do NOT attribute those to this phase.
</verification>

<success_criteria>
- One aggregate `useLiveQuery` in ItemList replaces ~4N per-card subscriptions; slices passed as primitive props.
- ItemCard is `React.memo`-wrapped and prop-driven for the 5 meta values; retry + house-only photo count preserved.
- Render-count test green; audio-status test green after prop-signature update.
- D-09 memory smoke documented as a runbook (not CI).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dexie (IndexedDB) → React render layer | Per-item meta read crosses here; same reads as before, hoisted to one subscription. No new external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-01 | Tampering | aggregate meta correctness | accept | Same tested helpers (audioRecordsForItem/getDexieItemId/hasPendingForItem) composed once each; render-count + audio-status tests prove behavior preserved. No data mutated — read-only aggregate. |
| T-34-02-02 | Information disclosure | dev render counter | accept | `__itemCardRenderCounts` is guarded by `import.meta.env.MODE !== "production"` → no-op + no data in prod builds. Test-visibility only. |
| T-34-02-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (guardrail). No legitimacy gate applicable. |
</threat_model>

<output>
Create `.planning/milestones/v1.3-phases/34-ios-memory-optimization/34-02-SUMMARY.md` when done.
</output>
