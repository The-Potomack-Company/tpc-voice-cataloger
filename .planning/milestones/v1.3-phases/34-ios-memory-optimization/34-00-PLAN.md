---
phase: 34-ios-memory-optimization
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/gemini-pipeline.test.ts
  - src/tests/item-card-render-count.test.tsx
autonomous: true
requirements: [PERF-1, PERF-3]
must_haves:
  truths:
    - "A multi-chunk (> chunk-window) base64 RED test exists and fails against the current per-byte encoder ONLY if mis-aligned — passes today, guards 3-alignment after refactor"
    - "A render-count RED test exists and fails to compile/run against current ItemCard (no __itemCardRenderCounts export, no prop-driven meta) → genuinely RED"
  artifacts:
    - path: "src/tests/gemini-pipeline.test.ts"
      provides: "multi-chunk blobToBase64 equality assertion vs reference whole-buffer btoa"
      contains: "multi-chunk"
    - path: "src/tests/item-card-render-count.test.tsx"
      provides: "render-fan-out assertion: one-item ai_status flip re-renders only that card"
      contains: "__itemCardRenderCounts"
  key_links:
    - from: "src/tests/item-card-render-count.test.tsx"
      to: "src/components/ItemList.tsx (ItemCard)"
      via: "import __itemCardRenderCounts from ItemCard; render ItemList"
      pattern: "__itemCardRenderCounts"
---

<objective>
Wave 0 (RED-first, TDD mode ON). Author the two failing tests that pin the
behavioral contracts for PERF-1 and PERF-3 BEFORE any production code changes,
so Plans 01 and 02 implement to green.

Purpose: TDD gate — the multi-chunk encoder test is the ONLY thing that catches a
non-3-byte-aligned chunk window (Pitfall 1); the render-count test is the
CI-testable PERF-3 win (D-08). The existing 11-byte `blobToBase64` test passes
regardless of alignment, so without this Wave the alignment bug ships silently.
Output: two test files in failing/forward-referencing state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/34-ios-memory-optimization/34-RESEARCH.md
@.planning/milestones/v1.3-phases/34-ios-memory-optimization/34-PATTERNS.md
@.planning/milestones/v1.3-phases/34-ios-memory-optimization/34-VALIDATION.md

<interfaces>
<!-- Contracts the Wave-0 tests assert against. Plan 01 implements the encoder; Plan 02 implements the ItemCard/ItemList shape these symbols reference. -->

blobToBase64 (current, src/services/gemini.ts:135 — output stays byte-identical after Plan 01):
  export async function blobToBase64(blob: Blob): Promise<string>
  // contract: same base64 string as reference `btoa` over the whole buffer

Symbols Plan 02 will introduce in src/components/ItemCard.tsx (the render-count test forward-references these — RED until Plan 02):
  export const __itemCardRenderCounts: Map<string, number>   // dev-only render counter, keyed by item.id
  // ItemCard gains required meta props: audioCount, latestAudioId, photoCount, dexieItemId, isPending
  // ItemCard becomes React.memo-wrapped; ItemList owns the single aggregate useLiveQuery

Existing mock harness to copy — src/tests/item-card-audio-status.test.tsx:29-78:
  vi.mock("../services/gemini", ...); vi.mock("../db", ...);
  vi.mock("dexie-react-hooks", () => ({ useLiveQuery: (fn) => { try { return fn(); } catch { return undefined; } } }));
  vi.mock("../hooks/useWriteAheadQueue", () => ({ hasPendingForItem: vi.fn().mockResolvedValue(false) }));
  vi.mock("../db/idMapping", () => ({ getDexieItemId: vi.fn().mockResolvedValue(10) }));
  vi.mock("../db/audioLookup", () => ({ audioRecordsForItem: vi.fn().mockResolvedValue([{ id: 10 }]) }));
  // makeItem factory (lines 55-65) + MemoryRouter wrap (67-78) copy directly
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add multi-chunk base64 RED test (PERF-1, guards 3-byte alignment)</name>
  <files>src/tests/gemini-pipeline.test.ts</files>
  <read_first>
    - src/tests/gemini-pipeline.test.ts:109-116 (existing `describe("blobToBase64")` block — the 11-byte baseline; extend this same describe)
    - 34-PATTERNS.md:203-216 (multi-chunk test instruction)
    - 34-RESEARCH.md:270-274 (Pitfall 1 — why 11 bytes is insufficient; chunk MUST be a multiple of 3)
  </read_first>
  <behavior>
    - Test "multi-chunk blob encodes identically to reference whole-buffer btoa": build a blob LARGER than the chunk window (use 100000 bytes, deterministic e.g. `i % 256`), encode via `blobToBase64`, assert it `.toBe(...)` a reference base64 computed by `btoa(String.fromCharCode(...wholeBuffer))` (or a chunk-free reference). Must pass with a 3-aligned window and fail with a non-aligned one.
    - Existing 11-byte "hello world" → "aGVsbG8gd29ybGQ=" test stays untouched and green.
  </behavior>
  <action>
    Add a sibling `it("multi-chunk ...")` inside the existing `describe("blobToBase64")` block (do not create a new describe). Construct a `Uint8Array` of length 100000 filled deterministically (e.g. `bytes[i] = i % 256`), wrap in `new Blob([bytes])`. Compute the reference base64 by encoding the entire buffer in one pass (build the binary string over the whole buffer then `btoa`, mirroring the CURRENT encoder — that is the known-correct oracle). Assert `await blobToBase64(blob)` `.toBe(reference)`. Use the literal token "multi-chunk" in the test name so `-t "multi-chunk"` selects it. Do NOT change the encoder in this task — it still passes today (current per-byte encoder is correct); this test becomes the alignment guard once Plan 01 chunks it.
  </action>
  <verify>
    <automated>npx vitest --run src/tests/gemini-pipeline.test.ts -t blobToBase64</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest --run src/tests/gemini-pipeline.test.ts -t blobToBase64` passes (both the existing 11-byte test and the new multi-chunk test) against the CURRENT encoder.
    - Test name contains the literal "multi-chunk".
    - Reference oracle does NOT call `blobToBase64` (no tautology) — it computes base64 independently over the whole buffer.
  </acceptance_criteria>
  <done>multi-chunk test added to the existing blobToBase64 describe; passes against current encoder; will fail if Plan 01 picks a non-3-aligned chunk size.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create render-count RED test (PERF-3, D-08)</name>
  <files>src/tests/item-card-render-count.test.tsx</files>
  <read_first>
    - src/tests/item-card-audio-status.test.tsx:29-78 (mock block + makeItem + MemoryRouter wrap — copy verbatim, adjust as below)
    - src/components/ItemList.tsx:1-35, 264-310 (render site; the aggregate + ItemCard live here — render ItemList, not bare ItemCard)
    - src/components/ItemCard.tsx:49-63 (current per-card audioData query the test's `__itemCardRenderCounts` will eventually replace)
    - 34-PATTERNS.md:171-199 (test structure, RED-first)
    - 34-RESEARCH.md:223-239 (Pattern 3 — __itemCardRenderCounts contract)
  </read_first>
  <behavior>
    - Render `ItemList` with 3 items (mocked `useSessionItems`/Zustand source) so the aggregate provider runs.
    - `beforeEach`: clear `__itemCardRenderCounts` (imported from ItemCard).
    - After initial render: capture each item's render count.
    - `rerender` with ONE item's `ai_status` flipped (e.g. "queued" → "done").
    - Assert ONLY the flipped item's count incremented; the other two stayed flat.
  </behavior>
  <action>
    Copy the mock block, `makeItem` factory, and `MemoryRouter` wrap from `item-card-audio-status.test.tsx:29-78`. Mock the item source so `ItemList` renders 3 deterministic items (mock `../hooks/useSessions`'s `useSessionItems` to return the 3-item array; flip one item's `ai_status` between renders by returning a new array). Import `{ __itemCardRenderCounts }` from `../components/ItemCard`. Use the synchronous `useLiveQuery` mock so the aggregate computes synchronously. Render `<ItemList sessionId="s1" mode="house" />` inside `MemoryRouter`. This test is RED now: `__itemCardRenderCounts` does not yet exist (Plan 02 adds it) and ItemCard is not yet memoized — the import will fail and/or all 3 cards re-render. That is the intended RED state. Do NOT modify ItemCard/ItemList in this task.
  </action>
  <verify>
    <automated>npx vitest --run src/tests/item-card-render-count.test.tsx 2>&1 | grep -Eiq "fail|error|cannot find|is not exported" &amp;&amp; echo RED-OK || echo UNEXPECTED-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - File `src/tests/item-card-render-count.test.tsx` exists and references `__itemCardRenderCounts`.
    - Running it today produces a failure (RED) — either an unresolved import of `__itemCardRenderCounts` or an assertion failure showing all 3 cards re-rendered. Verify command prints `RED-OK`.
    - Test renders `ItemList` (the container that will own the aggregate), not bare `ItemCard`.
    - Reuses the mock harness from `item-card-audio-status.test.tsx` (no new mock strategy invented).
  </acceptance_criteria>
  <done>RED render-count test committed; fails today; turns green when Plan 02 ships the prop-driven memoized ItemCard + ItemList aggregate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds only test files. No runtime input, no new data flow, no network or
auth surface. No trust boundary is crossed by test code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-00-01 | Tampering | test files only (no prod code, no installs) | accept | Test-only change; no production behavior altered. No npm installs this phase (guardrail). |
</threat_model>

<verification>
- `npx vitest --run src/tests/gemini-pipeline.test.ts -t blobToBase64` → green (both tests, current encoder).
- `npx vitest --run src/tests/item-card-render-count.test.tsx` → RED (expected; closes green in Plan 02).
</verification>

<success_criteria>
- Multi-chunk encoder test exists, passes against current encoder, guards 3-alignment.
- Render-count test exists and is RED today (forward-references Plan 02 symbols).
- No production source touched.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/34-ios-memory-optimization/34-00-SUMMARY.md` when done.
</output>
