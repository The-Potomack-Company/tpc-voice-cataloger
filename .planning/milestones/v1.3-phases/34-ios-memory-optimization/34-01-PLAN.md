---
phase: 34-ios-memory-optimization
plan: 01
type: tdd
wave: 1
depends_on: [34-00]
files_modified:
  - src/services/gemini.ts
  - src/services/geminiContinuous.ts
autonomous: true
requirements: [PERF-1, PERF-2]
must_haves:
  truths:
    - "blobToBase64 encodes multi-MB audio without holding 3+ full copies (no whole-buffer binary string)"
    - "blobToBase64 output is byte-identical to the previous encoder (existing + multi-chunk tests stay green)"
    - "Both gemini.ts and geminiContinuous.ts route audio through the single shared chunked encoder"
    - "PERF-2 continuous master-blob rework is captured as a tracked deferred note, not silently dropped"
  artifacts:
    - path: "src/services/gemini.ts"
      provides: "chunked (3-byte-aligned window) blobToBase64; freshBlob re-wrap dropped"
      contains: "subarray"
    - path: "src/services/geminiContinuous.ts"
      provides: "deferred PERF-2 note; unchanged encoder import (verified, not edited)"
      contains: "PERF-2"
  key_links:
    - from: "src/services/geminiContinuous.ts"
      to: "src/services/gemini.ts blobToBase64"
      via: "import { blobToBase64 } from './gemini'"
      pattern: "blobToBase64"
    - from: "src/services/gemini.ts processAudioWithAi"
      to: "Gemini inlineData.data payload"
      via: "blobToBase64 output as base64 audio"
      pattern: "inlineData"
---

<objective>
PERF-1: replace the memory-expensive per-byte `blobToBase64` (src/services/gemini.ts:135)
with a fixed-window chunked encoder, dropping the defensive `freshBlob` re-wrap copy
(D-02). One exported function fixes both the single-mode and continuous paths
(geminiContinuous.ts:11 already imports it — D-03 "one encoder" is structurally
satisfied). Capture the PERF-2 master-blob rework as a tracked deferred note (D-04).

Purpose: the current encoder holds 3+ full copies of multi-MB audio simultaneously
(re-wrap copy + whole-buffer binary string + btoa copy) → iOS PWA OOM. Chunked
encode bounds peak to ~one window + the growing output.
Output: chunked `blobToBase64`; deferred PERF-2 note; both base64 tests green.

TDD mode ON — this is a `type: tdd` plan. The RED test (multi-chunk, Plan 00) and
the baseline equality test (gemini-pipeline.test.ts:114) already exist; implement
the encoder to GREEN, then REFACTOR if a chunk-size tune is warranted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.3-phases/34-ios-memory-optimization/34-RESEARCH.md
@.planning/milestones/v1.3-phases/34-ios-memory-optimization/34-PATTERNS.md

<interfaces>
<!-- Encoder contract — signature and export name are LOCKED (two consumers). -->

src/services/gemini.ts (keep this exact signature — geminiContinuous.ts:11 and gemini.ts:202 consume it):
  export async function blobToBase64(blob: Blob): Promise<string>

Current body (the target — src/services/gemini.ts:135-145):
  const freshBlob = new Blob([blob], { type: blob.type });  // D-02: drop this full-copy re-wrap
  const buffer = await freshBlob.arrayBuffer();
  const bytes = new Uint8Array(buffer);
  let binary = ""; for (...) binary += String.fromCharCode(bytes[i]);  // PERF-1: full-size string
  return btoa(binary);

src/services/geminiContinuous.ts (DO NOT edit the encoder call — verified live):
  line 11:  import { blobToBase64 } from "./gemini";
  line 140: await blobToBase64(geminiAudioBlob);
</interfaces>
</context>

<feature>
  <name>Chunked 3-byte-aligned base64 encoder</name>
  <files>src/services/gemini.ts, src/services/geminiContinuous.ts</files>
  <read_first>
    - src/services/gemini.ts:132-145 (current blobToBase64 body — replace in place)
    - src/services/geminiContinuous.ts:11, 140 (import + call site — verify only, do NOT edit the encoder usage)
    - 34-RESEARCH.md:158-180 (Pattern 1 + the 3-byte-alignment requirement; D-02 re-wrap drop)
    - 34-RESEARCH.md:270-274 (Pitfall 1 — chunk size MUST be a multiple of 3)
    - 34-PATTERNS.md:27-58 (gemini.ts + geminiContinuous.ts assignments)
    - src/tests/gemini-pipeline.test.ts:109-116 (baseline equality test that must stay green)
  </read_first>
  <behavior>
    - RED (already authored): `gemini-pipeline.test.ts` "multi-chunk" test (Plan 00) + the 11-byte baseline (line 109). The multi-chunk test fails iff the chosen window is not a multiple of 3.
    - GREEN target: both tests pass; `processAudioWithAi` integration test (gemini-pipeline.test.ts:268) and `geminiContinuous.test.ts` stay green (payload `inlineData.data` unchanged, byte-identical).
    - Cases:
      - "hello world" (11 bytes, < window) → "aGVsbG8gd29ybGQ=" (unchanged).
      - 100000-byte deterministic blob (> window) → equals reference whole-buffer btoa.
  </behavior>
  <implementation>
    Replace the `blobToBase64` body in src/services/gemini.ts:
    - Drop the `freshBlob = new Blob([blob], …)` re-wrap (D-02). Call `await blob.arrayBuffer()` directly on the passed blob — `arrayBuffer()` works on any Blob/File origin. Do NOT add a WHY-comment for the drop (D-02: WHY-comment only required if the re-wrap is RETAINED). If a structured-clone failure surfaces during smoke, restore the re-wrap with a WHY-comment naming the exact failing case.
    - Declare `const CHUNK_SIZE = 32766;` (`0x8000 - 2`, divisible by 3 — the single correctness trap, Pitfall 1). A short WHY-comment on this line is warranted (non-obvious constraint: must be a multiple of 3 so per-chunk btoa concatenation equals whole-buffer btoa).
    - Iterate `bytes` in `CHUNK_SIZE` windows via `bytes.subarray(i, i + CHUNK_SIZE)` (subarray is a view — no copy), accumulate `result += btoa(String.fromCharCode(...chunk))`, return `result`.
    - Keep the export name and signature `(blob: Blob): Promise<string>` exactly.
    geminiContinuous.ts: do NOT touch the `blobToBase64` import/call (already shared — D-03 satisfied). Add a concise deferred-note comment near the master-blob assembly (the `geminiAudioBlob` materialization path) capturing D-04: PERF-2 continuous master-blob stream-append/segment-and-discard rework is deferred until continuous mode is re-enabled (D-050); include the token "PERF-2" and the D-04/D-050 references so it is greppable. This is the tracked deferred note — do not change runtime behavior.
  </implementation>
</feature>

<verification>
- RED→GREEN: `npx vitest --run src/tests/gemini-pipeline.test.ts -t blobToBase64` passes (baseline + multi-chunk).
- Pipeline intact: `npx vitest --run src/tests/gemini-pipeline.test.ts` (incl. processAudioWithAi inlineData test) green.
- Continuous regression: `npx vitest --run src/tests/geminiContinuous.test.ts` green.
- `grep -n "freshBlob" src/services/gemini.ts` → no match (re-wrap removed).
- `grep -n "PERF-2" src/services/geminiContinuous.ts` → deferred note present.
</verification>

<success_criteria>
- `blobToBase64` uses a 3-byte-aligned (32766) chunked window via `subarray`; no whole-buffer binary string; no `freshBlob` re-wrap.
- Output byte-identical (both encoder tests + pipeline + continuous suites green).
- Single shared encoder serves both paths (geminiContinuous.ts encoder usage unchanged).
- PERF-2 deferred note present and greppable in geminiContinuous.ts (D-04 not silently dropped).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA tab → AI proxy | Audio base64 payload crosses here (unchanged — same JSON shape, same bytes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | Tampering | blobToBase64 output integrity | accept | Output is byte-identical to the prior encoder; existing equality test (gemini-pipeline.test.ts:114) + new multi-chunk test prove no corruption. No change to what crosses the proxy boundary. |
| T-34-01-02 | Information disclosure | audio payload | accept | No new data flow; same audio, same `inlineData.data`, same `VITE_GEMINI_PROXY_URL`. `sanitizeForDataBlock`/SEC-5 prompt-injection guards untouched. |
| T-34-01-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (guardrail: no new npm deps). No legitimacy gate applicable. |
</threat_model>

<output>
Create `.planning/milestones/v1.3-phases/34-ios-memory-optimization/34-01-SUMMARY.md` when done.
</output>
