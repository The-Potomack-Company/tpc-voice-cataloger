---
phase: 31-sec-profiles-self-update-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/db/database.types.ts
# autonomous false: Codex review gate (D-046) + blocking prod db push (D-07) + authorized prod reads (D-08)
autonomous: false
requirements: []   # audit-sourced security phase; coverage targets are V-1..V-7 + D-01..D-09
nyquist_compliant: true

must_haves:
  truths:
    - "The hardening migration is applied to prod with NO unsafe sibling riding along"
    - "On prod, authenticated holds UPDATE only on walkthrough_completed and theme; anon holds no UPDATE on profiles"
    - "A specialist can still self-write walkthrough_completed and theme post-apply"
    - "admin-update-user still toggles is_active (activate AND deactivate) post-apply"
    - "The prod admin set is the known set — no prior self-promotion"
    - "npm run db:types produces zero diff"
  artifacts:
    - path: "src/db/database.types.ts"
      provides: "Regenerated types confirming zero schema-shape drift (V-7)"
  key_links:
    - from: "supabase db push"
      to: "prod wgrknodfxdjtddsirldw"
      via: "--dry-run isolation check then --yes apply"
      pattern: "db push"
---

<objective>
Gate the authored migration through the mandatory Codex adversarial review (D-046), apply it to prod isolated from the unsafe receipt-NULL sibling (D-07), run the authorized prod audit + grant verification (D-08, V-1, V-6), confirm zero types drift (V-7), and Tier-2 smoke the three legitimate write paths (V-3 walkthrough, V-4 theme, V-5 admin activate/deactivate).

Purpose: This is the in-phase prod cutover for the P0 vector. Claude owns the schema/auth change; Codex reviews but does NOT implement (D-046). The push is the only thing that reconciles the untracked drifted grants living in prod's catalog — build/type checks pass WITHOUT it (types come from config, not the live DB), so the push is mandatory to avoid false-positive verification.

Output: prod has the column-scoped grants + guard trigger; `src/db/database.types.ts` regenerated (expected zero diff); audit + smoke evidence recorded in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.3-phases/31-sec-profiles-self-update-hardening/31-CONTEXT.md
@.planning/milestones/v1.3-phases/31-sec-profiles-self-update-hardening/31-RESEARCH.md
@.planning/milestones/v1.3-phases/31-sec-profiles-self-update-hardening/31-VALIDATION.md
@.planning/milestones/v1.3-phases/31-sec-profiles-self-update-hardening/31-01-SUMMARY.md

<interfaces>
<!-- Concrete commands/contracts the executor needs. -->

CLI: node_modules/.bin/supabase (v2.81.3, vendored). Prod ref: wgrknodfxdjtddsirldw.
  Dry-run isolation:  node_modules/.bin/supabase db push --dry-run
  Non-interactive apply:  SUPABASE_DB_PASSWORD=*** node_modules/.bin/supabase db push --yes
  If CLI not linked/authed → user-only action: `supabase login` + `supabase link --project-ref wgrknodfxdjtddsirldw`.

Types regen: npm run db:types  (then `git diff --exit-code src/db/database.types.ts` — expect ZERO diff, V-7).

V-1 grant query: supabase/queries/verify-profiles-grants.sql (authored in Plan 01).
V-6 admin-list audit (D-08, authorized prod read):
  select id, display_name, role, created_at from public.profiles where role='admin';

Migration-history note: the unsafe sibling 20260527000003_add_items_receipt_unique.sql is ALREADY in prod history
  (siblings 20260528000003 etc. were applied after it). The --dry-run MUST still confirm the ONLY pending migration
  is 20260529000000_lock_profiles_self_update.sql before applying (D-07). If anything else is listed, STOP.
</interfaces>
</context>

<read_first>
- supabase/migrations/20260529000000_lock_profiles_self_update.sql  (the migration under review — read in full before submitting to Codex)
- supabase/queries/verify-profiles-grants.sql  (the V-1 assertion to run post-apply)
- supabase/functions/admin-update-user/index.ts  (the service_role path Tier-2 must exercise — activate :46-47, deactivate :69-70)
- src/components/walkthrough/useWalkthroughStatus.ts  (walkthrough_completed self-write — V-3)
- src/stores/themeStore.ts  (theme self-write — V-4)
- 31-RESEARCH.md §Non-interactive apply + §Common Pitfalls (push isolation, service-role confusion)
</read_first>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Mandatory Codex adversarial review of the migration (D-046)</name>
  <files>supabase/migrations/20260529000000_lock_profiles_self_update.sql</files>
  <what-built>The authored migration `supabase/migrations/20260529000000_lock_profiles_self_update.sql` (REVOKE + narrow GRANT + private guard trigger). Claude authored it; per D-046 Codex must adversarially review auth/schema changes before apply, and Codex is barred from implementing.</what-built>
  <action>
Submit the migration to Codex for adversarial review (D-046 — auth/schema change is Claude-owned, Codex reviews but does NOT implement). Focus the review on: table-form REVOKE correctly clears all column grants; GRANT allowlist is exactly (walkthrough_completed, theme) with no privileged column; guard trigger predicate (`current_user='service_role'` + `private.is_admin()`) cannot be bypassed by a specialist and does NOT break the service_role Edge path; errcode 42501; idempotency (re-runnable). Reference 31-RESEARCH.md §Security Domain STRIDE table. If any finding is `high` severity, Claude fixes the migration (Codex does not) and re-reviews until clean. Record the Codex verdict (pass / findings + resolutions) in the SUMMARY.
  </action>
  <how-to-verify>
1. Submit the migration to Codex with the review focus above.
2. Codex returns findings. If any finding is `high` severity, fix the migration (Claude edits — Codex does not) and re-review until clean.
3. Record the Codex verdict (pass / findings + resolutions) in the SUMMARY.
  </how-to-verify>
  <verify>
    <automated>MISSING — Codex adversarial review is a human-gated D-046 control; verdict recorded in SUMMARY. Automated migration grep gate already passed in Plan 01.</automated>
  </verify>
  <acceptance_criteria>
    - Codex adversarial review run on the migration (auth/schema → D-046 mandatory)
    - Zero unresolved `high`-severity findings; any findings fixed by Claude and re-reviewed
    - Codex verdict recorded in SUMMARY
  </acceptance_criteria>
  <done>Codex review passed with no unresolved high-severity findings; verdict recorded in SUMMARY.</done>
  <resume-signal>Type "approved" once Codex review passes with no unresolved high-severity findings, or describe findings to fix.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] db push isolation check + apply to prod (D-07)</name>
  <files>supabase/migrations/20260529000000_lock_profiles_self_update.sql</files>
  <what-built>The reviewed migration, ready to apply to prod via the vendored Supabase CLI. This is the in-phase prod cutover (D-07). Marked blocking because it mutates prod and may require user-only CLI auth.</what-built>
  <action>
Apply the reviewed migration to prod via the vendored Supabase CLI (D-07 in-phase prod push). PRECONDITION: verify the CLI is linked + authed — run `node_modules/.bin/supabase db push --dry-run`; if it errors not-linked/not-authed, STOP and hand the user the one-liner `node_modules/.bin/supabase login` then `node_modules/.bin/supabase link --project-ref wgrknodfxdjtddsirldw` (user-only auth). ISOLATION GATE (Pitfall 3, D-07): the `--dry-run` output's ONLY pending migration MUST be `20260529000000_lock_profiles_self_update.sql`; if ANY other migration (especially `20260527000003_add_items_receipt_unique.sql`) is listed pending, STOP — do not push (could destroy ~7 prod rows) and escalate. APPLY: `SUPABASE_DB_PASSWORD=<from env/user> node_modules/.bin/supabase db push --yes`. Confirm the CLI reports the migration applied + recorded in migration history.
  </action>
  <how-to-verify>
1. PRECONDITION — `node_modules/.bin/supabase db push --dry-run`; if not-linked/not-authed, STOP and hand the user the login+link one-liner.
2. ISOLATION GATE — the only pending migration listed MUST be `20260529000000_lock_profiles_self_update.sql`. If anything else is pending, STOP and escalate.
3. APPLY — `SUPABASE_DB_PASSWORD=<...> node_modules/.bin/supabase db push --yes`.
4. Confirm the migration is applied + recorded in migration history.
  </how-to-verify>
  <verify>
    <automated>MISSING — prod apply is verified via the dry-run isolation output + CLI apply confirmation (manual, authorized prod write per D-07). V-1/V-7 automated checks run in Tasks 3 and 4.</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --dry-run` shows ONLY 20260529000000_lock_profiles_self_update.sql pending (isolation confirmed)
    - Migration applied to prod (wgrknodfxdjtddsirldw) and recorded in migration history
    - No unsafe sibling (20260527000003) rode along
  </acceptance_criteria>
  <done>Migration applied to prod, isolation confirmed (only this migration pending), recorded in migration history.</done>
  <resume-signal>Type "applied" once the dry-run shows only this migration and the push succeeds, or report the isolation/auth blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify prod grants (V-1) + admin-list audit (V-6, D-08)</name>
  <files>(authorized prod reads — no files modified)</files>
  <action>
Run the two authorized prod reads against the live DB (D-08 authorizes the audit read).

(V-1) Run `supabase/queries/verify-profiles-grants.sql` against prod (via the Supabase MCP / CLI / authorized read). Assert the result is EXACTLY two rows: (authenticated, UPDATE, walkthrough_completed) and (authenticated, UPDATE, theme). There must be NO row for anon, and NO row for role or is_active for either grantee. If role/is_active still appear → the REVOKE used the wrong form; STOP and escalate.

(V-6) Run the admin-list audit: `select id, display_name, role, created_at from public.profiles where role='admin';`. Confirm the returned admin set matches ONLY the known admin(s) — no unexpected/self-promoted admin. If an unknown admin exists, escalate immediately (prior exploitation evidence).

Record both query outputs in the SUMMARY.
  </action>
  <verify>
    <automated>MISSING — both are authorized prod reads (information_schema grant assertion V-1; admin-list audit V-6/D-08). Verified by inspecting query output against the expected set; results recorded in SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - V-1: `information_schema.column_privileges` for profiles shows authenticated holds UPDATE on ONLY walkthrough_completed + theme; anon holds none; role/is_active hold none
    - V-6: admin-list audit returns only the known admin set (no rogue/self-promoted admin)
    - Both query outputs recorded in SUMMARY
  </acceptance_criteria>
  <done>Prod grants confirmed column-scoped (V-1) and admin set confirmed clean (V-6); evidence in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 4: Regen types (V-7) + Tier-2 smoke the three write paths (V-3/V-4/V-5)</name>
  <files>src/db/database.types.ts</files>
  <action>
(V-7) Run `npm run db:types` then `git diff --exit-code src/db/database.types.ts`. Expect ZERO diff (grants/triggers do not change row shape). If it diffs, that signals an unexpected schema change — investigate before committing. Commit the file only if it legitimately changed (it should not).

(V-3) Tier-2 smoke: as a specialist session, exercise the walkthrough self-write path (`src/components/walkthrough/useWalkthroughStatus.ts` writing `walkthrough_completed=true`) — confirm it succeeds post-apply (the GRANT keeps it working).

(V-4) Tier-2 smoke: as a specialist session, exercise the theme self-write (`src/stores/themeStore.ts` writing `theme`) — confirm it succeeds.

(V-5) Tier-2 smoke: invoke the `admin-update-user` Edge function with an admin token to toggle a test user is_active BOTH ways — deactivate (sets is_active=false, :69-70) AND reactivate (sets is_active=true, :46-47). Both MUST succeed — this proves the `current_user='service_role'` trigger exemption works (the critical compatibility seam). If either returns a 403/42501, the exemption predicate is wrong; STOP and escalate.

Record smoke evidence (pass/fail per path) in the SUMMARY.
  </action>
  <verify>
    <automated>npm run db:types && git diff --exit-code src/db/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - V-7: `npm run db:types` then `git diff --exit-code src/db/database.types.ts` exits 0 (zero diff)
    - V-3: specialist walkthrough_completed self-write succeeds post-apply
    - V-4: specialist theme self-write succeeds post-apply
    - V-5: admin-update-user toggles is_active BOTH ways (activate + deactivate) without 403/42501
    - Smoke evidence recorded in SUMMARY
  </acceptance_criteria>
  <done>db:types zero diff confirmed; all three legitimate write paths smoke green (walkthrough, theme, admin activate/deactivate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon-key client → PostgREST → prod public.profiles | Public anon key; post-apply, column grants + RLS + trigger are the enforced boundary. |
| service_role Edge (admin-update-user) → PostgREST → prod public.profiles | Trusted; must remain exempt (verified live by V-5). |
| local CLI → prod migration history | `db push` is greedy — applies ALL pending; isolation gate prevents an unsafe sibling cutover. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Elevation of Privilege | live self-promotion vector on prod profiles | mitigate | Applied REVOKE + narrow GRANT; verified live by V-1 grant query (Task 3). |
| T-31-03 | Elevation of Privilege | trigger bypass / service-role confusion on prod | mitigate | V-5 Tier-2 smoke proves service_role exemption works for the admin Edge path both ways. |
| T-31-06 | Tampering / data loss | unsafe receipt-NULL sibling (20260527000003) rides along on push | mitigate | `db push --dry-run` isolation gate (Task 2) — STOP if any migration other than 20260529000000 is pending. |
| T-31-07 | Repudiation | undetected prior exploitation (a rogue admin already exists) | mitigate | V-6 admin-list audit (D-08, Task 3) confirms the admin set is clean before declaring the vector closed. |
| T-31-SC | Tampering | CLI/package supply chain | accept | No installs; vendored CLI v2.81.3 is version-pinned and unchanged this phase (RESEARCH §Standard Stack). |

ASVS L1: high-severity EoP threats carry live-verified `mitigate` dispositions (V-1, V-5, V-6). The only `accept` is the no-install supply-chain item.
</threat_model>

<verification>
- V-1 grant query on prod: authenticated → only walkthrough_completed + theme; anon → none.
- V-6 admin-list audit: only the known admin set.
- V-7: `npm run db:types && git diff --exit-code src/db/database.types.ts` → exit 0.
- V-3/V-4/V-5 Tier-2 smoke: walkthrough write, theme write, admin activate+deactivate all green.
- `--dry-run` isolation: only 20260529000000 pending before apply.
</verification>

<success_criteria>
- Codex review passed (no unresolved high findings).
- Migration applied to prod, isolated from the unsafe sibling.
- V-1 + V-6 prod reads clean; V-7 zero diff; V-3/V-4/V-5 smoke green.
- P0 self-update privilege-escalation vector closed on prod.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/31-sec-profiles-self-update-hardening/31-02-SUMMARY.md` when done.
</output>
