---
phase: 31-sec-profiles-self-update-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260529000000_lock_profiles_self_update.sql
  - supabase/queries/verify-profiles-grants.sql
autonomous: true
requirements: []   # audit-sourced security phase; coverage targets are V-1..V-7 + D-01..D-09
nyquist_compliant: true

must_haves:
  truths:
    - "A non-admin authenticated session can never change role or is_active on public.profiles"
    - "authenticated holds UPDATE on only walkthrough_completed and theme; anon holds no UPDATE"
    - "The migration is re-runnable (idempotent) against partial-prior-state prod"
    - "The legitimate admin Edge path (service_role) is exempt from the guard trigger"
  artifacts:
    - path: "supabase/migrations/20260529000000_lock_profiles_self_update.sql"
      provides: "Table-form REVOKE + narrow column GRANT + private guard trigger fn + idempotent trigger"
      contains: "revoke update on public.profiles from authenticated, anon"
      min_lines: 25
    - path: "supabase/queries/verify-profiles-grants.sql"
      provides: "V-1 information_schema.column_privileges assertion query (authorized prod read)"
      contains: "information_schema.column_privileges"
  key_links:
    - from: "private.guard_profiles_privileged_columns()"
      to: "private.is_admin()"
      via: "admin exemption branch"
      pattern: "private\\.is_admin\\(\\)"
    - from: "trg_guard_profiles_privileged_columns"
      to: "public.profiles"
      via: "before update trigger"
      pattern: "before update on public\\.profiles"
---

<objective>
Author the single idempotent SQL migration that closes the live P0 self-update privilege-escalation vector on `public.profiles`, plus the V-1 grant-assertion query used to verify it. No prod side-effects in this plan — this is pure Claude-owned authoring (D-046). The migration is applied in Plan 02 after the mandatory Codex review.

Purpose: A non-admin specialist can currently `PATCH /rest/v1/profiles?id=eq.<uid> {role:'admin', is_active:true}` and self-promote, because untracked Supabase-default column grants give `authenticated` AND `anon` UPDATE on every column, the self-update RLS policy is row-scoped but not column-scoped, and no trigger guards `role`/`is_active`.

Output:
- `supabase/migrations/20260529000000_lock_profiles_self_update.sql`
- `supabase/queries/verify-profiles-grants.sql`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/31-sec-profiles-self-update-hardening/31-CONTEXT.md
@.planning/milestones/v1.3-phases/31-sec-profiles-self-update-hardening/31-RESEARCH.md
@.planning/milestones/v1.3-phases/31-sec-profiles-self-update-hardening/31-PATTERNS.md
@.planning/milestones/v1.3-phases/31-sec-profiles-self-update-hardening/31-VALIDATION.md

<interfaces>
<!-- Concrete contracts the executor needs. Do NOT re-explore the codebase. -->

profiles columns (current shape — GRANT allowlist sanity check):
  id, role, display_name, is_active, created_at, email, walkthrough_completed, theme
  GRANT only (walkthrough_completed, theme) to authenticated. NOT display_name (D-02), NOT email/id/role/is_active.

Existing admin helper to REUSE (do NOT re-query profiles inline) — supabase/migrations/20260318000004_helper_functions.sql:5-18:
  private.is_admin() returns boolean, language plpgsql, security definer set search_path = '';
  body: exists(select 1 from public.profiles where id=(select auth.uid()) and role='admin' and is_active=true)
  The `private` schema is created at 20260318000004:2 (`create schema if not exists private;`).

Legitimate admin write path the trigger MUST NOT break — supabase/functions/admin-update-user/index.ts:30,46-47,69-70:
  createAdminClient() (service_role JWT) → PostgREST SET LOCAL ROLE service_role → current_user = 'service_role'
  .from('profiles').update({ is_active: true })   (activate, :46-47)
  .from('profiles').update({ is_active: false })  (deactivate, :69-70)

RLS policy to KEEP UNTOUCHED (D-04) — supabase/migrations/20260320100000_add_walkthrough_completed.sql:7-12:
  "Users can update own walkthrough status" FOR UPDATE TO authenticated USING ((select auth.uid())=id) WITH CHECK ((select auth.uid())=id)
  Correct once grants are column-scoped. Do NOT drop/rewrite. Do NOT copy its uppercase DDL casing.
</interfaces>
</context>

<read_first>
- supabase/migrations/20260527000001_scope_photos_storage_rls.sql  (SEC-4 — header style + drop-if-exists idiom; primary analog)
- supabase/migrations/20260527000000_harden_handle_new_user_role.sql  (SEC-1 — guard-a-column-in-a-trigger; tight SEC-id header)
- supabase/migrations/20260318000004_helper_functions.sql  (private.is_admin envelope: language/security/search_path + create-trigger form)
- supabase/migrations/20260320100000_add_walkthrough_completed.sql  (the offending RLS policy — kept untouched, D-04)
- 31-RESEARCH.md §Code Examples + §Pattern 1/2/3 (full composed migration, current_user predicate rationale, idempotency)
- 31-PATTERNS.md §Pattern Assignments (A)-(E)  (exact file:line excerpts to mirror, lowercase-DDL house style)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Write V-1 grant-assertion query</name>
  <files>supabase/queries/verify-profiles-grants.sql</files>
  <action>
Create the `supabase/queries/` directory (does not exist yet) and write a read-only assertion query for V-1. Query `information_schema.column_privileges` filtered to `table_schema='public'`, `table_name='profiles'`, `grantee in ('authenticated','anon')`, `privilege_type='UPDATE'`, ordered by grantee, column_name. Add a trailing comment stating the EXPECTED result after the migration applies: exactly two rows — (authenticated, UPDATE, walkthrough_completed) and (authenticated, UPDATE, theme) — and NO rows for anon, role, or is_active. Lowercase DDL house style. Mirror the exact query in 31-RESEARCH.md §Code Examples "Verification query — prove grants are gone". This snippet is the authorized-prod-read assertion used in Plan 02.
  </action>
  <verify>
    <automated>test -f supabase/queries/verify-profiles-grants.sql && grep -qi "information_schema.column_privileges" supabase/queries/verify-profiles-grants.sql && grep -qi "walkthrough_completed" supabase/queries/verify-profiles-grants.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/queries/verify-profiles-grants.sql` exists
    - Contains a `select` from `information_schema.column_privileges` filtered to public.profiles, grantee in (authenticated, anon), privilege_type = UPDATE
    - Trailing comment documents the expected post-migration result (only walkthrough_completed + theme for authenticated; nothing for anon)
  </acceptance_criteria>
  <done>verify-profiles-grants.sql exists and queries column_privileges for profiles UPDATE grants with the expected-result comment.</done>
</task>

<task type="auto">
  <name>Task 2: Author the lock-profiles-self-update migration</name>
  <files>supabase/migrations/20260529000000_lock_profiles_self_update.sql</files>
  <action>
Write the four-part composite migration (lowercase DDL, mirror 31-PATTERNS.md (A)-(D)). Timestamp `20260529000000` sorts after the latest applied sibling `20260528000003`.

(A) Header comment: lead with the audit id, name the offending file `supabase/migrations/20260320100000_add_walkthrough_completed.sql:7-12` (row-scoped not column-scoped RLS) plus the untracked drifted column grants (Supabase defaults — both authenticated AND anon hold UPDATE on every column including role/is_active), state the three-part fix, cite `../_workspace/Urgent/sec-profiles-self-update-escalation.md`, D-046, D-001, D-003. Match the SEC-4/SEC-1 header style.

(B) REVOKE + narrow GRANT (D-01/D-02/D-03): emit `revoke update on public.profiles from authenticated, anon;` (TABLE form — no column list; this auto-clears ALL column grants per postgresql.org sql-revoke). Then `grant update (walkthrough_completed, theme) on public.profiles to authenticated;`. anon gets nothing back. Add a comment that display_name is intentionally NOT granted (server-set, D-02). TRAP: do NOT use `revoke update (role, is_active) ...` — per-column REVOKE is a documented no-op when a table grant exists.

(C) Guard function `private.guard_profiles_privileged_columns()` returns trigger, language plpgsql, **security invoker**, `set search_path = ''` (RESEARCH §Pattern 2: INVOKER makes current_user unambiguously the PostgREST-switched caller; the fn needs no privilege of its own). Body: `if (new.role is distinct from old.role) or (new.is_active is distinct from old.is_active) then` → `if current_user = 'service_role' then return new; end if;` (exempts the admin-update-user Edge path, D-06) → `if (select private.is_admin()) then return new; end if;` (exempts authenticated admins — reuse the existing helper, never re-query profiles inline) → else `raise exception 'Only an administrator may change role or is_active (attempted by %).', current_user using errcode = '42501';` (insufficient_privilege → clean PostgREST 403-class). `return new;` at the end. Use `create or replace function` for idempotency. Optionally re-assert `create schema if not exists private;` at the top (harmless, matches repo idiom).

(D) Idempotent trigger: `drop trigger if exists trg_guard_profiles_privileged_columns on public.profiles;` then `create trigger trg_guard_profiles_privileged_columns before update on public.profiles for each row execute function private.guard_profiles_privileged_columns();`.

Do NOT touch the existing "Users can update own walkthrough status" RLS policy (D-04). Do NOT add a WITH CHECK column restriction (deferred/optional — trigger + grants cover it). Do NOT bundle any other migration.
  </action>
  <verify>
    <automated>f=supabase/migrations/20260529000000_lock_profiles_self_update.sql; test -f "$f" && grep -qi "revoke update on public.profiles from authenticated, anon" "$f" && grep -qi "grant update (walkthrough_completed, theme) on public.profiles to authenticated" "$f" && grep -qi "security invoker" "$f" && grep -qi "set search_path = ''" "$f" && grep -qi "is distinct from old.role" "$f" && grep -qi "is distinct from old.is_active" "$f" && grep -qi "current_user = 'service_role'" "$f" && grep -qi "private.is_admin()" "$f" && grep -qi "42501" "$f" && grep -qi "drop trigger if exists trg_guard_profiles_privileged_columns" "$f" && grep -qi "before update on public.profiles" "$f" && ! grep -qi "display_name" "$f"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file `supabase/migrations/20260529000000_lock_profiles_self_update.sql` exists, timestamp sorts after 20260528000003
    - Contains `revoke update on public.profiles from authenticated, anon` (table form, no column list)
    - Contains `grant update (walkthrough_completed, theme) on public.profiles to authenticated` and NO grant of display_name/email/role/is_active
    - Guard fn is `security invoker` with `set search_path = ''`; raises with `errcode = '42501'` only when role OR is_active `is distinct from old`
    - Exemptions present: `current_user = 'service_role'` AND `(select private.is_admin())`; no inline profiles re-query
    - Idempotent: `create or replace function` + `drop trigger if exists trg_guard_profiles_privileged_columns` + `create trigger ... before update on public.profiles`
    - The "Users can update own walkthrough status" RLS policy is NOT modified (D-04)
  </acceptance_criteria>
  <done>Migration file present and passes the automated grep gate: table-form REVOKE, narrow GRANT (no display_name), invoker+search_path guard fn raising 42501 on role/is_active change with service_role + is_admin exemptions, idempotent trigger pair, offending RLS policy untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon-key client → PostgREST → public.profiles | Public anon key (D-003); RLS + column grants are the ONLY enforced boundary. Untrusted input crosses here. |
| service_role Edge fn (admin-update-user) → PostgREST → public.profiles | Trusted server path; SET LOCAL ROLE service_role; must remain exempt from the guard trigger. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Elevation of Privilege | PATCH /rest/v1/profiles {role:'admin', is_active:true} by authenticated specialist | mitigate | Table-form REVOKE UPDATE from authenticated,anon → narrow GRANT(walkthrough_completed,theme); column grant is the primary control. ASVS V4 least privilege. (V-1, V-2) |
| T-31-02 | Elevation of Privilege | Over-broad untracked default column grants (drift) | mitigate | Explicit table-form REVOKE wipes all column grants regardless of how created; verified via information_schema (V-1). |
| T-31-03 | Elevation of Privilege | Trigger bypass via service-role confusion | mitigate | Guard keyed on `current_user` (enforced PostgREST-switched role, not a parseable JWT claim); session_user/auth.uid() rejected as fragile (RESEARCH §Pattern 2). |
| T-31-04 | Elevation of Privilege | RLS row-scope without column-scope | mitigate | Column GRANT gates columns; existing RLS `auth.uid()=id` gates rows — both required and orthogonal (kept per D-04). |
| T-31-05 | Tampering | anon write to profiles | mitigate | anon granted ZERO UPDATE after REVOKE; RLS USING auth.uid()=id also blocks. |
| T-31-06 | Tampering / data loss | Defense-in-depth: future broad re-grant re-opens role/is_active | mitigate | BEFORE UPDATE guard trigger raises 42501 on any role/is_active change by a non-admin non-service caller, independent of grant state. |
| T-31-SC | Tampering | npm/supabase-cli installs | n/a | No package installs this phase (RESEARCH §Package Legitimacy Audit — N/A). |

ASVS L1: all high-severity EoP threats (T-31-01..04) carry `mitigate` dispositions with specific DB-layer controls. No `high` left as `accept`.
</threat_model>

<verification>
- `npm run test` passes (no app-layer regression — this plan touches only SQL files).
- Automated grep gates on both files (see task verify blocks).
- V-1 query authored (asserted live in Plan 02).
- No prod side-effects in this plan; the migration is NOT pushed here.
</verification>

<success_criteria>
- Migration file authored with all four parts, passing the Task 2 grep gate.
- verify-profiles-grants.sql authored.
- `npm run test` green.
- Ready for Codex adversarial review + prod push in Plan 02.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/31-sec-profiles-self-update-hardening/31-01-SUMMARY.md` when done.
</output>
