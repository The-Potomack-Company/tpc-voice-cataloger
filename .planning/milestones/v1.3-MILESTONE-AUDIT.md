---
milestone: v1.3
audited: 2026-06-04   # supersedes the 2026-06-02 audit (phases 31-40); now covers 31-45
status: tech_debt
scores:
  requirements: N/A    # v1.3 phases are bug/UAT/quality-driven — no REQ-IDs mapped (the 31 unchecked boxes in REQUIREMENTS.md are v1.2-era design-system reqs, out of scope)
  phases: 14/15 verified   # 41 shipped via urgent lane with no VERIFICATION artifact; 45 added to close SEAM-3
  integration: 3/3 seams fully wired   # SEAM-3 closed by Phase 45
  flows: passed            # 38, 42, 44 live UAT all passed 2026-06-04
closed_since_prior_audit:   # called out as future work in the 2026-06-02 audit, now resolved
  - "photoMigration house/sale ++id collision (was 38 tech-debt 'future phase') -> CLOSED by Phase 43."
  - "tpc-ai-proxy deploy.yml OPEN LANDMINE (was 40 tech-debt) -> CLOSED by Phase 40.1 (CI preflight secret gate)."
  - "live duplicate-receipt 23505 import copy (was 36 milestone-end UAT) -> addressed by Phase 44 (names the colliding receipt); live UAT still pending."
gaps:
  requirements: []
  integration:
    - id: "SEAM-3"
      severity: warning
      status: CLOSED
      closed_by: "Phase 45 (ai-write-precondition)"
      phases: ["39", "35", "45"]
      finding: "Live single-item AI write-back (gemini.ts processAudioWithAi) did a bare .update().eq(id) — unconditional last-writer-wins — bypassing Phase 39's preconditionUpdate."
      resolution: "Phase 45 routed the success write through preconditionUpdate carrying the snapshot updated_at token + per-field compare-and-skip reconcile mirroring geminiContinuous.ts:286-303. The failure-path ai_status write is intentionally left unconditional (control-only, cannot clobber catalog; WHY-commented). Verified 3/3, full suite 721 pass."
  flows: []
process_gaps:
  - id: "PHASE-41-UNVERIFIED"
    phase: "41"
    severity: process-only
    finding: "ai-pending-stranding shipped via the urgent lane with no GSD VERIFICATION/SUMMARY. Wiring present and correct (AppLayout drain chain; app-layout-drain.test.ts / audio-upload-status.test.ts cover it), but no phase-level verification record to cross-check."
deferred_uat:   # all passed in milestone-end UAT 2026-06-04
  - { phase: "38", status: passed, note: "migration-retryability — live partial-migration retry, no dup." }
  - { phase: "42", status: passed, note: "audio-upload-reliability — live cross-device audio recovery." }
  - { phase: "44", status: passed, note: "visibility-ux-polish — blocked-badge nav + live 23505 import toast." }
tech_debt:
  # New from the 43/44 follow-up phases:
  - phase: 43-photomigration-itemid-collision
    items:
      - "WR-01: legacy itemTable=undefined mapping rows miss the scoped photo lookup. Confirmed self-healing (migrateToSupabase re-migration backfill + skip-not-strand retry). Tracked in 43 deferred-items.md. No v1.3 action."
      - "INFO-01: ItemAudio sibling closed as safe-by-construction — audio writes itemId as a Supabase UUID at record time, never carries a legacy int through the collision lookup."
  - phase: 44-visibility-ux-polish
    items:
      - "WR-02: BlockedQueueBadge disappears silently when fetchBlockedItems errors (returns []). Out of phase-44 scope."
      - "IN-01: mode label `=== 'sale' ? 'Sale' : 'House'` mislabels non-sale values as House (untyped boundary)."
      - "IN-02: best-effort import rollback invisible on partial cleanup failure (no transactional RPC)."
  # Still-open from the 2026-06-02 audit (preserved by reference — see git history of this file at commit before 2026-06-04):
  - phase: 31-sec-profiles-self-update-hardening
    items:
      - "Prod profiles.theme column missing despite migration history; theme persistence off on prod (degrades gracefully)."
      - "database.types.ts stale vs prod (crm_* tables) — cross-app type cleanup."
  - phase: 38-migration-retryability
    items:
      - "WR-04 cross-store non-atomicity duplicate vector accepted for v1 — needs natural-key/upsert follow-up phase."
  - phase: 39-optimistic-locking
    items:
      - "WR-03 stale enqueue snapshot (low-sev); WR-05 exhaustion retry stacking (accepted for v1.3)."
  - note: "Other 2026-06-02 tech-debt items (32 audio placeholder blob, 33 D-12 manual re-save UI, 34 PERF-2 continuous rework + ItemList non-null assertions, 37 meta-viewport WCAG 1.4.4) remain open and unchanged."
---

# v1.3 Maturation — Milestone Audit (full milestone, 31–45)

**Verdict: `tech_debt`** — no hard blockers. All in-scope work verified; all deferred live UAT
passed (2026-06-04); SEAM-3 closed by Phase 45. One process gap (phase-41 missing verification
record) and a backlog of accepted tech debt remain — none block milestone completion.

This audit **supersedes the 2026-06-02 snapshot** (which covered 31–40 only). Items that prior audit
flagged as future work are now closed: photoMigration house/sale collision (**Phase 43**), the
`tpc-ai-proxy` deploy landmine (**Phase 40.1**), and the SEAM-3 lost-write gap (**Phase 45**).

## Phase verification roll-up (31–45)

| Phase | Status | Note |
|-------|--------|------|
| 31 sec-profiles-self-update-hardening | ✅ passed | theme/types prod drift (tech debt) |
| 32 audio-blob-supabase-persistence | ✅ passed | |
| 33 offline-reliability | ✅ passed | |
| 34 ios-memory-optimization | ✅ passed | |
| 35 ai-correctness-track-2 | ✅ passed | SEAM-3 closed by Phase 45 |
| 36 ux-visibility-polish | ✅ passed | |
| 37 a11y-foundation | ✅ passed | |
| 38 migration-retryability | ✅ passed | live retry UAT passed 2026-06-04 |
| 39 optimistic-locking | ✅ passed | SEAM-3 single-item coverage closed by Phase 45 |
| 40 ai-proxy-cloud-run-migration | ✅ passed | |
| 40.1 harden-ai-proxy-deploy-ci | ✅ passed | closed the prior-audit landmine |
| 41 ai-pending-stranding | ⚠ no artifact | urgent lane; wiring correct, no VERIFICATION record |
| 42 audio-upload-reliability | ✅ passed | cross-device audio recovery UAT passed 2026-06-04 |
| 43 photomigration-itemid-collision | ✅ passed | closed the prior-audit collision; WR-01 deferred (self-healing) |
| 44 visibility-ux-polish | ✅ passed | badge nav + 23505 toast UAT passed 2026-06-04 |
| 45 ai-write-precondition | ✅ passed | closed SEAM-3 |

## SEAM-3 — CLOSED by Phase 45

The live single-item AI write-back (`gemini.ts` `processAudioWithAi`) previously did an unconditional
last-writer-wins update, bypassing Phase 39's `preconditionUpdate`. Phase 45 routed the success write
through `preconditionUpdate` with a per-field compare-and-skip reconcile mirroring
`geminiContinuous.ts:286-303`: a catalog field another writer changed since the AI read now yields
(D-06), while control fields (`ai_status`, `completed_at`) and untouched catalog fields apply. The
failure-path `ai_status` write is intentionally left unconditional (control-only, cannot clobber
catalog content — WHY-commented). Verified 3/3; full suite 721 pass; tsc 0.

## Cross-phase integration (3 seams)

1. **38 ↔ 43 (itemTable discriminator):** WIRED.
2. **32/42 ↔ 41 ↔ 33 (audio persist/strand/drain):** WIRED.
3. **39 ↔ 35 ↔ 45 (optimistic-locking ↔ AI merge):** WIRED — SEAM-3 closed.

## Remaining before completion

- **Push `gsd/v1.3-maturation`** for a preview build (deferred per policy; nothing pushed yet).
- **Optionally backfill a phase-41 verification note** for the record (process gap only).
- Accepted tech-debt backlog (see `tech_debt` above) carries forward.
