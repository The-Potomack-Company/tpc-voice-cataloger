---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - index.html
  - src/index.css
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
  - src/db/index.ts
  - src/db/types.ts
  - src/tests/setup.ts
  - src/tests/db.test.ts
  - src/tests/pwa-manifest.test.ts
  - public/icons/icon-192x192.png
  - public/icons/icon-512x512.png
  - public/apple-touch-icon.png
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "App is a valid PWA that can be installed on phone and tablet"
    - "Dexie database opens with all 5 tables defined (sessions, houseVisitItems, saleItems, photos, audio)"
    - "TypeScript types exist for all entities and are importable"
    - "Dev server starts and renders a page without errors"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vite 7 + React + Tailwind CSS 4 + PWA plugin config"
      contains: "VitePWA"
    - path: "src/db/types.ts"
      provides: "Shared TypeScript interfaces for all entities"
      exports: ["Session", "HouseVisitItem", "SaleItem", "ItemPhoto", "ItemAudio"]
    - path: "src/db/index.ts"
      provides: "Dexie database instance with 5 tables"
      exports: ["db"]
    - path: "index.html"
      provides: "PWA-ready HTML with viewport, theme-color, apple-mobile-web-app meta tags"
      contains: "viewport-fit=cover"
    - path: "src/index.css"
      provides: "Tailwind CSS 4 import with custom theme (accent color)"
      contains: "@theme"
  key_links:
    - from: "src/db/index.ts"
      to: "src/db/types.ts"
      via: "import types for table definitions"
      pattern: "import.*types"
    - from: "vite.config.ts"
      to: "PWA manifest"
      via: "VitePWA plugin config"
      pattern: "VitePWA"
---

<objective>
Scaffold the Vite 7 + React 19 project, configure PWA installability, define the Dexie database schema with all shared TypeScript types, and set up the test infrastructure.

Purpose: Establishes the entire project foundation — build tooling, persistent storage schema, type contracts, and test framework — so Plan 02 can build the UI shell on top of a working project.
Output: A bootable React app with PWA manifest, Dexie database ready for use, and passing tests.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project with all dependencies and configuration</name>
  <files>package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts, index.html, src/index.css, src/main.tsx, src/App.tsx, src/vite-env.d.ts, public/icons/icon-192x192.png, public/icons/icon-512x512.png, public/apple-touch-icon.png</files>
  <action>
    1. Create the Vite 7 project using the react-ts template:
       ```
       npm create vite@latest . -- --template react-ts
       ```
       If the directory is not empty (due to .planning), use `--force` or create in a temp dir and move files.

    2. Install production dependencies:
       ```
       npm install react-router zustand dexie dexie-react-hooks
       ```

    3. Install dev dependencies:
       ```
       npm install -D tailwindcss @tailwindcss/vite vite-plugin-pwa
       npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
       ```

    4. Configure vite.config.ts exactly as specified in RESEARCH.md Pattern — Vite Configuration section:
       - react() plugin
       - tailwindcss() from @tailwindcss/vite
       - VitePWA with: registerType "autoUpdate", manifest with name "TPC Catalog", short_name "TPC Catalog", theme_color "#2563eb", background_color "#ffffff", display "standalone", orientation "any", start_url "/", scope "/", icons (192x192, 512x512, 512x512 maskable)
       - workbox config: globPatterns ["**/*.{js,css,html,ico,png,svg,woff2}"], maximumFileSizeToCacheInBytes 3MB
       - Add test config block: { globals: true, environment: "jsdom", setupFiles: ["src/tests/setup.ts"] }

    5. Replace index.html with the PWA-ready version from RESEARCH.md:
       - viewport meta: width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover
       - theme-color meta: #2563eb
       - apple-mobile-web-app-capable: yes
       - apple-mobile-web-app-status-bar-style: default
       - apple-mobile-web-app-title: TPC Catalog
       - link rel="apple-touch-icon" href="/apple-touch-icon.png"
       - title: TPC Catalog

    6. Replace src/index.css with Tailwind CSS 4 config:
       ```css
       @import "tailwindcss";

       @theme {
         --color-accent: #2563eb;
         --color-accent-hover: #1d4ed8;
       }
       ```

    7. Create src/main.tsx with BrowserRouter wrapping App (per RESEARCH.md Pattern 1).

    8. Create a minimal src/App.tsx that renders a placeholder div (Plan 02 will add routing/layout).

    9. Generate placeholder PWA icons:
       - Create public/icons/ directory
       - Generate simple colored square PNG icons (192x192 and 512x512) with "TPC" text or just solid blue squares. Use a canvas-based approach or create minimal valid PNGs. These are placeholder icons for an internal 2-5 person tool.
       - Copy the 180x180 version as public/apple-touch-icon.png

    10. Remove Vite default files: src/App.css, src/assets/react.svg, any default counter component code.

    IMPORTANT: Do NOT create tailwind.config.js — Tailwind CSS 4 uses CSS-based config only.
    IMPORTANT: Use BrowserRouter (pathname routing), NOT HashRouter — iOS microphone re-prompt issue.
    IMPORTANT: Do NOT index blob columns in Dexie schema.
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Vite dev server starts without errors. npm run build produces a dist/ folder. index.html has all PWA meta tags. vite.config.ts has react, tailwindcss, and VitePWA plugins configured. No tailwind.config.js exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Dexie schema, TypeScript types, and test infrastructure</name>
  <files>src/db/types.ts, src/db/index.ts, src/tests/setup.ts, src/tests/db.test.ts, src/tests/pwa-manifest.test.ts</files>
  <behavior>
    - Test: Dexie database opens successfully with fake-indexeddb
    - Test: All 5 tables exist (sessions, houseVisitItems, saleItems, photos, audio)
    - Test: Can create and read a Session record
    - Test: Can create and read a HouseVisitItem linked to a session
    - Test: Can create and read a SaleItem linked to a session
    - Test: PWA manifest config in vite.config.ts contains required fields (name, display, icons)
  </behavior>
  <action>
    1. Create src/db/types.ts with all entity interfaces exactly as specified in RESEARCH.md Pattern 2:
       - Session: id (auto), name, mode ("house"|"sale"), createdAt, updatedAt
       - HouseVisitItem: id (auto), sessionId, title?, description?, condition?, estimate?, category?, sortOrder, createdAt
       - SaleItem: id (auto), sessionId, receiptNumber?, title?, description?, condition?, estimate?, category?, sortOrder, createdAt
       - ItemPhoto: id (auto), itemId, itemType ("house"|"sale"), blob (Blob, NOT indexed), thumbnail? (Blob, NOT indexed), sortOrder, createdAt
       - ItemAudio: id (auto), itemId, itemType ("house"|"sale"), blob (Blob, NOT indexed), mimeType, durationMs?, createdAt

       Also define a versioned export schema interface for future use by Phase 6/7:
       ```typescript
       export interface ExportSchema {
         version: 1;
         exportedAt: string;
         session: Omit<Session, "id">;
         items: Array<{
           // Union of house visit and sale item fields
         }>;
       }
       ```

    2. Create src/db/index.ts with the Dexie database class exactly as specified in RESEARCH.md Pattern 2:
       - Use `++id` auto-increment (not UUID) per research recommendation
       - sessions: "++id, mode, createdAt"
       - houseVisitItems: "++id, sessionId, sortOrder"
       - saleItems: "++id, sessionId, receiptNumber, sortOrder"
       - photos: "++id, itemId, sortOrder" (blob NOT indexed)
       - audio: "++id, itemId" (blob NOT indexed)

    3. Install fake-indexeddb for testing:
       ```
       npm install -D fake-indexeddb
       ```

    4. Create src/tests/setup.ts:
       - Import @testing-library/jest-dom
       - Import "fake-indexeddb/auto" to polyfill IndexedDB in jsdom

    5. Create src/tests/db.test.ts (RED first, then GREEN):
       - Test that db opens and has 5 table names
       - Test CRUD: create a session, read it back, verify fields
       - Test CRUD: create a HouseVisitItem with a sessionId, read it back
       - Test CRUD: create a SaleItem with a sessionId, read it back
       - Clean up db between tests (db.delete() in afterEach)

    6. Create src/tests/pwa-manifest.test.ts:
       - Parse vite.config.ts to verify VitePWA manifest has: name="TPC Catalog", display="standalone", at least 2 icons, theme_color="#2563eb"
       - Alternative approach: import the vite config and inspect the VitePWA plugin options. If that's too complex, read the file as text and use regex assertions.
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass. src/db/types.ts exports Session, HouseVisitItem, SaleItem, ItemPhoto, ItemAudio interfaces. src/db/index.ts exports a Dexie db instance with 5 tables. Dexie CRUD works in tests. PWA manifest test confirms required fields.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without errors
2. `npx vitest run` — all tests pass
3. index.html contains viewport-fit=cover, theme-color, apple-mobile-web-app meta tags
4. No tailwind.config.js file exists (Tailwind CSS 4 uses CSS config)
5. src/db/types.ts is importable and exports all 5 entity interfaces
</verification>

<success_criteria>
- Project builds successfully with Vite 7
- PWA manifest is configured with correct name, display mode, icons
- Dexie database schema has all 5 tables with correct indexes
- TypeScript types are defined and exported for all entities
- All tests pass (db CRUD + PWA manifest validation)
- Tailwind CSS 4 is configured with blue accent theme via @theme in CSS
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
