---
phase: 07-extension-batch-import
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - C:/Users/maser/TPC_AI_Cataloger/tests/unit/content/modules/importController.test.js
autonomous: true
requirements:
  - EXT-01
  - EXT-02
  - EXT-03
  - EXT-04

must_haves:
  truths:
    - "Test file exists with behavioral stubs covering all four EXT requirements"
    - "Jest runs the test file without errors (all tests skip or pass)"
  artifacts:
    - path: "tests/unit/content/modules/importController.test.js"
      provides: "Behavioral test stubs for ImportController"
      contains: "ImportController"
  key_links: []
---

<objective>
Create the Wave 0 test scaffold for ImportController before any implementation begins.

Purpose: Establishes the test file that Plan 01 and Plan 02 executors will run as their automated verification. Without this, Plans 01 and 02 have no Nyquist-compliant automated verify commands.
Output: A Jest test file with behavioral stubs (describe/test.todo blocks) and mock setup for chrome APIs, DOM elements, and extension globals.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/maser/TPC_App/.planning/PROJECT.md
@C:/Users/maser/TPC_App/.planning/ROADMAP.md
@C:/Users/maser/TPC_App/.planning/phases/07-extension-batch-import/07-CONTEXT.md
@C:/Users/maser/TPC_App/.planning/phases/07-extension-batch-import/07-RESEARCH.md

IMPORTANT: All implementation work happens in the TPC_AI_Cataloger repo at C:/Users/maser/TPC_AI_Cataloger (NOT TPC_App).

<interfaces>
<!-- Existing test patterns to follow -->

From tests/unit/content/modules/formController.test.js (pattern reference):
- Load dependencies via require() at top
- Mock globals (TPC_CONSTANTS, Logger, DOMHelper, etc.) before loading module
- Set up DOM via document.body.innerHTML before loading module under test
- Use describe/test blocks grouped by public API method
- beforeEach resets mocks and DOM

From tests/setup.js (already mocked):
- chrome.storage.local.get/set (jest.fn with Promise.resolve)
- chrome.runtime.getURL (jest.fn)
- global.fetch (jest.fn)
- globalThis = global
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ImportController test scaffold with behavioral stubs</name>
  <files>C:/Users/maser/TPC_AI_Cataloger/tests/unit/content/modules/importController.test.js</files>
  <action>
Create `tests/unit/content/modules/importController.test.js` following the formController.test.js pattern.

**Mock setup (before loading the module):**

1. Load Logger dependency: `require('../../../../src/utils/logger.js')`
2. Mock global TPC_CONSTANTS with all import-relevant keys:
   ```javascript
   global.TPC_CONSTANTS = {
     RFC_SELECTORS: {
       TITLE_FIELD: '#fld1',
       DESCRIPTION_FIELD: '#fld2',
       SAVE_BUTTON: '#SaveButton',
       NEXT_BUTTON: 'button[name="btnItemSubmit"][value="Next"]',
       PAGE_TITLE: '.pagetitle',
     },
     STORAGE_KEYS: {
       IMPORT_STATE: 'importState',
     },
     MESSAGE_ACTIONS: {
       START_IMPORT: 'startImport',
       CANCEL_IMPORT: 'cancelImport',
     },
     IMPORT_CONFIG: {
       STATUS: {
         IDLE: 'idle',
         RUNNING: 'running',
         CANCELLED: 'cancelled',
         COMPLETED: 'completed',
         ERROR: 'error',
       },
       STATE_MAX_AGE: 30000,
     },
   };
   ```
3. Mock global helpers that ImportController depends on:
   ```javascript
   global.FormController = {
     getFormFields: jest.fn(() => ({
       titleField: document.querySelector('#fld1'),
       descField: document.querySelector('#fld2'),
     })),
   };
   global.NavigationHelper = {
     clickSave: jest.fn(() => Promise.resolve({ success: true })),
     clickNext: jest.fn(() => Promise.resolve({ success: true })),
     isLastItem: jest.fn(() => false),
     getCurrentReceipt: jest.fn(() => '12345-1'),
   };
   global.PageExtractor = {
     extractReceiptNumber: jest.fn(() => '12345-1'),
     extractItemPosition: jest.fn(() => ({ currentItem: 1, totalItems: 10 })),
   };
   global.ProgressBar = {
     show: jest.fn(),
     hide: jest.fn(),
     showIndeterminate: jest.fn(),
   };
   global.ConfirmationModal = {
     showBatchCompletionModal: jest.fn(),
   };
   global.ModalComponent = {
     show: jest.fn(),
   };
   global.StorageHelper = {
     set: jest.fn(() => Promise.resolve()),
     get: jest.fn(() => Promise.resolve(null)),
     remove: jest.fn(() => Promise.resolve()),
   };
   global.DOMHelper = {
     showSuccess: jest.fn(),
     showError: jest.fn(),
     showInfo: jest.fn(),
     highlightFields: jest.fn(),
   };
   global.Logger = {
     info: jest.fn(),
     warn: jest.fn(),
     error: jest.fn(),
     debug: jest.fn(),
   };
   ```
4. Add chrome.runtime.sendMessage mock (for getCurrentTabId):
   ```javascript
   global.chrome.runtime.sendMessage = jest.fn((msg, callback) => {
     if (callback) callback({ tabId: 123 });
     return Promise.resolve({ tabId: 123 });
   });
   ```
5. Set up DOM with form fields:
   ```javascript
   document.body.innerHTML = `
     <div class="pagetitle">Rcpt-Item 12345-1</div>
     <textarea id="fld1"></textarea>
     <textarea id="fld2"></textarea>
     <button id="SaveButton">Save</button>
     <button name="btnItemSubmit" value="Next">Next</button>
   `;
   ```
6. Do NOT load the actual importController.js yet (it doesn't exist as a full implementation). Instead, write the test structure with `test.todo()` stubs that will be converted to real tests as the implementation lands.

**Test structure (describe blocks with test.todo stubs):**

```javascript
describe('ImportController', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    // Reset DOM
    document.body.innerHTML = `
      <div class="pagetitle">Rcpt-Item 12345-1</div>
      <textarea id="fld1"></textarea>
      <textarea id="fld2"></textarea>
      <button id="SaveButton">Save</button>
      <button name="btnItemSubmit" value="Next">Next</button>
    `;
  });

  // EXT-01: Extension accepts imported JSON file
  describe('startImport', () => {
    test.todo('should accept valid JSON data with version 1 and session mode');
    test.todo('should set status to running when import starts');
    test.todo('should store import data and items from JSON');
    test.todo('should detect session mode (sale vs house) from JSON');
    test.todo('should show progress bar on import start');
    test.todo('should not start if already running');
  });

  // EXT-02: Extension matches items by receipt number
  describe('sale mode - receipt navigation', () => {
    test.todo('should read receipt number from each item');
    test.todo('should skip items with no receipt number');
    test.todo('should track skipped receipt numbers');
    test.todo('should navigate to receipt via receipt input');
    test.todo('should verify receipt number after navigation');
  });

  // EXT-02: House visit walk-forward
  describe('house visit mode - walk forward', () => {
    test.todo('should process items sequentially without receipt lookup');
    test.todo('should call NavigationHelper.clickNext after save');
    test.todo('should complete when all items processed');
  });

  // EXT-03: Extension fills title and description
  describe('fillFieldsVerbatim', () => {
    test.todo('should write title directly to titleField.value');
    test.todo('should write description directly to descField.value');
    test.todo('should dispatch input events after writing');
    test.todo('should NOT add AI Generated prefix');
    test.todo('should handle undefined title gracefully (empty string)');
    test.todo('should handle undefined description gracefully (empty string)');
  });

  // EXT-04: Batch processing with state recovery
  describe('state recovery', () => {
    test.todo('should save state to StorageHelper before page-changing actions');
    test.todo('should restore state from StorageHelper on page reload');
    test.todo('should reject stale state older than STATE_MAX_AGE');
    test.todo('should verify tab ID matches on resume');
    test.todo('should clear state on completion');
    test.todo('should clear state on cancellation');
  });

  // EXT-04: Cancellation
  describe('cancelImport', () => {
    test.todo('should set isCancelled flag');
    test.todo('should hide progress bar');
    test.todo('should show cancellation summary');
  });

  // EXT-04: Completion
  describe('completeImport', () => {
    test.todo('should show completion modal with statistics');
    test.todo('should list skipped receipts for sale mode');
    test.todo('should clear import state from storage');
  });

  // Public getters
  describe('getState', () => {
    test.todo('should return current status, index, and mode');
  });

  describe('getStatistics', () => {
    test.todo('should return success, skipped, and error counts');
  });

  describe('isRunning', () => {
    test.todo('should return true when status is running');
    test.todo('should return false when status is idle');
  });
});
```

IMPORTANT: Use `test.todo()` for all tests. This ensures Jest runs without failures (todos are reported but don't fail). As Plan 01 and Plan 02 executors implement features, they will convert relevant `test.todo()` stubs to real `test()` blocks with assertions.

The file should be runnable with `npx jest tests/unit/content/modules/importController.test.js --no-coverage` and report all todos as pending (0 failures).
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_AI_Cataloger && npx jest tests/unit/content/modules/importController.test.js --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>Test file exists at tests/unit/content/modules/importController.test.js. Jest runs it without errors. All behavioral stubs are present as test.todo() covering EXT-01 (startImport), EXT-02 (sale receipt nav, house walk-forward), EXT-03 (verbatim field fill), and EXT-04 (state recovery, cancel, complete).</done>
</task>

</tasks>

<verification>
1. `npx jest tests/unit/content/modules/importController.test.js --no-coverage` runs without errors
2. Test output shows todo stubs for all four EXT requirement areas
3. Mock setup covers all ImportController dependencies (FormController, NavigationHelper, StorageHelper, etc.)
</verification>

<success_criteria>
- Test file exists and Jest runs it cleanly (0 failures, N todos)
- Behavioral stubs cover all four EXT requirements
- Mock setup is comprehensive enough that converting todos to real tests requires no additional mock work
</success_criteria>

<output>
After completion, create `.planning/phases/07-extension-batch-import/07-00-SUMMARY.md`
</output>
