---
phase: 04-cataloging-modes
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pages/ItemEntry.tsx
  - src/components/PhotoCapture.tsx
  - src/components/PhotoLightbox.tsx
  - src/components/ReceiptNumberInput.tsx
  - src/components/ItemCounter.tsx
  - src/tests/item-entry.test.tsx
  - src/tests/photo-gallery.test.tsx
autonomous: false
requirements: [HOUSE-02, HOUSE-03, HOUSE-04, SALE-02, SALE-03]

must_haves:
  truths:
    - "User can take a photo in house visit mode and see it appear in the thumbnail strip"
    - "User can tap a thumbnail to open full-screen lightbox with swipe navigation and delete"
    - "User can enter a receipt number in XXXXX-N format in sale mode before recording"
    - "User can tap Next Item and get a fresh blank entry immediately"
    - "User gets a warning when advancing past an empty item (no recording, no photos)"
    - "User can navigate back to a previous item and edit it (add photos, re-record)"
    - "Item counter shows current position (e.g., Item 3 of 12)"
    - "Record button works on the item entry screen for both modes"
  artifacts:
    - path: "src/pages/ItemEntry.tsx"
      provides: "Shared item entry page with mode-specific top section"
      min_lines: 100
    - path: "src/components/PhotoCapture.tsx"
      provides: "Camera button + Keep/Retake preview + horizontal thumbnail strip"
      min_lines: 60
    - path: "src/components/PhotoLightbox.tsx"
      provides: "Full-screen photo viewer with swipe navigation and trash delete"
      min_lines: 50
    - path: "src/components/ReceiptNumberInput.tsx"
      provides: "Validated receipt number text field for sale mode"
      min_lines: 20
    - path: "src/components/ItemCounter.tsx"
      provides: "Item N of M counter display"
  key_links:
    - from: "src/pages/ItemEntry.tsx"
      to: "src/db/index.ts"
      via: "Dexie queries for item + photos + audio"
      pattern: "db\\.(houseVisitItems|saleItems|photos|audio)"
    - from: "src/components/PhotoCapture.tsx"
      to: "src/utils/image.ts"
      via: "resizeImage for photo + thumbnail generation"
      pattern: "resizeImage"
    - from: "src/components/PhotoCapture.tsx"
      to: "src/db/index.ts"
      via: "db.photos.add for immediate persistence"
      pattern: "db\\.photos\\.add"
    - from: "src/components/ReceiptNumberInput.tsx"
      to: "src/utils/receiptNumber.ts"
      via: "isValidReceiptNumber for validation"
      pattern: "isValidReceiptNumber|RECEIPT_PATTERN"
    - from: "src/pages/ItemEntry.tsx"
      to: "/session/:sessionId/item/:itemId"
      via: "navigate() for Next Item and back"
      pattern: "navigate.*session.*item"
    - from: "src/components/PhotoLightbox.tsx"
      to: "src/hooks/useBlobUrl.ts"
      via: "useBlobUrl for full-size photo display"
      pattern: "useBlobUrl"
---

<objective>
Build the complete item entry screen for both cataloging modes — house visit (photo capture + thumbnail strip + lightbox) and sale cataloging (receipt number input) — with Next Item progression, back navigation, and empty item warning.

Purpose: This is the core cataloging workflow. Auctioneers spend most of their time on the item entry screen, going item by item. Speed and simplicity are critical.

Output: Fully functional item entry screen with mode-specific capture, photo management, receipt number validation, and item-to-item navigation.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cataloging-modes/04-CONTEXT.md
@.planning/phases/04-cataloging-modes/04-RESEARCH.md
@.planning/phases/04-cataloging-modes/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor should reference these directly -->

From src/utils/image.ts (created in Plan 01):
```typescript
export async function resizeImage(file: File, maxDimension: number): Promise<Blob>;
```

From src/utils/receiptNumber.ts (created in Plan 01):
```typescript
export const RECEIPT_PATTERN: RegExp; // /^\d{5}-\d+$/
export function isValidReceiptNumber(value: string): boolean;
```

From src/hooks/useBlobUrl.ts (created in Plan 01):
```typescript
export function useBlobUrl(blob: Blob | undefined): string | undefined;
```

From src/db/types.ts:
```typescript
export interface HouseVisitItem {
  id?: number; sessionId: number; title?: string; description?: string;
  condition?: string; estimate?: string; category?: string;
  sortOrder: number; createdAt: Date;
}
export interface SaleItem {
  id?: number; sessionId: number; receiptNumber?: string; title?: string;
  description?: string; condition?: string; estimate?: string; category?: string;
  sortOrder: number; createdAt: Date;
}
export interface ItemPhoto {
  id?: number; itemId: number; itemType: "house" | "sale";
  blob: Blob; thumbnail?: Blob; sortOrder: number; createdAt: Date;
}
export interface ItemAudio {
  id?: number; itemId: number; itemType: "house" | "sale";
  blob: Blob; mimeType: string; durationMs?: number; createdAt: Date;
}
```

From src/db/index.ts:
```typescript
export { db }; // tables: sessions, houseVisitItems, saleItems, photos, audio
```

From src/components/RecordButton.tsx:
```typescript
export function RecordButton({ itemId, itemType }: { itemId: number; itemType: "house" | "sale" }): JSX.Element;
```

From src/components/ConfirmDialog.tsx:
```typescript
// Reusable confirmation modal — use for empty item warning
```

From src/components/RecordingIndicator.tsx:
```typescript
export function RecordingIndicator(): JSX.Element; // Timer + red border glow
```

From src/components/RecordingToast.tsx:
```typescript
export function RecordingToast(): JSX.Element; // "Recording saved" toast with play button
```

Routes registered in Plan 01:
```
/session/:sessionId — SessionDetail page
/session/:sessionId/item/new — ItemEntry page (new item)
/session/:sessionId/item/:itemId — ItemEntry page (edit existing)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Item entry page with photo capture (house mode) and receipt number input (sale mode)</name>
  <files>src/pages/ItemEntry.tsx, src/components/PhotoCapture.tsx, src/components/ReceiptNumberInput.tsx, src/components/ItemCounter.tsx, src/components/RecordingIndicator.tsx, src/components/RecordingToast.tsx</files>
  <action>
    **ItemEntry page (src/pages/ItemEntry.tsx):**
    Replace the Plan 01 stub. This is the core cataloging screen. Layout per CONTEXT.md locked decision: stacked layout with context area at top, record button at bottom.

    1. Read `sessionId` and `itemId` from URL params. If `itemId` is "new", create a new item in Dexie (houseVisitItems or saleItems based on session mode) with next sortOrder, then `navigate(\`/session/\${sessionId}/item/\${newId}\`, { replace: true })` to set the real item ID in the URL.
    2. Load session via `useLiveQuery(() => db.sessions.get(Number(sessionId)))`.
    3. Load current item via `useLiveQuery` from the appropriate table based on session mode.
    4. Load total item count for the session via `useLiveQuery`.
    5. Layout structure (flex column, full height):
       - **Top section (mode-specific):** `<PhotoCapture>` for house mode, `<ReceiptNumberInput>` for sale mode
       - **Middle:** `<ItemCounter>` showing "Item N of M"
       - **Bottom (fixed to viewport bottom):** `<RecordButton>` centered, with `<RecordingIndicator>` and `<RecordingToast>` overlays
    6. Prevent double-creation: use a ref or state flag to prevent "new" route from creating multiple items on re-renders.

    **PhotoCapture component (src/components/PhotoCapture.tsx):**
    Per CONTEXT.md: camera button above thumbnail strip, NOT near record button.

    1. Hidden `<input type="file" accept="image/*" capture="environment">` triggered by camera button tap
    2. On file selected: show Keep/Retake preview overlay (full-width image preview with two buttons)
    3. On "Keep": call `resizeImage(file, 2048)` for full image, `resizeImage(file, 200)` for thumbnail, then `db.photos.add({ itemId, itemType: "house", blob: resized, thumbnail, sortOrder: existingCount, createdAt: new Date() })`. Clear preview.
    4. On "Retake": clear preview, reset file input value, re-open camera
    5. Horizontal scrollable thumbnail strip below camera button: load photos via `useLiveQuery(() => db.photos.where({ itemId }).sortBy("sortOrder"), [itemId])`
    6. Each thumbnail: use `useBlobUrl(photo.thumbnail)` for the image src. Tap opens PhotoLightbox (handled in Task 2).
    7. Camera button: large tap target (min 48px), camera icon, styled consistently with app (accent color outline or fill)

    **ReceiptNumberInput component (src/components/ReceiptNumberInput.tsx):**
    Per CONTEXT.md: single text input, full XXXXX-N format typed manually, required before recording.

    1. Props: `value: string, onChange: (value: string) => void, disabled?: boolean`
    2. Text input with `inputMode="numeric"`, placeholder "XXXXX-N (e.g., 12345-1)"
    3. Validate on blur using `isValidReceiptNumber` from `src/utils/receiptNumber.ts`
    4. Show red border + error text "Format: XXXXX-N" when touched and invalid
    5. On change: trim whitespace, call onChange
    6. The receipt number should be saved to the SaleItem in Dexie on blur (debounce not needed — save on blur is sufficient). Update via `db.saleItems.update(itemId, { receiptNumber: value })`.

    **ItemCounter component (src/components/ItemCounter.tsx):**
    1. Props: `current: number, total: number`
    2. Display: "Item {current} of {total}" in a subtle style (text-sm, gray text)
    3. Positioned between mode-specific top and bottom record section

    **Integration with recording:**
    - For sale mode: disable RecordButton if receipt number is empty or invalid (per CONTEXT.md: "receipt number is required before recording"). Pass a `disabled` prop to RecordButton or wrap in conditional. If RecordButton does not accept `disabled`, wrap the button area with a visual overlay/message: "Enter receipt number to start recording."
    - For house mode: RecordButton is always enabled (photos are optional, recording is the main action).
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx tsc --noEmit && npx vite build</automated>
  </verify>
  <done>ItemEntry page renders with PhotoCapture for house mode and ReceiptNumberInput for sale mode. Photos captured via device camera are resized and stored in Dexie with thumbnails. Receipt numbers validate on blur with XXXXX-N format. RecordButton is integrated at bottom. Item counter shows position. New items are created in Dexie when navigating to /item/new route.</done>
</task>

<task type="auto">
  <name>Task 2: Photo lightbox, Next Item progression, back navigation, and empty item warning</name>
  <files>src/components/PhotoLightbox.tsx, src/pages/ItemEntry.tsx, src/components/PhotoCapture.tsx, src/tests/item-entry.test.tsx, src/tests/photo-gallery.test.tsx</files>
  <action>
    **PhotoLightbox component (src/components/PhotoLightbox.tsx):**
    Per CONTEXT.md: tap thumbnail opens full-screen lightbox with swipe left/right and trash icon.

    1. Props: `photos: ItemPhoto[], initialIndex: number, onClose: () => void, onDelete: (photoId: number) => void`
    2. Full-screen overlay (fixed inset-0, bg-black, z-50)
    3. Display current photo using `useBlobUrl(photos[currentIndex].blob)` for full-size image
    4. Swipe left/right to navigate between photos using touch events (touchstart/touchmove/touchend with 50px threshold for swipe detection)
    5. Close button (X icon) in top-left corner, min 48px tap target
    6. Trash icon in top-right corner, min 48px tap target. On tap: show ConfirmDialog "Delete this photo?", on confirm: call `onDelete(photos[currentIndex].id!)`, advance to next photo or close if last
    7. Photo counter "1 / 5" centered at bottom
    8. Handle edge cases: single photo (no swipe), deleted last photo (close lightbox)

    **Wire lightbox into PhotoCapture:** Add state for `lightboxIndex: number | null`. When thumbnail tapped, set lightboxIndex. Render `<PhotoLightbox>` when lightboxIndex !== null. onDelete calls `db.photos.delete(photoId)` — useLiveQuery will auto-update thumbnail strip. onClose sets lightboxIndex to null.

    **Next Item button on ItemEntry page:**
    1. Add "Next Item" button below the RecordButton area (or as a secondary action). Style: full-width button with right-arrow icon, prominent but not as large as RecordButton.
    2. On tap:
       a. Check if current item is completely empty: no audio (`db.audio.where({itemId}).count() === 0`) and no photos (`db.photos.where({itemId}).count() === 0` for house mode) and no receipt number (for sale mode).
       b. If empty: show ConfirmDialog "This item has no recording or photos. Skip it?" with "Skip" (confirm) and "Cancel" buttons.
       c. If not empty (or user confirms skip): navigate to `/session/${sessionId}/item/new` which creates the next item.
    3. Disable Next Item button while item creation is in progress (prevent rapid double-tap creating duplicates). Use a `isCreating` ref/state.

    **Back navigation:**
    1. Add a back/previous button in the top-left of ItemEntry page (arrow-left icon).
    2. If this is the first item (sortOrder === 0 or no previous item), back navigates to `/session/${sessionId}` (session detail).
    3. If there are previous items, back navigates to `/session/${sessionId}/item/${previousItemId}` where previousItemId is the item with the next-lower sortOrder.
    4. Load previous item ID via Dexie query: find items for this session with sortOrder < currentItem.sortOrder, take the last one.

    **Tests (src/tests/item-entry.test.tsx):**
    Test the Next Item logic:
    - Creating a new item increments sortOrder
    - Empty item detection works (no audio, no photos)
    - Navigation to next/previous item uses correct item IDs

    **Tests (src/tests/photo-gallery.test.tsx):**
    Test PhotoLightbox behavior:
    - Opens with correct initial photo index
    - Navigating forward/backward changes displayed photo
    - Delete triggers callback with correct photo ID
    - Closing calls onClose
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>PhotoLightbox displays full-screen photos with swipe navigation and delete. Next Item creates a new blank entry with empty item warning for completely empty items. Back button navigates to previous item or session detail. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete cataloging workflow</name>
  <files></files>
  <action>
    User verifies the complete house visit and sale cataloging workflows on device or browser.
  </action>
  <what-built>Complete item-by-item cataloging workflow for both house visit and sale modes, including session creation, photo capture with lightbox, receipt number validation, Next Item progression, and back navigation.</what-built>
  <how-to-verify>
    1. Open the app at https://localhost:5173 (or dev server URL)
    2. **Session creation:** Tap "Start New Session" > tap "House Visit" > verify you land on an empty session detail page
    3. **House visit item entry:** Tap "Add Item" or "Start Cataloging" > verify the item entry screen shows camera button at top and record button at bottom
    4. **Photo capture:** Tap camera icon > take/select a photo > verify Keep/Retake preview appears > tap Keep > verify thumbnail appears in horizontal strip
    5. **Take another photo:** Tap camera again > Keep > verify second thumbnail appears
    6. **Lightbox:** Tap a thumbnail > verify full-screen lightbox opens > swipe left/right between photos > tap trash icon > confirm delete > verify photo removed
    7. **Recording:** Tap record button > speak briefly > tap stop > verify RecordingToast appears
    8. **Next Item:** Tap Next Item > verify screen clears to blank entry with "Item 2 of 2" counter
    9. **Empty item warning:** Without recording or adding photos, tap Next Item > verify warning dialog appears "This item has no recording or photos. Skip it?"
    10. **Back navigation:** Tap back arrow > verify you return to Item 2 with its content intact > tap back again to Item 1 > verify photos and recording still present
    11. **Sale mode:** Go back to sessions > create new Sale Cataloging session > verify receipt number input appears at top
    12. **Receipt validation:** Enter "123" then tap away > verify red error border. Enter "12345-1" > verify it accepts
    13. **Sale recording gate:** Verify record button is disabled/blocked when receipt number is empty or invalid
    14. **Session list:** Go back to home > verify both sessions appear in the list with correct mode badges and item counts
    15. **Dark mode:** Toggle system dark mode > verify all screens are readable with correct contrast
  </how-to-verify>
  <verify>User confirms all 15 verification steps pass</verify>
  <done>User has approved the complete cataloging workflow for both house visit and sale modes.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — all tests pass
- `npx tsc --noEmit` — no TypeScript errors
- `npx vite build` — app builds successfully
- Manual verification: full house visit and sale cataloging workflows work end-to-end
</verification>

<success_criteria>
- House visit mode: camera capture with Keep/Retake, thumbnail strip, lightbox with swipe + delete, recording, Next Item progression
- Sale cataloging mode: receipt number input with XXXXX-N validation, recording blocked until valid receipt number, Next Item with fresh receipt field
- Both modes: item counter, back navigation to previous item, empty item warning on Next Item
- Photos resized to 2048px max and stored with 200px thumbnails in Dexie immediately on Keep
- Blob URLs properly managed (created and revoked) to prevent memory leaks
- 300+ items: item list uses lightweight rows, photos loaded per-item not per-session
- All tests pass, TypeScript compiles, app builds
</success_criteria>

<output>
After completion, create `.planning/phases/04-cataloging-modes/04-02-SUMMARY.md`
</output>
