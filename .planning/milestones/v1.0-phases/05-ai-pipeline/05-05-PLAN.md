---
phase: 05-ai-pipeline
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ItemCard.tsx
autonomous: true
gap_closure: true
requirements: [AI-01, AI-03]

must_haves:
  truths:
    - "Items with aiStatus=failed show a visible 'Failed' badge on the collapsed card row"
    - "Items with aiStatus=failed show a 'Retry AI' button in the expanded section"
    - "Tapping 'Retry AI' re-triggers processAudioWithAi for the item and the button shows a processing state"
    - "Items with aiStatus=processing show a 'Processing...' indicator on the collapsed card row"
  artifacts:
    - path: "src/components/ItemCard.tsx"
      provides: "AI status indicators and retry button"
      contains: "Retry AI"
  key_links:
    - from: "src/components/ItemCard.tsx"
      to: "src/services/gemini.ts"
      via: "processAudioWithAi call on retry button click"
      pattern: "processAudioWithAi"
    - from: "src/components/ItemCard.tsx"
      to: "src/services/offlineQueue.ts"
      via: "findAudioForItem pattern (inline or imported)"
      pattern: "audio.*where.*itemId"
---

<objective>
Add AI status indicators and a retry button to ItemCard so users can see which items failed AI processing and manually re-trigger it.

Purpose: Close the UAT gap — failed AI processing currently has no user-visible recovery path. Users need to see failure status and retry without re-recording.
Output: Updated ItemCard.tsx with status badges and retry functionality.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ai-pipeline/05-CONTEXT.md
@.planning/phases/05-ai-pipeline/05-UAT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/db/types.ts:
```typescript
export type AiStatus = "pending" | "processing" | "done" | "failed" | "queued";
```

From src/services/gemini.ts:
```typescript
export async function processAudioWithAi(
  audioId: number,
  itemId: number,
  itemType: "house" | "sale",
): Promise<void>;
```

From src/components/ItemCard.tsx (current):
- Already imports `processAudioWithAi` from "../services/gemini"
- Already imports `db` from "../db"
- Already has `isQueued` check for aiStatus === "queued"
- Already has handleMicClick that calls processAudioWithAi on recording stop

From src/services/offlineQueue.ts (pattern to reuse):
```typescript
// Find most recent audio for an item
const audios = await db.audio.where("itemId").equals(itemId).toArray();
const audioId = audios.reduce((max, a) => (a.id! > max ? a.id! : max), audios[0].id!);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI status indicators and retry button to ItemCard</name>
  <files>src/components/ItemCard.tsx</files>
  <action>
Modify ItemCard.tsx to add three things:

1. **Failed badge on collapsed row** — Next to the existing "Queued" badge, add a "Failed" badge for items with aiStatus==="failed". Use red styling (bg-red-100 dark:bg-red-900/30 text-red-700 dark:text-red-400) matching the Queued badge pattern but in red. Also add a "Processing..." badge for aiStatus==="processing" using blue styling (bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-400).

2. **Retry button in expanded section** — For items with aiStatus==="failed", add a "Retry AI" button above the Delete button in the expanded section. The button should:
   - Find the most recent audio record for this item from db.audio (same pattern as offlineQueue.ts: query by itemId, pick highest id)
   - Call processAudioWithAi(audioId, item.id!, mode) as fire-and-forget
   - Use useState for a `retrying` boolean — set true on click, set false in .then/.catch
   - While retrying, show "Retrying..." with a spinner/pulse and disable the button
   - If no audio exists for the item, show a disabled button with tooltip "No audio to retry"
   - Style: blue accent, same width as Delete button. Use: "w-full text-sm text-blue-600 dark:text-blue-400 font-medium py-2 rounded-lg border border-blue-200 dark:border-blue-800 hover:bg-blue-50 dark:hover:bg-blue-900/20 transition-colors disabled:opacity-50"

3. **Audio lookup for retry** — Add a useLiveQuery that fetches the most recent audio ID for the item (reuse the same db.audio.where("itemId").equals(item.id!) pattern already used for audioCount, but get the max id). This can be combined: query audio records, derive both count and latestAudioId from the same query. Replace the existing audioCount query with one that returns both values.

Do NOT add a retry button for "queued" items (they already have the queue drain mechanism from Phase 8).
Do NOT modify any other components or files.
Keep the existing opacity-50 treatment for queued items. Failed items should NOT be dimmed — they need user attention.
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Items with aiStatus=failed show a red "Failed" badge on the collapsed row
- Items with aiStatus=processing show a blue "Processing..." badge on the collapsed row
- Expanded failed items show a "Retry AI" button that re-triggers processAudioWithAi
- Retry button shows "Retrying..." state while processing
- Build succeeds with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds without errors
- ItemCard.tsx contains "Failed" badge conditional on aiStatus === "failed"
- ItemCard.tsx contains "Processing" badge conditional on aiStatus === "processing"
- ItemCard.tsx contains "Retry AI" button that calls processAudioWithAi
- Failed items are NOT dimmed (no opacity-50 on failed status)
</verification>

<success_criteria>
Users can see which items failed AI processing via a red "Failed" badge, and can tap "Retry AI" to re-trigger processing without re-recording audio. Items in processing show a "Processing..." indicator.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-pipeline/05-05-SUMMARY.md`
</output>
