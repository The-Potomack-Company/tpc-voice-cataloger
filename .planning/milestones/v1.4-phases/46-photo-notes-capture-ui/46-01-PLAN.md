---
phase: 46-photo-notes-capture-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/db/types.ts
  - src/db/index.ts
  - src/db/notePages.ts
  - src/stores/sessionStore.ts
  - src/tests/db.test.ts
  - src/tests/note-pages.test.ts
autonomous: true
requirements: [PHN-01, PHN-02]
user_setup: []

must_haves:
  truths:
    - "Dexie opens at version 13 with a notePages table; existing v12 data (sessions/items/photos) survives the upgrade untouched (SPEC Persistence; UAT-5)"
    - "notePages is keyed ++id with indexes pageUid, sessionId, sortOrder; a NotePage round-trips a Blob + thumbnail Blob + sortOrder + status 'captured' (SPEC NotePage shape)"
    - "getNotePages(sessionId) returns rows ordered by sortOrder ascending; reorder rewrites sortOrder so order is stable across reads (SPEC page list; UAT-3)"
    - "retakeNotePage replaces blob+thumbnail in place and keeps pageUid + sortOrder; deleteNotePage removes exactly one row by id (SPEC retake/delete; UAT-4)"
    - "deleting a session sweeps its notePages rows (sessionId match) and touches no other session's pages (SPEC Cleanup)"
  artifacts:
    - path: "src/db/notePages.ts"
      provides: "notePages data access: addNotePage, getNotePages, reorderNotePages, retakeNotePage, deleteNotePage, countNotePages, deleteNotePagesForSession"
      min_lines: 40
    - path: "src/db/types.ts"
      provides: "NotePage interface (pageUid, sessionId, blob, thumbnail, sortOrder, status, createdAt)"
      contains: "NotePage"
    - path: "src/db/index.ts"
      provides: "db.version(13) adding notePages store + EntityTable typing"
      contains: "notePages"
  key_links:
    - from: "src/db/index.ts"
      to: "src/db/types.ts"
      via: "EntityTable<NotePage, 'id'>"
      pattern: "NotePage"
    - from: "src/stores/sessionStore.ts"
      to: "src/db/notePages.ts"
      via: "deleteNotePagesForSession call on session.deleted success"
      pattern: "deleteNotePagesForSession"
---

<objective>
Add the Dexie v13 `notePages` table and its data-access module — the client-only
persistence floor for the photo-notes capture UI (Plan 02). No Supabase writes, no
schema migration, no proxy change (D-050 code untouched). Pages survive tab kill and
offline by living only in IndexedDB.

Purpose: gives Plan 02 a tested CRUD + ordering contract to bind the capture screen to,
and gives Phase 47 a row shape whose `pageUid` is the idempotency key and whose `status`
field already anticipates `processing` | `processed`.
Output: NotePage type, v13 store, `src/db/notePages.ts`, session-delete cleanup sweep, tests.
</objective>

<context>
@.planning/milestones/v1.4-phases/46-photo-notes-capture-ui/46-SPEC.md

<interfaces>
<!-- Existing idioms the executor reuses directly. -->

Dexie versioning idiom (src/db/index.ts) — each version re-declares ALL stores; a new
EMPTY table needs no .upgrade() (see v9 sessionAudio, v11 userEditedFields precedent).
Add the typed handle to the `db` cast object then a v13 block carrying every v12 store
plus `notePages: "++id, pageUid, sessionId, sortOrder"`.

NotePage row shape (SPEC lines 63-74, mirrors ItemPhoto in src/db/types.ts):
  interface NotePage {
    id?: number;
    pageUid: string;        // crypto.randomUUID() — stable identity for retake/reorder + Phase 47 idempotency
    sessionId: string;      // Supabase session UUID (useParams sessionId), NOT the Dexie ++id
    blob: Blob;             // resized JPEG, maxDimension 2048
    thumbnail: Blob;
    sortOrder: number;
    status: "captured";     // Phase 47 extends: "processing" | "processed"
    createdAt: string;      // ISO
  }

Ordered read idiom (src/components/PhotoCapture.tsx:101): db.<table>.where("sessionId").equals(id).sortBy("sortOrder").

Session-delete success seam (src/stores/sessionStore.ts:301-303) — after the Supabase
delete resolves and BEFORE `trackEvent({ event_type: "session.deleted" })`, fire the
Dexie sweep. There is NO existing Dexie photo-cleanup-on-session-delete pattern (photos
are keyed by itemId and rely on Supabase cascade), so per SPEC add a sessionId sweep for
notePages ONLY — do not expand scope to photos.

Test harness: fake-indexeddb/auto is loaded in src/tests/setup.ts; the db.test.ts
afterEach does `await db.delete(); await db.open();`. Blobs construct via
`new Blob(["x"], { type: "image/jpeg" })`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotePage type + v13 notePages store</name>
  <files>src/db/types.ts, src/db/index.ts</files>
  <read_first>
    - src/db/types.ts (ItemPhoto shape, lines ~55-74 — mirror it)
    - src/db/index.ts (v11/v12 blocks — copy the all-stores re-declaration pattern)
  </read_first>
  <action>
    In src/db/types.ts add the `NotePage` interface exactly per the interfaces block
    (sessionId is a string UUID; status is the literal "captured"; createdAt is an ISO
    string). In src/db/index.ts: import NotePage, add
    `notePages: EntityTable<NotePage, "id">;` to the db cast object, and add a
    `db.version(13).stores({ ...all v12 stores..., notePages: "++id, pageUid, sessionId, sortOrder" })`
    block. New empty table → no `.upgrade()` (v9/v11 precedent). Leave v1–v12 blocks byte-for-byte unchanged.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit && grep -q 'notePages' src/db/index.ts && grep -q 'interface NotePage' src/db/types.ts && echo OK</automated>
  </verify>
  <done>tsc clean; notePages typed on db; NotePage exported. No edits to any prior version block.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: notePages data-access module (RED → GREEN)</name>
  <files>src/db/notePages.ts, src/tests/note-pages.test.ts</files>
  <read_first>
    - src/db/notePages.ts output of Task 1 (NotePage type)
    - src/tests/db.test.ts (afterEach reset idiom, Blob construction, table-shape assertions)
  </read_first>
  <behavior>
    - addNotePage({ sessionId, blob, thumbnail }): assigns pageUid = crypto.randomUUID(),
      sortOrder = current count for that session, status "captured", createdAt ISO now;
      returns the new id.
    - getNotePages(sessionId): rows for that session ordered by sortOrder ascending.
    - countNotePages(sessionId): number of rows for that session.
    - reorderNotePages(sessionId, orderedIds): rewrites sortOrder to match the given id
      order (0..N-1) inside one transaction; a read afterwards reflects the new order.
    - retakeNotePage(id, { blob, thumbnail }): replaces blob+thumbnail in place; pageUid,
      sessionId, sortOrder unchanged.
    - deleteNotePage(id): removes exactly that row.
    - deleteNotePagesForSession(sessionId): deletes all rows for the session, leaves other
      sessions' rows intact.
  </behavior>
  <action>
    Write src/tests/note-pages.test.ts FIRST covering every behavior above plus: capturing
    3 pages yields sortOrder 0,1,2 in capture order (UAT-2); reorder [2,0,1] then getNotePages
    returns that order and persists across a re-read (UAT-3); retake keeps pageUid + sortOrder,
    swaps blob (UAT-4); deleteNotePagesForSession on session A leaves session B's pages (Cleanup).
    Also assert db opens with the notePages table present and its sortOrder/sessionId/pageUid
    indexes. Run — RED. Then implement src/db/notePages.ts (import { db }, type NotePage).
    Keep functions pure data-access (no UI, no Supabase). Run — GREEN.
  </action>
  <verify>
    <automated>npm test -- --run src/tests/note-pages.test.ts</automated>
  </verify>
  <done>All CRUD + ordering + cleanup behaviors pass against fake-indexeddb.</done>
</task>

<task type="auto">
  <name>Task 3: session-delete cleanup sweep + db.test.ts table-count bump</name>
  <files>src/stores/sessionStore.ts, src/tests/db.test.ts</files>
  <read_first>
    - src/stores/sessionStore.ts:260-303 (deleteSession success path)
    - src/tests/db.test.ts (the two "12 tables" / toHaveLength(12) assertions)
  </read_first>
  <action>
    In sessionStore.deleteSession, after the Supabase delete succeeds and before
    `trackEvent({ event_type: "session.deleted" })`, call
    `await deleteNotePagesForSession(id)` (import from "../db/notePages"). Wrap so a
    sweep failure does not revert the already-committed Supabase delete — best-effort
    (`.catch(() => {})` or try/catch) is acceptable; the rows are client-only.
    In src/tests/db.test.ts update the table-count assertions from 12 → 13, add
    "notePages" to the sorted table-name array, and add a notePages index assertion
    (sessionId, sortOrder, pageUid present). Add a focused "v13 keeps v12 data" test:
    seed a session+page at the current version, reopen, assert both survive.
  </action>
  <verify>
    <automated>npm test -- --run src/tests/db.test.ts && grep -q 'deleteNotePagesForSession' src/stores/sessionStore.ts && echo OK</automated>
  </verify>
  <done>db.test.ts green at 13 tables; deleteSession sweeps notePages; no other store assertions regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture → IndexedDB | note blobs persist client-side only; no network, no Supabase this phase |
| session delete → Dexie sweep | sweep must scope to the deleted session's sessionId only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | deleteNotePagesForSession | mitigate | `where("sessionId").equals(id)` scopes the sweep; test proves session B's pages survive |
| T-46-02 | Information Disclosure | NotePage blobs | accept | Client-only Dexie storage this phase; Supabase durability is the Phase 47 / R-4 decision, out of scope here |
| T-46-03 | Denial of Service | v13 upgrade | mitigate | New empty table, no .upgrade() callback — cannot corrupt or block on existing v12 rows (v9/v11 precedent); test asserts v12 data survives |
</threat_model>

<verification>
- `npm test -- --run src/tests/note-pages.test.ts src/tests/db.test.ts` green.
- `npx tsc -b --noEmit` clean.
- No edits to geminiContinuous.ts / gemini.ts / items/export paths / Supabase schema.
</verification>

<success_criteria>
Dexie v13 ships a notePages table with a tested CRUD + ordering + cleanup contract,
existing data survives the upgrade, and session deletion sweeps orphaned note pages —
all client-only, zero Supabase/proxy/schema change.
</success_criteria>

<output>
Create `.planning/milestones/v1.4-phases/46-photo-notes-capture-ui/46-01-SUMMARY.md` when done.
Record the final notePages.ts export surface so Plan 02 binds the capture screen without re-reading source.
</output>
