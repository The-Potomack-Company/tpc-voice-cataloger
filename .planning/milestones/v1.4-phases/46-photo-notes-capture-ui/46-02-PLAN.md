---
phase: 46-photo-notes-capture-ui
plan: 02
type: tdd
wave: 2
depends_on: ["46-01"]
files_modified:
  - src/App.tsx
  - src/pages/PhotoNotes.tsx
  - src/hooks/useNotePages.ts
  - src/services/notesProcessing.ts
  - src/pages/SessionDetail.tsx
  - src/tests/photo-notes.test.tsx
  - src/tests/session-detail-photo-notes.test.tsx
autonomous: true
requirements: [PHN-01, PHN-02]
user_setup: []

must_haves:
  truths:
    - "From an active session (house AND sale) the cataloger sees a 'Photo notes' action; a read-only (submitted/exported-specialist) session does NOT, and it is hidden while continuousActive (SPEC Entry point; UAT-1)"
    - "The entry action shows a count chip when captured-but-unprocessed pages exist for the session (SPEC Badge; UAT-5)"
    - "PhotoNotes is a full-screen route session/:sessionId/photo-notes; capturing pages via a hidden <input capture=environment> resizes to 2048 + a thumbnail and stores a notePage; the list shows numbered ordered thumbnails (SPEC Capture surface; UAT-2)"
    - "Each page row offers retake (in place), delete (ConfirmDialog), and reorder via up/down buttons operable without drag (SPEC page list + A11y; UAT-3,4,8)"
    - "The sticky 'Process N pages' footer is disabled when N===0 or offline, with the saved-pages hint; online with N>0 it calls processNotes(sessionId) which no-ops with a toast and changes no item data (SPEC Process button; UAT-6,7)"
  artifacts:
    - path: "src/pages/PhotoNotes.tsx"
      provides: "Full-screen photo-notes capture/manage screen (capture loop, page list, reorder/retake/delete, Process stub footer)"
      min_lines: 120
    - path: "src/hooks/useNotePages.ts"
      provides: "liveQuery hooks: useNotePages(sessionId) ordered list + useNotePageCount(sessionId) for the badge"
      min_lines: 15
    - path: "src/services/notesProcessing.ts"
      provides: "processNotes(sessionId) stub seam — no-op this phase, real pipeline in Phase 47"
      contains: "processNotes"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/PhotoNotes.tsx"
      via: "Route path session/:sessionId/photo-notes"
      pattern: "photo-notes"
    - from: "src/pages/SessionDetail.tsx"
      to: "src/pages/PhotoNotes.tsx"
      via: "navigate(`/session/${sessionId}/photo-notes`) from the Photo notes action"
      pattern: "photo-notes"
    - from: "src/pages/PhotoNotes.tsx"
      to: "src/db/notePages.ts"
      via: "addNotePage/reorderNotePages/retakeNotePage/deleteNotePage"
      pattern: "addNotePage"
    - from: "src/pages/PhotoNotes.tsx"
      to: "src/services/notesProcessing.ts"
      via: "Process button onClick"
      pattern: "processNotes"
---

<objective>
Build the photo-notes capture surface on top of Plan 01's notePages store: the
SessionDetail entry action (gated + badged), the full-screen PhotoNotes route, the
tap-tap-tap capture loop, the reorder/retake/delete page list, and the offline-gated
"Process N pages" footer terminating in a stub. Entirely client-side; the Process action
is a wired seam for Phase 47.

Output: PhotoNotes page, route, liveQuery hooks, processNotes stub, SessionDetail entry, tests.
</objective>

<context>
@.planning/milestones/v1.4-phases/46-photo-notes-capture-ui/46-SPEC.md
@.planning/milestones/v1.4-phases/46-photo-notes-capture-ui/46-01-PLAN.md

<interfaces>
<!-- Existing idioms the executor reuses directly. -->

Camera input (verbatim shape from src/components/PhotoCapture.tsx:95-118):
  <input ref type="file" accept="image/*" capture="environment" onChange className="hidden" />
  handleCameraClick: input.value=""; input.click()
  resizeImage(file, 2048) for the page blob + resizeImage(file, 200) for the thumbnail
  (src/utils/image.ts). NO getUserMedia.

Route registration (src/App.tsx) — sibling of the existing item route, inside the same
AppLayout/ProtectedRoute group:
  <Route path="session/:sessionId/photo-notes" element={<PhotoNotesPage />} />
sessionId from useParams<{ sessionId: string }>() is the Supabase UUID (string) — pass it
straight to the notePages helpers (their sessionId field is that string).

Online status: `useOnlineStatus()` (src/hooks/useOnlineStatus.ts) returns boolean from
uiStore.isOnline (already synced to navigator.onLine + online/offline events). Use it for
the Process gate so tests can drive it via the store.

liveQuery hooks: model on src/components/PhotoCapture.tsx:101 useLiveQuery; wrap
getNotePages(sessionId)/countNotePages(sessionId) from src/db/notePages.ts.

Entry-point host (src/pages/SessionDetail.tsx) — the lifecycle action stack at lines
~481-541 renders inside `{!roleLoading && (...)}`. Add the "Photo notes" action there,
gated `!isReadOnly && !continuousActive` (the same guards the Add-Item FAB uses at
:735). `isReadOnly` = src/pages/SessionDetail.tsx:214-215; `continuousActive` = :109.
Navigate with useNavigate() (already imported) to `/session/${sessionId}/photo-notes`.
Badge: useNotePageCount(sessionId) > 0 → render a count chip (reuse the <Badge> primitive
already imported at :25, or a tpc chip span).

Primitives: <Button> (src/ui/Button.tsx, variants primary|secondary|ghost|danger, icon
prop), <Icon name=...> (camera, image, plus, trash, arrowUp, arrowDown, refresh, back, x —
verified in src/ui/icons.tsx), <ConfirmDialog open title message destructive onConfirm
onCancel> (src/components/ConfirmDialog.tsx) for delete, <Modal> underneath it.

Toast for the stub: there is no generic toast store (notificationStore is error-only).
Reuse SessionDetail's local-state toast idiom (src/pages/SessionDetail.tsx importToast at
:104 + render at the bottom) — a local `useState<string|null>` + a timed clear inside
PhotoNotes. Do NOT add a new global store (SPEC: no new Zustand store this phase).

A11y bar (Phase 37): every icon button has aria-label; touch targets ≥44px (min-h-12 /
explicit 44px); ConfirmDialog for delete; reorder via up/down buttons (not drag-only);
respect prefers-reduced-motion on any list animation (window.matchMedia idiom,
src/ui/Waveform.tsx:21-25).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: liveQuery hooks + processNotes stub + route</name>
  <files>src/hooks/useNotePages.ts, src/services/notesProcessing.ts, src/App.tsx</files>
  <read_first>
    - src/db/notePages.ts (Plan 01 export surface)
    - src/hooks/useOnlineStatus.ts (hook idiom)
    - src/App.tsx (route group)
  </read_first>
  <action>
    src/hooks/useNotePages.ts: export useNotePages(sessionId: string) → useLiveQuery
    ordered list (default []), and useNotePageCount(sessionId: string | undefined) →
    number (default 0). src/services/notesProcessing.ts: export async
    processNotes(sessionId: string): Promise<void> that no-ops this phase (a WHY-comment
    naming Phase 47 as the real implementation is warranted — non-obvious deliberate stub).
    src/App.tsx: register `<Route path="session/:sessionId/photo-notes" element={<PhotoNotesPage />} />`
    inside the AppLayout group, importing PhotoNotesPage from "./pages/PhotoNotes".
  </action>
  <verify>
    <automated>npx tsc -b --noEmit && grep -q 'photo-notes' src/App.tsx && grep -q 'processNotes' src/services/notesProcessing.ts && echo OK</automated>
  </verify>
  <done>Hooks + stub compile; route registered (PhotoNotes page authored in Task 2).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: PhotoNotes capture screen (RED → GREEN)</name>
  <files>src/pages/PhotoNotes.tsx, src/tests/photo-notes.test.tsx</files>
  <read_first>
    - src/components/PhotoCapture.tsx (camera input + resize idiom)
    - src/pages/ItemEntry.tsx:1-60 (full-screen page + back nav + useParams shape)
    - src/db/notePages.ts (CRUD surface)
    - src/components/ConfirmDialog.tsx (delete confirm)
  </read_first>
  <behavior>
    - Full-screen page; back control returns to the session. Reads useParams sessionId.
    - "Add page" primary action opens the hidden capture input; on file pick, resizeImage
      to 2048 (blob) + 200 (thumbnail), addNotePage(...). Multi-page = repeat (no forced menu).
    - Page list: ordered numbered thumbnails (1..N), each row with retake (replaces in
      place via retakeNotePage), delete (ConfirmDialog destructive → deleteNotePage), and
      up/down reorder buttons (reorderNotePages with the swapped id order); up disabled on
      first row, down disabled on last.
    - Sticky footer "Process N pages": disabled when N===0 OR !online; when disabled by
      offline show the hint "Processing needs a connection — pages are saved"; when enabled,
      onClick calls processNotes(sessionId) then shows the local toast
      "Processing lands in the next update". It writes NO item data.
    - All icon buttons aria-labelled; targets ≥44px; reduced-motion respected on list anim.
  </behavior>
  <action>
    Write src/tests/photo-notes.test.tsx FIRST (render within MemoryRouter at
    /session/:id/photo-notes, seed notePages via Plan 01 helpers or addNotePage):
      • renders N numbered thumbnails in sortOrder (seed 3 → labels 1,2,3) (UAT-2)
      • up/down buttons reorder and the numbering updates; up disabled on row 1 (UAT-3,8)
      • delete shows ConfirmDialog, confirming removes the row (UAT-4)
      • Process disabled when count 0; with pages + offline (uiStore.setOnline(false))
        disabled + hint shown; online + pages → enabled, click shows the stub toast and
        calls the processNotes spy, asserts no items table write (UAT-6,7)
    Run — RED. Implement src/pages/PhotoNotes.tsx. Run — GREEN. Keep it presentational
    over Plan 01's data module; no Supabase, no Zustand store.
  </action>
  <verify>
    <automated>npm test -- --run src/tests/photo-notes.test.tsx</automated>
  </verify>
  <done>Capture/list/reorder/delete + offline-gated Process stub all pass; jest-axe (if asserted) clean.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: SessionDetail entry action + badge (RED → GREEN)</name>
  <files>src/pages/SessionDetail.tsx, src/tests/session-detail-photo-notes.test.tsx</files>
  <read_first>
    - src/pages/SessionDetail.tsx:214-215 (isReadOnly), :109 (continuousActive), :481-541 (action stack), :735 (FAB guards)
    - src/hooks/useNotePages.ts (useNotePageCount)
    - src/tests/session-lifecycle.test.tsx (SessionDetail render-with-router test idiom + role mocking)
  </read_first>
  <behavior>
    - A "Photo notes" action renders in the lifecycle action stack when
      `!isReadOnly && !continuousActive`, for BOTH house and sale sessions; clicking it
      navigates to `/session/${sessionId}/photo-notes`.
    - When useNotePageCount(sessionId) > 0 the action shows a count chip.
    - On a submitted/exported specialist session (isReadOnly) the action is absent.
  </behavior>
  <action>
    Write src/tests/session-detail-photo-notes.test.tsx FIRST mirroring the existing
    SessionDetail test setup (mock useUserRole, seed a session): assert the action shows
    for active house and active sale, is absent for a submitted specialist session, shows
    the chip when a notePage exists, and navigates on click. Run — RED. Add the action +
    badge to SessionDetail (useNavigate already imported; import useNotePageCount). Touch
    nothing else in the file. Run — GREEN.
  </action>
  <verify>
    <automated>npm test -- --run src/tests/session-detail-photo-notes.test.tsx</automated>
  </verify>
  <done>Entry action gated + badged correctly; existing SessionDetail tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read-only session → mutating UI | the Photo notes action must not appear on a locked session |
| Process button → pipeline | this phase the seam must be inert (no item/Supabase write) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04 | Elevation of Privilege | Photo notes entry action | mitigate | Gated `!isReadOnly && !continuousActive` (same guards as Add-Item FAB); test asserts absence on submitted/exported specialist sessions |
| T-46-05 | Tampering | processNotes stub | mitigate | Stub is a pure no-op; test asserts tapping Process writes no item data — the Phase 47 seam cannot mutate this phase |
| T-46-06 | Denial of Service | offline capture | accept | Capture path touches only Dexie (works offline by construction); Process is correctly disabled offline with a saved-pages hint |
</threat_model>

<verification>
- `npm test -- --run src/tests/photo-notes.test.tsx src/tests/session-detail-photo-notes.test.tsx` green.
- `npm run lint`, `npx tsc -b --noEmit`, full `npm test`, `npm run build` green.
- No change to geminiContinuous.ts / gemini.ts / items/export/import / Supabase schema / proxy.
</verification>

<success_criteria>
The cataloger reaches a full-screen photo-notes screen from an active session, captures /
reorders / retakes / deletes ordered note pages that persist offline and across tab kill,
and an offline-aware "Process N pages" button terminates in an inert, badge-discoverable
stub — zero schema, proxy, or items writes.
</success_criteria>

<output>
Create `.planning/milestones/v1.4-phases/46-photo-notes-capture-ui/46-02-SUMMARY.md` when done.
Record the PhotoNotes route, the processNotes seam signature, and the entry-action gate so
Phase 47 wires the real pipeline against them.
</output>
