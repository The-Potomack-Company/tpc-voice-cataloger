---
phase: quick-260528-hm6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260528000000_d038_dev_users_filter.sql
  - supabase/config.toml
requirements: [QUICK-260528-HM6]
autonomous: false

must_haves:
  truths:
    - "prod `private.dev_users` table exists and contains the admin (josh@potomackco.com) user_id"
    - "prod PostgREST exposes the `private` schema (admin-list-users no longer 500s with 'Invalid schema: private')"
    - "7 RESTRICTIVE 'no_dev_data_for_non_dev_viewers' policies exist on sessions, items, photos, export_history, ui_interactions, analytics_events, profiles"
    - "Codex-rescue adversarial review (D-046) ran clean on the new RLS migration BEFORE `supabase db push`"
    - "admin-list-users edge fn redeployed from the clean worktree code (verify-admin.ts at HEAD has no debug headers)"
    - "https://app.potomackco.com admin Users page loads the account list (no 'Could not load accounts' error)"
    - "build + lint + tests are green on tip"
    - "PR opened against `urgent/dat-receipt-unique` (stacked on #30, not main)"
  artifacts:
    - path: "supabase/migrations/20260528000000_d038_dev_users_filter.sql"
      provides: "D-038 dev-data filter ported into voice-cataloger migration history with today's timestamp"
      contains: "create schema if not exists private"
    - path: "supabase/config.toml"
      provides: "Supabase CLI config exposing the `private` schema to PostgREST for future `supabase start`/`db reset` runs"
      contains: "schemas = [\"public\", \"graphql_public\", \"private\"]"
  key_links:
    - from: "supabase/functions/admin-list-users/index.ts:71"
      to: "private.dev_users"
      via: "supabaseAdmin.schema('private').from('dev_users').select('user_id')"
      pattern: "schema\\('private'\\)\\.from\\('dev_users'\\)"
    - from: "private.classify_dev_user trigger"
      to: "auth.users insert/update"
      via: "AFTER INSERT OR UPDATE OF email"
      pattern: "on_auth_user_dev_classify"
---

<objective>
Push D-038 dev-users RLS filter to prod (cataloger Supabase project `wgrknodfxdjtddsirldw`) and expose the `private` schema so the broken `admin-list-users` edge function (v6 returns "Invalid schema: private") can read `private.dev_users` and the admin Users page loads accounts again.

Purpose: prod admin UI is currently dead → blocks UAT (UAT needs the admin UI to provision Specialist A/B accounts) → blocks the rest of the D-051 stacked-PR sweep from landing.
Output: new migration file applied to prod, `config.toml` exposing the private schema, redeployed edge fn, green stacked PR on `urgent/dat-receipt-unique`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Source migration (hub repo — paused milestone, source of truth for D-038)
@/home/spoods/Projects/TPC/tpc-hub/supabase/migrations/20260519000000_dev_users_filter.sql

# Consumer of private.dev_users (the broken edge fn)
@supabase/functions/admin-list-users/index.ts

# Edge fn auth helper — CONFIRMED clean at HEAD (no debug headers); just needs redeploy
@supabase/functions/_shared/verify-admin.ts

# D-046 — Claude owns schema/auth plumbing; mandates Codex adversarial review for RLS/auth migrations
@/home/spoods/Projects/TPC/_workspace/Decisions/D-046-claude-owns-schema-auth-codex-barred.md

<interfaces>
<!-- Existing voice-cataloger config.toml is ABSENT. Hub repo's config.toml is the template
     pattern to mirror (but with `private` added). Confirmed at /home/spoods/Projects/TPC/tpc-hub/supabase/config.toml: -->

From tpc-hub/supabase/config.toml (lines 3-7):
```toml
[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
```

<!-- For prod, the dashboard's "Exposed schemas" setting under
     API Settings is the authoritative knob (config.toml only governs local
     supabase start). Both must be set so a future `supabase db reset` doesn't
     drop `private` exposure locally. -->

From supabase/functions/admin-list-users/index.ts (lines 69-72):
```typescript
const { data: devUsers, error: devUsersError } = await supabaseAdmin
  .schema('private')
  .from('dev_users')
  .select('user_id')
```

<!-- The .schema('private') call requires PostgREST to have 'private' in its
     db-schemas list. Without it: "Invalid schema: private" 400/500. -->
</interfaces>

# Linked project info (don't re-discover):
# - Supabase project ref: wgrknodfxdjtddsirldw (linked via supabase/.temp/project-ref)
# - Supabase CLI: invoke via `npx supabase` (not on PATH); v2.81.3 installed
# - Current branch: urgent/admin-private-schema-d038 (stacked on urgent/dat-receipt-unique = PR #30 tip)
# - Node: fnm-managed v24.15.0 (default)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port D-038 migration + add supabase/config.toml exposing private schema</name>
  <files>supabase/migrations/20260528000000_d038_dev_users_filter.sql, supabase/config.toml</files>
  <action>
    Copy `/home/spoods/Projects/TPC/tpc-hub/supabase/migrations/20260519000000_dev_users_filter.sql` to `supabase/migrations/20260528000000_d038_dev_users_filter.sql` VERBATIM — do not rename, reorder, or simplify any of the 7 RESTRICTIVE policies, the `private.classify_dev_user` trigger function, the `is_dev_user`/`is_dev_email` helpers, or the conditional Josh-seed block. The hub migration's auto-classify logic (null email OR empty email OR `lower(email) = 'josh@potomackco.com'`) is the correct heuristic and must NOT be widened — admins on prod whose emails do not match those three predicates will NOT be misclassified. The conditional seed for `a70ae46e-5d51-47cb-9dff-a6a8a7a08bfd` (Josh) is safe on prod because that auth.users row already exists; the `where exists (select 1 from auth.users where id = ...)` guard makes it a no-op if absent. Use today's timestamp `20260528000000` (NOT `20260519000000` — that ID belongs to the hub repo's history and reusing it would corrupt voice-cataloger migration ordering relative to the existing `20260527000003_*` tip).

    Then create `supabase/config.toml` (file does NOT exist in this repo today; voice-cataloger has been running on Supabase CLI defaults). Mirror the hub repo's `supabase/config.toml` structure but set `schemas = ["public", "graphql_public", "private"]` and `project_id = "tpc-voice-cataloger"`. This governs local `supabase start` behavior only — prod schema exposure is controlled by the Supabase Dashboard (API Settings → Exposed schemas), which gets set automatically when `supabase db push --linked` runs in step 3 IF the linked project config is synced; if dashboard does not auto-update, a manual dashboard tweak is needed (call it out in the SUMMARY).

    No WHY-comments needed in the migration (it's verbatim from hub, and the hub file already has them). Atomic commits per Spoods convention: commit (a) = the migration file alone, commit (b) = `supabase/config.toml` alone. Both end with the `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` trailer.
  </action>
  <verify>
    <automated>diff <(sed 's/20260519/20260528/g' /home/spoods/Projects/TPC/tpc-hub/supabase/migrations/20260519000000_dev_users_filter.sql) supabase/migrations/20260528000000_d038_dev_users_filter.sql && grep -q '"private"' supabase/config.toml && grep -q 'schemas.*private' supabase/config.toml</automated>
  </verify>
  <done>
    `supabase/migrations/20260528000000_d038_dev_users_filter.sql` byte-identical to hub source (modulo the timestamp suffix in comments, if any). `supabase/config.toml` exists with `schemas = ["public", "graphql_public", "private"]`. Two atomic commits on `urgent/admin-private-schema-d038` with the required Co-Authored-By trailer.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Codex adversarial review of the RLS migration (D-046 mandatory gate)</name>
  <what-built>
    The new migration `supabase/migrations/20260528000000_d038_dev_users_filter.sql` is committed but NOT YET PUSHED TO PROD. D-046 requires Codex adversarial review for any schema/auth/RLS change before it lands on a shared database. Per the user's "planning is 95% of the work" rule + the decisions-vault directive, this gate is non-negotiable and must not be auto-advanced.

    Before pausing for review, the executor MUST run `/codex:rescue` (or the project's configured codex-review command) targeted at the new migration file and capture the verdict. Typical adversarial checks Codex will run:
      - Are any of the 7 RESTRICTIVE policies missing a permissive counterpart, accidentally locking authenticated users out of their own rows?
      - Can the `private.is_dev_user` SECURITY DEFINER function be invoked by an attacker to enumerate dev users? (search_path = '' mitigates; confirm the attack model.)
      - Does the `classify_dev_user` trigger silently misclassify a legitimate admin on email change (e.g., admin renames mailbox to a placeholder)?
      - Does the conditional Josh-seed FK-guard race with concurrent `auth.users` deletes?
  </what-built>
  <how-to-verify>
    1. Executor runs the Codex adversarial review on `supabase/migrations/20260528000000_d038_dev_users_filter.sql` and pastes the full verdict into chat.
    2. If Codex flags any HIGH/CRITICAL findings, executor proposes a fix-up commit on the same branch and re-runs Codex until clean — do NOT skip to `db push` with open findings.
    3. User reviews the Codex verdict + any fix-up diff. User confirms with "approved" (or "approved with fixes" + describes remaining concerns).
    4. If the Codex review surfaces a misclassification risk for any non-josh admin on prod, executor must run a read-only DB probe (`select id, email from public.profiles where role='admin'`) BEFORE push and confirm none of those rows would be auto-classified into `private.dev_users`.
  </how-to-verify>
  <resume-signal>User types "approved" (clean) or "approved with fixes" (after re-review) — do not proceed to Task 3 until either is given.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Push migration to prod, redeploy edge fn, smoke test, open stacked PR</name>
  <files>(no source edits; deploys + verification + PR)</files>
  <action>
    Execute the prod push + ship sequence. Stop and surface any failure immediately rather than continuing past it.

    1. **Push the migration to prod.** Run `npx supabase db push --linked` from the repo root. The linked project is `wgrknodfxdjtddsirldw` (confirmed via `supabase/.temp/project-ref`). Capture the output. If it asks for confirmation, accept. If it errors with "migration history mismatch" or similar, STOP and surface the error — do NOT pass `--include-all` or any history-rewriting flag without explicit user approval.

    2. **Verify the schema landed in prod.** Run a read-only probe via the Supabase CLI: `npx supabase db remote --linked --schema private --schema public --type-name lint 2>&1 | head -50` is unreliable; instead use a direct SQL check. Either via `npx supabase db execute --linked` (if subcommand exists in v2.81.3) or by querying through a one-off Node script using the service-role key from `.env.local`. Confirm: (a) `private.dev_users` table exists, (b) `select count(*) from private.dev_users` returns ≥1 row (the Josh seed succeeded), (c) all 7 policies present via `select policyname from pg_policies where policyname like '%_no_dev_data_for_non_dev_viewers'` returns 7 rows. If the CLI lacks a direct-execute subcommand on v2.81.3, fall back to writing a one-shot `scripts/verify-d038-prod.mjs` that imports `@supabase/supabase-js` with the service-role key (read from `.env.local`, do NOT commit the script with the key inlined; it stays uncommitted).

    3. **Verify Supabase Dashboard exposes the `private` schema for PostgREST.** Open https://supabase.com/dashboard/project/wgrknodfxdjtddsirldw/settings/api and confirm "Exposed schemas" includes `private`. If it does not (config.toml does NOT propagate to prod automatically), add it via the dashboard (or via the Management API: `PATCH /v1/projects/{ref}/postgrest -d '{"db_schemas":"public,graphql_public,private"}'` using `SUPABASE_ACCESS_TOKEN`). This is the actual fix for the "Invalid schema: private" error — the migration alone does not expose the schema to PostgREST. Note the result in the SUMMARY (CLI/API path vs. dashboard click).

    4. **Redeploy `admin-list-users` edge function.** Confirm `supabase/functions/_shared/verify-admin.ts` at HEAD has no debug headers (already verified in planning — file is clean). Run `npx supabase functions deploy admin-list-users --project-ref wgrknodfxdjtddsirldw`. Capture the new version number from output.

    5. **Smoke test prod.** Visit https://app.potomackco.com and log in as admin (josh@potomackco.com). Navigate to the admin Users page. Confirm accounts list loads (no "Could not load accounts" banner, no console 500 from `/functions/v1/admin-list-users`). If it fails, capture the response body via DevTools Network tab and surface it — do NOT proceed to PR until smoke clean.

    6. **Build / lint / test the tip.** `npm run build && npm run lint && npm test`. All three must be green. If `npm test` flags the 18 pre-existing `localStorage.clear is not a function` failures noted in STATE.md as deferred-items, exclude them from the gate (they are not in scope for this hotfix); any NEW failures block.

    7. **Open the stacked PR.** Push the branch (`git push -u origin urgent/admin-private-schema-d038`). Then `gh pr create --base urgent/dat-receipt-unique --head urgent/admin-private-schema-d038 --title "urgent: push D-038 dev-users filter + expose private schema (admin Users page fix)" --body @-` with a HEREDOC body covering: (a) why (admin page broken on prod, blocks UAT), (b) what (migration + config.toml + dashboard schema-expose + edge fn redeploy), (c) Codex review verdict reference, (d) smoke test result, (e) stacking note ("Stacks on #30 / `urgent/dat-receipt-unique`; merge after #30 lands"). End the body with the standard "🤖 Generated with Claude Code" trailer.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm test -- --reporter=basic 2>&1 | tail -20</automated>
    <human-check>
      - Prod admin page at https://app.potomackco.com/admin/users loads the accounts list (no error banner, network tab shows admin-list-users 200).
      - `private.dev_users` row count ≥ 1 on prod (Josh seeded).
      - All 7 RESTRICTIVE policies present on prod (verified via pg_policies query).
      - Dashboard "Exposed schemas" includes `private`.
      - PR created against `urgent/dat-receipt-unique` (NOT main) and visible in `gh pr list --base urgent/dat-receipt-unique`.
    </human-check>
  </verify>
  <done>
    Migration applied to prod cleanly; `private` schema exposed to PostgREST (confirmed via dashboard); `admin-list-users` redeployed with a version > v6; smoke test on https://app.potomackco.com admin Users page passes (accounts visible); `npm run build && npm run lint && npm test` green; stacked PR open against `urgent/dat-receipt-unique`. PR URL returned to user in the final message.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase Edge Function | admin user JWT crosses here; verify-admin.ts is the gate |
| Edge Function → Postgres (service role) | service-role bypasses RLS; relies on private.dev_users for filter logic |
| Postgres anon JWT → public tables | the 7 new RESTRICTIVE policies AND with existing permissives to filter dev rows |
| Supabase Dashboard → PostgREST config | "Exposed schemas" setting controls whether `.schema('private')` calls succeed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-hm6-01 | Information Disclosure | RESTRICTIVE policies on 7 tables (sessions, items, photos, export_history, ui_interactions, analytics_events, profiles) | mitigate | Codex adversarial review in Task 2 (D-046 mandatory gate); each policy uses `created_by = auth.uid() OR not is_dev_user(created_by)` so users keep visibility of own rows; if Codex flags any path that locks legit users out, fix before push. |
| T-hm6-02 | Elevation of Privilege | `private.classify_dev_user` trigger auto-classifying admins as dev users | mitigate | Classifier matches only (null email OR empty email OR `lower(email)='josh@potomackco.com'`) — verified the heuristic; Task 2 mandates a read-only `select id, email from profiles where role='admin'` probe to confirm no prod admin matches before push. |
| T-hm6-03 | Tampering | Migration history corruption from timestamp collision with hub repo's `20260519000000_dev_users_filter.sql` | mitigate | Use today's timestamp `20260528000000` per Task 1; migration history in this repo currently tops out at `20260527000003_*` so the new file orders correctly after the tip. |
| T-hm6-04 | Information Disclosure | `private.is_dev_user` SECURITY DEFINER function callable by any authenticated client | accept | `set search_path = ''` mitigates schema-shadow attacks; function only returns boolean, no row data leakage; threat captured for Codex review. |
| T-hm6-05 | Tampering | `npx supabase db push --linked` could rewrite prod history if `--include-all` were passed | mitigate | Task 3 explicitly forbids `--include-all` / history-rewriting flags without user approval; stop on any "history mismatch" error and surface to user. |
| T-hm6-06 | Information Disclosure | service-role key from `.env.local` used in optional verification probe script | mitigate | If a one-shot verify script is needed, it stays UNCOMMITTED and reads the key from `.env.local`; never hardcoded; deleted after use. |
| T-hm6-SC | Tampering | npm/supabase CLI installs | accept | No new npm packages added in this hotfix (only `npx supabase` already-installed v2.81.3); migration is pure SQL. Package legitimacy gate not triggered. |
</threat_model>

<verification>
- `private.dev_users` table exists on prod with ≥ 1 row (Josh seed)
- 7 RESTRICTIVE policies present on prod (queryable via `select policyname from pg_policies where policyname ilike '%_no_dev_data_for_non_dev_viewers'`)
- Supabase Dashboard → API Settings → Exposed schemas includes `private`
- `admin-list-users` edge fn deployed at a version > v6, reading from `private.dev_users` successfully
- https://app.potomackco.com admin Users page loads accounts (no 500)
- Codex adversarial review (D-046) verdict captured in chat + referenced in PR body
- `npm run build && npm run lint && npm test` green on tip (excluding 18 pre-existing `localStorage.clear` failures per STATE.md deferred-items)
- PR open against `urgent/dat-receipt-unique` (not main); stacked correctly on #30
</verification>

<success_criteria>
- Prod admin Users page loads the account list without error
- D-038 RLS filter active on prod (7 policies + `private.dev_users` + classify trigger)
- `private` schema exposed to PostgREST (dashboard setting + local config.toml for parity)
- `admin-list-users` redeployed with clean code (no debug headers)
- Codex adversarial review ran clean (or fixes applied + re-reviewed clean) BEFORE prod push
- 4 atomic commits on `urgent/admin-private-schema-d038`: (a) migration, (b) config.toml, (c) any Codex fix-ups, (d) PR-artifacts if any
- Stacked PR open and visible against `urgent/dat-receipt-unique`
</success_criteria>

<output>
Create `.planning/quick/260528-hm6-push-d-038-dev-users-filter-migration-to/260528-hm6-SUMMARY.md` when done.
Include in SUMMARY: migration commit SHA, config.toml commit SHA, Codex verdict (clean / fixes / re-clean), prod policy count (must be 7), prod dev_users row count, edge fn version, smoke-test screenshot path or curl output, PR URL.
</output>
