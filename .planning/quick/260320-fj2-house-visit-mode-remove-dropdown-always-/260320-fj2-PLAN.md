---
phase: quick-260320-fj2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ItemCard.tsx
  - src/components/ItemList.tsx
  - src/pages/ItemEntry.tsx
autonomous: true
requirements: [HV-NAV, HV-CARD, HV-ARROWS]

must_haves:
  truths:
    - "Tapping an item card (not chevron) in house mode navigates to /session/:sessionId/item/:itemId"
    - "Tapping the chevron in house mode toggles a read-only field summary (title, description, measurements, condition, estimate, category) -- no mic, no transcript, no edit, no delete"
    - "No mic button appears on ItemCard in house mode"
    - "Sale mode ItemCard behavior is completely unchanged"
    - "Item page in house mode shows a 'Back to Session' link (always goes to session, not previous item)"
    - "Left arrow navigates to previous item, disabled on first item"
    - "Right arrow navigates to next item, creates new item on last item"
    - "Left/right arrows positioned at middle edges of the screen"
  artifacts:
    - path: "src/components/ItemCard.tsx"
      provides: "House mode: click-to-navigate + chevron-only summary toggle"
    - path: "src/pages/ItemEntry.tsx"
      provides: "Back-to-session header, left/right navigation arrows"
  key_links:
    - from: "src/components/ItemCard.tsx"
      to: "/session/:sessionId/item/:itemId"
      via: "navigate() on card body click (house mode only)"
      pattern: "navigate.*session.*item"
    - from: "src/pages/ItemEntry.tsx"
      to: "/session/:sessionId"
      via: "Back to Session link"
      pattern: "navigate.*session/${sessionId}"
    - from: "src/pages/ItemEntry.tsx"
      to: "createBlankItem"
      via: "Right arrow on last item creates new item"
      pattern: "createBlankItem"
---

<objective>
Overhaul house visit mode UX: item cards navigate to full item page on tap, chevron toggles read-only summary, remove inline mic/editing from cards, and add back-to-session + left/right item navigation on the item page.

Purpose: House visit workflow needs fast item-to-item navigation via the full item page (photos, recording, details) rather than inline editing in a dropdown.
Output: Modified ItemCard, ItemList, and ItemEntry components.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/ItemCard.tsx
@src/components/ItemList.tsx
@src/pages/ItemEntry.tsx
@src/pages/SessionDetail.tsx
@.planning/quick/260320-fj2-house-visit-mode-remove-dropdown-always-/260320-fj2-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ItemCard house mode -- navigate on tap, chevron-only summary, remove mic</name>
  <files>src/components/ItemCard.tsx</files>
  <action>
Modify ItemCard to handle house mode differently from sale mode. Sale mode is COMPLETELY UNCHANGED.

For house mode (`item.mode === "house"`):

1. **Split click handlers on collapsed row:**
   - The chevron arrow gets its own click handler with `e.stopPropagation()` that calls `onToggle` (existing behavior).
   - The rest of the collapsed row div, when clicked, navigates to `/session/${sessionId}/item/${item.id}` instead of calling `onToggle`.
   - Implementation: Change the main row div's `onClick` to check `item.mode === "house"` -- if house, navigate; if sale, call `onToggle` as before. Then wrap the chevron SVG in a `button` with `onClick={(e) => { e.stopPropagation(); onToggle(); }}` so chevron still toggles expand in house mode.

2. **Remove mic button in house mode:**
   - The inline mic button (lines ~193-222) should only render when `item.mode !== "house"`. Add `item.mode !== "house"` to the existing `!readOnly && !isQueued && !isProcessing` condition.

3. **Simplify expanded section for house mode:**
   - When `item.mode === "house"` AND expanded, show ONLY read-only field values (no EditableField, no transcript, no delete button, no retry button, no "Photos & Details" button). Display a simple list:
     - Title, Description, Measurements, Condition, Estimate, Category
     - Each as a label + value pair. If value is null/empty, skip that field entirely.
     - Use static text (not EditableField). Style: label in `text-xs font-medium text-gray-500 uppercase` and value in `text-sm text-gray-900 dark:text-gray-100`.
   - When `item.mode !== "house"` (sale mode), keep the ENTIRE existing expanded section exactly as-is (EditableField components, transcript, retry, delete, etc.).

Keep all existing imports. The `useAudioRecorder`, `processAudioWithAi`, and editing-related code can remain imported -- they are still used in sale mode.
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>In house mode: tapping card body navigates to item page, chevron toggles read-only summary without mic/editing/transcript, sale mode completely unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: ItemEntry -- back-to-session header and left/right navigation arrows</name>
  <files>src/pages/ItemEntry.tsx</files>
  <action>
Modify ItemEntry page to improve house visit mode navigation.

1. **Replace BackButton behavior:**
   - The existing `BackButton` component navigates to previous item or session. Change it so it ALWAYS navigates to `/session/${sessionId}` with label "Back to Session" (remove the previous-item logic entirely). This is simpler and matches the user decision -- left arrow handles item-to-item navigation now.

2. **Add left/right navigation arrows at screen edges:**
   - Add two fixed-position arrow buttons at the vertical center of the screen (left edge and right edge).
   - Only render these arrows when `mode === "house"` AND the item exists (not `isNewItem`).
   - Style: `fixed top-1/2 -translate-y-1/2` with `left-1` for left arrow and `right-1` for right arrow. Each is a circular button (`w-10 h-10 rounded-full bg-white dark:bg-gray-800 shadow-lg border border-gray-200 dark:border-gray-700 flex items-center justify-center`). Use chevron-left and chevron-right SVG icons (same style as existing chevrons in the codebase).
   - **Left arrow:** Navigates to the previous item by sort_order. Find the item with the highest sort_order that is less than `item.sort_order` from the `items` array. If no previous item exists (first item), render the button with `opacity-30 pointer-events-none` (visually disabled).
   - **Right arrow:** If there is a next item (lowest sort_order greater than current), navigate to it. If current item is the LAST item (no next item exists), call `createBlankItem(sessionId!, mode)` and navigate to the new item's page (`/session/${sessionId}/item/${newId}`). Use a `navigatingRef` guard (like existing `isCreatingNext`) to prevent double-creation.
   - Navigation: `navigate(\`/session/${sessionId}/item/${targetId}\`)` for existing items.

3. **Compute prev/next items:**
   - `const prevItem = items.filter(i => i.sort_order < item.sort_order).sort((a, b) => b.sort_order - a.sort_order)[0] ?? null;`
   - `const nextItem = items.filter(i => i.sort_order > item.sort_order).sort((a, b) => a.sort_order - b.sort_order)[0] ?? null;`
   - Place these computations in the component body (not in BackButton).

4. **Keep existing "Next Item" button at bottom** -- it still works for both modes. The arrows are an additional navigation affordance for house mode.
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Item page shows "Back to Session" link (always to session detail), left/right arrows at screen edges for house mode item-to-item navigation, right arrow on last item creates new item.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. Manual test: Open a house visit session, tap an item card body -- should navigate to item page
3. Manual test: Tap chevron on house mode card -- should toggle read-only summary (no mic, no editing)
4. Manual test: On item page, left/right arrows work, right arrow on last item creates new item
5. Manual test: Sale mode cards are completely unchanged (editable dropdown, mic button, etc.)
</verification>

<success_criteria>
- House mode item cards navigate to full item page on body tap
- Chevron toggles bare read-only summary in house mode
- No mic button on house mode item cards
- Back to Session always goes to session detail page
- Left/right arrows at screen edges for house mode item navigation
- Right arrow on last item creates new item and navigates to it
- Sale mode is completely untouched
</success_criteria>

<output>
After completion, create `.planning/quick/260320-fj2-house-visit-mode-remove-dropdown-always-/260320-fj2-SUMMARY.md`
</output>
