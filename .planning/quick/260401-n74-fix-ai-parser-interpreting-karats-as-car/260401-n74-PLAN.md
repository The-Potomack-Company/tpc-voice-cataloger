---
phase: quick
plan: 260401-n74
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/gemini.ts
  - src/services/geminiSchema.ts
autonomous: true
must_haves:
  truths:
    - "AI never interprets karats/carats as carrots in auction context"
    - "Measurements field correctly formats karat values as Nkt"
  artifacts:
    - path: "src/services/gemini.ts"
      provides: "System prompt with explicit karat disambiguation"
      contains: "NEVER interpret.*carrot"
    - path: "src/services/geminiSchema.ts"
      provides: "Schema description reinforcing karat context"
      contains: "gold purity"
  key_links: []
---

<objective>
Fix the AI parser interpreting "karats" (gold purity) as "carrots" (vegetable) in audio transcription. Add explicit disambiguation to the system prompt and schema so the Gemini model always defaults to karats/carats in this auction house context.

Purpose: Auction catalog entries must never contain "carrots" when the auctioneer says "karats"
Output: Updated system prompt and schema with karat disambiguation
</objective>

<context>
@src/services/gemini.ts
@src/services/geminiSchema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add karat disambiguation to system prompt and schema</name>
  <files>src/services/gemini.ts, src/services/geminiSchema.ts</files>
  <action>
In src/services/gemini.ts SYSTEM_PROMPT:

1. Add a new rule in the CRITICAL RULES section (after rule 4, as rule 5):
   "5. AUCTION CONTEXT: This is an auction house application. Any spoken word that sounds like 'karats', 'carats', or 'carrots' refers to GOLD/GEM PURITY (karats), NEVER the vegetable. Always output karat values in measurements as 'Nkt' (e.g., '14kt', '18kt', '24kt'). Similarly, 'karat' or 'carat' in descriptions refers to precious metal/gem purity."

2. In the measurements instruction block (lines 17-23), after the existing "Karats: 'Nkt' (e.g., '18kt')." line (line 21), add:
   "IMPORTANT: 'karats'/'carats'/'carrots' always means gold/gem purity in this auction context. Never interpret as the vegetable."

In src/services/geminiSchema.ts:

3. Update the measurements field .describe() string (line 27) to append before the "Return null" sentence:
   "IMPORTANT: In auction context, 'karats'/'carats'/'carrots' always means gold/gem purity (Nkt), never the vegetable. "
  </action>
  <verify>
    <automated>grep -n "carrot\|gold.gem purity\|AUCTION CONTEXT" src/services/gemini.ts src/services/geminiSchema.ts</automated>
  </verify>
  <done>
    - SYSTEM_PROMPT contains explicit rule that karats/carats/carrots = gold purity, never vegetable
    - Measurements instruction block reinforces karat disambiguation
    - Schema description for measurements field includes karat disambiguation
    - Existing functionality unchanged (no structural modifications)
  </done>
</task>

</tasks>

<verification>
- grep confirms "carrot" disambiguation text exists in both files
- grep confirms "AUCTION CONTEXT" rule in gemini.ts
- No TypeScript errors: npx tsc --noEmit (spot check)
</verification>

<success_criteria>
Both gemini.ts and geminiSchema.ts contain explicit disambiguation instructing the AI that karats/carats/carrots always means gold/gem purity in the auction context, never the vegetable.
</success_criteria>

<output>
After completion, create `.planning/quick/260401-n74-fix-ai-parser-interpreting-karats-as-car/260401-n74-SUMMARY.md`
</output>
