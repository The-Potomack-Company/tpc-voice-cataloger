---
phase: quick-260407-ke7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/gemini.ts
  - src/services/geminiSchema.ts
  - src/tests/gemini-schema.test.ts
autonomous: true
requirements: [KEK-01, KEK-02, KEK-03]

must_haves:
  truths:
    - "AI distinguishes gold karat (purity) from gem carat (weight) using context clues"
    - "Gold purity outputs as Nkt in measurements (existing behavior preserved)"
    - "Gem weight outputs as Nct in measurements (new format)"
    - "Description text uses correct spelling: karat for gold, carat for gems"
    - "Speaker explicit clarification overrides context-based inference"
  artifacts:
    - path: "src/services/gemini.ts"
      provides: "Updated system prompt with karat vs carat disambiguation rules"
      contains: "carat"
    - path: "src/services/geminiSchema.ts"
      provides: "Updated schema docs reflecting both Nkt and Nct formats"
      contains: "Nct"
    - path: "src/tests/gemini-schema.test.ts"
      provides: "Test cases covering gem carat measurements"
      contains: "ct"
  key_links:
    - from: "src/services/gemini.ts"
      to: "Gemini AI model"
      via: "system prompt instructions"
      pattern: "carat.*ct|karat.*kt"
---

<objective>
Update the AI system prompt and schema to differentiate between gold karat (purity, Nkt) and gem carat (weight, Nct). Currently all karat/carat references are treated as gold purity. The AI needs context-based disambiguation with speaker override priority.

Purpose: Accurate jewelry catalog entries distinguishing gold purity from gemstone weight.
Output: Updated prompt, schema, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/services/gemini.ts
@src/services/geminiSchema.ts
@src/tests/gemini-schema.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add carat test cases to gemini-schema.test.ts</name>
  <files>src/tests/gemini-schema.test.ts</files>
  <behavior>
    - Test: validates measurements with gem carat weight — input with measurements "1.5ct" parses successfully
    - Test: validates measurements mixing karats and carats — input with measurements "2.1 oz., 14kt, 1.5ct" parses successfully
    - Test: validates description uses "carat" spelling for gems — description "1.5 carat diamond ring" parses successfully
    - Test: validates description uses "karat" spelling for gold — description "14 karat gold band" parses successfully
  </behavior>
  <action>
Add new test cases to gemini-schema.test.ts after the existing "validates measurements with weight and karats" test:

1. A test for gem carat in measurements: item with measurements "1.5ct, 0.8 oz." — a gemstone-only item.
2. A test for mixed karat+carat: measurements "14kt, 1.5ct, 2.1 oz." — a gold ring with a gemstone.
3. A test for correct spelling in descriptions: one with "14 karat gold" and another with "1.5 carat diamond" — both should parse fine (schema is string-based so these pass, but they document expected AI output format).

These tests validate the schema accepts the new Nct format alongside existing Nkt.
  </action>
  <verify>
    <automated>npx vitest run src/tests/gemini-schema.test.ts</automated>
  </verify>
  <done>New test cases pass covering gem carat (Nct) format in measurements and correct karat/carat spelling in descriptions.</done>
</task>

<task type="auto">
  <name>Task 2: Update system prompt and schema with karat vs carat disambiguation</name>
  <files>src/services/gemini.ts, src/services/geminiSchema.ts</files>
  <action>
**In src/services/gemini.ts SYSTEM_PROMPT:**

1. Replace the current karat-only measurement instruction (line 22 area: `Karats: "Nkt"`) with dual-format instructions:
   ```
   Gold purity (karat): "Nkt" (e.g., "14kt", "18kt", "24kt"). Common karat numbers: 10, 14, 18, 22, 24.
   Gem weight (carat): "Nct" (e.g., "1.5ct", "3ct", "0.25ct"). Typically fractional/decimal numbers associated with gemstones.
   ```

2. Replace the IMPORTANT line (line 23 area) and rule 5 (line 33 area) with context-based disambiguation rules:
   ```
   KARAT vs CARAT DISAMBIGUATION:
   - 'karat'/'carat'/'carrot' — NEVER the vegetable in auction context.
   - Gold purity (KARAT): Use "Nkt" when the number is a common gold purity (10, 14, 18, 22, 24) AND context involves gold, metal, or jewelry without specific gemstone references.
   - Gem weight (CARAT): Use "Nct" when the number is fractional/decimal AND context involves gemstones (diamond, ruby, sapphire, emerald, etc.).
   - SPEAKER OVERRIDE: If the speaker explicitly clarifies (e.g., "that's carats not karats" or "gem weight"), follow their clarification regardless of context clues.
   - In descriptions, use correct spelling: "karat" for gold purity, "carat" for gem weight. Normalize regardless of what the speaker actually said.
   - When ambiguous and no speaker clarification, default to karat (gold purity) as the more common auction usage.
   ```

3. Update the measurements example in the combine line (line 24 area) to show both:
   ```
   Example: "4 x 6 in. (10.2 x 15.2 cm.), 2.5 oz., 18kt" or "1.5ct, 0.8 oz."
   ```

**In src/services/geminiSchema.ts:**

Update the measurements field `.describe()` string (line 27 area) to document both formats:
- Change `Karats: 'Nkt'` to `Gold purity (karat): 'Nkt' (e.g., '14kt'). Gem weight (carat): 'Nct' (e.g., '1.5ct').`
- Update the example to include both: `Example: '4 x 6 in. (10.2 x 15.2 cm.), 2.5 oz., 18kt' or '1.5ct, 0.8 oz.'`
- Change the IMPORTANT note to reflect disambiguation: `In auction context, 'karats'/'carats'/'carrots' means gold purity (Nkt) or gem weight (Nct), never the vegetable. Use context to determine which.`
  </action>
  <verify>
    <automated>npx vitest run src/tests/gemini-schema.test.ts && npx vitest run src/tests/formatMeasurements.test.ts</automated>
  </verify>
  <done>System prompt distinguishes karat (Nkt for gold) from carat (Nct for gems) with context-based disambiguation, speaker override priority, and correct spelling enforcement in descriptions. Schema docs reflect both formats. All existing tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audio input -> AI | Speaker dictation interpreted by Gemini model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ke7-01 | Tampering | System prompt | accept | Prompt changes are text-only instructions to AI; no code injection vector. Output validated by Zod schema. |
</threat_model>

<verification>
1. `npx vitest run src/tests/gemini-schema.test.ts` — all tests pass including new carat cases
2. `npx vitest run src/tests/formatMeasurements.test.ts` — existing measurement tests unchanged
3. Manual: review system prompt reads naturally and covers disambiguation rules
</verification>

<success_criteria>
- System prompt contains karat vs carat disambiguation rules with context clues and speaker override
- Schema documentation reflects both Nkt and Nct formats
- Gold purity uses "Nkt", gem weight uses "Nct" in AI instructions
- Description spelling rules: "karat" for gold, "carat" for gems
- All existing tests pass, new carat test cases pass
</success_criteria>

<output>
After completion, create `.planning/quick/260407-ke7-jewelry-karat-vs-carat-differentiation-i/260407-ke7-SUMMARY.md`
</output>
