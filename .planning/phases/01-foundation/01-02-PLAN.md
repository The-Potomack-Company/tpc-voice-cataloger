---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/App.tsx
  - src/main.tsx
  - src/layouts/AppLayout.tsx
  - src/pages/Sessions.tsx
  - src/pages/NewSession.tsx
  - src/pages/Settings.tsx
  - src/components/InstallBanner.tsx
  - src/components/Walkthrough.tsx
  - src/stores/uiStore.ts
  - src/tests/layout.test.ts
autonomous: false
requirements:
  - UX-02
  - UX-03
  - UX-04
must_haves:
  truths:
    - "Bottom tab bar shows Sessions, New, and Settings tabs with 48px+ tap targets"
    - "Tapping each tab navigates to the correct page via pathname routing"
    - "App layout fills the viewport without horizontal scroll in both portrait and landscape"
    - "Recording and navigation controls are at bottom of screen, reachable with one thumb"
    - "Install banner appears for non-installed users and is dismissable"
    - "First-time users see a welcome walkthrough before the main UI"
    - "Light and dark mode follow system preference automatically"
  artifacts:
    - path: "src/layouts/AppLayout.tsx"
      provides: "Main app shell with bottom tab bar and content outlet"
      min_lines: 40
    - path: "src/pages/Sessions.tsx"
      provides: "Sessions list page with empty-state CTA"
      min_lines: 20
    - path: "src/pages/NewSession.tsx"
      provides: "Mode picker page (house visit vs sale cataloging)"
      min_lines: 20
    - path: "src/pages/Settings.tsx"
      provides: "Settings page stub"
      min_lines: 10
    - path: "src/components/InstallBanner.tsx"
      provides: "PWA install banner with iOS fallback instructions"
      min_lines: 30
    - path: "src/components/Walkthrough.tsx"
      provides: "2-3 step welcome walkthrough for first-time users"
      min_lines: 30
    - path: "src/stores/uiStore.ts"
      provides: "Zustand store for walkthrough state"
      exports: ["useUIStore"]
  key_links:
    - from: "src/App.tsx"
      to: "src/layouts/AppLayout.tsx"
      via: "React Router Route element"
      pattern: "AppLayout"
    - from: "src/layouts/AppLayout.tsx"
      to: "react-router"
      via: "NavLink for tab navigation, Outlet for page content"
      pattern: "NavLink.*Outlet"
    - from: "src/layouts/AppLayout.tsx"
      to: "src/components/InstallBanner.tsx"
      via: "Component rendered at top of layout"
      pattern: "InstallBanner"
    - from: "src/pages/Sessions.tsx"
      to: "src/stores/uiStore.ts"
      via: "Check walkthrough completion to show walkthrough or session list"
      pattern: "useUIStore"
---

<objective>
Build the mobile-optimized app shell with bottom tab bar navigation, page stubs, PWA install banner, welcome walkthrough, and Zustand UI state. Verify on device that the PWA installs and the layout is thumb-friendly.

Purpose: Delivers the visible, interactive shell that auctioneers will use daily. After this plan, the app looks and feels like a real mobile app with working navigation, install experience, and mobile-first layout.
Output: A fully navigable PWA shell with 3 tabs, install banner, walkthrough, and dark mode support.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs that this plan depends on -->

From src/db/types.ts:
```typescript
export interface Session {
  id?: number;
  name: string;
  mode: "house" | "sale";
  createdAt: Date;
  updatedAt: Date;
}
```

From src/db/index.ts:
```typescript
export { db }; // Dexie instance with tables: sessions, houseVisitItems, saleItems, photos, audio
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build app shell with bottom tab bar, routing, pages, install banner, and walkthrough</name>
  <files>src/App.tsx, src/layouts/AppLayout.tsx, src/pages/Sessions.tsx, src/pages/NewSession.tsx, src/pages/Settings.tsx, src/components/InstallBanner.tsx, src/components/Walkthrough.tsx, src/stores/uiStore.ts, src/tests/layout.test.ts</files>
  <behavior>
    - Test: AppLayout renders a nav element with 3 NavLink children (Sessions, New, Settings)
    - Test: Each NavLink has min-h-12 and min-w-12 classes (48px tap targets)
    - Test: Tab bar nav has pb-[env(safe-area-inset-bottom)] for notched devices
    - Test: Root container uses h-dvh class (not h-screen)
    - Test: Sessions page renders without crashing
    - Test: NewSession page renders mode picker with "House Visit" and "Sale Cataloging" options
  </behavior>
  <action>
    1. Create src/stores/uiStore.ts exactly as specified in RESEARCH.md Pattern 6:
       - hasCompletedWalkthrough (boolean, persisted to localStorage via zustand/persist)
       - completeWalkthrough action
       - resetWalkthrough action (sets hasCompletedWalkthrough to false)
       - Store name: "tpc-ui-state"

    2. Create src/layouts/AppLayout.tsx following RESEARCH.md Pattern 4:
       - Root div: `flex flex-col h-dvh bg-white dark:bg-gray-900`
       - InstallBanner component at the top (above main)
       - Main content area: `flex-1 overflow-y-auto` with Outlet
       - Bottom nav: `flex items-center justify-around` with border-t, bg-white dark:bg-gray-900, pb-[env(safe-area-inset-bottom)]
       - Three NavLink tabs: "/" (Sessions), "/new" (New), "/settings" (Settings)
       - Each NavLink: `flex flex-col items-center py-3 px-4 min-h-12 min-w-12`
       - Active state: text-accent color. Inactive: text-gray-500 dark:text-gray-400
       - Use simple SVG icons inline for each tab (list icon for Sessions, plus-circle for New, gear for Settings). Keep icons simple — no external icon library needed for 3 icons.
       - Add pt-[env(safe-area-inset-top)] to the root div for notched device top padding

    3. Create src/pages/Sessions.tsx:
       - Check useUIStore().hasCompletedWalkthrough
       - If false: render Walkthrough component
       - If true and no sessions exist: render empty state with large "Start New Session" CTA button (blue accent, min-h-12, centered). The CTA links to "/new".
       - If true and sessions exist: render a placeholder list (actual session list comes in Phase 3). For now, show the empty state since no sessions can be created yet.
       - Use landscape-aware layout: `portrait:px-4 landscape:px-8 landscape:max-w-3xl landscape:mx-auto`

    4. Create src/pages/NewSession.tsx:
       - Mode picker with two large tap-friendly cards/buttons:
         a. "House Visit" — icon + description ("Catalog items with photos during a house visit")
         b. "Sale Cataloging" — icon + description ("Enter receipt numbers and dictate items for a sale")
       - Each card: min-h-[80px], full width, rounded, border, hover state. 48px+ tap targets.
       - Cards are placeholder — tapping them does nothing yet (Phase 4 adds functionality). Show a toast or subtle message "Coming in a future update" or simply do nothing on tap.
       - Use responsive grid: `grid gap-4 portrait:grid-cols-1 landscape:grid-cols-2`

    5. Create src/pages/Settings.tsx:
       - Simple settings stub with:
         a. "About" section: "TPC Catalog v1.0 — Speech-to-catalog tool for auctioneers"
         b. "Storage" section: placeholder showing "Database: Active" (Phase 3+ can add real stats)
         c. "Reset Walkthrough" button that calls useUIStore().resetWalkthrough() (allows re-viewing walkthrough)
       - Use consistent padding and 48px+ tap targets on all interactive elements

    6. Create src/components/InstallBanner.tsx following RESEARCH.md Pattern 5:
       - Capture beforeinstallprompt event for Chrome/Android
       - Detect iOS via user agent and show manual instructions: "To install, tap the share icon then 'Add to Home Screen'"
       - Detect standalone mode — if already installed, don't show banner
       - Dismissable — save dismiss state to localStorage ("install-banner-dismissed")
       - Banner style: bg-accent text-white, flex items-center justify-between, px-4 py-3
       - Install button: bg-white text-accent, min-h-10, rounded
       - Dismiss X button: min-h-10 min-w-10 for tap target

    7. Create src/components/Walkthrough.tsx:
       - 2-3 step walkthrough (Claude's discretion on exact content):
         Step 1: "Welcome to TPC Catalog" — brief intro about dictating catalog entries by voice
         Step 2: "Two Modes" — explain House Visit (photos + voice) and Sale Cataloging (receipt numbers + voice)
         Step 3: "Get Started" — tap to complete walkthrough
       - Each step: centered content, large "Next" / "Get Started" button (min-h-12, blue accent)
       - Progress dots at bottom showing current step
       - On completion: call useUIStore().completeWalkthrough()
       - Full-screen overlay or page replacement (not a modal)
       - Clean, minimal design consistent with Notion/Linear aesthetic

    8. Update src/App.tsx with React Router routes:
       ```tsx
       <Routes>
         <Route element={<AppLayout />}>
           <Route index element={<SessionsPage />} />
           <Route path="new" element={<NewSessionPage />} />
           <Route path="settings" element={<SettingsPage />} />
         </Route>
       </Routes>
       ```

    9. Create src/tests/layout.test.ts:
       - Test AppLayout renders nav with 3 links
       - Test each link has proper min-h/min-w classes for 48px targets
       - Test root container uses h-dvh
       - Test Sessions page renders
       - Test NewSession page renders mode picker options

    IMPORTANT: All interactive elements must have min-h-12 min-w-12 (48px tap targets) per UX-02.
    IMPORTANT: Use h-dvh (not h-screen) for the root container — h-screen is broken on mobile browsers.
    IMPORTANT: Apply env(safe-area-inset-bottom) to tab bar and env(safe-area-inset-top) to top of app for notched iPhones.
    IMPORTANT: All text and backgrounds must have dark: variants for dark mode support.
    IMPORTANT: Use NavLink from react-router (not Link) for active state styling on tabs.
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass. App renders with 3-tab bottom navigation. Each tab navigates to its page. All tap targets are 48px+. Layout uses h-dvh and safe-area-insets. Dark mode classes present on all elements. Install banner shows with platform-specific behavior. Walkthrough appears for first-time users and is dismissable. npm run build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify PWA shell on device</name>
  <files>N/A</files>
  <action>
    Human verifies the complete PWA shell: bottom tab bar (Sessions/New/Settings), mobile-optimized layout, install banner, welcome walkthrough, and dark/light mode following system preference. The app must be installable as a PWA.

    Steps:
    1. Run `npm run dev` and open http://localhost:5173 on your phone (same network) or use Chrome DevTools mobile emulation
    2. Verify the welcome walkthrough appears on first visit (2-3 steps)
    3. Complete the walkthrough — verify you see the Sessions page with "Start New Session" CTA
    4. Tap each tab (Sessions, New, Settings) — verify navigation works and active tab is highlighted in blue
    5. On the "New" tab, verify you see House Visit and Sale Cataloging mode cards
    6. Verify all buttons and tabs are comfortably tappable (48px+ targets)
    7. Rotate your phone — verify layout adjusts without horizontal scrolling
    8. Toggle system dark mode — verify the app switches to dark theme
    9. Check the install banner appears (on Android Chrome: install button; on iOS Safari: share instructions)
    10. Try installing the PWA to your home screen — verify it opens in standalone mode with "TPC Catalog" label and blue status bar
  </action>
  <verify>Human types "approved" or describes issues</verify>
  <done>User confirms PWA installs correctly, layout is thumb-friendly in both orientations, tabs navigate properly, dark mode works, and install banner appears with platform-appropriate instructions.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass (db, manifest, layout)
2. `npm run build` — succeeds without errors
3. App is installable as PWA on both iOS and Android
4. Bottom tab bar has 3 tabs with 48px+ tap targets
5. Layout works in both portrait and landscape without horizontal scroll
6. Dark/light mode follows system preference
7. Welcome walkthrough appears on first visit
8. Install banner shows platform-appropriate instructions
</verification>

<success_criteria>
- Human verifies the PWA shell looks and feels like a real mobile app
- Navigation between all 3 tabs works smoothly
- Install experience works on target devices
- Layout is thumb-friendly and orientation-responsive
- All automated tests from Plan 01 and Plan 02 still pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
