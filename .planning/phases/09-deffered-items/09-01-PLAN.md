---
phase: 09-deffered-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/types.ts
  - src/db/index.ts
  - src/utils/estimateFormat.ts
  - src/tests/estimate-format.test.ts
  - src/tests/db.test.ts
  - src/tests/gemini-schema.test.ts
  - src/tests/import-receipts.test.ts
  - src/tests/export-history.test.ts
  - src/tests/sessions.test.ts
autonomous: true
requirements:
  - MIGRATE-01
must_haves:
  truths:
    - "Dexie v4 migration runs without errors on app load"
    - "EstimateRange type is available for use across the codebase"
    - "ExportHistoryRecord type is available for use across the codebase"
    - "Session type includes optional archivedAt field"
    - "Existing items with string estimates are migrated to null"
    - "formatEstimate and estimateToExportString produce correct output"
  artifacts:
    - path: "src/db/types.ts"
      provides: "EstimateRange, ExportHistoryRecord, updated Session/HouseVisitItem/SaleItem types"
      contains: "EstimateRange"
    - path: "src/db/index.ts"
      provides: "Dexie v4 schema with exportHistory table"
      contains: "version(4)"
    - path: "src/utils/estimateFormat.ts"
      provides: "formatEstimate, estimateToExportString helpers"
      exports: ["formatEstimate", "estimateToExportString"]
    - path: "src/tests/estimate-format.test.ts"
      provides: "Test scaffolds for estimate formatting"
    - path: "src/tests/import-receipts.test.ts"
      provides: "Test scaffolds for receipt import"
  key_links:
    - from: "src/db/index.ts"
      to: "src/db/types.ts"
      via: "import types for EntityTable declarations"
      pattern: "import.*types"
---

<objective>
Foundation: Dexie v4 migration, updated types, estimate utilities, and Wave 0 test scaffolds for Phase 9.

Purpose: All three Phase 9 features (receipt import, estimate extraction, export history/archive) depend on shared type changes and a Dexie schema migration. This plan establishes the foundation so subsequent plans can build independently.

Output: Updated types, v4 migration, estimate formatting utilities, and test scaffold files for the entire phase.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-deffered-items/09-CONTEXT.md
@.planning/phases/09-deffered-items/09-RESEARCH.md

@src/db/types.ts
@src/db/index.ts
@src/tests/db.test.ts
@src/tests/gemini-schema.test.ts
@src/tests/sessions.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, migration, and estimate utilities</name>
  <files>src/db/types.ts, src/db/index.ts, src/utils/estimateFormat.ts, src/tests/estimate-format.test.ts, src/tests/db.test.ts</files>
  <behavior>
    - formatEstimate({ low: 300, high: 500 }) returns "$300-$500"
    - formatEstimate(null) returns ""
    - formatEstimate({ low: 1000, high: 2000 }) returns "$1,000-$2,000" (uses toLocaleString)
    - estimateToExportString({ low: 300, high: 500 }) returns "$300-$500"
    - estimateToExportString(null) returns undefined
    - Dexie v4 migration adds exportHistory table
    - Dexie v4 migration sets existing string estimates to null
  </behavior>
  <action>
    1. Update `src/db/types.ts`:
       - Add `EstimateRange` interface: `{ low: number; high: number }`
       - Add `ExportHistoryRecord` interface: `{ id?: number; sessionId: number; sessionName: string; sessionMode: "house" | "sale"; itemCount: number; exportedAt: Date }`
       - Add `archivedAt?: Date` to `Session` interface
       - Change `estimate` field on both `HouseVisitItem` and `SaleItem` from `string` to `EstimateRange | null` (remove the `?` optional marker, make it explicitly `EstimateRange | null` with no `undefined`)
       - Update `ExportSchema` items array: `estimate` stays as `string` (for backward compatibility with extension)

    2. Update `src/db/index.ts`:
       - Import `ExportHistoryRecord` from types
       - Add `exportHistory: EntityTable<ExportHistoryRecord, "id">` to the db type assertion
       - Add `db.version(4)` with stores including `exportHistory: "++id, sessionId, exportedAt"` (keep all existing table declarations unchanged)
       - Add `.upgrade()` function that migrates both houseVisitItems and saleItems: if `typeof item.estimate === 'string'`, set `item.estimate = null`

    3. Create `src/utils/estimateFormat.ts`:
       - Import `EstimateRange` from `../db/types`
       - `formatEstimate(estimate: EstimateRange | null): string` -- returns empty string for null, otherwise `$${low.toLocaleString()}\u2013$${high.toLocaleString()}` (en-dash)
       - `estimateToExportString(estimate: EstimateRange | null): string | undefined` -- returns undefined for null, otherwise `$${low}-$${high}` (plain hyphen, no locale formatting, for JSON export backward compat)

    4. Create `src/tests/estimate-format.test.ts` with tests for both functions (see behavior above).

    5. Update `src/tests/db.test.ts`: Add a test group `describe("v4 migration")` that verifies the exportHistory table exists and string estimates are migrated to null. Use the existing test patterns in db.test.ts.
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx vitest run src/tests/estimate-format.test.ts src/tests/db.test.ts --reporter=verbose</automated>
  </verify>
  <done>EstimateRange and ExportHistoryRecord types exported, Session has archivedAt, estimate fields are EstimateRange | null, Dexie v4 migration runs, formatEstimate and estimateToExportString work correctly, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test scaffolds for remaining Phase 9 features</name>
  <files>src/tests/import-receipts.test.ts, src/tests/export-history.test.ts, src/tests/gemini-schema.test.ts, src/tests/sessions.test.ts</files>
  <action>
    1. Create `src/tests/import-receipts.test.ts` with `test.todo()` stubs:
       - "parses receipt numbers from CSV file" (IMPORT-01)
       - "parses receipt numbers from XLSX file" (IMPORT-02)
       - "skips invalid receipt numbers" (IMPORT-03)
       - "creates blank items for each valid receipt number" (IMPORT-04)

    2. Create `src/tests/export-history.test.ts` with `test.todo()` stubs:
       - "creates export history record after export" (DATA-01a)
       - "re-exports session from history entry" (DATA-01b)

    3. Update `src/tests/gemini-schema.test.ts`: Add `test.todo()` stubs within or after existing describe block:
       - "returns structured estimate object with low and high" (AI-06a)
       - "returns null estimate when not mentioned" (AI-06a)

    4. Update `src/tests/sessions.test.ts`: Add `test.todo()` stubs:
       - "archived session is hidden from active sessions" (DATA-01c)
       - "un-archived session returns to main list" (DATA-01d)

    Use `test.todo("description")` pattern so Vitest reports them as pending without failures.
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All four test files exist with todo stubs, full test suite passes (todos reported as pending, no failures)</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` passes with no failures
- `src/db/types.ts` exports EstimateRange, ExportHistoryRecord, updated Session
- `src/db/index.ts` has version(4) with exportHistory table and estimate migration
- `src/utils/estimateFormat.ts` exports formatEstimate and estimateToExportString
- All Wave 0 test scaffold files exist with todo stubs
</verification>

<success_criteria>
- Dexie v4 migration compiles and runs without errors
- Type changes compile across the project (no TS errors from estimate field change)
- Estimate formatting tests pass
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/09-deffered-items/09-01-SUMMARY.md`
</output>
