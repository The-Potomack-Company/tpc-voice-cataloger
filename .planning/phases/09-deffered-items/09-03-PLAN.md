---
phase: 09-deffered-items
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/services/geminiSchema.ts
  - src/services/gemini.ts
  - src/components/EstimateField.tsx
  - src/components/ItemCard.tsx
  - src/utils/export.ts
  - src/tests/gemini-schema.test.ts
  - src/tests/estimate-format.test.ts
autonomous: true
requirements:
  - AI-06
must_haves:
  truths:
    - "AI returns structured estimate with low and high dollar values"
    - "Single spoken value gets +-20% spread automatically"
    - "Range spoken values use the stated numbers"
    - "Estimate displays as dollar range in the UI"
    - "Estimate is editable inline with two number inputs"
    - "Export flattens structured estimate to string for extension compatibility"
  artifacts:
    - path: "src/services/geminiSchema.ts"
      provides: "Updated Zod schema with structured estimate object"
      contains: "z.object"
    - path: "src/components/EstimateField.tsx"
      provides: "Inline-editable estimate range component"
      exports: ["EstimateField"]
    - path: "src/components/ItemCard.tsx"
      provides: "Updated to use EstimateField instead of EditableField for estimate"
      contains: "EstimateField"
  key_links:
    - from: "src/services/gemini.ts"
      to: "src/db/types.ts"
      via: "writes EstimateRange to item record"
      pattern: "estimate"
    - from: "src/components/ItemCard.tsx"
      to: "src/components/EstimateField.tsx"
      via: "renders EstimateField for estimate display/edit"
      pattern: "EstimateField"
    - from: "src/utils/export.ts"
      to: "src/utils/estimateFormat.ts"
      via: "flattens EstimateRange to string for export"
      pattern: "estimateToExportString"
---

<objective>
Estimate extraction: AI returns structured low/high dollar ranges from spoken prices, displayed and editable in the UI.

Purpose: Auctioneers speak price estimates naturally ("three to five hundred"). The AI now returns structured `{ low, high }` instead of raw text, enabling consistent "$300-$500" display and future price-aware features.

Output: Updated Gemini schema, EstimateField component, ItemCard integration, export compatibility.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-deffered-items/09-CONTEXT.md
@.planning/phases/09-deffered-items/09-RESEARCH.md
@.planning/phases/09-deffered-items/09-01-SUMMARY.md

@src/services/geminiSchema.ts
@src/services/gemini.ts
@src/components/ItemCard.tsx
@src/components/EditableField.tsx
@src/utils/export.ts
@src/utils/estimateFormat.ts
@src/db/types.ts

<interfaces>
<!-- Key types from Plan 01 -->

From src/db/types.ts (after Plan 01):
```typescript
export interface EstimateRange {
  low: number;
  high: number;
}

// HouseVisitItem.estimate and SaleItem.estimate are now: EstimateRange | null
```

From src/utils/estimateFormat.ts (after Plan 01):
```typescript
export function formatEstimate(estimate: EstimateRange | null): string;
export function estimateToExportString(estimate: EstimateRange | null): string | undefined;
```

From src/components/EditableField.tsx:
```typescript
interface EditableFieldProps {
  label: string;
  value?: string;
  onSave: (value: string) => void;
  placeholder?: string;
  multiline?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gemini schema and AI pipeline update</name>
  <files>src/services/geminiSchema.ts, src/services/gemini.ts, src/tests/gemini-schema.test.ts</files>
  <behavior>
    - catalogFieldsSchema validates estimate as { low: number, high: number } | null
    - catalogFieldsSchema rejects estimate as plain string
    - catalogFieldsSchema accepts null estimate
    - Gemini response with structured estimate writes EstimateRange to item record
    - Gemini response with null estimate writes null to item record
  </behavior>
  <action>
    1. Update `src/services/geminiSchema.ts`:
       - Change `estimate` field from `z.string().nullable()` to `z.object({ low: z.number(), high: z.number() }).nullable()`
       - Update the `.describe()` to: "Price estimate as dollar range. Single value: use +-20% spread. Range: use stated values. Null if not mentioned."
       - The `CatalogFields` type will automatically update via z.infer

    2. Update `src/services/gemini.ts`:
       - Update SYSTEM_PROMPT: replace the estimate line with: "- estimate: A price range as {low, high} dollar values. If a single value is spoken (e.g. 'about five hundred'), use +-20% spread (low: 400, high: 600). If a range is spoken (e.g. 'three to five hundred'), use stated values (low: 300, high: 500). Return null if no estimate mentioned."
       - In `processAudioWithAi`, update the field-writing section: when `fields.estimate !== null`, write `updateData.estimate = fields.estimate` (it's already an object, no conversion needed since Dexie stores objects fine)

    3. Update `src/tests/gemini-schema.test.ts`: Replace the todo stubs with real tests:
       - Test that schema validates `{ low: 300, high: 500 }` as valid estimate
       - Test that schema validates null as valid estimate
       - Test that schema rejects a plain string estimate
       - Test that schema rejects estimate with missing low or high
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx vitest run src/tests/gemini-schema.test.ts --reporter=verbose</automated>
  </verify>
  <done>Gemini schema expects structured estimate, AI pipeline writes EstimateRange objects to Dexie, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: EstimateField component, ItemCard integration, and export update</name>
  <files>src/components/EstimateField.tsx, src/components/ItemCard.tsx, src/utils/export.ts</files>
  <action>
    1. Create `src/components/EstimateField.tsx`:
       - Props: `{ value: EstimateRange | null; onSave: (range: EstimateRange) => void }`
       - Display mode: shows `formatEstimate(value)` or "No estimate" when null. Tap to enter edit mode.
       - Edit mode: two number inputs side by side labeled "Low" and "High", pre-filled with current values (or empty if null). On blur of either input (when both have values), call onSave with the new range. Escape cancels edit.
       - Styling: match EditableField pattern -- label "Estimate" at top, same font sizes, same rounded-lg border styling, same bg-gray-50 dark:bg-gray-800
       - Display the formatted range with en-dash between values
       - Inputs should have `type="number"` with `inputMode="numeric"` for mobile number pad

    2. Update `src/components/ItemCard.tsx`:
       - Import EstimateField and EstimateRange type
       - Replace the EditableField for "Estimate" with EstimateField
       - Pass `item.estimate` (now EstimateRange | null) as value
       - onSave handler: call `updateItemField(item.id!, mode, "estimate", range)` -- but note updateItemField currently accepts string. Instead, directly use `db.houseVisitItems.update(item.id!, { estimate: range })` or `db.saleItems.update(item.id!, { estimate: range })` via the table variable. Add a new inline handler `handleEstimateSave` that does this directly.
       - Import `db` from `../db` (already imported in ItemCard)

    3. Update `src/utils/export.ts`:
       - Import `estimateToExportString` from `./estimateFormat`
       - In `buildExportData`, change the estimate mapping in exportItems: instead of `estimate: item.estimate`, use `estimate: estimateToExportString(item.estimate as any)` -- this flattens EstimateRange to "$300-$500" string for backward compatibility with the Chrome extension
       - The ExportSchema type still has `estimate?: string` which is correct
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>EstimateField renders dollar range or "No estimate", editable with two number inputs, ItemCard uses EstimateField, export produces string estimates for extension compatibility, TypeScript compiles clean</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` all pass
- `npx tsc --noEmit` compiles without errors
- Gemini schema validates structured estimate objects
- EstimateField component exists and renders estimate range
- ItemCard uses EstimateField instead of EditableField for estimate
- Export flattens EstimateRange to string
</verification>

<success_criteria>
- AI returns `{ low: 300, high: 500 }` for "three to five hundred"
- UI displays "$300-$500" with inline editing via two number inputs
- Export JSON contains string estimate "$300-$500" (extension backward compatible)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-deffered-items/09-03-SUMMARY.md`
</output>
