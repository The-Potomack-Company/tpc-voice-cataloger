---
phase: 09-deffered-items
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/utils/importReceipts.ts
  - src/components/ImportReceiptsButton.tsx
  - src/pages/SessionDetail.tsx
  - src/tests/import-receipts.test.ts
autonomous: true
requirements:
  - IMPORT-01
  - IMPORT-02
  - IMPORT-03
  - IMPORT-04
must_haves:
  truths:
    - "Auctioneer can upload a CSV file and have blank items created per receipt number"
    - "Auctioneer can upload an XLSX file and have blank items created per receipt number"
    - "Invalid receipt numbers are skipped with feedback"
    - "Import button is only visible on sale mode sessions"
  artifacts:
    - path: "src/utils/importReceipts.ts"
      provides: "parseReceiptNumbers and importReceiptsToSession functions"
      exports: ["parseReceiptNumbers", "importReceiptsToSession"]
    - path: "src/components/ImportReceiptsButton.tsx"
      provides: "File input component for CSV/XLSX upload"
      exports: ["ImportReceiptsButton"]
    - path: "src/tests/import-receipts.test.ts"
      provides: "Tests for CSV/XLSX parsing and validation"
  key_links:
    - from: "src/utils/importReceipts.ts"
      to: "src/utils/receiptNumber.ts"
      via: "import isValidReceiptNumber for validation"
      pattern: "isValidReceiptNumber"
    - from: "src/utils/importReceipts.ts"
      to: "src/db/items.ts"
      via: "createBlankItem for batch item creation"
      pattern: "createBlankItem"
    - from: "src/pages/SessionDetail.tsx"
      to: "src/components/ImportReceiptsButton.tsx"
      via: "render ImportReceiptsButton in sale mode only"
      pattern: "ImportReceiptsButton"
---

<objective>
Receipt number list import: auctioneer uploads a CSV or XLSX spreadsheet to pre-populate a sale session with blank items.

Purpose: Auctioneers receive receipt number lists from sale managers. Instead of typing each one manually, they upload the spreadsheet and the app creates blank items ready for voice dictation.

Output: Import utility, UI button, and tests for CSV/XLSX receipt number import.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-deffered-items/09-CONTEXT.md
@.planning/phases/09-deffered-items/09-RESEARCH.md
@.planning/phases/09-deffered-items/09-01-SUMMARY.md

@src/utils/receiptNumber.ts
@src/db/items.ts
@src/pages/SessionDetail.tsx

<interfaces>
<!-- Key types and contracts from the codebase -->

From src/utils/receiptNumber.ts:
```typescript
export const RECEIPT_PATTERN = /^\d{5}-\d+$/;
export function isValidReceiptNumber(value: string): boolean;
```

From src/db/items.ts:
```typescript
export async function createBlankItem(sessionId: number, mode: "house" | "sale"): Promise<number>;
export async function updateItemField(id: number, mode: "house" | "sale", field: string, value: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Receipt parsing utility and tests</name>
  <files>src/utils/importReceipts.ts, src/tests/import-receipts.test.ts</files>
  <behavior>
    - parseReceiptNumbers with CSV content returns array of first-column values as strings
    - parseReceiptNumbers with XLSX content returns array of first-column values as strings
    - parseReceiptNumbers skips empty rows
    - parseReceiptNumbers trims whitespace from values
    - importReceiptsToSession creates one blank SaleItem per valid receipt number with receiptNumber set
    - importReceiptsToSession skips invalid receipt numbers (not matching XXXXX-N)
    - importReceiptsToSession returns { created: number, skipped: number }
  </behavior>
  <action>
    1. Install SheetJS: `npm install xlsx`

    2. Create `src/utils/importReceipts.ts`:
       - `parseReceiptNumbers(file: File): Promise<string[]>` -- reads file with `XLSX.read(await file.arrayBuffer())`, gets first sheet, converts to array-of-arrays via `XLSX.utils.sheet_to_json(sheet, { header: 1 })`, extracts first column, trims, filters empty strings. Import from `xlsx` (not `xlsx/xlsx.mini` -- tree-shaking will handle it via Vite).
       - `importReceiptsToSession(sessionId: number, file: File): Promise<{ created: number; skipped: number }>` -- calls parseReceiptNumbers, filters with `isValidReceiptNumber`, for each valid receipt: calls `createBlankItem(sessionId, "sale")` to get the new item ID, then calls `updateItemField(id, "sale", "receiptNumber", receipt)` to set the receipt number. Returns counts.
       - Validate file extension after selection (`.csv`, `.xlsx`, `.xls`). Throw error for unsupported types (handles iOS Safari not enforcing accept attribute).

    3. Update `src/tests/import-receipts.test.ts`: Replace todo stubs with real tests.
       - Mock xlsx module to return controlled data for parseReceiptNumbers tests
       - Test CSV-like data (array of arrays with receipt numbers)
       - Test that invalid receipt numbers are filtered in importReceiptsToSession
       - Test that createBlankItem and updateItemField are called correctly
       - Use vi.mock for db/items functions
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx vitest run src/tests/import-receipts.test.ts --reporter=verbose</automated>
  </verify>
  <done>parseReceiptNumbers extracts first-column values from CSV/XLSX, importReceiptsToSession creates blank items with receipt numbers set, invalid receipts skipped, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Import button UI and SessionDetail integration</name>
  <files>src/components/ImportReceiptsButton.tsx, src/pages/SessionDetail.tsx</files>
  <action>
    1. Create `src/components/ImportReceiptsButton.tsx`:
       - Props: `{ sessionId: number; onImportComplete?: (result: { created: number; skipped: number }) => void }`
       - Hidden `<input type="file" accept=".csv,.xlsx,.xls" />` triggered by button click
       - On file select: validate extension (`.csv`, `.xlsx`, `.xls`), call `importReceiptsToSession(sessionId, file)`
       - Show loading state during import
       - On success: call onImportComplete callback
       - On error: log to console (toast can be added later)
       - Button styling: outline style matching existing action buttons in SessionDetail, with upload icon (arrow-up-tray SVG)
       - Text: "Import Receipt Numbers"

    2. Update `src/pages/SessionDetail.tsx`:
       - Import ImportReceiptsButton
       - Add it in the action buttons section, BEFORE the Export button, but only when `session.mode === "sale"`
       - On import complete, show a simple window.alert with the result counts: "Created X items, skipped Y invalid numbers" (lightweight, no toast dependency needed)
       - The import button should be disabled when session status is "completed"
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>ImportReceiptsButton renders in SessionDetail for sale mode sessions only, file picker opens on click, import creates blank items with receipt numbers, TypeScript compiles clean</done>
</task>

</tasks>

<verification>
- `npx vitest run src/tests/import-receipts.test.ts --reporter=verbose` passes
- `npx tsc --noEmit` compiles without errors
- ImportReceiptsButton only renders for sale mode sessions
- Import creates blank SaleItems with receiptNumber field populated
</verification>

<success_criteria>
- CSV file with receipt numbers creates correct number of blank items
- XLSX file with receipt numbers creates correct number of blank items
- Invalid receipt numbers are skipped (not matching XXXXX-N format)
- Import button visible only on sale mode session detail pages
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-deffered-items/09-02-SUMMARY.md`
</output>
