---
phase: 09-deffered-items
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
files_modified:
  - src/utils/export.ts
  - src/db/sessions.ts
  - src/hooks/useSessions.ts
  - src/components/ExportHistoryList.tsx
  - src/pages/Sessions.tsx
  - src/pages/SessionDetail.tsx
  - src/tests/export-history.test.ts
  - src/tests/sessions.test.ts
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "Each export creates a history record with session name, date, item count"
    - "Export history is visible as expandable rows"
    - "Re-export from history rebuilds and downloads the session"
    - "Completed+exported sessions can be archived"
    - "Archived sessions are hidden from main session list"
    - "Archived sessions appear in a collapsible Archive section"
    - "Archived sessions can be un-archived"
  artifacts:
    - path: "src/utils/export.ts"
      provides: "recordExport function called after each export"
      exports: ["recordExport"]
    - path: "src/db/sessions.ts"
      provides: "archiveSession, unarchiveSession functions"
      exports: ["archiveSession", "unarchiveSession"]
    - path: "src/hooks/useSessions.ts"
      provides: "useArchivedSessions hook"
      exports: ["useArchivedSessions"]
    - path: "src/components/ExportHistoryList.tsx"
      provides: "Expandable export history rows"
      exports: ["ExportHistoryList"]
    - path: "src/pages/Sessions.tsx"
      provides: "Archive section below Completed"
      contains: "Archived"
  key_links:
    - from: "src/utils/export.ts"
      to: "src/db/index.ts"
      via: "db.exportHistory.add to record exports"
      pattern: "db\\.exportHistory"
    - from: "src/hooks/useSessions.ts"
      to: "src/db/index.ts"
      via: "filter sessions by archivedAt"
      pattern: "archivedAt"
    - from: "src/pages/Sessions.tsx"
      to: "src/hooks/useSessions.ts"
      via: "useArchivedSessions for archive section"
      pattern: "useArchivedSessions"
    - from: "src/pages/SessionDetail.tsx"
      to: "src/db/sessions.ts"
      via: "archiveSession after export+complete"
      pattern: "archiveSession"
---

<objective>
Export history tracking and session archiving: track each export event, allow re-export from history, and let auctioneers archive completed sessions to declutter the session list.

Purpose: After weeks of cataloging, the session list gets long. Archiving hides completed work while preserving it. Export history provides an audit trail and quick re-export capability.

Output: Export recording, archive CRUD, archive section in Sessions page, archive offer after export+complete.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-deffered-items/09-CONTEXT.md
@.planning/phases/09-deffered-items/09-RESEARCH.md
@.planning/phases/09-deffered-items/09-01-SUMMARY.md
@.planning/phases/09-deffered-items/09-03-SUMMARY.md

@src/utils/export.ts
@src/db/sessions.ts
@src/hooks/useSessions.ts
@src/pages/Sessions.tsx
@src/pages/SessionDetail.tsx
@src/db/types.ts
@src/db/index.ts

<interfaces>
<!-- Key types from Plan 01 -->

From src/db/types.ts (after Plan 01):
```typescript
export interface ExportHistoryRecord {
  id?: number;
  sessionId: number;
  sessionName: string;
  sessionMode: "house" | "sale";
  itemCount: number;
  exportedAt: Date;
}

export interface Session {
  id?: number;
  name: string;
  mode: "house" | "sale";
  status: "active" | "completed";
  notes: string;
  deletedAt?: Date;
  archivedAt?: Date;  // NEW in v4
  createdAt: Date;
  updatedAt: Date;
}
```

From src/db/index.ts (after Plan 01):
```typescript
// db.exportHistory: EntityTable<ExportHistoryRecord, "id">
// exportHistory table: "++id, sessionId, exportedAt"
```

From src/utils/export.ts:
```typescript
export async function buildExportData(sessionId: number): Promise<ExportSchema>;
export async function exportSession(sessionId: number): Promise<void>;
```

From src/db/sessions.ts:
```typescript
export async function updateSession(id: number, changes: Partial<Pick<Session, "name" | "notes" | "status">>): Promise<void>;
export async function softDeleteSession(id: number): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export history recording, archive CRUD, and session hooks</name>
  <files>src/utils/export.ts, src/db/sessions.ts, src/hooks/useSessions.ts, src/tests/export-history.test.ts, src/tests/sessions.test.ts</files>
  <behavior>
    - recordExport creates an ExportHistoryRecord in Dexie with correct fields
    - exportSession calls recordExport after successful export
    - archiveSession sets archivedAt on the session
    - unarchiveSession removes archivedAt from the session
    - useActiveSessions excludes archived sessions
    - useCompletedSessions excludes archived sessions
    - useArchivedSessions returns only sessions with archivedAt set (excluding deleted)
  </behavior>
  <action>
    1. Update `src/utils/export.ts`:
       - Import `db` (already imported)
       - Add `recordExport` function: `async function recordExport(sessionId: number, sessionName: string, sessionMode: "house" | "sale", itemCount: number): Promise<void>` -- adds record to `db.exportHistory` with `exportedAt: new Date()`
       - Update `exportSession`: after the file is created/shared (after the if/else block), call `recordExport(sessionId, data.session.name, data.session.mode, data.items.length)`. Accept sessionId is already available. The session data is in `data.session`.
       - Export `recordExport` for testing

    2. Update `src/db/sessions.ts`:
       - Add `archiveSession(id: number): Promise<void>` -- sets `archivedAt: new Date()` and `updatedAt: new Date()` on the session
       - Add `unarchiveSession(id: number): Promise<void>` -- uses `db.sessions.where("id").equals(id).modify()` to delete the archivedAt property (same pattern as restoreSession deletes deletedAt), and sets `updatedAt: new Date()`
       - Update `updateSession` type signature to also accept `archivedAt` if needed -- actually, keep archiveSession/unarchiveSession as dedicated functions for clarity

    3. Update `src/hooks/useSessions.ts`:
       - Update `useActiveSessions`: add `!s.archivedAt` to the filter (alongside existing `!s.deletedAt`)
       - Update `useCompletedSessions`: add `!s.archivedAt` to the filter
       - Add `useArchivedSessions`: query all sessions where `archivedAt` is defined and `deletedAt` is not, sorted by archivedAt descending
       - Add `useExportHistory(sessionId?: number)`: uses `useLiveQuery` to query `db.exportHistory`. If sessionId provided, filter by it. Otherwise return all. Sort by exportedAt descending.

    4. Update `src/tests/export-history.test.ts`: Replace todo stubs:
       - Test recordExport creates a record in the exportHistory table with correct fields
       - Test that multiple exports for the same session create multiple records

    5. Update `src/tests/sessions.test.ts`: Replace archive todo stubs:
       - Test archiveSession sets archivedAt
       - Test unarchiveSession removes archivedAt
       - Test useActiveSessions excludes archived sessions (may need to test the filter logic directly rather than the hook)
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx vitest run src/tests/export-history.test.ts src/tests/sessions.test.ts --reporter=verbose</automated>
  </verify>
  <done>recordExport creates history entries, archiveSession/unarchiveSession work, session hooks exclude archived sessions, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Export history list, archive section in Sessions page, and archive offer in SessionDetail</name>
  <files>src/components/ExportHistoryList.tsx, src/pages/Sessions.tsx, src/pages/SessionDetail.tsx</files>
  <action>
    1. Create `src/components/ExportHistoryList.tsx`:
       - Props: `{ sessionId: number }` (shows export history for a specific session)
       - Uses `useExportHistory(sessionId)` hook
       - Renders expandable rows: each row shows export date (formatted) and item count
       - Expand to show a "Re-export" button that calls `exportSession(sessionId)` to rebuild and download
       - If no export history, show "No exports yet" message
       - Styling: match the existing collapsible section pattern (chevron toggle, compact rows)
       - Date formatting: use `toLocaleDateString` with time

    2. Update `src/pages/Sessions.tsx`:
       - Import `useArchivedSessions` from hooks
       - Import `unarchiveSession` from db/sessions
       - Add state: `const [archivedExpanded, setArchivedExpanded] = useState(false)` (collapsed by default)
       - After the Completed section, add an Archive section (only if archivedSessions.length > 0):
         - Same collapsible pattern as Completed section (chevron + header)
         - Header: "Archived ({count})"
         - Collapsed by default
         - Each session card includes a "Restore" action to unarchive (in addition to existing delete/rename)
       - Filter search to also include archived sessions
       - archivedSessions should also be filtered by searchQuery

    3. Update `src/pages/SessionDetail.tsx`:
       - Import `archiveSession` from db/sessions
       - Import `ExportHistoryList` component
       - Add export history section: between the Notes section and Items section, add a collapsible "Export History" section that renders `<ExportHistoryList sessionId={sessionId} />`
       - After successful export of a completed session (when `handleExport` finishes and session.status === "completed"), show a confirm dialog offering to archive: "Archive this session? It will be moved to the Archive section." with "Archive" / "Keep" buttons. On confirm, call `archiveSession(sessionId)` and navigate to "/".
       - Add `confirmAction` option "archive" to the existing confirmAction state union type
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>Export history shows expandable rows per session, Archive section appears in Sessions page when archived sessions exist, exporting a completed session offers to archive, un-archive restores to main list, TypeScript compiles clean</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` all pass
- `npx tsc --noEmit` compiles without errors
- Export creates history records in Dexie
- Export history section visible on SessionDetail page
- Archive section visible on Sessions page when archived sessions exist
- Archived sessions hidden from Active/Completed sections
- Un-archive restores session to correct section
</verification>

<success_criteria>
- Each export creates a history record with session name, date, item count
- Re-export from history downloads the session JSON
- Completed+exported session offers to archive
- Archived sessions appear only in Archive section (not Active/Completed)
- Un-archive moves session back to Completed
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-deffered-items/09-04-SUMMARY.md`
</output>
