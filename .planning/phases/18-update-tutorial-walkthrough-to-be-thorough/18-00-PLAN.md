---
phase: 18-update-tutorial-walkthrough-to-be-thorough
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/walkthrough.test.tsx
  - src/tests/walkthrough-status.test.ts
autonomous: true
requirements: [WT-01, WT-02, WT-03, WT-04, WT-05, WT-06, WT-07, WT-08]

must_haves:
  truths:
    - "Test stubs exist for all eight WT requirements and can be run without errors"
    - "Each stub test is marked as todo so the suite stays green while stubs are unfilled"
  artifacts:
    - path: "src/tests/walkthrough.test.tsx"
      provides: "Stub tests covering WT-01, WT-02, WT-03, WT-06, WT-07, WT-08"
      contains: "describe"
    - path: "src/tests/walkthrough-status.test.ts"
      provides: "Stub tests covering WT-04, WT-05"
      contains: "describe"
  key_links: []
---

<objective>
Create stub test files for all Phase 18 walkthrough requirements so that downstream plans can reference specific test commands in their verify blocks.

Purpose: Satisfy Nyquist compliance -- every plan task needs an automated verify command that targets real test files. These stubs establish the test scaffolding that Plans 01 and 02 will fill in as they implement features.
Output: Two test stub files with todo markers for each WT requirement.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-update-tutorial-walkthrough-to-be-thorough/18-CONTEXT.md
@.planning/phases/18-update-tutorial-walkthrough-to-be-thorough/18-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub test files for walkthrough component and walkthrough status hook</name>
  <files>src/tests/walkthrough.test.tsx, src/tests/walkthrough-status.test.ts</files>
  <action>
Create two test stub files. Each test uses `it.todo()` so the suite stays green while stubs are unfilled. Plans 01 and 02 will fill in implementations as they build each feature.

**1. Create `src/tests/walkthrough.test.tsx`:**
```typescript
import { describe, it } from 'vitest';

describe('Walkthrough', () => {
  describe('shared steps', () => {
    it.todo('WT-01: renders shared steps for specialist role (8 shared + 2 specialist = 10 total)');
    it.todo('WT-01: first step shows Welcome to TPC Catalog title');
    it.todo('WT-01: last shared step shows Export to Chrome Extension');
  });

  describe('admin steps', () => {
    it.todo('WT-02: admin sees 12 steps (8 shared + 4 admin)');
    it.todo('WT-02: admin steps include Manage Accounts, Assign Sessions, Review & Export, Import Receipts');
    it.todo('WT-02: admin role section label shows ADMIN FEATURES');
  });

  describe('specialist steps', () => {
    it.todo('WT-03: specialist sees 10 steps (8 shared + 2 specialist)');
    it.todo('WT-03: specialist steps include Submit Your Work and Review Notes');
    it.todo('WT-03: specialist role section label shows YOUR WORKFLOW');
  });

  describe('gate', () => {
    it.todo('WT-06: Sessions page shows walkthrough when walkthroughCompleted is false');
    it.todo('WT-06: Sessions page does NOT show walkthrough when walkthroughCompleted is true');
    it.todo('WT-06: Sessions page does NOT read hasCompletedWalkthrough from uiStore');
  });

  describe('back navigation', () => {
    it.todo('WT-07: Back button is hidden on first step');
    it.todo('WT-07: Back button appears from step 2 onward');
    it.todo('WT-07: Back button returns to previous step');
  });

  describe('completion', () => {
    it.todo('WT-08: final step shows Start Cataloging button');
    it.todo('WT-08: clicking Start Cataloging calls onComplete/completeWalkthrough');
    it.todo('WT-08: Skip tutorial link calls onComplete/completeWalkthrough');
  });
});
```

**2. Create `src/tests/walkthrough-status.test.ts`:**
```typescript
import { describe, it } from 'vitest';

describe('useWalkthroughStatus', () => {
  describe('complete', () => {
    it.todo('WT-04: completeWalkthrough writes walkthrough_completed=true to Supabase profiles');
    it.todo('WT-04: completeWalkthrough sets local walkthroughCompleted to true optimistically');
    it.todo('WT-04: fetches walkthrough_completed and role in a single query');
  });

  describe('reset', () => {
    it.todo('WT-05: resetWalkthrough writes walkthrough_completed=false to Supabase profiles');
    it.todo('WT-05: resetWalkthrough sets local walkthroughCompleted to false optimistically');
    it.todo('WT-05: Settings reset button calls resetWalkthrough from hook');
  });

  describe('loading', () => {
    it.todo('returns loading=true initially, then false after query resolves');
    it.todo('defaults to walkthroughCompleted=false when no user is logged in');
    it.todo('defaults to role=specialist when profile query fails');
  });
});
```

After creating both files, update `18-VALIDATION.md` frontmatter: set `wave_0_complete: true` and `nyquist_compliant: true`.
  </action>
  <verify>
    <automated>npx vitest run src/tests/walkthrough.test.tsx src/tests/walkthrough-status.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Two stub test files exist with todo markers for all WT-01 through WT-08 requirements. Suite runs green (todo tests are skipped). VALIDATION.md updated with wave_0_complete: true.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/tests/walkthrough.test.tsx src/tests/walkthrough-status.test.ts` passes (all todo tests are skipped, zero failures)
- Both test files exist in `src/tests/`
- VALIDATION.md has `wave_0_complete: true`
</verification>

<success_criteria>
- Stub test files exist for walkthrough component and walkthrough-status hook
- All todo tests appear in vitest output as skipped/todo
- Suite remains green (zero failures)
- VALIDATION.md wave_0_complete flag is set to true
</success_criteria>

<output>
After completion, create `.planning/phases/18-update-tutorial-walkthrough-to-be-thorough/18-00-SUMMARY.md`
</output>
