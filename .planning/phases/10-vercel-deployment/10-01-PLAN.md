---
phase: 10-vercel-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
  - vite.config.ts
  - .github/workflows/ci.yml
  - proxy/src/index.ts
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04

must_haves:
  truths:
    - "Production build succeeds without basicSsl warnings or errors"
    - "vercel.json provides SPA catch-all rewrite for React Router deep links"
    - "Security headers (X-Frame-Options, X-Content-Type-Options, Referrer-Policy) are configured"
    - "CI workflow runs lint, typecheck, test, and build on push to main and PRs"
    - "Cloudflare Worker CORS is restricted to production Vercel domain only"
  artifacts:
    - path: "vercel.json"
      provides: "SPA rewrite and security headers for Vercel deployment"
      contains: "rewrites"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline definition"
      contains: "vitest run"
    - path: "vite.config.ts"
      provides: "Conditional basicSsl plugin (dev only)"
      contains: "mode"
    - path: "proxy/src/index.ts"
      provides: "CORS-restricted Gemini proxy"
      contains: "tpc-catalog.vercel.app"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts"
      via: "npm run lint, npx tsc -b, npx vitest run, npm run build"
      pattern: "npm run (lint|build)"
    - from: "vercel.json"
      to: "index.html"
      via: "SPA catch-all rewrite"
      pattern: "destination.*index\\.html"
---

<objective>
Add all configuration files and code changes needed for Vercel deployment with CI/CD.

Purpose: Prepare the codebase so that connecting Vercel GitHub integration produces a working production deployment with CI checks on every PR.
Output: vercel.json, .github/workflows/ci.yml, updated vite.config.ts (conditional basicSsl), updated proxy CORS
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-vercel-deployment/10-CONTEXT.md
@.planning/phases/10-vercel-deployment/10-RESEARCH.md

<interfaces>
<!-- Key files the executor needs to understand -->

From vite.config.ts (current — needs modification):
```typescript
export default defineConfig({
  server: { host: true, https: true },
  plugins: [
    basicSsl(),
    react(),
    tailwindcss(),
    VitePWA({ /* ... */ }),
  ],
  test: { globals: true, environment: "jsdom", setupFiles: ["src/tests/setup.ts"] },
});
```

From proxy/src/index.ts (current — needs CORS modification):
```typescript
const CORS_HEADERS: Record<string, string> = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
  "Access-Control-Allow-Headers": "Content-Type",
};
```

From package.json scripts:
```json
{
  "build": "tsc -b && vite build",
  "lint": "eslint .",
  "dev": "vite",
  "preview": "vite preview"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vercel.json, CI workflow, and conditional basicSsl</name>
  <files>vercel.json, .github/workflows/ci.yml, vite.config.ts</files>
  <action>
1. Create `vercel.json` in project root with:
   - `$schema` pointing to `https://openapi.vercel.sh/vercel.json`
   - SPA catch-all rewrite: `{ "source": "/(.*)", "destination": "/index.html" }`
   - Security headers on all routes `/(.*)`):
     - `X-Frame-Options: DENY`
     - `X-Content-Type-Options: nosniff`
     - `Referrer-Policy: strict-origin-when-cross-origin`
   - Do NOT add `framework`, `buildCommand`, or `outputDirectory` — Vercel auto-detects Vite.
   - Do NOT put env vars in vercel.json.

2. Create `.github/workflows/ci.yml` with:
   - Trigger: push to main + pull_request to main
   - Single job `ci` on `ubuntu-latest`
   - Steps: checkout@v4, setup-node@v4 (node 22, cache: npm), `npm ci`, lint (`npm run lint`), typecheck (`npx tsc -b`), test (`npx vitest run`), build (`npm run build`)
   - Use sequential steps in one job (not matrix — project is small, total CI under 2 min)

3. Modify `vite.config.ts`:
   - Change `defineConfig({...})` to `defineConfig(({ mode }) => ({...}))` (function form)
   - Wrap `basicSsl()` with `mode === "development" && basicSsl()` and add `.filter(Boolean)` to plugins array
   - Wrap `https: true` in server block with `...(mode === "development" && { https: true })`
   - Keep all other config (PWA manifest, workbox, test) exactly as-is
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>vercel.json exists with SPA rewrite and security headers. CI workflow exists with all 4 check steps. vite.config.ts uses conditional basicSsl. `npm run build` succeeds without SSL warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Restrict Cloudflare Worker CORS to production domain</name>
  <files>proxy/src/index.ts</files>
  <action>
Update `proxy/src/index.ts` to restrict CORS from wildcard `*` to the production Vercel domain:

1. Replace the static `CORS_HEADERS` object with a function-based approach:
   - Define `ALLOWED_ORIGINS` array containing `"https://tpc-catalog.vercel.app"`
   - Also include `"http://localhost:5173"` and `"https://localhost:5173"` for local development (so `vite dev` still works with the deployed proxy)
   - Create a `getCorsHeaders(requestOrigin: string | null)` function that:
     - Checks if `requestOrigin` is in `ALLOWED_ORIGINS`
     - If yes, returns headers with that origin
     - If no, returns headers with the first allowed origin (production)
   - Update all three response paths (OPTIONS, error, success) to use `getCorsHeaders(request.headers.get("Origin"))`

2. Keep all other proxy logic (Gemini URL construction, request forwarding, error handling) unchanged.

Note: Preview deployments on Vercel will get unique URLs not in ALLOWED_ORIGINS. This is intentional — AI features won't work on preview deploys, but UI review still works. Per user decision in CONTEXT.md.
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App/proxy && npx tsc --noEmit --skipLibCheck 2>&1; echo "exit: $?"</automated>
  </verify>
  <done>Proxy CORS restricted to production domain and localhost dev origins. TypeScript compiles without errors. Wildcard `*` origin no longer present in proxy source.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds (no basicSsl issues in production mode)
- `npm run lint && npx tsc -b && npx vitest run` all pass (full CI pipeline locally)
- `vercel.json` exists with rewrite and headers
- `.github/workflows/ci.yml` exists with lint/typecheck/test/build steps
- `proxy/src/index.ts` no longer contains `"Access-Control-Allow-Origin": "*"`
</verification>

<success_criteria>
All config files created/updated. Full CI pipeline passes locally (`npm run lint && npx tsc -b && npx vitest run && npm run build`). Proxy CORS locked to production domain. Codebase is ready for Vercel GitHub integration connection.
</success_criteria>

<output>
After completion, create `.planning/phases/10-vercel-deployment/10-01-SUMMARY.md`
</output>
