---
phase: 06-review-edit-export
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/ItemCard.tsx
  - src/components/ItemList.tsx
  - src/pages/SessionDetail.tsx
  - src/utils/export.ts
  - src/tests/item-list.test.tsx
  - src/tests/re-record.test.ts
autonomous: true
requirements:
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - EDIT-04
  - EXPO-01
  - EXPO-03

must_haves:
  truths:
    - "User sees all items in a session as expandable cards with AI fields"
    - "Collapsed row shows item number (or receipt number) + title + description preview"
    - "User can tap any field in expanded card and edit inline with auto-save on blur"
    - "User can delete an item via swipe or button with confirmation"
    - "User can tap mic icon on collapsed card to re-record without expanding"
    - "User can add a blank item for manual entry"
    - "User can tap Export and receive a JSON file via share sheet or download"
    - "Active session export shows a warning before proceeding"
  artifacts:
    - path: "src/components/ItemCard.tsx"
      provides: "Expandable item card with inline editing, delete, mic icon"
      exports: ["ItemCard"]
    - path: "src/components/ItemList.tsx"
      provides: "Refactored item list using ItemCard with expand state management"
      exports: ["ItemList"]
    - path: "src/pages/SessionDetail.tsx"
      provides: "Session detail with export button and add-item for manual entry"
    - path: "src/tests/item-list.test.tsx"
      provides: "Tests for item list rendering with expandable cards"
    - path: "src/tests/re-record.test.ts"
      provides: "Tests for re-record append logic"
  key_links:
    - from: "src/components/ItemCard.tsx"
      to: "src/components/EditableField.tsx"
      via: "imports EditableField for each catalog field"
      pattern: "import.*EditableField"
    - from: "src/components/ItemCard.tsx"
      to: "src/db/items.ts"
      via: "calls updateItemField on save, deleteItem on confirm"
      pattern: "import.*updateItemField|deleteItem"
    - from: "src/components/ItemList.tsx"
      to: "src/components/ItemCard.tsx"
      via: "renders ItemCard for each item"
      pattern: "import.*ItemCard"
    - from: "src/pages/SessionDetail.tsx"
      to: "src/utils/export.ts"
      via: "calls exportSession on export button tap"
      pattern: "import.*exportSession"
    - from: "src/pages/SessionDetail.tsx"
      to: "src/db/items.ts"
      via: "calls createBlankItem for manual add"
      pattern: "import.*createBlankItem"
---

<objective>
Build the review UI with expandable item cards, inline field editing, item deletion, re-record mic icon, manual item creation, and export button on the session detail page.

Purpose: This is the main user-facing work of Phase 6 -- auctioneers can review, edit, delete, re-record, and export their cataloged items.

Output: Refactored `ItemList.tsx`, new `ItemCard.tsx`, updated `SessionDetail.tsx` with export button.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-review-edit-export/06-01-SUMMARY.md

@src/db/types.ts
@src/db/index.ts
@src/components/ItemList.tsx
@src/components/SwipeableRow.tsx
@src/components/ConfirmDialog.tsx
@src/components/RecordButton.tsx
@src/pages/SessionDetail.tsx
@src/hooks/useAudioRecorder.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/db/items.ts (created in Plan 01):
```typescript
export function updateItemField(id: number, mode: "house" | "sale", field: string, value: string): Promise<void>;
export function deleteItem(id: number, mode: "house" | "sale"): Promise<void>;
export function createBlankItem(sessionId: number, mode: "house" | "sale"): Promise<number>;
export function appendToItemField(id: number, mode: "house" | "sale", field: string, newContent: string): Promise<void>;
```

From src/components/EditableField.tsx (created in Plan 01):
```typescript
export function EditableField(props: {
  value: string | undefined;
  onSave: (val: string) => void;
  placeholder?: string;
  multiline?: boolean;
  label?: string;
}): JSX.Element;
```

From src/utils/export.ts (created in Plan 01):
```typescript
export function exportSession(sessionId: number): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ItemCard component with expandable view, inline editing, delete, and mic icon</name>
  <files>src/components/ItemCard.tsx, src/components/ItemList.tsx, src/tests/item-list.test.tsx, src/tests/re-record.test.ts</files>
  <action>
    Create `src/components/ItemCard.tsx`:

    Props: `item: HouseVisitItem | SaleItem`, `mode: "house" | "sale"`, `isExpanded: boolean`, `onToggle: () => void`.

    **Collapsed row (always visible):**
    - Clickable row (button element) that calls `onToggle` on tap
    - Shows: item number (`Item {sortOrder + 1}`) or receipt number for sale mode
    - Shows: title (truncated) + description preview (truncated, gray)
    - Right side: small mic icon button (24x24, stops propagation so tap does not toggle expand). Mic icon triggers re-record flow. For now, the mic button should call `useAudioRecorder().startRecording(item.id, mode)`. Import `RecordButton` is too large for collapsed row -- instead create a small inline mic SVG button that calls `startRecording` directly.
    - Chevron icon (rotates when expanded)
    - Audio/photo count indicators (carry forward from existing ItemRow pattern with per-row useLiveQuery)

    **Expanded section (conditional on isExpanded):**
    - Renders below collapsed row with border-top separator
    - All 5 fields using `EditableField`: Title, Description (multiline), Condition, Estimate, Category
    - Each field's `onSave` calls `updateItemField(item.id!, mode, fieldName, value)`
    - For sale mode, also show receipt number as editable field
    - Delete button at bottom of expanded section: red text, calls `deleteItem` after `ConfirmDialog` confirmation
    - Use `SwipeableRow` wrapping the entire card for swipe-to-delete as alternative delete gesture

    Wrap the card with `SwipeableRow` for swipe-to-delete. The swipe action triggers the same `ConfirmDialog` flow.

    **Refactor `src/components/ItemList.tsx`:**
    - Replace `ItemRow` with `ItemCard`
    - Manage expand state as `Set<number>` (item IDs) in local React state (not Zustand -- keeps it simple)
    - Toggle function: adds/removes item ID from the Set
    - Keep existing `useLiveQuery` for items list
    - Remove the `useNavigate` / navigate-to-item-entry behavior from rows (replaced by expandable cards)
    - Add "Add Item" button at top of list when items exist (or the existing empty state message). This button calls `createBlankItem(sessionId, mode)` from `src/db/items.ts`. The newly created item's card should auto-expand (add its ID to the expanded Set after creation).

    **Re-record integration:**
    - The small mic icon on collapsed cards needs to invoke `useAudioRecorder`. Since `useAudioRecorder` is a hook, it must be called at component level. Call `useAudioRecorder()` inside `ItemCard` and wire `startRecording` to the mic button.
    - After recording completes (detected via status change from "recording" to "idle"), the AI pipeline (Phase 5) will process and write results. Phase 6 does not need to manually trigger AI -- it just records audio. The `appendToItemField` function is available for Phase 5's AI callback to use.
    - Show `RecordingIndicator` and `RecordingToast` overlays. These are already rendered in `ItemEntryPage` -- for the review screen, add them to `SessionDetail.tsx` or `ItemList.tsx` (whichever is the top-level for the review context).

    Write `src/tests/item-list.test.tsx`:
    - Test that items render as cards with correct item numbers
    - Test that tapping a card expands it to show all fields
    - Test that empty state shows "No items" message

    Write `src/tests/re-record.test.ts`:
    - Test appendToItemField: appending to existing value joins with newline
    - Test appendToItemField: appending to empty/undefined sets value directly
    (These test the data function, not the UI hook interaction)
  </action>
  <verify>
    <automated>npx vitest run src/tests/item-list.test.tsx src/tests/re-record.test.ts --reporter=verbose</automated>
  </verify>
  <done>ItemCard renders collapsed/expanded states. Collapsed shows item number, title preview, mic icon, chevron. Expanded shows all EditableField instances. ItemList manages expand state via Set. Swipe-to-delete and button delete both work via ConfirmDialog. Add Item creates blank item. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Export button on SessionDetail with active session warning</name>
  <files>src/pages/SessionDetail.tsx</files>
  <action>
    Update `src/pages/SessionDetail.tsx` to add export functionality:

    1. **Import** `exportSession` from `src/utils/export.ts` and `ConfirmDialog` (already imported).

    2. **Add export state:**
       - `exporting: boolean` (loading state during export)
       - Add `"export"` to the `confirmAction` union type

    3. **Export button** placed in the action buttons section, ABOVE the "Mark Complete" / "Delete Session" buttons:
       - Full-width button styled with accent color outline: `border border-accent text-accent hover:bg-accent/10`
       - Text: "Export Session"
       - Shows spinner/disabled state when `exporting` is true
       - SVG download/share icon on left

    4. **Export flow:**
       - If `session.status === "active"`, tapping Export sets `confirmAction = "export"` to show warning dialog first
       - If `session.status === "completed"`, tapping Export calls `handleExport()` directly
       - Warning dialog: title "Export Active Session", message "This session is still active. Items may be incomplete. Export anyway?", confirmLabel "Export Anyway"
       - `handleExport` async function: sets `exporting = true`, calls `await exportSession(sessionId)`, sets `exporting = false`. Wrap in try/catch -- on error, show a simple alert or log.

    5. **Add `RecordingIndicator` and `RecordingToast`** imports and render them at the bottom of the page JSX (so re-recording from ItemList mic icons shows the overlay correctly).

    6. **Update the floating "Add Item" button** to also work when session is completed but user is reviewing items. Actually, per locked decisions, the "Add Item" creates a blank item for manual entry. The existing button already navigates to `/item/new` which creates an item. Change this: instead of navigating to ItemEntry, call `createBlankItem(sessionId, session.mode)` directly and the new item will appear in the ItemList (which auto-updates via useLiveQuery). The ItemList's "Add Item" button (from Task 1) handles this, so the floating button in SessionDetail should do the same thing -- call createBlankItem and let the card auto-expand. Import `createBlankItem` from `src/db/items.ts`.

    Keep existing functionality (name editing, notes, metadata, status controls, delete) untouched.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Export button visible on SessionDetail page. Active sessions show warning before export. Completed sessions export directly. Export produces JSON file via share/download. Recording overlays render for re-record. Full test suite passes.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run --reporter=verbose
```
All tests pass including new item-list, re-record, and existing suite.

Manual check (for future checkpoint if needed):
1. Navigate to a session with items
2. Items display as expandable cards
3. Tap card to expand, see all fields with EditableField
4. Edit a field, blur, confirm value persisted
5. Swipe card to delete with confirmation
6. Tap mic icon to re-record (recording indicator appears)
7. Tap Export, receive JSON file
</verification>

<success_criteria>
- Items display as expandable cards with collapsed preview and expanded fields
- Inline editing saves on blur to Dexie
- Delete removes item + related data with confirmation
- Mic icon on collapsed cards triggers recording
- Export button on SessionDetail produces valid JSON file
- Active session export shows warning dialog first
- Add Item creates blank item for manual entry
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-review-edit-export/06-02-SUMMARY.md`
</output>
