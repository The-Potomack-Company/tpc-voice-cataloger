---
phase: 16-session-lifecycle
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified: []
autonomous: false
requirements: [LIFE-01, LIFE-02, LIFE-03, LIFE-04, LIFE-05, LIFE-06]

must_haves:
  truths:
    - "Full lifecycle flow verified end-to-end: submit, lock, admin edit, return with notes, re-submit, export"
    - "Role-based rendering verified: specialist and admin see correct buttons and sections"
    - "Status banners display correctly on submitted and returned sessions"
    - "Sticky review notes banner stays pinned while scrolling items"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete session lifecycle workflow end-to-end through manual testing of both specialist and admin views.

Purpose: The lifecycle flow involves multiple role-based UI states, status transitions, and visual elements that cannot be fully verified by automated tests alone. This checkpoint ensures the submit-review-return-export cycle works correctly for real users.
Output: Verified lifecycle workflow.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-session-lifecycle/16-01-SUMMARY.md
@.planning/phases/16-session-lifecycle/16-02-SUMMARY.md
@.planning/phases/16-session-lifecycle/16-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of complete session lifecycle workflow</name>
  <files></files>
  <action>
Present the verification checklist to the user. The lifecycle flow was built across Plans 01 and 02. This checkpoint verifies all 6 LIFE requirements work correctly in both specialist and admin views.

What was built:
- Specialist submit action with pre-submit gate and confirmation dialog
- Read-only lock on submitted sessions for specialist (no editing, no recording, no adding items)
- Admin can always edit item fields on submitted sessions
- Admin "Return to Specialist" button with ReturnDialog (optional review notes)
- Returned sessions show sticky amber banner with review notes for specialist
- Admin-only export button (hidden from specialist), sets status to 'exported'
- Sessions page with role-aware lifecycle sections (specialist: Needs Attention/Active/Submitted/Exported; admin: Awaiting Review/Active/Returned/Exported)
- SessionCard status pills (Submitted=yellow, Returned=orange, Exported=green)
- Old Mark Complete/Reopen buttons removed

How to verify (run `npx vite dev` and open in browser):

Test 1 - Specialist Submit Flow: Log in as specialist, open active session, tap Submit for Review, confirm via dialog, verify blue banner + read-only state + session moves to Submitted section.

Test 2 - Pre-Submit Gate: Open active session with queued items, verify Submit button disabled with processing count.

Test 3 - Admin Review Flow: Log in as admin, verify Awaiting Review section, open submitted session, verify editable fields + Export + Return buttons.

Test 4 - Admin Return Flow: Tap Return to Specialist, type review notes in dialog, confirm, verify status changes to Returned.

Test 5 - Specialist Returned Session: Log in as specialist, verify Needs Attention section with amber header, open returned session, verify sticky amber banner with notes, verify editing restored.

Test 6 - Admin Export Flow: As admin, tap Export Session, verify JSON downloads, verify status changes to Exported.

Test 7 - Export Gating: As specialist, verify Export button is completely absent.

Test 8 - Status Pills: Verify SessionCard pills match statuses (yellow/orange/green).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms all 8 test scenarios pass. Lifecycle workflow works end-to-end for both roles.</done>
</task>

</tasks>

<verification>
All 8 test scenarios pass with correct role-based behavior.
</verification>

<success_criteria>
- Submit, lock, admin edit, return with notes, re-submit, and export all work end-to-end
- Role-based rendering correct for both specialist and admin
- Status banners display appropriately
- Sticky review notes banner works on mobile scroll
- Export gated to admin only
- Sessions page sections match role
</success_criteria>

<output>
After completion, create `.planning/phases/16-session-lifecycle/16-03-SUMMARY.md`
</output>
