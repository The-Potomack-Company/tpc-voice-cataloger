---
phase: 20-fix-house-session-json-import-on-rfc
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: []
autonomous: false
requirements:
  - D-05

must_haves:
  truths:
    - "Exported house session JSON contains photos array with base64 data URLs for every item that has photos"
    - "Import of a multi-item house session fills all text fields AND uploads all photos on each RFC page"
    - "Style dropdown is correctly handled when starting from a non-General style"
    - "Import recovers correctly after page reloads (save, navigate, style change)"
  artifacts: []
  key_links:
    - from: "TPC_App buildExportData()"
      to: "TPC_AI_Cataloger importController processNextHouseItem"
      via: "JSON file with ExportSchema format"
      pattern: "photos.*blob.*base64"
---

<objective>
Verify the end-to-end house session import pipeline works: export from TPC_App, import in TPC_AI_Cataloger extension, confirm all fields + photos land on RFC pages.

Purpose: Per D-05, both repos must be verified as a single pipeline. The export side was assessed as correct during research, but live verification is required. The import side was rewritten in plan 01 and needs manual E2E testing.

Output: Human-verified confirmation that the full export-import pipeline works.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-fix-house-session-json-import-on-rfc/20-CONTEXT.md
@.planning/phases/20-fix-house-session-json-import-on-rfc/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify export JSON photo data completeness</name>
  <files></files>
  <read_first>
    C:/Users/maser/TPC_App/src/utils/export.ts
    C:/Users/maser/TPC_App/src/db/types.ts
  </read_first>
  <action>
Verify the export side produces correct JSON for house sessions (per D-05). Read `src/utils/export.ts` and confirm:

1. `buildExportData()` reads photos from Dexie first (`db.photos.where("itemId").equals(lookupId).sortBy("sortOrder")`)
2. Falls back to Supabase Storage download when no local blobs
3. `blobToBase64()` uses `FileReader.readAsDataURL()` which produces `"data:image/jpeg;base64,..."` format (the format `base64DataUrlToFile` in importController expects)
4. Photos are included in the `photos` array per item with `{ blob: string, sortOrder: number }` shape
5. Items with no photos get `photos: []` (empty array, not undefined)
6. The `session.mode` field is included (importController checks `jsonData.session.mode` to determine house vs sale)
7. The `department` field is mapped from `item.category` in Supabase

If any issues are found, fix them. Based on research, no issues are expected -- this is a verification-only task.

Also run the existing export test if it exists:
```bash
cd C:/Users/maser/TPC_App && npx vitest run src/utils/export.test.ts 2>/dev/null || echo "No export test file found"
```
  </action>
  <verify>
    <automated>cd C:/Users/maser/TPC_App && grep -n "readAsDataURL\|blobToBase64\|photos:" src/utils/export.ts | head -10</automated>
  </verify>
  <acceptance_criteria>
    - export.ts `blobToBase64` uses `FileReader.readAsDataURL` (produces data URL format compatible with import)
    - export.ts `buildExportData` includes `photos: photoData` in each item
    - export.ts includes `session.mode` in the exported JSON (via `Omit<Session, "id">` which retains `mode`)
    - export.ts maps `item.category` to `department` field
    - No code changes needed (verification only) OR any issues found are fixed
  </acceptance_criteria>
  <done>Export side verified as producing correct JSON format for import consumption</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end house session import verification</name>
  <files></files>
  <action>Human verifies the complete import pipeline on a live RFC Invaluable page.</action>
  <what-built>Complete house session import pipeline: TPC_App exports JSON with text fields + base64 photos, TPC_AI_Cataloger extension imports it on RFC Invaluable -- setting Style to General, filling all text fields, uploading photos sequentially, saving, and navigating to next item.</what-built>
  <how-to-verify>
    1. **Prepare test data:** In TPC_App (production or local), create or find a house visit session with at least 2 items, each having at least 1 photo and filled text fields (title, description, condition, estimate)
    2. **Export:** Click Export on the session to download the JSON file
    3. **Load extension:** In Chrome, go to chrome://extensions, enable Developer Mode, click "Load unpacked" and select the TPC_AI_Cataloger folder at C:/Users/maser/Projects/TPC_AI_Cataloger/
    4. **Navigate to RFC:** Go to an RFC Invaluable item edit page (any existing item)
    5. **Optional style test:** If the current page has Style set to something other than General, great -- the import should handle it. If it is already General, the import will skip the style step.
    6. **Start import:** Click the TPC extension icon, select "Import JSON", choose the exported JSON file
    7. **Observe import flow:**
       - Style dropdown should be set to General (if it was not already) -- page may reload
       - All text fields should fill (title, description, condition, estimate, measurements, department)
       - Photos should upload one by one (visible in picture panel area)
       - Page should save after fields + photos are done
       - Should navigate to Next item (or Add if Next not available)
       - Repeat for item 2
    8. **Verify results:** After import completes, go back and check each item page -- all fields filled, all photos visible
    9. **Check completion modal:** Should show success count matching number of items
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification required -- human confirms import pipeline on live RFC"</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms all text fields (title, description, condition, estimate, measurements, department) filled on each RFC item page
    - Human confirms all photos visible in picture panel on each RFC item page
    - Human confirms import completion modal shows correct success count
    - Human confirms no "Please change the Style to General" error during import
  </acceptance_criteria>
  <done>Human approved end-to-end house session import pipeline</done>
  <resume-signal>Type "approved" if import works end-to-end, or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
1. Export JSON contains base64 photo data URLs in correct format
2. Human confirms: all text fields filled on each RFC item page
3. Human confirms: all photos uploaded and visible on each RFC item page
4. Human confirms: Style dropdown handling works (no "Please change the Style to General" error)
5. Human confirms: import processes all items sequentially and shows completion summary
</verification>

<success_criteria>
- Export JSON verified as correct format (data URLs, all fields, session mode)
- Human confirms end-to-end import works: text fields + photos + style + navigation
- Both repos verified as working together (per D-05)
</success_criteria>

<output>
After completion, create `.planning/phases/20-fix-house-session-json-import-on-rfc/20-02-SUMMARY.md`
</output>
